---
phase: 44-engine-owns-the-pool
plan: 06
type: execute
wave: 4
depends_on: ["44-03"]
files_modified:
  - docs/src/how-to/connection-pools.rst
  - docs/src/reference/config.rst
  - docs/src/tutorials/first-query.rst
  - docs/src/how-to/queries.rst
  - docs/src/index.rst
  - docs/src/how-to/backends/overview.rst
  - docs/src/how-to/backends/snowflake.rst
  - docs/src/how-to/backends/databricks.rst
  - docs/src/how-to/backends/duckdb.rst
  - docs/src/how-to/web-api.rst
  - docs/src/how-to/warehouse-testing.rst
  - docs/src/how-to/codegen-credentials.rst
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every connection example in the docs uses create_engine(...) + register('name', engine) — no pool_from_config / register(pool, dialect) / get_pool examples remain"
    - "The connection-pools how-to teaches the two connection patterns (direct Engine + named registry) clean-break, with no deprecation notices"
    - "docs build green (just docs-build) with no broken cross-references"
  artifacts:
    - path: "docs/src/how-to/connection-pools.rst"
      provides: "Canonical create_engine + register(engine) connection guide (two patterns)"
      contains: "create_engine"
    - path: "docs/src/tutorials/first-query.rst"
      provides: "Tutorial wired to create_engine + register(engine)"
      contains: "create_engine"
  key_links:
    - from: "docs/src/how-to/connection-pools.rst"
      to: "semolina.create_engine / register"
      via: "documented connection workflow"
      pattern: "create_engine"
---

<objective>
Migrate every connection example in the documentation to the new API — `create_engine(config |
name)` + `register("name", engine)` + `Query.using("name")` resolving an Engine — and remove all
traces of the old `pool_from_config` / `register(pool, dialect=...)` / `get_pool` surface. This
is a clean break (pre-1.0, [[feedback_v03_engine_removal]]): NO deprecation notices, no
"previously you would…" asides. The connection-pools how-to becomes the canonical guide teaching
the two connection patterns (direct Engine instance + named registry).

Purpose: The public API changed across 12 docs pages (CONTEXT scope: "all docs"). Leaving stale
`pool_from_config`/3-arg-register examples would actively mislead readers, since those symbols no
longer exist. Docs must match the shipped surface from Plan 03.
Output: 12 docs pages updated to the create_engine/register(engine) API; `just docs-build` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/semolina-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-engine-owns-the-pool/44-CONTEXT.md
@.planning/phases/44-engine-owns-the-pool/44-RESEARCH.md
@CLAUDE.md
@src/semolina/__init__.py
@src/semolina/config.py
</context>

<artifacts_this_phase_produces>
This plan produces no source symbols. It aligns the documentation with the phase-wide public
surface shipped in Plan 03: `create_engine`, `register("name", engine)`, `get_engine`,
`Query.using("name")`. Reference docs (`docs/src/reference/`) are sphinx-autoapi auto-generated —
do NOT hand-write API reference; only the prose/example pages change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the canonical connection guide + config reference</name>
  <files>docs/src/how-to/connection-pools.rst, docs/src/reference/config.rst</files>
  <read_first>
    - .claude/skills/semolina-docs-author/SKILL.md (MANDATORY per CLAUDE.md — Diataxis classification + Semolina voice + humanizer pass)
    - docs/src/how-to/connection-pools.rst (32 connection-API references — the heaviest page; the canonical connection guide)
    - docs/src/reference/config.rst (4 references — note: API reference is sphinx-autoapi auto-generated; only fix hand-written prose/examples, not the autoapi directives)
    - src/semolina/config.py (create_engine signature from Plan 02) + src/semolina/__init__.py (the shipped public surface from Plan 03)
    - src/semolina/registry.py (register(engine) / get_engine docstring examples — mirror these in prose)
  </read_first>
  <action>
    Apply the semolina-docs-author skill (Diataxis: this is a how-to + reference). Rewrite
    `connection-pools.rst` as the canonical connection guide teaching the TWO patterns (D4 hybrid
    model): (1) SQLAlchemy-style direct Engine —
    `engine = create_engine(SnowflakeConfig(...))` then `engine.execute(query)` /
    `with engine.connect() as conn: ...`; (2) Django-style named registry —
    `register("default", create_engine("default"))` then `Query.using("default").execute()`.
    Show both the config-object arm and the `.semolina.toml` connection-name arm of
    `create_engine` (D1). REMOVE every `pool_from_config`, `register(pool, dialect=...)`, and
    `get_pool` example — clean break, NO deprecation notices ([[feedback_v03_engine_removal]]).
    Fix any hand-written prose/examples in `reference/config.rst` that reference the old surface;
    do NOT edit the sphinx-autoapi auto-generated reference directives. Use the how-to tab-set with
    `:sync-group: warehouse` where SQL-dialect examples differ. Run the humanizer pass. Add/refresh
    the "See also" links at the page bottom.
  </action>
  <verify>
    <automated>grep -c 'pool_from_config\|get_pool\|register(.*dialect=' docs/src/how-to/connection-pools.rst docs/src/reference/config.rst | grep -v ':0' || echo NONE-LEFT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pool_from_config' docs/src/how-to/connection-pools.rst` == 0 and same for `get_pool` and `register(.*dialect='
    - `connection-pools.rst` documents both the direct-Engine pattern (`engine.execute` / `engine.connect`) and the named-registry pattern (`register("default", engine)` + `.using(`)
    - `connection-pools.rst` shows both `create_engine(<config object>)` and `create_engine("<name>")` arms (grep both forms)
    - no deprecation/"previously"/"old API" language present (grep absent)
    - `reference/config.rst` hand-written examples reference create_engine, not pool_from_config; autoapi directives untouched
  </acceptance_criteria>
  <done>connection-pools.rst is the canonical two-pattern create_engine/register(engine) guide; config reference prose updated; no old-API examples or deprecation notices remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update the tutorial + general how-to query/index pages</name>
  <files>docs/src/tutorials/first-query.rst, docs/src/how-to/queries.rst, docs/src/index.rst, docs/src/how-to/warehouse-testing.rst, docs/src/how-to/codegen-credentials.rst</files>
  <read_first>
    - .claude/skills/semolina-docs-author/SKILL.md (MANDATORY — tutorials show runnable code with expected output; how-tos show illustrative snippets)
    - docs/src/tutorials/first-query.rst (8 references — runnable tutorial; imports + expected output must stay correct)
    - docs/src/how-to/queries.rst (3 references)
    - docs/src/index.rst (3 references — landing-page snippet)
    - docs/src/how-to/warehouse-testing.rst (2 references) + docs/src/how-to/codegen-credentials.rst (2 references)
    - the rewritten connection-pools.rst from Task 1 (the canonical pattern to cross-link, not duplicate)
  </read_first>
  <action>
    Apply the semolina-docs-author skill. Update `first-query.rst` (tutorial — keep it runnable:
    imports, `create_engine` + `register("default", engine)`, then the query, with correct
    expected output) — the tutorial owns full setup. Update `queries.rst`, `index.rst`,
    `warehouse-testing.rst`, and `codegen-credentials.rst` to the new API; how-to pages show
    illustrative snippets and may defer full connection setup to a "See also →
    connection-pools.rst" link rather than repeating it. Replace every `pool_from_config` /
    3-arg `register` / `get_pool` occurrence. Clean break — no deprecation notices. Run the
    humanizer pass; verify "See also" cross-links resolve to the rewritten connection guide.
  </action>
  <verify>
    <automated>grep -rc 'pool_from_config\|get_pool' docs/src/tutorials/first-query.rst docs/src/how-to/queries.rst docs/src/index.rst | grep -v ':0' || echo NONE-LEFT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'pool_from_config\|get_pool' docs/src/tutorials/first-query.rst docs/src/how-to/queries.rst docs/src/index.rst docs/src/how-to/warehouse-testing.rst docs/src/how-to/codegen-credentials.rst` returns 0 for every file
    - no 3-arg `register(.*dialect=` remains in those five files
    - first-query.rst shows a runnable `create_engine` + `register("default", engine)` setup with correct expected output
    - the how-to pages cross-link to connection-pools.rst rather than duplicating full setup
    - no deprecation/"previously" language present
  </acceptance_criteria>
  <done>Tutorial + queries/index/warehouse-testing/codegen-credentials pages use the create_engine/register(engine) API; cross-links resolve; no old-API examples.</done>
</task>

<task type="auto">
  <name>Task 3: Update backend + web-api pages and confirm docs build green</name>
  <files>docs/src/how-to/backends/overview.rst, docs/src/how-to/backends/snowflake.rst, docs/src/how-to/backends/databricks.rst, docs/src/how-to/backends/duckdb.rst, docs/src/how-to/web-api.rst</files>
  <read_first>
    - .claude/skills/semolina-docs-author/SKILL.md (MANDATORY — backend pages are how-to with :sync-group: warehouse tab-sets)
    - docs/src/how-to/backends/overview.rst (7 references) + snowflake.rst (5) + databricks.rst (5) + duckdb.rst (6)
    - docs/src/how-to/web-api.rst (5 references — FastAPI/app-backend integration example, the target audience per CLAUDE.md)
    - the rewritten connection-pools.rst (Task 1) for the canonical pattern
    - src/semolina/config.py (SnowflakeConfig/DatabricksConfig/DuckDBConfig — the config objects create_engine accepts; mention the Databricks introspection caveat if Plan 04 took Path B)
  </read_first>
  <action>
    Apply the semolina-docs-author skill. Update the four backend how-to pages
    (overview/snowflake/databricks/duckdb) and `web-api.rst` to `create_engine(<BackendConfig>(...))`
    + `register("name", engine)` + `Query.using(...)`. For each backend show the config-object and
    the `.semolina.toml` name arms. In `web-api.rst` (the app-backend integration example for the
    primary audience) wire the FastAPI/dependency example to a module-level
    `register("default", create_engine("default"))` and `Query.using("default")` in the handler.
    If Plan 04 shipped the Databricks NotImplementedError fallback (Path B), note in
    `databricks.rst` that metric-view introspection/codegen is pending the Foundry ADBC driver
    (query/execution still works). Remove all old-API examples; clean break, no deprecation
    notices. Run the humanizer pass. THEN run `just docs-build` and fix any broken cross-references
    or Sphinx warnings the migration introduced.
  </action>
  <verify>
    <automated>just docs-build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'pool_from_config\|get_pool' docs/src/how-to/backends/ docs/src/how-to/web-api.rst` returns 0 for every file
    - each backend page shows `create_engine(<Config>(...))` and a `.semolina.toml` name example (grep create_engine in each)
    - web-api.rst wires register("default", create_engine(...)) + Query.using("default")
    - if Plan 04 = Path B, databricks.rst notes the introspection-pending caveat
    - `just docs-build` exits 0 with no new broken-reference warnings
  </acceptance_criteria>
  <done>Backend + web-api docs use the create_engine/register(engine) API with per-dialect examples; Databricks caveat noted if applicable; docs build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs examples → reader copy-paste | Connection examples are copied verbatim by readers; must not model insecure secret handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-10 | Information Disclosure | docs modeling credential handling | mitigate | Examples source secrets from `.semolina.toml` / `SNOWFLAKE_*`/`DATABRICKS_*` env (the existing documented pattern); never hard-code a literal token/password in an example |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Docs-only edits; no package installs; phase adds zero new external packages (44-RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `grep -rc 'pool_from_config\|get_pool' docs/src` returns 0 across all edited pages; no 3-arg `register(.*dialect=` examples remain.
- All 12 pages use `create_engine` + `register("name", engine)` + `Query.using(...)`.
- `just docs-build` exits 0 with no new broken-reference/Sphinx warnings.
- semolina-docs-author skill applied (Diataxis classification + humanizer pass) on every rewritten page; "See also" cross-links resolve.
</verification>

<success_criteria>
- Every connection example across the 12 docs pages uses the new API (D1, D4); the old surface (pool_from_config / 3-arg register / get_pool) is gone (D5).
- connection-pools.rst is the canonical two-pattern guide; other pages cross-link to it rather than duplicate setup.
- Clean break — no deprecation notices anywhere ([[feedback_v03_engine_removal]]).
- `just docs-build` green; the docs match the shipped public surface from Plan 03.
</success_criteria>

<output>
Create `.planning/phases/44-engine-owns-the-pool/44-06-SUMMARY.md` when done.
</output>

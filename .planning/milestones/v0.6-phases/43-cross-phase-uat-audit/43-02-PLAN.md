---
phase: 43-cross-phase-uat-audit
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "REQUIREMENTS.md requirements list and Traceability table agree for STREAM-01 and STREAM-02 (both checked / both Complete)"
    - "ROADMAP SC1 wording names the actual report filename v0.5-MILESTONE-AUDIT.md"
    - "AUDIT-01 is flipped to [x]/Complete ONLY after the audit verdict is status: passed"
    - "No surfaced gap is left silently open (doc/traceability fixed here; any functional gap noted in Future Requirements)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Internally-consistent v0.5 requirements list + fully-populated Traceability table"
      contains: "AUDIT-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md AUDIT-01 row"
      to: ".planning/v0.5-MILESTONE-AUDIT.md status: passed"
      via: "verdict-gated close-out (flip last)"
      pattern: "AUDIT-01.*Complete"
---

<objective>
Reconcile the v0.5 traceability defects the Plan 01 audit confirmed, then close AUDIT-01 —
gated on the audit verdict being status passed. Three edits: (1) flip the stale STREAM-01
(REQUIREMENTS.md:14) and STREAM-02 (:15) checkboxes from unchecked to checked so the requirements
list agrees with the Traceability table (rows :63/:64 already say Complete); (2) amend ROADMAP SC1
wording to name the real report file v0.5-MILESTONE-AUDIT.md (the in-character traceability/wording
fix this phase exists to make); (3) LAST, flip AUDIT-01 (REQUIREMENTS.md:29 list unchecked and table
:68 Pending) to checked/Complete, gated on .planning/v0.5-MILESTONE-AUDIT.md carrying status passed.

Purpose: Discharges the v0.4.0 lesson #2 (stale/inconsistent traceability) and formally closes
AUDIT-01 only when the milestone is genuinely clean. Order matters: traceability fixes first,
verdict-gated requirement flip last.
Output: an internally-consistent REQUIREMENTS.md and a wording-corrected ROADMAP SC1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-cross-phase-uat-audit/43-RESEARCH.md
@.planning/phases/43-cross-phase-uat-audit/43-CONTEXT.md

<defect_lines>
Confirmed live against the current tree (line numbers stable):
- REQUIREMENTS.md:14 unchecked STREAM-01 list item (stale; table row :63 says Complete)
- REQUIREMENTS.md:15 unchecked STREAM-02 list item (stale; table row :64 says Complete)
- REQUIREMENTS.md:29 unchecked AUDIT-01 list item; table row :68 reads AUDIT-01 | Phase 43 | Pending
Resolution direction (from research, corroborated by ROADMAP checked Phase 39, 39-VERIFICATION passed,
39-02-SUMMARY close): the CHECKBOXES are the stale side — flip them to checked. The table is correct.
ROADMAP SC1 is at line 164: it names .planning/milestones/v0.5-UAT-AUDIT.md "(or equivalent path)"
— amend to name .planning/v0.5-MILESTONE-AUDIT.md (root, where complete-milestone reads it).
</defect_lines>

<gating_rule>
PITFALL (research #5): never flip AUDIT-01 before the verdict is passed. The executor MUST read
.planning/v0.5-MILESTONE-AUDIT.md frontmatter and confirm status passed BEFORE editing the
AUDIT-01 list checkbox or table row. If the report says gaps_found, do the STREAM-01/02 + ROADMAP
wording fixes (those are unconditional traceability fixes) but leave AUDIT-01 unchecked/Pending,
and instead record any deferred functional gap in REQUIREMENTS.md Future Requirements per SC3.
</gating_rule>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile STREAM-01/02 checkboxes + amend ROADMAP SC1 wording</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being modified — read lines 14-15 list checkboxes and 63-64 table rows to confirm the list-vs-table disagreement before editing)
    - .planning/ROADMAP.md (the file being modified — read SC1 at line 164 in the Phase 43 section)
    - .planning/phases/43-cross-phase-uat-audit/43-RESEARCH.md ("Lesson #2" resolution + "Report Format + Path (RESOLVED)" for the SC1 wording-fix rationale)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md, change the line 14 unchecked STREAM-01 list item to checked
    and the line 15 unchecked STREAM-02 list item to checked (the Traceability table rows :63/:64
    already say Complete — this makes the list agree with the table; the table is the correct side).
    Do NOT touch STREAM-03/DKGEN-04/DKGEN-05 rows — those are already consistent (checked + Complete).
    Update the REQUIREMENTS.md footer "Last updated" note to record the STREAM-01/02 checkbox
    reconciliation. In .planning/ROADMAP.md, amend Phase 43 SC1 (line 164) so it names the actual
    report path .planning/v0.5-MILESTONE-AUDIT.md instead of .planning/milestones/v0.5-UAT-AUDIT.md
    (the established prior pattern and the name /gsd-complete-milestone reads). Keep the edit minimal —
    a one-line wording fix; do not restructure the SC list. Do NOT flip AUDIT-01 in this task.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && test "$(grep -c '^- \[ \] \*\*STREAM-0[12]' .planning/REQUIREMENTS.md)" = "0" && grep -qE '^- \[x\] \*\*STREAM-01' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*STREAM-02' .planning/REQUIREMENTS.md && grep -q 'v0.5-MILESTONE-AUDIT.md' .planning/ROADMAP.md && ! grep -q 'v0.5-UAT-AUDIT.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*STREAM-0[12]' .planning/REQUIREMENTS.md` returns 0 (no stale unchecked STREAM-01/02 list items remain).
    - STREAM-01 and STREAM-02 list items are now checked and agree with their Complete table rows (:63/:64).
    - STREAM-03/DKGEN-04/DKGEN-05 rows are unchanged.
    - ROADMAP Phase 43 SC1 names `v0.5-MILESTONE-AUDIT.md`; no `v0.5-UAT-AUDIT.md` string remains in ROADMAP.md.
    - AUDIT-01 is NOT yet flipped (still unchecked/Pending after this task).
  </acceptance_criteria>
  <done>STREAM-01/02 list and table agree; ROADMAP SC1 names the real report file; AUDIT-01 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Close AUDIT-01 — gated on the audit verdict being status: passed</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/v0.5-MILESTONE-AUDIT.md (the Plan 01 output — read its frontmatter `status:` field FIRST; this gates the entire task)
    - .planning/REQUIREMENTS.md (the file being modified — AUDIT-01 list item at :29, Traceability table row at :68, the Coverage block at :70-73, and the footer)
    - .planning/phases/43-cross-phase-uat-audit/43-RESEARCH.md ("Lesson #2" closing steps + Pitfall #5 gating rule)
    - .planning/phases/43-cross-phase-uat-audit/43-CONTEXT.md (SC3 gap-disposition rule: doc/traceability fix in-phase; functional defer to v0.6)
  </read_first>
  <action>
    First read .planning/v0.5-MILESTONE-AUDIT.md frontmatter. If `status: passed`: in
    .planning/REQUIREMENTS.md flip the AUDIT-01 list item (:29) from unchecked to checked, and change
    the Traceability table row (:68) from Pending to Complete; update the footer "Last updated" line to
    record the AUDIT-01 close on a passed verdict. The Coverage block (Mapped 6, Unmapped 0) stays as-is —
    all six requirements are now Complete. If the report instead says `status: gaps_found`: do NOT flip
    AUDIT-01 — leave it unchecked/Pending, and for each functional gap the audit deferred, add a bullet
    to the REQUIREMENTS.md Future Requirements section tagged for v0.6 (per SC3, no gap left silently
    open). Do not invent gaps; per research the expected verdict is passed and the only finding is the
    STREAM-01/02 traceability defect already fixed in Task 1.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && if grep -qE '^status: passed' .planning/v0.5-MILESTONE-AUDIT.md; then grep -qE '^- \[x\] \*\*AUDIT-01' .planning/REQUIREMENTS.md && grep -qE '\| AUDIT-01 \| Phase 43 \| Complete \|' .planning/REQUIREMENTS.md; else grep -qE '^- \[ \] \*\*AUDIT-01' .planning/REQUIREMENTS.md; fi</automated>
  </verify>
  <acceptance_criteria>
    - The executor read `.planning/v0.5-MILESTONE-AUDIT.md` frontmatter before editing AUDIT-01.
    - If verdict is `passed`: AUDIT-01 list item is checked AND its Traceability row reads `| AUDIT-01 | Phase 43 | Complete |`.
    - If verdict is `gaps_found`: AUDIT-01 remains unchecked/Pending AND every deferred functional gap has a v0.6-tagged Future Requirements bullet.
    - No v0.5 Traceability row is left in an unresolved state (no silently-open gaps).
  </acceptance_criteria>
  <done>AUDIT-01 closed if and only if the audit verdict is passed; otherwise gaps are explicitly deferred per SC3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Read-only audit-reconciliation phase. No user input, no network, no auth, no data handling. Edits are confined to local `.planning/` markdown tracking files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Tampering (integrity) | AUDIT-01 flipped to Complete while the milestone has open gaps | mitigate | Verdict gate: Task 2 reads `.planning/v0.5-MILESTONE-AUDIT.md` `status:` and only flips AUDIT-01 on `passed`; the `<verify>` automation enforces the conditional. Process control, not a security control. No high-severity threat. |

No Spoofing / Repudiation / Information-disclosure / DoS / Elevation surface exists in markdown
tracking-file edits. No package installs occur, so the supply-chain threat is N/A.
</threat_model>

<verification>
- `grep -c '^- \[ \] \*\*STREAM-0[12]' .planning/REQUIREMENTS.md` returns 0.
- ROADMAP.md names `v0.5-MILESTONE-AUDIT.md` and no longer contains `v0.5-UAT-AUDIT.md`.
- AUDIT-01 is Complete iff the report `status:` is `passed`; otherwise gaps are deferred to v0.6.
</verification>

<success_criteria>
- REQUIREMENTS.md requirements list and Traceability table are internally consistent (STREAM-01/02 reconciled).
- ROADMAP SC1 wording names the real report file.
- AUDIT-01 closed only on a passed verdict; any functional gap explicitly deferred per SC3.
</success_criteria>

<output>
Create `.planning/phases/43-cross-phase-uat-audit/43-02-SUMMARY.md` when done.
</output>

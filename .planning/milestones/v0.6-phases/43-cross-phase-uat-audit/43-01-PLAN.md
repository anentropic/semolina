---
phase: 43-cross-phase-uat-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v0.5-MILESTONE-AUDIT.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "An empty-UAT-queue baseline is documented (audit-uat reports 0 outstanding items)"
    - "Every v0.5 success criterion (Phases 39-42) is verified against the shipped surface with a cited file:line or command, not 'VERIFICATION.md says so'"
    - "The v0.4.0 lesson #1 (API-name vs requirement-text drift) is shown CLEAN for v0.5 with grep evidence"
    - "The audit emits a reproducible verdict (passed | gaps_found)"
    - "Any surfaced gap is classified per SC3 (doc/traceability = fix in-phase; functional = defer to v0.6)"
  artifacts:
    - path: ".planning/v0.5-MILESTONE-AUDIT.md"
      provides: "Hand-authored v0.5 milestone audit report with YAML frontmatter and a status verdict"
      contains: "status:"
      min_lines: 60
  key_links:
    - from: ".planning/v0.5-MILESTONE-AUDIT.md"
      to: "src/semolina/cursor.py / python_renderer.py / cli/codegen.py / engines/"
      via: "cited file:line evidence per SC"
      pattern: "cursor\\.py|python_renderer\\.py|codegen\\.py|engines/"
---

<objective>
Produce the v0.5 cross-phase milestone audit report at `.planning/v0.5-MILESTONE-AUDIT.md`.
This is a REAL SC-by-SC audit (locked user decision), not a trivial UAT-queue sweep:
run `audit-uat` once as a documented "0 outstanding items" baseline, then verify each
v0.5 success criterion (Phases 39-42) against the OBSERVABLY SHIPPED SURFACE — actual API
names in `src/semolina/`, actual docs, actual CLI behaviour — citing a file:line or command
for every PASSED criterion. The report emits a `passed | gaps_found` verdict. That verdict
is the gate for the AUDIT-01 close-out in Plan 02, so it MUST be produced here first.

Purpose: Closes the v0.4.0 retrospective gap (no structured cross-phase audit was run) and
inoculates v0.5 against the two v0.4.0 hygiene failures (API-name drift; stale traceability).
Output: `.planning/v0.5-MILESTONE-AUDIT.md` (root `.planning/`, NOT `milestones/`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-cross-phase-uat-audit/43-CONTEXT.md
@.planning/phases/43-cross-phase-uat-audit/43-RESEARCH.md
@.planning/phases/43-cross-phase-uat-audit/43-VALIDATION.md
@.planning/milestones/v0.4.0-MILESTONE-AUDIT.md

<filename_resolution>
RESOLVED IN RESEARCH — do NOT deviate. The report filename is `v0.5-MILESTONE-AUDIT.md`
written to root `.planning/` (NOT `v0.5-UAT-AUDIT.md`, NOT under `milestones/`).
`milestone.complete` (milestone.cjs:173) globs exactly `.planning/v{version}-MILESTONE-AUDIT.md`
in root and moves it into `milestones/` itself; `artifacts.cjs:31` only matches the regex
`/^v\d+\.\d+(?:\.\d+)?-MILESTONE-AUDIT\.md$/i`. A `UAT-AUDIT` name is silently left un-archived.
Any task writing a filename containing `UAT-AUDIT` is wrong.
</filename_resolution>

<sc_evidence_map>
Grep-confirmed shipped surface (re-run each command as the observable check; cite the
file:line in the report). These were all re-verified live against the current tree:

Phase 39 (STREAM-01, STREAM-02):
- `fetch_record_batch()` -> `pyarrow.RecordBatchReader` : `src/semolina/cursor.py:164`
- lazy `__iter__`/`__next__` -> `Row` : `src/semolina/cursor.py:222` / `:237`
- (also note `fetch_arrow_table()` -> `pyarrow.Table` at `cursor.py:138`, the v0.4.0 method docs reference)

Phase 40 (STREAM-03):
- streaming how-to page exists : `docs/src/how-to/streaming.rst`

Phase 41 (DKGEN-04):
- path normalization : `src/semolina/cli/codegen.py:29` `def _normalize_database_path`
- codegen how-to amended : `docs/src/how-to/codegen.rst`

Phase 42 (DKGEN-05):
- DuckDB role source `DESCRIBE SEMANTIC VIEW` : `src/semolina/engines/duckdb.py:202`
- Snowflake role source `SHOW COLUMNS IN VIEW` : `src/semolina/engines/snowflake.py:328`
- Databricks role source `DESCRIBE TABLE EXTENDED ... AS JSON` : `src/semolina/engines/databricks.py:330`
- strict role map + raise : `src/semolina/codegen/python_renderer.py:22` (`_ROLE_TO_CLASS`), `:66` (`_field_class_for`), `:82` (dict lookup -> raises on KeyError)

Lesson #1 result: all five named-API claims match shipped code -> v0.4.0 `to_arrow()`-vs-`fetch_arrow_table()`
drift does NOT recur. Record this explicitly as PASSED with the citations above.
</sc_evidence_map>

<frontmatter_to_model>
Model the report frontmatter on `.planning/milestones/v0.4.0-MILESTONE-AUDIT.md` (read it):
`milestone: v0.5`, `audited: <ISO timestamp>`, `status: passed`, `scores:` (requirements 6/6
[STREAM-01/02/03, DKGEN-04/05, AUDIT-01], phases 4/4 [39-42; 43 is this audit, not counted],
integration deferred, flows deferred), `gaps:` (requirements/integration/flows lists),
`tech_debt:`, and a `nyquist:` block recording 4/4 (all of 39/40/41/42 have VALIDATION.md).
Section structure to mirror: `# Title`, `## Scope`, `## Requirements Coverage`,
`## Phase Verification Summary`, `## Cross-Phase Integration`, `## Tech Debt`,
`## Notes for Future Milestones`, `## Verdict`.
</frontmatter_to_model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture empty-queue baseline + verify every v0.5 SC against the shipped surface</name>
  <files>(read-only audit — writes no file; greps src/semolina/ + docs/src/how-to/ and runs gsd-sdk query audit-uat; findings feed Task 2)</files>
  <read_first>
    - .planning/phases/43-cross-phase-uat-audit/43-RESEARCH.md (per-SC observable-check map with grep citations; filename resolution; lesson #1/#2 analysis)
    - .planning/phases/43-cross-phase-uat-audit/43-VALIDATION.md (per-task verification map + self-validation invariants)
    - .planning/ROADMAP.md (Phases 39-42 success criteria at lines 101-157; Phase 43 contract at 159-169)
    - .planning/REQUIREMENTS.md (STREAM-01/02/03, DKGEN-04/05, AUDIT-01 text + Traceability table)
    - src/semolina/cursor.py, src/semolina/codegen/python_renderer.py, src/semolina/cli/codegen.py, src/semolina/engines/{duckdb,snowflake,databricks}.py (the shipped surface to grep)
  </read_first>
  <action>
    Run `gsd-sdk query audit-uat --raw` and record the result (expect `total_items: 0`) as the
    documented baseline — note "0 outstanding UAT items" but do NOT treat "All Clear" as the
    deliverable. Then perform the manual SC walk: re-run each grep in the sc_evidence_map above
    and confirm the cited line for each criterion of Phases 39-42. Specifically confirm:
    `cursor.py:164` fetch_record_batch -> RecordBatchReader (STREAM-01); `cursor.py:222/:237`
    __iter__/__next__ -> Row (STREAM-02); `docs/src/how-to/streaming.rst` exists (STREAM-03);
    `cli/codegen.py:29` _normalize_database_path + `docs/src/how-to/codegen.rst` amended (DKGEN-04);
    the three metadata queries (`engines/duckdb.py:202`, `engines/snowflake.py:328`,
    `engines/databricks.py:330`) and the strict role map/raise (`python_renderer.py:22/:66/:82`)
    (DKGEN-05). For the v0.4.0 lesson #1 check, confirm every requirement's named API exists under
    that exact name and record the result as CLEAN with grep citations. Cross-reference each SC
    against its 39/40/41/42-VERIFICATION.md as corroborating evidence (cite it alongside the code
    line, never instead of it). Classify any discrepancy you find per SC3: documentation/traceability
    gap (note it for Plan 02 to fix in-phase) vs functional gap (defer to v0.6 — note it for Plan 02
    to add to REQUIREMENTS.md Future Requirements). Per research, the only expected discrepancy is the
    STREAM-01/02 checkbox-vs-table defect (a traceability gap, reconciled in Plan 02) — do NOT fix
    REQUIREMENTS.md here; this task only AUDITS and records.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && gsd-sdk query audit-uat --raw | grep -q '"total_items": 0' && grep -nE "def (fetch_record_batch|__iter__|__next__)" src/semolina/cursor.py && grep -n "_normalize_database_path" src/semolina/cli/codegen.py && grep -rnE "DESCRIBE SEMANTIC VIEW|SHOW COLUMNS IN VIEW|DESCRIBE TABLE EXTENDED" src/semolina/engines/ | grep -v '^[^:]*:[0-9]*: *#' | grep -q "execute"</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-sdk query audit-uat --raw` returns `"total_items": 0` (baseline documented).
    - All five named-API greps from sc_evidence_map return a match at the cited line.
    - Every v0.5 SC (Phases 39-42) has an identified observable check (file:line or command), with no SC resting on VERIFICATION.md alone.
    - Lesson #1 (API-name vs requirement-text) is determined CLEAN with cited grep evidence.
    - Any non-traceability discrepancy is classified per SC3 (doc-fix-in-phase vs functional-defer-v0.6); the STREAM-01/02 checkbox defect is noted as the one traceability gap for Plan 02.
  </acceptance_criteria>
  <done>Baseline captured and every v0.5 SC has a cited observable check ready to write into the report; gap dispositions decided.</done>
</task>

<task type="auto">
  <name>Task 2: Write the v0.5-MILESTONE-AUDIT.md report with a reproducible verdict</name>
  <files>.planning/v0.5-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v0.4.0-MILESTONE-AUDIT.md (frontmatter shape + section structure + "Notes for Future Milestones" precedent — model the new report on this)
    - .planning/milestones/v0.3-MILESTONE-AUDIT.md (additional frontmatter reference for scores/gaps/tech_debt shape)
    - .planning/phases/43-cross-phase-uat-audit/43-RESEARCH.md ("Report Format + Path (RESOLVED)" + frontmatter shape block)
    - .planning/phases/43-cross-phase-uat-audit/43-VALIDATION.md (audit self-validation invariants the report must satisfy)
  </read_first>
  <action>
    Using the Write tool, create `.planning/v0.5-MILESTONE-AUDIT.md` in root `.planning/` (NOT
    `milestones/`, NOT named `UAT-AUDIT`). Frontmatter: `milestone: v0.5`, `audited:` with a real
    ISO timestamp, `status: passed` (all SCs verified clean per research — but set `gaps_found` instead
    if Task 1 surfaced an unresolvable functional gap), `scores:` requirements 6/6 + phases 4/4 +
    integration deferred + flows deferred, `gaps:` with empty lists on a clean audit, `tech_debt:`
    (carry only genuinely-open items; the Phase 42 IN-01/02/03 info items are out of scope per CONTEXT —
    mention in Notes, not as gaps), and a `nyquist:` block recording 4/4 (all of 39/40/41/42 have
    VALIDATION.md). Body sections mirroring v0.4.0: `## Scope` (table of Phases 39-42 with VERIFICATION
    source); `## Requirements Coverage` (per-REQ row with the cited file:line evidence from Task 1 —
    every PASSED row MUST carry a concrete citation, e.g. STREAM-01 -> `cursor.py:164`); `## Phase
    Verification Summary` (per-phase PASSED/gaps line); `## Cross-Phase Integration` (note the
    audit-uat empty-queue baseline + de-facto integration via the test suite); `## Tech Debt`;
    `## Notes for Future Milestones` (state lesson #1 CLEAN for v0.5 with grep evidence, contrasting
    v0.4.0 ARROW-02; document the STREAM-01/02 checkbox-vs-table drift as the one lesson-#2 finding being
    reconciled in this phase); `## Verdict` (explicit PASSED line that unblocks `/gsd-complete-milestone`).
    Do NOT flip AUDIT-01 or edit REQUIREMENTS.md here — that is Plan 02, gated on this verdict.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && test -f .planning/v0.5-MILESTONE-AUDIT.md && grep -qE "^status: (passed|gaps_found)" .planning/v0.5-MILESTONE-AUDIT.md && grep -qE "cursor\.py:164" .planning/v0.5-MILESTONE-AUDIT.md && grep -qE "python_renderer\.py" .planning/v0.5-MILESTONE-AUDIT.md && ! ls .planning/*UAT-AUDIT*.md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v0.5-MILESTONE-AUDIT.md` exists in root `.planning/` (NOT `milestones/`); no `*UAT-AUDIT*.md` file was created.
    - Frontmatter has `status:` (passed | gaps_found), `scores:` (requirements 6/6, phases 4/4), `gaps:`, and a `nyquist:` block recording 4/4.
    - Every PASSED requirement row in `## Requirements Coverage` cites a concrete file:line or command (verified: `cursor.py:164` and `python_renderer.py` appear in the report).
    - `## Notes for Future Milestones` states lesson #1 CLEAN for v0.5 and documents the STREAM-01/02 traceability finding.
    - Report has a `## Verdict` section with an explicit PASSED/gaps line.
  </acceptance_criteria>
  <done>`.planning/v0.5-MILESTONE-AUDIT.md` is written, evidence-cited, and carries a reproducible verdict that gates Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Read-only audit phase. No user input, no network, no auth, no data handling. Inputs are local source/docs/planning files; output is a local markdown report. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering (integrity) | The audit report asserting `status: passed` without evidence | mitigate | Validation Architecture evidence-gating: every PASSED SC cites a file:line/command (acceptance criteria enforce `cursor.py:164` + `python_renderer.py` citations); this is a process control, not a security control. No high-severity threat. |

No Spoofing / Repudiation / Information-disclosure / DoS / Elevation surface exists in a read-only
markdown-writing audit. No package installs occur, so the supply-chain (`T-{phase}-SC`) threat is N/A.
</threat_model>

<verification>
- `gsd-sdk query audit-uat --raw` returns `total_items: 0` (baseline).
- The five sc_evidence_map greps each match their cited line.
- `.planning/v0.5-MILESTONE-AUDIT.md` exists with a `status:` verdict and per-SC citations.
- No `*UAT-AUDIT*.md` artifact was created.
</verification>

<success_criteria>
- v0.5 milestone audit report written to `.planning/v0.5-MILESTONE-AUDIT.md` (root).
- Every v0.5 success criterion verified against the shipped surface with a cited observable check.
- Lesson #1 recorded CLEAN; the STREAM-01/02 traceability finding documented for Plan 02.
- A reproducible `passed | gaps_found` verdict emitted (gates AUDIT-01 close-out in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/43-cross-phase-uat-audit/43-01-SUMMARY.md` when done.
</output>

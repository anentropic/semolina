---
phase: 41
plan: 03
type: execute
wave: 2
depends_on:
  - 41-01
  - 41-02
files_modified:
  - tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr
  - docs/src/how-to/codegen.rst
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DKGEN-04
must_haves:
  truths:
    - "Running `uv run pytest tests/unit/codegen/test_codegen_e2e.py -x` exits 0 — the E2E test passes against the captured syrupy snapshot"
    - "A syrupy snapshot file exists for the file-backed-DuckDB codegen test and is committed to git"
    - "`docs/src/how-to/codegen.rst` documents `--database <path>` accepting `~`-expansion, relative paths, and absolute paths, plus a note that `semantic_views` is auto-installed on first run (needs network)"
    - "`just docs-build` (Sphinx `-W`) passes with no warnings on the amended file"
    - "`.planning/REQUIREMENTS.md` Traceability table row for DKGEN-04 is updated to `Complete` and the requirement text is amended to reflect the fixture-generation strategy (CONTEXT.md override)"
    - "The full quality gate is green: `prek run --all-files`, `just test`, `just docs-build`"
  artifacts:
    - path: "tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr"
      provides: "Captured syrupy snapshot for the file-backed codegen E2E test"
      contains: "test_codegen_file_backed_duckdb"
    - path: "docs/src/how-to/codegen.rst"
      provides: "Amended DuckDB section covering --database <path> behavior + INSTALL note"
      contains: "expanduser"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated DKGEN-04 wording + Traceability table entry"
      contains: "DKGEN-04"
  key_links:
    - from: "tests/unit/codegen/test_codegen_e2e.py"
      to: "tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr"
      via: "syrupy `assert result.output == snapshot` reads from this file"
      pattern: "test_codegen_file_backed_duckdb"
    - from: "docs/src/how-to/codegen.rst (DuckDB tab + new section)"
      to: "src/semolina/cli/codegen.py::_normalize_database_path"
      via: "documented behavior matches implementation (--database <path> with ~ expansion)"
      pattern: "--database"
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: "phases/41-duckdb-file-backed-codegen/"
      via: "row marks DKGEN-04 as Complete"
      pattern: "DKGEN-04 \\| Phase 41 \\| Complete"
---

<objective>
Wave 2 close-out: capture the E2E syrupy snapshot, amend the existing DuckDB
codegen how-to (NOT a new page), and update REQUIREMENTS.md to close DKGEN-04
with the wording amendment flagged in RESEARCH.md.

Purpose: Take the codegen surface end-to-end against the fixture .db now that
Plan 02 has shipped the INSTALL hook and path normalization; honor CONTEXT.md
success criterion #5 (amend, not new page); honor the audit trail by amending
REQUIREMENTS.md instead of silently overriding it; ship the docs build gate.

Output:
- `tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` — captured snapshot (committed to git).
- `docs/src/how-to/codegen.rst` — amended DuckDB section covering `--database` path behavior.
- `.planning/REQUIREMENTS.md` — DKGEN-04 wording amended and Traceability row updated to Complete.

This plan has `autonomous: false` because Task 2 (docs amendment) follows
Diataxis + humanizer workflow with a human-verifiable Sphinx build artifact,
and Task 1 includes a manual review of the captured snapshot before commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/semolina-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-duckdb-file-backed-codegen/41-CONTEXT.md
@.planning/phases/41-duckdb-file-backed-codegen/41-RESEARCH.md
@.planning/phases/41-duckdb-file-backed-codegen/41-VALIDATION.md
@.planning/phases/41-duckdb-file-backed-codegen/41-01-PLAN.md
@.planning/phases/41-duckdb-file-backed-codegen/41-02-PLAN.md
@CLAUDE.md
@docs/src/how-to/codegen.rst
@tests/unit/codegen/test_codegen_e2e.py
@tests/conftest.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture and verify the E2E syrupy snapshot</name>
  <files>tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr</files>
  <read_first>
    - tests/unit/codegen/test_codegen_e2e.py (the E2E test from Plan 01 Task 3)
    - tests/conftest.py (the `duckdb_file_backed_db` fixture from Plan 01 Task 1)
    - tests/integration/test_queries.py (existing syrupy snapshot idiom — establishes that .ambr files live next to test files in __snapshots__/)
    - .planning/phases/41-duckdb-file-backed-codegen/41-RESEARCH.md (Pattern 4 — expected codegen output shape)
  </read_first>
  <action>
    Run the E2E test with --snapshot-update to record the snapshot, then PAUSE for human review before commit.

    Step 1 — Record the snapshot:
    ```
    uv run pytest tests/unit/codegen/test_codegen_e2e.py --snapshot-update
    ```
    This writes `tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` (syrupy's default location, alongside the test file).

    Step 2 — Re-run WITHOUT --snapshot-update and confirm exit code 0:
    ```
    uv run pytest tests/unit/codegen/test_codegen_e2e.py -x
    ```

    Step 3 — Print the captured snapshot file content into the chat so the user can review it:
    ```
    cat tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr
    ```

    Step 4 — Hand off to human verification (see `<verify>` block). Do NOT git-add the snapshot until the user types `approved`.

    Notes:
    - Phase 42 will refine field-type inference; this phase locks in the currently-shipping output. The snapshot is regenerable via `--snapshot-update` when Phase 42 lands, so brittleness is bounded.
    - If the test cannot fetch the community extension (no network on the dev machine and a cold cache), run the cache-warming step from `.github/workflows/ci.yml:117-127` once locally, then retry.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/codegen/test_codegen_e2e.py -x</automated>
    Human-review: the captured snapshot should look like a normal codegen output for `sales_view`. Specifically:
    - Contains `from semolina import SemanticView, Metric, Dimension, Fact` (or similar imports header).
    - Contains `class SalesView(SemanticView, view="sales_view"):`.
    - Contains `revenue = Metric[...]` and `cost = Metric[...]` (both METRIC kind).
    - Contains `country = Dimension[...]` and `region = Dimension[...]` (both DIMENSION kind).
    - Contains `unit_price = Fact[...]` (FACT kind — exercises the FACTS clause from the fixture).

    If the snapshot looks correct, type `approved`. If anything is off (e.g., missing field kind, wrong class name, bogus imports), type `rejected` with notes and the executor will diagnose before recording the final snapshot.
  </verify>
  <done>Snapshot file exists, `pytest` passes WITHOUT `--snapshot-update`, user has typed `approved`, and the file is staged for commit.</done>
  <acceptance_criteria>
    - File `tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` exists.
    - `grep "test_codegen_file_backed_duckdb" tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` returns ≥1 match.
    - `grep "class SalesView" tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` returns ≥1 match.
    - `grep "Metric\[" tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` returns ≥2 matches (revenue + cost).
    - `grep "Dimension\[" tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` returns ≥2 matches (country + region).
    - `grep "Fact\[" tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` returns ≥1 match (unit_price).
    - `uv run pytest tests/unit/codegen/test_codegen_e2e.py -x` (without --snapshot-update) exits 0.
    - Human review approves the snapshot before commit.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the snapshot looks correct, or describe issues so the executor can re-run.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Amend docs/src/how-to/codegen.rst with --database path behavior</name>
  <files>docs/src/how-to/codegen.rst</files>
  <read_first>
    - docs/src/how-to/codegen.rst (full file — note the existing DuckDB tab content at lines 152-187 and the existing `--database` reference at line 63)
    - .claude/skills/semolina-docs-author/SKILL.md (mandatory — Diataxis how-to classification + humanizer pass)
    - .planning/phases/41-duckdb-file-backed-codegen/41-CONTEXT.md (specifics — what the how-to amendment should at minimum cover)
    - src/semolina/cli/codegen.py (verify final `--database` option help text matches what the docs claim)
  </read_first>
  <action>
    This is an AMENDMENT to an existing how-to page (CONTEXT.md success criterion #5 — NOT a new page). Apply the semolina-docs-author skill workflow:

    Step 1 — Classify: this is a how-to guide (`docs/src/how-to/`). Reader supplies their own .db file; we show the key concepts (path forms, INSTALL note). No new tutorial-style runnable scripts.

    Step 2 — Identify the insertion point. The current page has:
    - Line 63: a brief mention of `--database` in the "Choose a backend" section.
    - Lines 152-187: a DuckDB tab inside "Understand the generated output" with a DDL example and a `semolina codegen sales_view --backend duckdb --database /path/to/db` invocation.

    Step 3 — Add a new section AFTER "Choose a backend" (currently ending around line 67) and BEFORE "Understand the generated output" (currently around line 69). The new section header is:

    ```rst
    Point DuckDB codegen at a database file
    ---------------------------------------
    ```

    Step 4 — Write the new section. Cover the four items from CONTEXT.md "specifics":

    1. `--database <path>` usage — three accepted forms:
       - Absolute path (`/path/to/sales.duckdb`).
       - Relative path (`./sales.duckdb`) — resolved against the current working directory.
       - `~`-expanded path (`~/data/sales.duckdb`) — expanded to the user's home directory.
    2. The `DUCKDB_DATABASE` environment variable accepts the same forms.
    3. `:memory:` is still the implicit default when neither `--database` nor `DUCKDB_DATABASE` is set (note: based on RESEARCH.md, the engine's default; verify before claiming — if the CLI raises BadParameter when no value is given, document THAT behavior instead).
    4. The `semantic_views` community extension is auto-installed on the codegen connection on first run, which requires one-time network access to `community.duckdb.org`. Subsequent runs use the local cache at `~/.duckdb/extensions/`.

    Voice + style requirements (from semolina-docs-author skill):
    - Second person ("you").
    - Warm but efficient — no marketing language.
    - Code blocks use `.. code-block:: bash` directives (matches existing file).
    - Self-contained section with no external prerequisites beyond what the rest of the page assumes.

    Step 5 — Humanizer pass (mandatory for this section per CLAUDE.md and the skill SKILL.md). Eliminate:
    - "powerful", "seamlessly", "robust", "comprehensive", "effortlessly".
    - "delve", "leverage", "streamline", "it's worth noting", "ensure that".
    - "this allows you to", "this enables".
    - Max one em dash per paragraph.
    - No rule-of-three rhetorical lists.

    Step 6 — Do NOT touch the existing DuckDB tab (lines 152-187) unless the codegen output diverges from what is shown there. The Wave-2 captured snapshot in Task 1 should match the existing example (revenue / country / unit_price). If they DIVERGE (e.g., the fixture has region as well), update the existing DuckDB tab's example to match what users will actually see when they run codegen against the fixture-style DDL.

    Step 7 — Build verification:
    ```
    just docs-build
    ```
    Must exit 0 with no warnings (`-W` flag is in `just docs-build`).
  </action>
  <verify>
    <automated>just docs-build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Point DuckDB codegen at a database file" docs/src/how-to/codegen.rst` returns 1 match (new section header exists).
    - `grep -E "(expanduser|~/|tilde|relative path|absolute path)" docs/src/how-to/codegen.rst` returns ≥1 match in the new section (path-form coverage).
    - `grep -E "(community.duckdb.org|semantic_views.*extension.*install|auto-install)" docs/src/how-to/codegen.rst` returns ≥1 match (INSTALL note).
    - `grep "DUCKDB_DATABASE" docs/src/how-to/codegen.rst` returns ≥1 match (env-var alternative documented).
    - `just docs-build` exits 0 with no warnings (Sphinx `-W` strictness).
    - Humanizer pass cleanliness check — none of these tokens appear in the new section:
      ```
      grep -iE "(powerful|seamlessly|robust|comprehensive|effortlessly|delve|leverage|streamline|it.s worth noting|ensure that|this allows you to|this enables)" docs/src/how-to/codegen.rst
      ```
      should return no matches WITHIN the new section's line range. (Outside the new section, pre-existing copy is out of scope.)
    - Existing "See also" section at the bottom remains intact and references the same anchors.
    - `prek run --all-files` passes (rstcheck or rst-lint, if configured).
  </acceptance_criteria>
  <done>The how-to has a new section between "Choose a backend" and "Understand the generated output" covering `--database` path behavior + INSTALL note. Sphinx `-W` build passes. Humanizer pass applied.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md DKGEN-04 (text amendment + Traceability close)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — focus on DKGEN-04 at line 22 and the Traceability table at lines 57-68)
    - .planning/phases/41-duckdb-file-backed-codegen/41-CONTEXT.md (fixture strategy locked decision — "do NOT commit a binary `.db` blob")
    - .planning/phases/41-duckdb-file-backed-codegen/41-RESEARCH.md (Open Question 3 — explicit recommendation to amend REQUIREMENTS.md wording at phase close)
  </read_first>
  <action>
    Two edits to .planning/REQUIREMENTS.md:

    Edit A — Amend DKGEN-04 wording (line 22). The current text reads:

    ```
    - [ ] **DKGEN-04**: `semolina codegen --backend duckdb --database <path>` accepts filesystem paths (relative, `~`-expansion, absolute), opens read-only, runs `INSTALL/LOAD semantic_views` on the native codegen connection, and is verified against a fixture `.db` committed to the test suite
    ```

    Replace with (marking complete `[x]` and amending the wording per RESEARCH.md Open Question 3 recommendation):

    ```
    - [x] **DKGEN-04**: `semolina codegen --backend duckdb --database <path>` accepts filesystem paths (relative, `~`-expansion, absolute), opens read-only, runs `INSTALL/LOAD semantic_views` on the native codegen connection, and is verified against a fixture `.db` generated at test-collection time by a committed pytest fixture (`tests/conftest.py::duckdb_file_backed_db`)
    ```

    Edit B — Update the Traceability table row at line 66. Current row:

    ```
    | DKGEN-04 | Phase 41 | Pending |
    ```

    Replace with:

    ```
    | DKGEN-04 | Phase 41 | Complete |
    ```

    Edit C — Update the "Last updated" line at the bottom of the file. Current:

    ```
    *Last updated: 2026-05-15 — STREAM-03 marked Complete at Phase 40 close*
    ```

    Replace with:

    ```
    *Last updated: <today's date YYYY-MM-DD> — DKGEN-04 marked Complete at Phase 41 close; wording amended to reflect fixture-generation strategy (see Phase 41 CONTEXT.md)*
    ```

    Use the current date from the environment (today is 2026-05-15 per the system reminder, but executor should re-read at execution time in case of slip).

    Do NOT modify any other requirement row. Do NOT alter the "Coverage" counts (still 6 total, still 6 mapped — DKGEN-04 was already mapped, only its status changes).
  </action>
  <verify>
    <automated>grep -E "DKGEN-04.*generated at test-collection time" .planning/REQUIREMENTS.md && grep -E "DKGEN-04 \| Phase 41 \| Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DKGEN-04" .planning/REQUIREMENTS.md | grep -c "\[x\]"` returns 1 (DKGEN-04 is checked off).
    - `grep "DKGEN-04" .planning/REQUIREMENTS.md | grep "committed to the test suite"` returns 0 matches (old wording removed).
    - `grep "generated at test-collection time" .planning/REQUIREMENTS.md` returns 1 match (new wording present).
    - `grep "duckdb_file_backed_db" .planning/REQUIREMENTS.md` returns 1 match (fixture name documented for audit traceability).
    - `grep -E "\| DKGEN-04 \| Phase 41 \| Complete \|" .planning/REQUIREMENTS.md` returns 1 match (Traceability row updated).
    - `grep -E "\| DKGEN-04 \| Phase 41 \| Pending \|" .planning/REQUIREMENTS.md` returns 0 matches (old status removed).
    - The "Last updated" footer line mentions DKGEN-04 and Phase 41.
    - No other requirements rows are modified: `git diff .planning/REQUIREMENTS.md` shows changes confined to the DKGEN-04 line, the DKGEN-04 traceability row, and the footer.
  </acceptance_criteria>
  <done>REQUIREMENTS.md DKGEN-04 wording matches the fixture-generation strategy, the status is Complete, and the audit trail (CONTEXT.md decision, RESEARCH.md recommendation, this amendment) is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated snapshot file -> git history | Captured output committed to source control; reviewed before merge. |
| Documentation -> users following it | Users will follow the documented `--database` invocation pattern to point codegen at their own .db files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Tampering | Syrupy snapshot captures unexpected/incorrect output | mitigate | Task 1 is a `checkpoint:human-verify` — the captured snapshot is printed to the chat and the user must explicitly approve before commit. Pattern matches the existing tests/integration/test_queries.py syrupy idiom; brittleness is bounded by the `--snapshot-update` regenerate-on-flag affordance. |
| T-41-03-02 | Information Disclosure | Documentation reveals path-handling internals | accept | All documented behavior is public surface (CLI options, env var names, extension install). No secrets, credentials, or internal-only paths are documented. |
| T-41-03-03 | Repudiation | REQUIREMENTS.md wording amendment | mitigate | The amendment preserves the audit trail by documenting WHY in the footer ("wording amended to reflect fixture-generation strategy (see Phase 41 CONTEXT.md)"). The CONTEXT.md decision and RESEARCH.md recommendation are both committed to git, providing the reasoning chain. |
| T-41-03-04 | Information Disclosure | Sphinx build warnings leak in CI output | accept | The `-W` flag turns warnings into errors, so any leak makes the build fail loudly. No silent information disclosure path. |
</threat_model>

<verification>
After all three tasks complete:

1. `uv run pytest tests/unit/codegen/test_codegen_e2e.py -x` exits 0 (E2E snapshot matches).
2. `uv run pytest -x` exits 0 (full unit suite green).
3. `just test` exits 0 (full suite incl. jaffle-shop mock tests).
4. `just docs-build` exits 0 (Sphinx `-W` strict).
5. `prek run --all-files` exits 0 (ruff, format, basedpyright, rstcheck if configured).
6. `grep -E "\| DKGEN-04 \| Phase 41 \| Complete \|" .planning/REQUIREMENTS.md` returns 1 match.
7. Phase quality gate confirmed green before final commit.
</verification>

<success_criteria>
- E2E test passes against captured snapshot (human-reviewed before commit).
- `docs/src/how-to/codegen.rst` documents the `--database` path behavior (amendment, not new page) and Sphinx -W builds cleanly.
- `.planning/REQUIREMENTS.md` DKGEN-04 is `[x]` Complete, wording amended to match the fixture-generation strategy, Traceability table reflects the close, footer notes the amendment for audit.
- Full quality gate green: `prek run --all-files`, `just test`, `just docs-build`.
</success_criteria>

<output>
After completion, create `.planning/phases/41-duckdb-file-backed-codegen/41-03-SUMMARY.md`
documenting:
- The three files modified (snapshot file, docs page, REQUIREMENTS.md).
- The captured snapshot's high-level shape (class name, field kinds present).
- The exact REQUIREMENTS.md wording change (before/after diff).
- Confirmation that all quality gates passed.
- Any deviations from the plan and rationale (expected: none).
</output>

---
phase: 32-v03-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/semolina/conftest.py
  - src/semolina/query.py
  - tests/unit/test_query.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "conftest.py registers a MockPool with dialect='mock', not a MockEngine"
    - "query.py using() parameter is named pool_name, not engine_name"
    - "query.py docstrings say 'pool' where they previously said 'engine' for the registered backend"
    - "test_query.py creates MockPool instances instead of MockEngine for all 21 register() calls"
    - "test_query.py error assertions match the updated error message text from query.py"
    - "All 127 existing tests pass with zero DeprecationWarnings from test_query.py"
  artifacts:
    - path: "src/semolina/conftest.py"
      provides: "Doctest fixtures using MockPool + dialect='mock'"
      contains: "MockPool"
    - path: "src/semolina/query.py"
      provides: "using() method with pool_name parameter and pool-based docstrings"
      contains: "pool_name"
    - path: "tests/unit/test_query.py"
      provides: "Test suite using MockPool + dialect='mock' register() calls"
      contains: "from semolina.pool import MockPool"
  key_links:
    - from: "src/semolina/query.py"
      to: "tests/unit/test_query.py"
      via: "Error message text in using() TypeError must match pytest.raises match= strings"
      pattern: "requires pool name string"
---

<objective>
Clean up deprecated MockEngine usage and stale "engine" terminology across source and test files.

Purpose: Remove DeprecationWarnings from the test suite and align internal code with the v0.3 pool-based API that shipped in Phase 25. This is the final tech debt item from the v0.3 milestone audit.

Output: Three updated files with all MockEngine references converted to MockPool and all "engine" terminology updated to "pool" where it refers to the registered backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-v03-tech-debt-cleanup/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update conftest.py and query.py source files</name>
  <files>src/semolina/conftest.py, src/semolina/query.py</files>
  <read_first>
    src/semolina/conftest.py
    src/semolina/query.py
    src/semolina/pool.py
    src/semolina/registry.py
  </read_first>
  <action>
**conftest.py changes (8 edits):**

1. Line 4: Change "Injects a pre-configured MockEngine" to "Injects a pre-configured MockPool"
2. Line 16: Change `from semolina.engines.mock import MockEngine` to `from semolina.pool import MockPool`
3. Lines 39-40: Change "Registers a MockEngine as 'default'" to "Registers a MockPool as 'default'"
4. Line 45: Change "mock_engine: MockEngine with sample rows loaded" to "mock_pool: MockPool with sample rows loaded"
5. Line 57: Change `engine = MockEngine()` to `pool = MockPool()`
6. Lines 58-65: Change all `engine.load(` to `pool.load(`
7. Line 67: Change `register("default", engine)` to `register("default", pool, dialect="mock")`
8. Line 71: Change `doctest_namespace["mock_engine"] = engine` to `doctest_namespace["mock_pool"] = pool`

**query.py changes (13 edits):**

1. Line 47: Add a blank line between the code example closing and `Attributes:` to fix the doctest parse error. The line `    Attributes:` must have a blank line before it (after `            2`).
2. Line 53: Change "Engine name for lazy resolution" to "Pool name for lazy resolution"
3. Line 71: Change "engine binding" to "pool binding"
4. Line 305: Change `def using(self, engine_name: Any)` to `def using(self, pool_name: Any)`
5. Line 307: Change "Select engine for this query by name." to "Select pool for this query by name."
6. Line 309: Change "Engine is resolved lazily" to "Pool is resolved lazily"
7. Line 310: Change "engines are registered" to "pools are registered"
8. Line 314: Change "engine_name: Registered engine name" to "pool_name: Registered pool name"
9. Line 320: Change "TypeError: If engine_name is not a string" to "TypeError: If pool_name is not a string"
10. Line 329: Change `if not isinstance(engine_name, str):` to `if not isinstance(pool_name, str):`
11. Lines 330-332: Change error message from:
    `f"using() requires engine name string, got {type(engine_name).__name__}. "`
    `f"Register engine first: semolina.register('name', engine)"`
    to:
    `f"using() requires pool name string, got {type(pool_name).__name__}. "`
    `f"Register pool first: semolina.register('name', pool, dialect='snowflake')"`
12. Line 334: Change `return self._replace(_using=engine_name)` to `return self._replace(_using=pool_name)`

**DO NOT change:**
- The `execute()` method body (lines 382-439) -- it still needs the legacy engine fallback path
- The `_LegacyResultCursor`, `_NoOpConn`, `_NoOpPool` classes -- these support backward compat
- The `to_sql()` docstring reference to "Engine.to_sql(query)" -- that's documenting the old API as context
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && uv run pytest --doctest-modules src/semolina/query.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MockPool" src/semolina/conftest.py` returns 3 (import, docstring, namespace assignment)
    - `grep -c "MockEngine" src/semolina/conftest.py` returns 0
    - `grep "dialect=" src/semolina/conftest.py` matches `dialect="mock"`
    - `grep "def using" src/semolina/query.py` shows `pool_name` not `engine_name`
    - `grep "requires pool name string" src/semolina/query.py` matches the error message
    - `grep "Pool name for lazy resolution" src/semolina/query.py` matches the Attributes docstring
    - `uv run pytest --doctest-modules src/semolina/query.py -x -q` passes (no doctest parse error)
  </acceptance_criteria>
  <done>conftest.py uses MockPool with dialect="mock", query.py using() parameter is pool_name with pool-based docstrings, doctests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update test_query.py MockEngine to MockPool and verify full suite</name>
  <files>tests/unit/test_query.py</files>
  <read_first>
    tests/unit/test_query.py
    src/semolina/query.py
  </read_first>
  <action>
**Import change (1 edit):**
- Line 31: Change `from semolina.engines.mock import MockEngine` to `from semolina.pool import MockPool`

**Error message assertion updates (2 edits) -- must match the new error text from Task 1:**
- Line 501: Change `match="requires engine name string"` to `match="requires pool name string"`
- Line 503: Change `match="requires engine name string"` to `match="requires pool name string"`

**21 register() call site conversions -- for each site, apply these mechanical changes:**

For each test method that creates a MockEngine and registers it:
1. Change `engine = MockEngine()` to `pool = MockPool()`
   (Also `engine1`/`engine2` to `pool1`/`pool2`, `engine_empty`/`engine_filled` to `pool_empty`/`pool_filled`)
2. Change `engine.load(` to `pool.load(`
3. Change `semolina.register("name", engine)` to `semolina.register("name", pool, dialect="mock")`
4. Update ALL local variable references from `engine` to `pool` within each method

**Specific call sites (by class and line):**

TestQueryFetch (9 sites):
- test_fetch_returns_semolina_cursor (lines 527-529)
- test_fetch_row_attribute_access (lines 544-546)
- test_fetch_row_dict_access (lines 558-560)
- test_fetch_with_default_engine (lines 572-574) -- also update docstring "default engine" -> "default pool"
- test_fetch_with_named_engine (lines 586-588) -- also update docstring "named engine" -> "named pool"
- test_fetch_no_engine_raises (line 596-599): update docstring "no engines" -> "no pool or engine". DO NOT change the `match="No engine registered"` assertion -- this tests the fallback error path
- test_fetch_wrong_engine_name_raises (lines 602-610): update docstring. DO NOT change the `match="No engine registered with name 'other'"` assertion
- test_fetch_empty_query_raises (lines 617-618)
- test_fetch_empty_fixtures (lines 628-629)

TestQueryFetch continued:
- test_fetch_lazy_resolution (lines 644-646) -- update docstring "Engine should be resolved" -> "Pool should be resolved" and "Register engine AFTER" -> "Register pool AFTER"

TestQueryFetchIntegration (5 sites):
- test_full_pipeline (lines 664-673) -- update docstring "register engine" -> "register pool"
- test_multiple_engines (lines 697-703): engine1/engine2 -> pool1/pool2, update docstring and register calls
- test_query_reuse_with_different_engines (lines 722-729): engine1/engine2 -> pool1/pool2, update docstring

TestExecuteMethod (6 sites):
- test_execute_returns_semolina_cursor (lines 935-937)
- test_execute_cursor_has_row_objects (lines 948-955)
- test_execute_rows_support_iteration (lines 968-976)
- test_execute_rows_support_indexing (lines 988-996)
- test_execute_empty_vs_nonempty (lines 1008-1020): engine_empty -> pool_empty, engine_filled -> pool_filled

TestModelCentricWorkflow (1 site):
- test_model_centric_workflow_complete (lines 1065-1074): update comment "Register engine" -> "Register pool"

**Also update test class/method docstrings that say "engine" when meaning the registered backend:**
- TestQueryUsing class docstring (line 483): "per-query engine selection" -> "per-query pool selection"
- test_using_stores_engine_name docstring (line 494): "engine name" -> "pool name"
- TestQueryFetch class docstring (line 521): keep as-is (it says "registry integration" which is correct)
- TestQueryFetchIntegration class docstring (line 657): keep as-is

**DO NOT change these error-path assertions (they test the legacy engine fallback):**
- Line 478: `match="No engine registered"` -- KEEP
- Line 599: `match="No engine registered"` -- KEEP
- Line 610: `match="No engine registered with name 'other'"` -- KEEP

After all edits, run the full test suite to confirm zero failures and zero DeprecationWarnings.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && uv run pytest tests/unit/test_query.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MockEngine" tests/unit/test_query.py` returns 0
    - `grep -c "MockPool" tests/unit/test_query.py` returns at least 22 (1 import + 21 instantiations)
    - `grep 'dialect="mock"' tests/unit/test_query.py | wc -l` returns 21
    - `grep "requires pool name string" tests/unit/test_query.py` matches 2 lines
    - `grep "No engine registered" tests/unit/test_query.py` matches exactly 3 lines (the preserved error-path tests)
    - `uv run pytest tests/unit/test_query.py -x -q` passes all 127 tests
    - `uv run pytest tests/unit/test_query.py -x -q 2>&1 | grep -c "DeprecationWarning"` returns 0
    - `uv run pytest` full suite passes
    - `uv run basedpyright` passes
    - `uv run ruff check` passes
  </acceptance_criteria>
  <done>All 21 register() calls use MockPool + dialect="mock", error assertions updated, all 127 tests pass, zero DeprecationWarnings, full quality gates green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A -- This phase is a mechanical internal refactoring with no security implications. No new inputs, outputs, network calls, or trust boundaries are introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | N/A | Internal refactoring only | accept | No security-relevant changes |
</threat_model>

<verification>
1. `uv run pytest` -- full test suite passes (unit + doctests)
2. `uv run basedpyright` -- type checker passes
3. `uv run ruff check` -- linter passes
4. `uv run ruff format --check` -- formatter passes
5. `grep -r "MockEngine" src/semolina/conftest.py tests/unit/test_query.py` -- returns nothing
6. `uv run pytest tests/unit/test_query.py -W error::DeprecationWarning -x -q` -- passes (proves no deprecation warnings)
</verification>

<success_criteria>
- conftest.py creates MockPool, registers with dialect="mock", no MockEngine references
- query.py using() has pool_name parameter, all docstrings say "pool" not "engine" for registered backend
- query.py _Query class docstring has blank line before Attributes: (doctest parse fix)
- test_query.py uses MockPool for all 21 register() sites, error assertions match updated messages
- Three error-path tests (lines 478, 599, 610) preserve "No engine registered" assertions unchanged
- All quality gates pass: pytest, basedpyright, ruff check, ruff format
</success_criteria>

<output>
After completion, create `.planning/phases/32-v03-tech-debt-cleanup/32-01-SUMMARY.md`
</output>

---
phase: 31-fix-mockpool-doc-example
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/how-to/warehouse-testing.rst
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "test_filtered_query() example asserts len(rows) == 2, matching the 2-row fixture"
    - "Section narrative explains MockPool returns all fixture data regardless of .where() filters"
    - "Section cross-references the 'Inspect generated SQL' section for verifying filter SQL"
    - "sphinx-build -W passes with no warnings"
  artifacts:
    - path: "docs/src/how-to/warehouse-testing.rst"
      provides: "Corrected test_filtered_query() example with accurate assertions"
      contains: "assert len(rows) == 2"
  key_links:
    - from: "docs/src/how-to/warehouse-testing.rst (test_filtered_query)"
      to: "docs/src/how-to/warehouse-testing.rst (Inspect generated SQL)"
      via: "cross-reference in prose"
      pattern: "Inspect generated SQL"
---

<objective>
Fix the broken `test_filtered_query()` example in `warehouse-testing.rst` so it matches
actual MockPool behavior.

Purpose: The current example asserts `len(rows) == 1` and `rows[0].country == "US"`,
but MockPool's DBAPI `cursor.execute()` path returns ALL fixture rows regardless of
`.where()` filters. The existing assertion would fail at runtime. This fix closes the
remaining DOCS-03 gap from the v0.3 milestone audit.

Output: Corrected `warehouse-testing.rst` with accurate section title, narrative, code
example, and assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/semolina-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-fix-mockpool-doc-example/31-RESEARCH.md

@docs/src/how-to/warehouse-testing.rst
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite test_filtered_query section in warehouse-testing.rst</name>
  <files>docs/src/how-to/warehouse-testing.rst</files>
  <read_first>
    - docs/src/how-to/warehouse-testing.rst (current state of the file -- the broken section is lines 99-117)
    - tests/unit/test_pool.py (lines 327-352 -- test_execute_returns_all_fixture_data proves MockPool returns all rows)
  </read_first>
  <action>
Replace lines 99-117 of `docs/src/how-to/warehouse-testing.rst` (the "Test conditional filters" section) with the following corrected content. This is a minor fix (well under 50% of the page), so the full docs-author workflow (Diataxis + humanizer) is NOT required per CLAUDE.md rules.

The replacement content for lines 99-117:

```rst
Verify filter SQL
-----------------

``MockPool`` returns all fixture data regardless of ``.where()`` filters -- it does
not evaluate predicates. Use the result to confirm your query runs, and check
:ref:`inspect-generated-sql` to verify the filter appears in the generated SQL:

.. code-block:: python

   def test_filtered_query():
       cursor = (
           Sales.query()
           .metrics(Sales.revenue)
           .dimensions(Sales.country)
           .where(Sales.country == "US")
           .execute()
       )
       rows = cursor.fetchall_rows()
       assert len(rows) == 2  # MockPool returns all fixture rows
```

Additionally, add an RST label `.. _inspect-generated-sql:` immediately before the "Inspect generated SQL" heading (currently line 119) so the cross-reference resolves. Insert this label on the line before the heading:

```rst
.. _inspect-generated-sql:

Inspect generated SQL
---------------------
```

Key details:
- Section title changes from "Test conditional filters" to "Verify filter SQL"
- Assertion changes from `len(rows) == 1` to `len(rows) == 2` (matching the 2-row fixture in mock_pool)
- The `assert rows[0].country == "US"` line is removed (not meaningful since all rows are returned)
- One-sentence explanation that MockPool does not evaluate predicates
- Cross-reference to the existing "Inspect generated SQL" section via `:ref:` link
- The `.. _inspect-generated-sql:` label must have a blank line after it, before the heading
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && uv run sphinx-build -W docs/src docs/_build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "Verify filter SQL" docs/src/how-to/warehouse-testing.rst returns a match (new section title)
    - grep "assert len(rows) == 2" docs/src/how-to/warehouse-testing.rst returns a match (corrected assertion)
    - grep "assert len(rows) == 1" docs/src/how-to/warehouse-testing.rst returns NO match (old broken assertion removed)
    - grep "assert rows\[0\].country" docs/src/how-to/warehouse-testing.rst returns NO match (removed meaningless assertion)
    - grep "does not evaluate predicates" docs/src/how-to/warehouse-testing.rst returns a match (explanation present)
    - grep "inspect-generated-sql" docs/src/how-to/warehouse-testing.rst returns matches (label and ref)
    - uv run sphinx-build -W docs/src docs/_build exits 0
  </acceptance_criteria>
  <done>
    warehouse-testing.rst "Verify filter SQL" section has assertion `len(rows) == 2`,
    explains MockPool does not filter, cross-references "Inspect generated SQL" section,
    and sphinx-build -W passes clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- this is a documentation-only change with no code execution paths affected.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| N/A | N/A | N/A | N/A | Documentation-only change; no security surface |
</threat_model>

<verification>
1. `uv run sphinx-build -W docs/src docs/_build` exits 0
2. The "Verify filter SQL" section in warehouse-testing.rst asserts `len(rows) == 2`
3. The section explains MockPool returns all fixture data
4. The `:ref:` cross-reference to "Inspect generated SQL" resolves
5. Reading the page top-to-bottom has a coherent narrative flow
</verification>

<success_criteria>
- warehouse-testing.rst builds without warnings
- test_filtered_query() example has correct assertion matching MockPool behavior (2 rows from 2-row fixture)
- Section narrative accurately describes MockPool filter behavior
- Cross-reference to "Inspect generated SQL" section works
</success_criteria>

<output>
After completion, create `.planning/phases/31-fix-mockpool-doc-example/31-01-SUMMARY.md`
</output>

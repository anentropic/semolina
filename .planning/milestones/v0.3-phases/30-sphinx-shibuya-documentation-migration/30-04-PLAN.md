---
phase: 30-sphinx-shibuya-documentation-migration
plan: 04
type: execute
wave: 3
depends_on:
  - 30-02
  - 30-03
files_modified:
  - .github/workflows/docs.yml
  - CLAUDE.md
  - .claude/skills/semolina-docs-author/SKILL.md
autonomous: true
requirements:
  - SPHINX-03
  - SPHINX-04

must_haves:
  truths:
    - "CI workflow uses sphinx-build command and docs/_build artifact path"
    - "CLAUDE.md docs build gate references sphinx-build, not mkdocs"
    - "Docs author skill references RST and Sphinx, not MkDocs and Markdown tabs"
    - "sphinx-autoapi generates reference pages for all public semolina modules"
    - "sphinx-build -W exits 0 as final validation"
  artifacts:
    - path: ".github/workflows/docs.yml"
      provides: "CI workflow with Sphinx build and deploy"
      contains: "sphinx-build"
    - path: "CLAUDE.md"
      provides: "Updated project instructions"
      contains: "sphinx-build"
    - path: ".claude/skills/semolina-docs-author/SKILL.md"
      provides: "Updated docs author skill for RST"
      contains: "rst"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "docs/src/conf.py"
      via: "sphinx-build command"
      pattern: "sphinx-build.*docs/src.*docs/_build"
    - from: "CLAUDE.md"
      to: "docs/src/conf.py"
      via: "docs build gate command"
      pattern: "sphinx-build"
---

<objective>
Verify sphinx-autoapi reference generation, update CI workflow for Sphinx, update CLAUDE.md
and the docs author skill to reference Sphinx/RST instead of MkDocs/Markdown. Final cleanup.

Purpose: Complete the migration by updating all tooling references and ensuring CI deploys correctly.

Output: Updated CI workflow, CLAUDE.md, docs author skill. All project references point to Sphinx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-sphinx-shibuya-documentation-migration/30-RESEARCH.md
@.planning/phases/30-sphinx-shibuya-documentation-migration/30-02-SUMMARY.md
@.planning/phases/30-sphinx-shibuya-documentation-migration/30-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify autoapi reference and update CI workflow</name>
  <files>.github/workflows/docs.yml</files>
  <read_first>
    - .github/workflows/docs.yml (current MkDocs CI workflow)
    - docs/src/conf.py (autoapi configuration to verify)
    - .planning/phases/30-sphinx-shibuya-documentation-migration/30-RESEARCH.md (Pitfall 7: CI artifact path changes)
  </read_first>
  <action>
1. Verify sphinx-autoapi reference generation by running:
   ```
   uv run sphinx-build -W docs/src docs/_build
   ```
   Then check that `docs/_build/reference/` exists and contains HTML for semolina modules. If autoapi is not generating pages, debug by checking:
   - `autoapi_dirs` path is correct relative to conf.py location
   - `autoapi_root = "reference"` is set
   - No `autoapi_ignore` patterns are too broad

2. Update .github/workflows/docs.yml with these specific changes:

   a. Change the "Build docs" step command from:
      ```yaml
      run: uv run mkdocs build --strict
      ```
      to:
      ```yaml
      run: uv run sphinx-build -W docs/src docs/_build
      ```

   b. Remove the `env:` block under "Build docs" (DOCSEARCH_APP_ID and DOCSEARCH_API_KEY are MkDocs-specific and not needed by Sphinx).

   c. Change the "Upload pages artifact" step path from:
      ```yaml
      path: site/
      ```
      to:
      ```yaml
      path: docs/_build/
      ```

   Leave all other workflow configuration (triggers, permissions, concurrency, deploy job) unchanged.
  </action>
  <verify>
    <automated>grep -q "sphinx-build" .github/workflows/docs.yml && grep -q "docs/_build" .github/workflows/docs.yml && ! grep -q "mkdocs" .github/workflows/docs.yml && ! grep -q "DOCSEARCH" .github/workflows/docs.yml && test -d docs/_build/reference && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/docs.yml contains "uv run sphinx-build -W docs/src docs/_build"
    - .github/workflows/docs.yml contains "path: docs/_build/"
    - .github/workflows/docs.yml does NOT contain "mkdocs"
    - .github/workflows/docs.yml does NOT contain "DOCSEARCH_APP_ID" or "DOCSEARCH_API_KEY"
    - docs/_build/reference/ directory exists with HTML files after build
    - docs/_build/reference/semolina/index.html exists (autoapi root)
  </acceptance_criteria>
  <done>CI workflow updated for Sphinx, autoapi reference verified to generate correctly</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and docs author skill for Sphinx/RST</name>
  <files>CLAUDE.md, .claude/skills/semolina-docs-author/SKILL.md</files>
  <read_first>
    - CLAUDE.md (current project instructions referencing mkdocs)
    - .claude/skills/semolina-docs-author/SKILL.md (current docs author skill referencing MkDocs tabbed syntax)
  </read_first>
  <action>
1. Update CLAUDE.md with these specific changes:

   a. Quality gates section: Change docs build command from:
      `**Docs build:** \`uv run mkdocs build --strict\``
      to:
      `**Docs build:** \`uv run sphinx-build -W docs/src docs/_build\``

   b. Documentation standards > Content types section: Change:
      - "**Reference** (`docs/src/reference/`): Auto-generated via mkdocstrings. Do not hand-write API docs."
      to:
      - "**Reference** (`docs/src/reference/`): Auto-generated via sphinx-autoapi. Do not hand-write API docs."

   c. Documentation standards > Content types: Change how-to guides description from:
      "Use MkDocs tabbed content (`=== \"Snowflake\"` / `=== \"Databricks\"`) for SQL dialect examples."
      to:
      "Use sphinx-design tab-set with ``:sync-group: warehouse`` for SQL dialect examples."

   d. Project structure section: Change:
      "Docs: `docs/src/` (MkDocs Material with Diataxis tabs)"
      to:
      "Docs: `docs/src/` (Sphinx + shibuya theme with Diataxis tabs)"

   e. Docstring examples section: Change:
      "rendered as **markdown** by mkdocstrings"
      to:
      "rendered by sphinx-autoapi with napoleon"

      Also remove the `returns_multiple_items: false` note since that was mkdocstrings-specific:
      "Multi-line `Returns:` descriptions are safe — `returns_multiple_items: false` is set in `mkdocs.yml` so the full description is treated as one item."
      to:
      "Multi-line `Returns:` descriptions are safe — napoleon parses them as a single return value by default."

2. Update .claude/skills/semolina-docs-author/SKILL.md with these specific changes:

   a. Step 1 table: Change Reference row from:
      "**Auto-generated via mkdocstrings — do not hand-write**"
      to:
      "**Auto-generated via sphinx-autoapi — do not hand-write**"

   b. Step 2 > How-to guides section: Change tab syntax from:
      ```
      === "Snowflake"
          [Snowflake-specific content]

      === "Databricks"
          [Databricks-specific content]
      ```
      to:
      ```
      .. tab-set::
         :sync-group: warehouse

         .. tab-item:: Snowflake
            :sync: snowflake

            [Snowflake-specific content]

         .. tab-item:: Databricks
            :sync: databricks

            [Databricks-specific content]
      ```

   c. Quality checklist: Change:
      - "How-to snippets use `=== \"Snowflake\"` / `=== \"Databricks\"` tabs where dialect differs"
      to:
      - "How-to snippets use ``.. tab-set::`` with ``:sync-group: warehouse`` where dialect differs"

   d. Quality checklist: Change:
      - "`uv run mkdocs build --strict` passes"
      to:
      - "`uv run sphinx-build -W docs/src docs/_build` passes"

3. Run final validation: `uv run sphinx-build -W docs/src docs/_build` to confirm everything still builds cleanly after all changes.
  </action>
  <verify>
    <automated>grep -q "sphinx-build" CLAUDE.md && ! grep -q "mkdocs build" CLAUDE.md && grep -q "sphinx-autoapi" CLAUDE.md && grep -q "tab-set" .claude/skills/semolina-docs-author/SKILL.md && grep -q "sphinx-build" .claude/skills/semolina-docs-author/SKILL.md && ! grep -q "mkdocs build" .claude/skills/semolina-docs-author/SKILL.md && uv run sphinx-build -W docs/src docs/_build 2>&1 | tail -3 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "uv run sphinx-build -W docs/src docs/_build" (docs build gate)
    - CLAUDE.md contains "sphinx-autoapi" (not "mkdocstrings")
    - CLAUDE.md contains "Sphinx + shibuya theme" (not "MkDocs Material")
    - CLAUDE.md contains "sphinx-design tab-set" (not "MkDocs tabbed content")
    - CLAUDE.md does NOT contain "mkdocs build" anywhere
    - CLAUDE.md contains "napoleon" in docstring examples section
    - .claude/skills/semolina-docs-author/SKILL.md contains "tab-set" and "sync-group: warehouse"
    - .claude/skills/semolina-docs-author/SKILL.md contains "sphinx-build" in quality checklist
    - .claude/skills/semolina-docs-author/SKILL.md does NOT contain "mkdocs build"
    - `uv run sphinx-build -W docs/src docs/_build` exits 0
  </acceptance_criteria>
  <done>CLAUDE.md and docs author skill updated to reference Sphinx/RST throughout. Final build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline | GitHub Actions workflow -- no secrets exposed in build step after DOCSEARCH env removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | I (Information Disclosure) | CI env vars | mitigate | Remove DOCSEARCH_APP_ID and DOCSEARCH_API_KEY env vars from docs.yml since Sphinx does not use them |
</threat_model>

<verification>
- `uv run sphinx-build -W docs/src docs/_build` exits 0 (final gate)
- docs/_build/reference/semolina/ contains HTML reference pages
- .github/workflows/docs.yml references sphinx-build and docs/_build
- CLAUDE.md and skill file reference Sphinx/RST throughout
- No remaining references to mkdocs, mkdocstrings, or MkDocs Material
</verification>

<success_criteria>
Phase 30 migration complete: CI deploys Sphinx site, all project tooling references updated,
autoapi generates reference for all public modules, `sphinx-build -W` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/30-sphinx-shibuya-documentation-migration/30-04-SUMMARY.md`
</output>

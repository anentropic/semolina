---
phase: 29-documentation-update
plan: 03
type: execute
wave: 2
depends_on: ["29-01", "29-02"]
files_modified:
  - docs/src/how-to/queries.md
  - docs/src/how-to/filtering.md
  - docs/src/how-to/ordering.md
  - docs/src/how-to/models.md
  - docs/src/how-to/codegen.md
  - docs/src/how-to/warehouse-testing.md
  - docs/src/how-to/index.md
  - docs/src/explanation/semantic-views.md
autonomous: true
requirements: [DOCS-03, DOCS-04]

must_haves:
  truths:
    - "queries.md shows SemolinaCursor usage with fetchall_rows(), not direct iteration"
    - "queries.md shows query() shorthand alongside fluent chain"
    - "queries.md says 'pool' not 'engine' in .using() section"
    - "filtering.md execute examples use cursor.fetchall_rows()"
    - "ordering.md execute examples use cursor.fetchall_rows()"
    - "warehouse-testing.md references MockPool not MockEngine"
    - "No stale Engine/Credentials terms remain anywhere in docs/src/"
    - "API reference auto-generates for SemolinaCursor, Dialect, pool_from_config, register via mkdocs build"
  artifacts:
    - path: "docs/src/how-to/queries.md"
      provides: "Updated query how-to with SemolinaCursor and shorthand"
      contains: "fetchall_rows"
    - path: "docs/src/how-to/filtering.md"
      provides: "Updated filtering how-to with cursor usage"
      contains: "fetchall_rows"
    - path: "docs/src/how-to/ordering.md"
      provides: "Updated ordering how-to with cursor usage"
      contains: "fetchall_rows"
  key_links:
    - from: "docs/src/how-to/queries.md"
      to: "docs/src/how-to/backends/overview.md"
      via: "See also link"
      pattern: "backends/overview"
---

<objective>
Update all remaining how-to guides, minor pages, and run a final stale-term verification sweep to ensure zero Engine API references remain in docs. Verify API reference auto-generation covers v0.3 modules.

Purpose: Complete the documentation update by catching every remaining Engine/Credentials reference and updating result access patterns throughout.
Output: Updated `queries.md`, `filtering.md`, `ordering.md`, `models.md`, `codegen.md`, `warehouse-testing.md`, `index.md`, `semantic-views.md`. Zero stale terms across all docs.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
@.claude/skills/semolina-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-documentation-update/29-RESEARCH.md

@docs/src/how-to/queries.md
@docs/src/how-to/filtering.md
@docs/src/how-to/ordering.md
@docs/src/how-to/models.md
@docs/src/how-to/codegen.md
@docs/src/how-to/warehouse-testing.md
@docs/src/how-to/index.md
@docs/src/explanation/semantic-views.md
@src/semolina/__init__.py
@src/semolina/cursor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update queries.md with SemolinaCursor and shorthand</name>
  <files>docs/src/how-to/queries.md</files>
  <read_first>
    - docs/src/how-to/queries.md (current state -- has direct iteration, engine refs)
    - src/semolina/cursor.py (SemolinaCursor methods for accurate examples)
    - src/semolina/__init__.py (public API)
    - .claude/skills/semolina-docs-author/SKILL.md (mandatory doc writing workflow)
    - .planning/phases/29-documentation-update/29-RESEARCH.md (v0.3 patterns)
  </read_first>
  <action>
MAJOR UPDATE of `docs/src/how-to/queries.md`. Keep the overall structure and all query-building sections (metrics, dimensions, where, order_by, limit, to_sql, fork, incremental). Update these specific areas:

**1. "Override the engine" section** -- Rename to "Override the connection pool". Change:
- "Use `.using()` to select a different registered engine" -> "Use `.using()` to select a different registered pool"
- "Engine resolution is lazy -- it happens at `.execute()` time" -> "Pool resolution is lazy -- it happens at `.execute()` time"
- "If no `.using()` call is made, Semolina uses the engine registered as `\"default\"`." -> "If no `.using()` call is made, Semolina uses the pool registered as `\"default\"`."
- Change heading from "## Override the engine" to "## Override the connection pool"

**2. "Execute and read results" section** -- Rewrite to show SemolinaCursor:

```python
cursor = (
    Sales.query()
    .metrics(Sales.revenue)
    .dimensions(Sales.country)
    .execute()
)

for row in cursor.fetchall_rows():
    print(row.country, row.revenue)  # attribute access
    print(row["country"])  # dict-style access
```

Update description: "`.execute()` validates the query (at least one metric or dimension required), resolves the pool, runs the SQL, and returns a `SemolinaCursor`. Call `.fetchall_rows()` to get `Row` objects, or use the raw DBAPI methods (`.fetchall()`, `.fetchone()`) for tuples."

Add a sub-section showing all fetch methods:

```python
# Row objects (primary pattern)
rows = cursor.fetchall_rows()        # list[Row]
row = cursor.fetchone_row()          # Row | None
batch = cursor.fetchmany_rows(10)    # list[Row]

# Raw DBAPI tuples
raw = cursor.fetchall()              # list[tuple]
raw_one = cursor.fetchone()          # tuple | None

# Context manager (closes cursor + connection on exit)
with Sales.query(metrics=[Sales.revenue]).execute() as cursor:
    rows = cursor.fetchall_rows()
```

**3. Add "Use query shorthand" section** -- Insert after "Select dimensions" and before "Filter with .where()":

```
## Use query shorthand

Pass `metrics` and `dimensions` directly to `query()` as keyword arguments:

\```python
cursor = Sales.query(
    metrics=[Sales.revenue, Sales.cost],
    dimensions=[Sales.country],
).execute()
\```

This is equivalent to the fluent chain:

\```python
cursor = (
    Sales.query()
    .metrics(Sales.revenue, Sales.cost)
    .dimensions(Sales.country)
    .execute()
)
\```

Shorthand and builder methods are additive. Calling `.metrics()` after `query(metrics=...)` adds to the selection:

\```python
cursor = (
    Sales.query(metrics=[Sales.revenue])
    .metrics(Sales.cost)  # now selects both revenue and cost
    .dimensions(Sales.country)
    .execute()
)
\```
```

**4. Update all remaining `results = ...execute()` / `for row in results:` patterns** in the page:
- In "Build queries incrementally" section: `results = filtered.execute()` -> `cursor = filtered.execute()` and `for row in cursor.fetchall_rows():`
- In "Filter with .where()" section "Build filters conditionally" subsection: `results = query.execute()` -> `cursor = query.execute()`

**5. Update the "Inspect generated SQL" note:** Remove the line "For backend-specific SQL, call `engine.to_sql(query)` on a bound engine instance." -- that API no longer exists.

Apply humanizer pass to newly written sections. Existing query-building sections that are unchanged do not need the full humanizer pass.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/src/how-to/queries.md contains "fetchall_rows()"
    - docs/src/how-to/queries.md contains "fetchone_row()"
    - docs/src/how-to/queries.md contains "fetchmany_rows("
    - docs/src/how-to/queries.md contains "query(metrics="
    - docs/src/how-to/queries.md contains "SemolinaCursor"
    - docs/src/how-to/queries.md contains "Override the connection pool"
    - docs/src/how-to/queries.md does NOT contain "Override the engine"
    - docs/src/how-to/queries.md does NOT contain "for row in results:"
    - docs/src/how-to/queries.md does NOT contain "engine.to_sql("
    - uv run mkdocs build --strict exits 0
  </acceptance_criteria>
  <done>queries.md updated with SemolinaCursor usage, all fetch methods documented, query shorthand section added, .using() section says "pool" not "engine", no direct iteration patterns remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update minor how-to pages and run stale-term sweep</name>
  <files>docs/src/how-to/filtering.md, docs/src/how-to/ordering.md, docs/src/how-to/models.md, docs/src/how-to/codegen.md, docs/src/how-to/warehouse-testing.md, docs/src/how-to/index.md, docs/src/explanation/semantic-views.md</files>
  <read_first>
    - docs/src/how-to/filtering.md (current state -- check for direct iteration in execute examples)
    - docs/src/how-to/ordering.md (current state -- check for direct iteration)
    - docs/src/how-to/models.md (current state -- check for engine references)
    - docs/src/how-to/codegen.md (current state -- has credential reference links)
    - docs/src/how-to/warehouse-testing.md (current state -- has MockEngine, backend_engine fixtures)
    - docs/src/how-to/index.md (current state)
    - docs/src/explanation/semantic-views.md (current state -- check for "swapping the engine")
    - .planning/phases/29-documentation-update/29-RESEARCH.md (change inventory for each file)
  </read_first>
  <action>
Update each file with the specific changes listed below. These are MINOR updates (<20% content change per file) so full humanizer pass is NOT required.

**filtering.md:**
- Search for `results = (...).execute()` and `for row in results:` patterns
- The page has several execute examples in "Combine conditions with OR", "Combine conditions with AND", "Negate conditions with NOT", "Build complex nested conditions", and "Build filters conditionally" sections
- Replace each `results = (...).execute()` with `cursor = (...).execute()`
- Replace each `for row in results:` with `for row in cursor.fetchall_rows():`
- In "Build filters conditionally" section, replace `results = query.execute()` with `cursor = query.execute()`

Specific replacements (there are exactly 5 `results = ` assignments in the file):
1. Line ~268: `results = (Sales.query()...execute())` -> `cursor = (...).execute()`
2. Line ~301: `results = (Sales.query()...execute())` -> `cursor = (...).execute()`
3. Line ~322/338: Two more in the AND section examples -> same replacement
4. Line ~354: NOT section -> same replacement
5. Line ~392: Complex nested -> same replacement
6. Line ~426/449: Conditional building -> same replacement

**ordering.md:**
- In "Build 'top N' queries" section (~line 149): `results = query.execute()` -> `cursor = query.execute()` and `for row in results:` -> `for row in cursor.fetchall_rows():`

**models.md:**
- Scan for engine references. The file is mostly about field types. Check for any "engine" mentions and replace with "pool" if contextually appropriate. Likely no changes needed (verify).

**codegen.md:**
- The "Choose a backend" section says: "Credentials come from the same environment variables as the query engine (for example, `SNOWFLAKE_ACCOUNT` for Snowflake)."
- Change to: "Credentials come from environment variables (for example, `SNOWFLAKE_ACCOUNT` for Snowflake)."
- The links to snowflake.md and databricks.md in the "Choose a backend" section say "See [Snowflake connection](backends/snowflake.md) or [Databricks connection](backends/databricks.md) for the full list." -- keep these links, they still point to valid pages.
- In "See also" section, update link text from "Snowflake connection" to "Snowflake" and "Databricks connection" to "Databricks" to match the new page titles if they changed.

**warehouse-testing.md:**
- Replace ALL occurrences of "MockEngine" with "MockPool" in explanatory text
- In code examples showing `MockEngine.load()`, update to `MockPool().load()`
- In "Use the `backend_engine` fixture" section: rename to "Use the `backend_pool` fixture"... Actually, check the actual fixture names in conftest.py first. The fixtures still use `backend_engine`, `snowflake_engine`, `databricks_engine` names since the test code hasn't been updated yet. Keep the fixture names as-is in the doc but update the text descriptions:
  - "connects to Snowflake when recording, MockEngine when replaying" -> "connects to Snowflake when recording, MockPool when replaying"
  - "connects to Databricks when recording, MockEngine when replaying" -> "connects to Databricks when recording, MockPool when replaying"
- In the test example code, update `result = (...).execute()` -> `cursor = (...).execute()` and `rows = [dict(row.items()) for row in result]` -> `rows = [dict(row.items()) for row in cursor.fetchall_rows()]`
- In "Record snapshots" section: replace "SnowflakeEngine" with "a Snowflake pool" and "DatabricksEngine" with "a Databricks pool"
- In "Run in replay mode" section: replace "MockEngine loaded with TEST_DATA" with "MockPool loaded with TEST_DATA"
- Update "See also" at the bottom: "Your first query -- getting started with MockEngine" -> "Your first query -- getting started with MockPool"

**how-to/index.md:**
- No page renames, so no nav changes needed. Verify current content is accurate. No changes expected.

**explanation/semantic-views.md:**
- In the "Where Semolina fits" section, change "by swapping the engine" to "by swapping the connection pool" in the sentence about backend-agnostic queries. The actual text is "**Backend-agnostic queries** -- write once, run against Snowflake or Databricks by swapping the engine" -> change to "by changing the connection config"

**FINAL STALE-TERM SWEEP:**

After all files are updated, run these grep commands to verify zero stale terms:

```bash
grep -rn "MockEngine" docs/src/
grep -rn "SnowflakeEngine" docs/src/
grep -rn "DatabricksEngine" docs/src/
grep -rn "SnowflakeCredentials" docs/src/
grep -rn "DatabricksCredentials" docs/src/
grep -rn "for row in results" docs/src/
grep -rn "for row in query.execute()" docs/src/
```

If any matches remain, fix them. Exception: codegen.md may reference engine introspection since codegen still uses engines internally -- that's acceptable if it's about the codegen CLI tool itself, not about query-time connection.

**VERIFY API REFERENCE:**

Run `uv run mkdocs build --strict` and verify it exits 0. The reference pages are auto-generated by gen_ref_pages.py and will automatically include SemolinaCursor, Dialect, pool_from_config, and the updated register() signature since they have docstrings.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/semolina && uv run mkdocs build --strict 2>&1 | tail -5 && echo "---STALE TERM CHECK---" && grep -rn "MockEngine\|SnowflakeEngine\|DatabricksEngine\|SnowflakeCredentials\|DatabricksCredentials" docs/src/ || echo "No stale terms found"</automated>
  </verify>
  <acceptance_criteria>
    - docs/src/how-to/filtering.md does NOT contain "for row in results:"
    - docs/src/how-to/filtering.md contains "cursor.fetchall_rows()"
    - docs/src/how-to/ordering.md does NOT contain "for row in results:"
    - docs/src/how-to/ordering.md contains "cursor.fetchall_rows()"
    - docs/src/how-to/warehouse-testing.md does NOT contain "MockEngine"
    - docs/src/how-to/warehouse-testing.md contains "MockPool"
    - docs/src/how-to/codegen.md does NOT contain "query engine"
    - docs/src/explanation/semantic-views.md does NOT contain "swapping the engine"
    - grep -rn "MockEngine" docs/src/ returns no matches
    - grep -rn "SnowflakeEngine" docs/src/ returns no matches
    - grep -rn "DatabricksEngine" docs/src/ returns no matches
    - grep -rn "SnowflakeCredentials" docs/src/ returns no matches
    - grep -rn "DatabricksCredentials" docs/src/ returns no matches
    - uv run mkdocs build --strict exits 0
    - uv run --extra dev pytest exits 0
  </acceptance_criteria>
  <done>All remaining doc pages updated. Zero stale Engine/Credentials terms across entire docs/src/. All execute examples use cursor.fetchall_rows(). mkdocs build --strict and pytest both pass. API reference auto-generates for all v0.3 modules.</done>
</task>

</tasks>

<verification>
- `uv run mkdocs build --strict` passes with zero warnings
- `uv run --extra dev pytest` passes (existing tests still green)
- Full stale-term sweep returns zero matches:
  - `grep -rn "MockEngine" docs/src/` -> no matches
  - `grep -rn "SnowflakeEngine" docs/src/` -> no matches
  - `grep -rn "DatabricksEngine" docs/src/` -> no matches
  - `grep -rn "SnowflakeCredentials" docs/src/` -> no matches
  - `grep -rn "DatabricksCredentials" docs/src/` -> no matches
  - `grep -rn "for row in results" docs/src/` -> no matches
</verification>

<success_criteria>
All documentation across docs/src/ reflects v0.3 API exclusively. Zero stale Engine/Credentials/MockEngine terms. All execute examples use SemolinaCursor fetch methods. query() shorthand documented. API reference auto-generates for all new modules. mkdocs build --strict and pytest both pass.
</success_criteria>

<output>
After completion, create `.planning/phases/29-documentation-update/29-03-SUMMARY.md`
</output>

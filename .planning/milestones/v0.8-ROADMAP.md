# Roadmap: Semolina v0.8 — Hardening

**Status:** DRAFT (2026-09-06) — not yet the active milestone. Becomes the v0.8 section of
`.planning/ROADMAP.md` once v0.7 is tagged. Requirements: `v0.8-REQUIREMENTS.md`.
Source review: `.planning/research/2026-09-06-CODEBASE-REVIEW.md`.

**Milestone goal:** make the existing surface true to Semolina's core promise — one query API
that works identically across the three backends — before adding anything new.

**Working rule for every code fix:** failing test first, then the fix, in separate commits
(CLAUDE.md). Where a finding says "silently", the test asserts the *loud* behaviour (an
exception type and message), not merely the absence of the old bug.

## Sequencing

| Phase | Theme | Breaking? | Blocks |
|------|-------|-----------|--------|
| 51 | Ship v0.7 safely: release + CI gates | No | everything |
| 52 | Core object semantics | No | 53 |
| 53 | Portable result column names | **Yes** | 54-03, 56 |
| 54 | Filter semantics and SQL edge cases | Minor | — |
| 55 | Codegen hardening | No | — |
| 56 | Public surface and packaging | **Yes** | — |
| 57 | Test-suite structure | No | — |

Phases 54, 55 and 57 are independent of each other and can interleave. 53 and 56 carry the
breaking changes and ship together under one `0.8.0` tag with a migration guide (API-11).

**Phase 51 lands before v0.7 is tagged.** The current release path would publish
`semolina-0.6.0` from a `v0.7.0` tag with zero tests run.

## Phases

### 📝 v0.8 Hardening (Phases 51-57) — DRAFT

- [ ] Phase 51: Ship v0.7 Safely (5 plans) — release gating, PR trigger, docs in CI, `just test` parity, then tag v0.7
- [ ] Phase 52: Core Object Semantics (6 plans) — `Row`, equality, inheritance, `in_()`, cursor parity, DTO exactness
- [ ] Phase 53: Portable Result Column Names (5 plans) — alias every selected column to its Python field name on all backends
- [ ] Phase 54: Filter Semantics (5 plans) — `None`, LIKE escaping, `to_sql()` literals, metric-in-WHERE, introspect quoting
- [ ] Phase 55: Codegen Hardening (4 plans) — validity gate, credential redaction, exit-code parity, output plumbing
- [ ] Phase 56: Public Surface & Packaging (6 plans) — typed builder, public `Query`, `SemolinaError`, `SQLDialect`, `[cli]` extra, wheel contents, 0.8.0
- [ ] Phase 57: Test-Suite Structure (4 plans) — vacuous tests, cassettes, planning coupling, timing isolation

## Phase Details

### Phase 51: Ship v0.7 Safely

**Goal**: a tag cannot publish an untested or mis-versioned package, and a PR cannot merge
with a broken docs build or a silently weaker local test run.
**Depends on**: nothing.
**Requirements**: REL-01..08
**Success Criteria** (what must be TRUE):

  1. A `vX.Y.Z` tag with `X.Y.Z != semolina.__version__` fails release before `uv build`,
     naming both values (REL-01)
  2. `publish` runs only after CI is green on the tagged commit (REL-02)
  3. CI has a `pull_request` trigger; the coverage comment runs on PRs; `fail_under` is set
     just below today's 96% and the XML is uploaded (REL-03)
  4. `sphinx-build -W` runs in `ci.yml` on every push and PR (REL-04)
  5. `just test` and CI produce the same skip count; `MAINTAINER.md` says what to install
     (REL-05)
  6. The scope-fence resolves-here test skips with a message on a shallow clone outside CI
     (REL-06)
  7. Pre-commit ruff equals the locked ruff (REL-07); matrix covers 3.11–3.14 or documents
     the gap; `.python-version` names a released interpreter (REL-08)

**Plans**: 0/5 plans executed

Plans:
**Wave 1** (independent)

- [ ] 51-01-PLAN.md — release gating: tag/version assertion, `needs` on CI, drop the needless `uv sync --dev` before `uv build`
- [ ] 51-02-PLAN.md — `pull_request` trigger, coverage `fail_under` + artifact, docs build job in `ci.yml`
- [ ] 51-03-PLAN.md — `just test` parity with CI, `MAINTAINER.md`, pre-commit ruff alignment, matrix and `.python-version`
- [ ] 51-04-PLAN.md — scope-fence shallow-clone guard (test-first: run the test under a `--depth 1` clone in a tmp dir and assert Skipped)

**Wave 2** *(blocked on Wave 1)*

- [ ] 51-05-PLAN.md — bump `version` to `0.7.0`, tag, and let the new gates prove themselves on the real v0.7 release

### Phase 52: Core Object Semantics

**Goal**: the objects a user holds (`Row`, fields, queries, cursors) behave like ordinary
Python objects and fail loudly on misuse. No public API changes.
**Depends on**: Phase 51.
**Requirements**: CORE-01..12
**Success Criteria** (what must be TRUE):

  1. `copy`, `deepcopy` and `pickle` round-trip a `Row`; `Row` is hashable, has `.get()`,
     and is a registered `Mapping` (CORE-01); method-name collisions are reachable via item
     access and documented (CORE-02)
  2. `Sales.cost in [Sales.revenue]` is `False`; `OrderTerm`s and `_Query`s for different
     fields are unequal; the tautological `test_query.py` assertions now fail on the wrong
     field (CORE-03)
  3. Model inheritance works or refuses clearly, per D1 (CORE-04)
  4. `in_("US")` raises `TypeError`; a generator works; placeholders always equal params
     (CORE-05); `Engine.execute(_Query())` raises `ValueError` (CORE-06)
  5. Sync `close()` returns the connection even when `cursor.close()` raises and never masks
     the body's exception (CORE-07); iterate-then-fetch raises a named error on both cursors
     and the `acursor.py` docstring is corrected (CORE-08); every sync row method is
     pyarrow-guarded like the async twin (CORE-09)
  6. Duplicate selection raises in the builder and duplicate column names raise in `Row`
     (CORE-10); `timestamp` into `date` is refused by the fast-path check (CORE-11)
  7. Dead `pool` arg gone; sync reader bookkeeping matches async (CORE-12)

**Settled going in**: `OrderTerm` and `_Query` get `eq=False` plus an explicit `__eq__`
comparing field *identity*; `Field.__eq__` itself must keep returning a predicate — that is
the DSL.

**Plans**: 0/6 plans executed

Plans:
**Wave 1** (independent)

- [ ] 52-01-PLAN.md — `Row`: pickle/copy, hash, `get`, `Mapping` registration, duplicate-column raise
- [ ] 52-02-PLAN.md — equality: `OrderTerm`, `_Query`; rewrite the tautological tests
- [ ] 52-03-PLAN.md — `in_()` validation; `Engine.execute` validation; builder dedupe
- [ ] 52-04-PLAN.md — cursor parity: sync `close()` safety, mixed-consumption error, pyarrow guards on the row path, reader bookkeeping, docstring correction, `snowflake`/`databricks` extras decision
- [ ] 52-05-PLAN.md — DTO date/datetime exactness

**Wave 2** *(blocked on D1)*

- [ ] 52-06-PLAN.md — model inheritance (or the clear refusal), with `how-to/models.rst`

### Phase 53: Portable Result Column Names

**Goal**: `row.revenue` and a DTO field named `revenue` work identically on all three
backends. The single largest usability finding in the review.
**Depends on**: Phase 52 (dedupe and `Row` duplicate handling must exist, because aliasing
makes duplicate names a hard error).
**Requirements**: ALIAS-01..06
**Success Criteria** (what must be TRUE):

  1. The same query yields keys `revenue` and `country` on all three backends, proven by
     re-recorded Snowflake/Databricks cassettes and live DuckDB (ALIAS-01)
  2. A plain-named DTO converts everywhere with no alias (ALIAS-02); `codegen-dto` output
     is backend-independent except for driver-type differences (ALIAS-03)
  3. A legacy alias fails the schema check with a message naming the new key (ALIAS-04)
  4. README and the two how-to pages run unchanged on Snowflake; the column-key warning is
     gone (ALIAS-05)
  5. DuckDB no longer widens the projection silently (ALIAS-06)

**Design (recommended, confirmed at 53-01)**: the builder emits `AS "<python_field_name>"`
for every selected metric and dimension on Snowflake and Databricks; on DuckDB it wraps the
table function (`SELECT "revenue" AS "revenue", … FROM semantic_view(...)`) so a
`source=`-mapped field also returns under its Python name. One site:
`SQLBuilder._build_select_list`. `Dialect.metric_result_column_name` is retired from the
query path. Migration ships as `0.8.0` with a loud one-release shim: a field whose alias
looks like `AGG("X")` or `measure(x)` and matches no column raises
`SemolinaSchemaMismatchError` naming the plain key. Never accept both silently.

**Plans**: 0/5 plans executed

Plans:
**Wave 1**

- [ ] 53-01-PLAN.md — design checkpoint (D2): aliasing strategy, DuckDB wrapping, shim wording written to DECISIONS.md — **blocking human review**

**Wave 2** *(blocked on Wave 1)*

- [ ] 53-02-PLAN.md — builder: alias emission per dialect, test-first against `to_sql()` snapshots
- [ ] 53-03-PLAN.md — cursor/DTO: legacy-alias shim error; DuckDB wrapping and projection

**Wave 3** *(blocked on Wave 2)*

- [ ] 53-04-PLAN.md — re-record Snowflake and Databricks cassettes (credentialed, local); record a metric-in-WHERE and a Snowflake introspection while connected (feeds 54-03 and 57-03)
- [ ] 53-05-PLAN.md — `codegen-dto` drops backend aliases; README, tutorials and how-to pages updated

### Phase 54: Filter Semantics and SQL Edge Cases

**Goal**: every predicate the DSL can express compiles to SQL that means what the Python
reads as, or raises.
**Depends on**: 53-04 for the metric-in-WHERE recording (54-03 only).
**Requirements**: FILT-01..08
**Success Criteria** (what must be TRUE):

  1. `== None` → `IS NULL`, `!= None` → `IS NOT NULL`, `between(x, None)` raises, per D3
     (FILT-01)
  2. Prefix/suffix/iexact lookups escape wildcards with an `ESCAPE` clause per dialect;
     `like`/`ilike` pass through and say so; Databricks backslash is tested (FILT-02)
  3. `to_sql()` renders valid SQL for apostrophes, dates, decimals and `None` (FILT-03)
  4. Metric-in-WHERE is `HAVING` or rejected, per D4, and the tutorial is true (FILT-04)
  5. `introspect()` quotes names on all engines and DuckDB honours the schema prefix
     (FILT-05); dotted pre-quoted segments raise and pre-quoted segments are escaped
     (FILT-06); `?` in identifiers works on Databricks (FILT-07); error mapping is complete
     (FILT-08)

**Plans**: 0/5 plans executed

Plans:
**Wave 1** (independent)

- [ ] 54-01-PLAN.md — `None` handling (D3) and `between` validation
- [ ] 54-02-PLAN.md — LIKE escaping with `ESCAPE` per dialect; docstring truth
- [ ] 54-04-PLAN.md — `to_sql()` through `render_literal`; narrow the how-to warning
- [ ] 54-05-PLAN.md — introspect quoting and error mapping; pre-quoted segments; `?` in identifiers

**Wave 2** *(blocked on 53-04)*

- [ ] 54-03-PLAN.md — metric-in-WHERE (D4): `HAVING` or reject; DuckDB projection resolved the same way; tutorial updated

### Phase 55: Codegen Hardening

**Goal**: `semolina codegen` cannot emit a file that does not parse, cannot print a secret,
and behaves like `codegen-dto` on errors and output.
**Depends on**: nothing (runs alongside 52–54).
**Requirements**: GEN-01..10
**Success Criteria** (what must be TRUE):

  1. Invalid identifiers, case collisions and leading-digit view names exit non-zero naming
     the offender, and rendered source is `ast.parse`d before emission on both commands
     (GEN-01); ruff failure is loud (GEN-02)
  2. A config `ValidationError` never prints `input_value`; a test asserts the password is
     absent from stderr (GEN-03)
  3. Both commands share one exception-to-exit table covering missing extras, unmapped
     ADBC errors and dotted-backend constructor failures (GEN-04)
  4. `> models.py` and `--output` are byte-identical and pass `ruff format --check`
     (GEN-05); `codegen` has atomic `--output` and `--check` drops the `--model` workaround
     (GEN-06); output is cwd-independent (GEN-07)
  5. `~` expands before joining; env overrides are announced (GEN-08); `--check` handles
     untyped `Metric()` and reports probe fallback via exit code (GEN-09); dead
     `cli/utils.py` code is gone (GEN-10)

**Plans**: 0/4 plans executed

Plans:
**Wave 1** (independent)

- [ ] 55-01-PLAN.md — validity gate: name validation in `_build_model_context`, `ast.parse` before emit, loud ruff failure — test-first with a mocked engine returning `CLASS` and `"ORDER DATE"`
- [ ] 55-02-PLAN.md — credential redaction; shared exception-to-exit table
- [ ] 55-03-PLAN.md — output plumbing: `nl=False`, `--output`, atomic write, `--isolated`

**Wave 2** *(blocked on Wave 1)*

- [ ] 55-04-PLAN.md — config paths and precedence; `--check` fixes; dead code removal; `how-to/codegen.rst` for `--output`

### Phase 56: Public Surface & Packaging

**Goal**: the types, names and dependencies a user sees match a strongly typed library for
backend services.
**Depends on**: Phase 53 (ships in the same breaking release).
**Requirements**: API-01..11
**Success Criteria** (what must be TRUE):

  1. Builder methods are typed and a pyright negative-test file proves misuse is reported
     (API-01)
  2. `Query`, `Field`, `Engine`, `AsyncEngine` and the predicate composites are exported;
     every public module has `__all__` (API-02)
  3. `SemolinaError` exists and every Semolina error derives from it, per D5 (API-03)
  4. One thing is called `Dialect`; `to_sql()` defaults to the registered engine's dialect
     when `.using()` names one, per D7 (API-04); sync/async naming has a stated rule (API-05)
  5. `[cli]` extra per D6; base install imports no typer/rich/jinja2 (API-06); `import
     semolina` no longer imports `config` eagerly (API-07); `conftest.py` and `testing/` are
     out of the wheel with a smoke assertion (API-08)
  6. Dead type-ignores removed and `reportUnnecessaryTypeIgnoreComment` on (API-09);
     registry locked with a concurrency test (API-10)
  7. `/changelog/` exists on the docs site and 0.8.0 ships with a migration guide (API-11)

**Plans**: 0/6 plans executed

Plans:
**Wave 1**

- [ ] 56-01-PLAN.md — decision checkpoint (D5, D6, D7): `SemolinaError`, `[cli]` extra, `Dialect` rename — **blocking human review**; decisions written down

**Wave 2** *(blocked on Wave 1; plans independent of each other)*

- [ ] 56-02-PLAN.md — builder typing + pyright negative tests; `Query` rename and exports; `__all__`
- [ ] 56-03-PLAN.md — exception hierarchy; `get_engine` error type; `web-api.rst`
- [ ] 56-04-PLAN.md — packaging: `[cli]` extra, lazy `config` import, wheel contents, smoke steps
- [ ] 56-05-PLAN.md — `SQLDialect` rename and `to_sql()` default; registry lock; type-ignore cleanup; naming rule

**Wave 3** *(blocked on Wave 2 and Phase 53)*

- [ ] 56-06-PLAN.md — changelog page, 0.8 migration guide, tag `0.8.0`

### Phase 57: Test-Suite Structure

**Goal**: the suite proves what it claims and does not depend on one contributor's
environment.
**Depends on**: 53-04 for new cassettes (57-03 only).
**Requirements**: TEST-01..06
**Success Criteria** (what must be TRUE):

  1. The `test_engines.py` abstract-method tests fail if a method stops being abstract; the
     `to_sql` test is gone (TEST-01)
  2. Snowflake introspection has a cassette; copied cassettes are visibly marked or removed
     (TEST-02)
  3. The type-fidelity artifact lives under `tests/`; a pin bump cannot fail the byte
     comparison; the duckdb-bump PR triggers CI (TEST-03)
  4. Timing tests run outside `-n auto` or carry reasoned margins; extension install retries
     once (TEST-04)
  5. `semolina-jaffle-shop/` is type-checked in CI (TEST-05); root markers are used or
     removed (TEST-06)

**Plans**: 0/4 plans executed

Plans:
**Wave 1** (independent)

- [ ] 57-01-PLAN.md — vacuous tests fixed; marker cleanup; jaffle-shop typecheck job
- [ ] 57-02-PLAN.md — type-fidelity artifact relocation; bump-PR CI trigger
- [ ] 57-04-PLAN.md — timing-test isolation and install retry

**Wave 2** *(blocked on 53-04)*

- [ ] 57-03-PLAN.md — cassette recordings and copied-cassette marking

## Decisions (blocking checkpoints)

| # | Decision | Recommendation | Where |
|---|----------|----------------|-------|
| D1 | Model inheritance: support or refuse clearly? | Support, with `abstract = True` bases | before 52-06 |
| D2 | Result aliasing strategy and migration shim wording | Alias to Python field names everywhere; loud shim for one release | 53-01 |
| D3 | `== None`: rewrite to `IS NULL` or raise? | Rewrite for `==`/`!=`; raise elsewhere | 54-01 |
| D4 | Metric in WHERE: HAVING or reject? | Verify live in 53-04 first; HAVING if both accept it, else reject | 54-03 |
| D5 | Introduce `SemolinaError` base? | Yes | 56-01 |
| D6 | Move CLI deps to `[cli]` extra? | Yes, at 0.8.0 | 56-01 |
| D7 | Rename the ABC `Dialect` → `SQLDialect`? | Yes, alias kept one release | 56-01 |

## Explicitly not planned

- `.offset()` — the keyset-pagination rationale in the docs is sound.
- The Databricks decimal-as-string and Snowflake `FIXED(…,0)` → `Decimal` policies —
  documented, measured, consistent.
- Replacing `adbc-poolhouse` as the hard dependency — revisit only if 56-04's lazy import
  is not enough.

## Size estimate

35 plans across 7 phases. Phases 51 and 55 are a few days each; 52 and 56 about a week
each; 53 needs design time plus a credentialed recording session; 54 and 57 are a few days
each but 54-03 and 57-03 wait on that same session. Two credentialed sessions in total
(Snowflake + Databricks) cover every recording need in the milestone.

## Progress

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 51. Ship v0.7 Safely | v0.8 | 0/5 | Not started | |
| 52. Core Object Semantics | v0.8 | 0/6 | Not started | |
| 53. Portable Result Column Names | v0.8 | 0/5 | Not started | |
| 54. Filter Semantics | v0.8 | 0/5 | Not started | |
| 55. Codegen Hardening | v0.8 | 0/4 | Not started | |
| 56. Public Surface & Packaging | v0.8 | 0/6 | Not started | |
| 57. Test-Suite Structure | v0.8 | 0/4 | Not started | |

---

*Roadmap drafted 2026-09-06 from the codebase review; Phases 51-57 mapped to all 61 v0.8 requirements*

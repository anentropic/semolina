---
phase: 14-documentation-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - docs/src/how-to/index.md
  - docs/src/how-to/models.md
  - docs/src/how-to/queries.md
  - docs/src/how-to/filtering.md
  - docs/src/how-to/ordering.md
  - docs/src/how-to/backends/overview.md
  - docs/src/how-to/backends/snowflake.md
  - docs/src/how-to/backends/databricks.md
  - docs/src/how-to/codegen.md
  - docs/src/how-to/warehouse-testing.md
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "All how-to guides follow diataxis how-to structure: goal-oriented, practical directions for competent users"
    - "How-to guides show SQL output using tabbed content (Snowflake | Databricks) where applicable"
    - "Backend guides cover Snowflake vs Databricks differences and connection setup"
    - "Each how-to guide is self-contained with See also links at bottom"
    - "All rewritten prose passes humanizer tone review (warm but efficient, second person)"
    - "Existing code examples are preserved verbatim; only prose is rewritten"
  artifacts:
    - path: "docs/src/how-to/models.md"
      provides: "How-to: define SemanticView models with Metric, Dimension, Fact fields"
    - path: "docs/src/how-to/queries.md"
      provides: "How-to: build queries with .metrics(), .dimensions(), .order_by(), .limit()"
    - path: "docs/src/how-to/filtering.md"
      provides: "How-to: filter with field operators and boolean composition"
    - path: "docs/src/how-to/backends/overview.md"
      provides: "How-to: choose and configure a backend with tabbed SQL comparison"
    - path: "docs/src/how-to/backends/snowflake.md"
      provides: "How-to: connect to Snowflake"
    - path: "docs/src/how-to/backends/databricks.md"
      provides: "How-to: connect to Databricks"
  key_links:
    - from: "docs/src/how-to/queries.md"
      to: "docs/src/how-to/filtering.md"
      via: "See also link"
      pattern: "filtering"
    - from: "docs/src/how-to/backends/overview.md"
      to: "docs/src/how-to/backends/snowflake.md"
      via: "See also link"
      pattern: "snowflake"
---

<objective>
Rewrite all how-to guide pages using the diataxis-documentation skill for structure and the humanizer skill for tone. Add tabbed SQL dialect examples (Snowflake | Databricks) where applicable.

Purpose: Transform existing guides from mixed tutorial/explanation content into proper Diataxis how-to guides: goal-oriented, practical, for competent users who know what they want to do. Add side-by-side SQL dialect examples using MkDocs Material tabbed content.

Output: 10 rewritten how-to guide pages with consistent diataxis structure, tabbed SQL examples, and human-sounding prose.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-overhaul/14-CONTEXT.md
@.planning/phases/14-documentation-overhaul/14-RESEARCH.md
@.planning/phases/14-documentation-overhaul/14-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite core how-to guides (models, queries, filtering, ordering)</name>
  <files>
    docs/src/how-to/models.md
    docs/src/how-to/queries.md
    docs/src/how-to/filtering.md
    docs/src/how-to/ordering.md
    docs/src/how-to/index.md
  </files>
  <action>
    **Prerequisite: Load skills.** Read:
    1. `~/.claude/skills/diataxis-documentation/SKILL.md`
    2. `~/.claude/skills/diataxis-documentation/references/how-to-guides.md` for how-to-specific rules
    3. `~/.claude/skills/humanizer/SKILL.md` for tone review

    **Workflow for each how-to page:**

    1. Read the existing page content at its new location (moved by Plan 01)
    2. Extract all code examples verbatim (preserve unchanged per user decision)
    3. Rewrite ALL prose from scratch following diataxis how-to guide rules:
       - Goal-oriented: each page addresses a specific task ("How to define models", "How to filter queries")
       - Practical: show the key concept with illustrative snippets, reader supplies own setup
       - For competent users: don't re-explain basics, link to tutorials for that
       - Direct: get to the point quickly, minimal preamble
    4. Add tabbed SQL output where applicable using MkDocs Material syntax:
       ```markdown
       === "Snowflake"

           ```sql
           SELECT AGG("revenue"), "country"
           FROM "sales"
           ```

       === "Databricks"

           ```sql
           SELECT MEASURE(`revenue`), `country`
           FROM `sales`
           ```
       ```
    5. Add "See also" section at bottom of each page with 2-4 cross-references to related pages
    6. Run humanizer tone check on rewritten prose

    **Page-specific guidance:**

    **models.md:**
    - Goal: define a SemanticView subclass with Metric, Dimension, Fact fields
    - Show field types and their purposes
    - Mention view="name" parameter
    - Add tabbed SQL showing what the model represents in each dialect
    - See also: queries, filtering, API reference for fields

    **queries.md:**
    - Goal: build and execute queries using .metrics(), .dimensions(), .order_by(), .limit()
    - Show method chaining pattern
    - Show .execute() and Result iteration
    - Add tabbed SQL output showing generated queries
    - See also: filtering, ordering, models

    **filtering.md:**
    - Goal: filter queries with field operators (==, !=, >, <, >=, <=) and named methods (.between(), .in_(), .like(), etc.)
    - Show boolean composition with & (AND), | (OR), ~ (NOT)
    - Show .where() usage on queries
    - Add tabbed SQL showing generated WHERE clauses
    - This page already has substantial content from Phase 13.1 -- preserve the field method reference table and code examples, rewrite surrounding prose
    - See also: queries, models, API reference for filters

    **ordering.md:**
    - Goal: order and limit query results
    - Show .order_by() with Field.asc() and Field.desc()
    - Show NullsOrdering options
    - Show .limit()
    - Add tabbed SQL for ORDER BY clause
    - See also: queries, filtering

    **how-to/index.md:**
    - Update section landing page (from Plan 01 placeholder)
    - Brief description: "Practical guides for specific tasks. Each guide focuses on one topic and assumes you already know the basics."
    - Categorized links to all how-to pages

    **Writing constraints (from CONTEXT.md locked decisions):**
    - Illustrative snippets (not full runnable programs -- reader supplies setup)
    - SQL output shown in how-to guides using tabbed content (Snowflake | Databricks)
    - Self-contained pages with "See also" links
    - Second person ("you"), warm but efficient
    - Assume user is a data/analytics engineer comfortable with SQL and Python
  </action>
  <verify>
    Run `uv run mkdocs build --strict` -- must succeed.
    For each page verify:
    - Has a clear goal statement or heading indicating what the reader will accomplish
    - Code examples preserved from original
    - At least one tabbed SQL example (queries.md, filtering.md, ordering.md -- models.md if applicable)
    - "See also" section at bottom with cross-references
    - All prose uses "you" (second person)
    - No AI vocabulary words from humanizer checklist
    - No tutorial-style "you will learn" language -- how-to guides are "here's how to do X"
  </verify>
  <done>
    Four core how-to guides (models, queries, filtering, ordering) rewritten with diataxis how-to structure. Tabbed SQL examples show Snowflake and Databricks side by side. Each page self-contained with See also links. Section index updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite backend and tooling how-to guides</name>
  <files>
    docs/src/how-to/backends/overview.md
    docs/src/how-to/backends/snowflake.md
    docs/src/how-to/backends/databricks.md
    docs/src/how-to/codegen.md
    docs/src/how-to/warehouse-testing.md
  </files>
  <action>
    **Skills already loaded from Task 1.** Re-read `~/.claude/skills/humanizer/SKILL.md` if needed for tone reference.

    **Same workflow as Task 1:** Read existing content, extract code examples, rewrite prose from scratch, add tabbed SQL where applicable, add "See also" links, humanizer tone check.

    **Page-specific guidance:**

    **backends/overview.md:**
    - Goal: understand the differences between Snowflake and Databricks backends and choose one
    - This is the key DOCS-05 page: backend comparison
    - Use tabbed SQL content extensively to show Snowflake vs Databricks SQL differences side by side:
      - SELECT syntax (AGG vs MEASURE)
      - Identifier quoting (double quotes vs backticks)
      - GROUP BY handling
    - Show engine registration pattern for each backend
    - Do NOT explain what semantic views are (link to explanation/ for that)
    - See also: snowflake, databricks, explanation/semantic-views

    **backends/snowflake.md:**
    - Goal: connect Cubano to Snowflake and run queries
    - Show SnowflakeEngine setup with credentials
    - Show SnowflakeCredentials loading pattern
    - Keep practical: credential loading, engine registration, common connection options
    - See also: overview, databricks, warehouse-testing

    **backends/databricks.md:**
    - Goal: connect Cubano to Databricks and run queries
    - Show DatabricksEngine setup with credentials
    - Show DatabricksCredentials loading pattern
    - Same structure as snowflake.md for consistency
    - See also: overview, snowflake, warehouse-testing

    **codegen.md:**
    - Goal: generate warehouse-native SQL from Cubano models using the CLI
    - Show `cubano codegen` command with flags (--backend, --output)
    - Show example input model and generated output
    - Add tabbed content for generated Snowflake SQL vs Databricks YAML
    - See also: models, backends/overview

    **warehouse-testing.md:**
    - Goal: test queries against real warehouses using snapshot recording/replay
    - Show backend_engine fixture, --snapshot-update workflow, replay mode
    - This page was recently corrected in Phase 13-03 -- preserve the corrected file paths and constant names
    - Rewrite prose but keep technical accuracy from Phase 13 corrections
    - See also: backends/overview, tutorials/first-query

    **Humanizer pass on all 5 pages:** After writing, re-read and fix any AI patterns. Pay particular attention to:
    - Promotional language around Cubano features ("powerful", "seamless")
    - Vague claims about backend differences without specifics
    - Formulaic "Challenges and..." sections
    - Em dash overuse
  </action>
  <verify>
    Run `uv run mkdocs build --strict` -- must succeed.
    For each page verify:
    - backends/overview.md has at least 2 tabbed SQL comparison blocks
    - snowflake.md and databricks.md show credential loading and engine registration
    - codegen.md shows CLI invocation and generated output
    - warehouse-testing.md has correct file paths from Phase 13 corrections
    - All pages have "See also" sections
    - No AI vocabulary words from humanizer checklist
    - No promotional language
    Run all quality gates: `uv run basedpyright && uv run ruff check && uv run --extra dev pytest` -- all pass (docs changes should not affect code tests, but verify no doctest breakage)
  </verify>
  <done>
    Five backend/tooling how-to guides rewritten with diataxis structure. Backend overview has tabbed SQL comparisons for DOCS-05. All pages self-contained with See also links. All quality gates pass.
  </done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` succeeds
2. All 10 how-to pages follow diataxis how-to structure (goal-oriented, practical, no tutorial language)
3. Backend overview has tabbed Snowflake/Databricks SQL comparisons
4. At least queries.md, filtering.md, ordering.md, and backends/overview.md have tabbed SQL examples
5. Every page has "See also" section with cross-references
6. All prose uses second person and warm-but-efficient tone
7. `uv run --extra dev pytest` passes (no doctest breakage)
</verification>

<success_criteria>
- 10 how-to guides rewritten with consistent diataxis structure
- Tabbed SQL dialect examples (Snowflake | Databricks) in relevant guides
- Backend comparison (DOCS-05) shows concrete SQL differences side by side
- Query language guides (DOCS-03) cover .metrics(), .dimensions(), .where(), .order_by(), .limit()
- Filtering guide (DOCS-04) covers field operators and boolean composition
- All pages self-contained with See also cross-references
- Warm-but-efficient tone throughout, no AI patterns
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-overhaul/14-03-SUMMARY.md`
</output>

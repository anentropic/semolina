---
phase: 13.1-implement-filter-lookup-system-and-where-clause-compiler
plan: 04
type: execute
wave: 3
depends_on: ["13.1-02", "13.1-03"]
files_modified:
  - src/cubano/query.py
  - src/cubano/__init__.py
  - src/cubano/conftest.py
  - tests/unit/test_query.py
  - docs/src/guides/filtering.md
  - docs/src/guides/queries.md
  - cubano-jaffle-shop/tests/test_mock_queries.py
  - cubano-jaffle-shop/tests/test_warehouse_queries.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Query._filters type is Predicate | None (not Q | None)"
    - "Query.where() accepts varargs *conditions: Predicate | None"
    - "Predicate exported from cubano.__init__ (not Q)"
    - "Q is NOT exported from cubano package"
    - "All quality gates pass: typecheck, lint, format, full test suite"
    - "filtering.md documents Field methods (not Q objects) as primary filter API"
    - "queries.md .where() examples use field operators (not Q constructor)"
    - "jaffle-shop tests use Predicate-based filters (not Q)"
    - "Doctest conftest.py provides Predicate instead of Q in namespace"
  artifacts:
    - path: "src/cubano/query.py"
      provides: "_Query with _filters: Predicate | None and varargs where()"
      contains: "def where(self, *conditions"
    - path: "src/cubano/__init__.py"
      provides: "Public API exporting Predicate instead of Q"
      contains: "Predicate"
    - path: "docs/src/guides/filtering.md"
      provides: "Rewritten filtering guide using Field methods"
      contains: ".between("
    - path: "tests/unit/test_query.py"
      provides: "Updated query tests using Predicate instead of Q"
      min_lines: 500
  key_links:
    - from: "src/cubano/query.py"
      to: "src/cubano/filters.py"
      via: "TYPE_CHECKING import of Predicate"
      pattern: "from .filters import Predicate"
    - from: "src/cubano/__init__.py"
      to: "src/cubano/filters.py"
      via: "public export"
      pattern: "from .filters import Predicate"
---

<objective>
Wire the predicate tree into the query builder, update public API exports, rewrite test_query.py, update documentation, and update jaffle-shop example tests. This plan makes the full test suite pass end-to-end.

Purpose: Final integration step that connects all three preceding plans and makes the system fully operational.
Output: All quality gates passing, docs updated, jaffle-shop updated, Q fully removed from public API.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@_ideas/filter-api-design.md
@.planning/phases/13.1-implement-filter-lookup-system-and-where-clause-compiler/13.1-CONTEXT.md
@.planning/phases/13.1-implement-filter-lookup-system-and-where-clause-compiler/13.1-RESEARCH.md
@.planning/phases/13.1-implement-filter-lookup-system-and-where-clause-compiler/13.1-01-SUMMARY.md
@.planning/phases/13.1-implement-filter-lookup-system-and-where-clause-compiler/13.1-02-SUMMARY.md
@.planning/phases/13.1-implement-filter-lookup-system-and-where-clause-compiler/13.1-03-SUMMARY.md
@src/cubano/query.py
@src/cubano/__init__.py
@src/cubano/conftest.py
@tests/unit/test_query.py
@docs/src/guides/filtering.md
@docs/src/guides/queries.md
@cubano-jaffle-shop/tests/test_mock_queries.py
@cubano-jaffle-shop/tests/test_warehouse_queries.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Predicate into query.py, __init__.py, conftest.py</name>
  <files>src/cubano/query.py, src/cubano/__init__.py, src/cubano/conftest.py</files>
  <action>
    **query.py changes:**
    1. Change TYPE_CHECKING import: replace `from .filters import Q` with `from .filters import Predicate`
    2. Change `_filters: Q | None = None` to `_filters: Predicate | None = None`
    3. Rewrite `where()` method signature from `def where(self, condition: Q | None) -> _Query` to `def where(self, *conditions: Predicate | None) -> _Query`
    4. Implement varargs logic in where():
       - Filter out None values from conditions
       - If no non-None conditions remain, return self (no-op)
       - Combine all non-None conditions with & (AND)
       - Combine result with existing self._filters using & if self._filters is not None
       - Return replace(self, _filters=new_filter)
    5. Update where() docstring: document varargs signature, Predicate type, None handling
    6. Update _Query class docstring: change Q references to Predicate
    7. Update _Query.to_sql() docstring example: use field operators instead of Q

    **__init__.py changes:**
    1. Replace `from .filters import Q` with `from .filters import Predicate`
    2. Replace `"Q"` with `"Predicate"` in __all__ list

    **conftest.py changes:**
    1. Remove `from cubano.filters import Q` import
    2. Add `from cubano.filters import Predicate` import
    3. Replace `doctest_namespace["Q"] = Q` with `doctest_namespace["Predicate"] = Predicate`
    4. Optionally add key lookup types to namespace for doctest convenience:
       `from cubano.filters import Exact, Gt, Lt` etc. if needed for existing doctests

    Run `uv run basedpyright` on all three files to verify type changes are clean.
  </action>
  <verify>
    `uv run basedpyright src/cubano/query.py src/cubano/__init__.py src/cubano/conftest.py` passes with 0 errors.
    `uv run ruff check src/cubano/query.py src/cubano/__init__.py src/cubano/conftest.py` passes.
  </verify>
  <done>
    query.py uses Predicate | None for _filters, where() accepts *conditions varargs.
    __init__.py exports Predicate instead of Q.
    conftest.py provides Predicate in doctest namespace.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_query.py and update jaffle-shop tests</name>
  <files>tests/unit/test_query.py, cubano-jaffle-shop/tests/test_mock_queries.py, cubano-jaffle-shop/tests/test_warehouse_queries.py</files>
  <action>
    **test_query.py changes:**
    1. Replace all `from cubano.filters import Q` with `from cubano.filters import Predicate, Exact, Gt, And, Or, Not`
    2. Rewrite TestQueryFilter class:
       - `_Query().where(Exact("country", "US"))` instead of `_Query().where(Q(country="US"))`
       - Multiple where() calls AND together: verify _filters is And(...)
       - Varargs: `_Query().where(Exact("a", 1), Gt("b", 2))` produces And(Exact, Gt)
       - `_Query().where(None)` returns same instance
       - `_Query().where(Exact("a", 1), None, Gt("b", 2))` ignores None, ANDs rest
       - `_Query().where()` with no args returns self
    3. Rewrite TestQueryWhere class similarly
    4. Rewrite TestFieldOperators class in test_query.py:
       - Change assertions from `isinstance(q, Q)` to `isinstance(q, Predicate)` / `isinstance(q, Exact)` etc.
       - Remove `.is_leaf()`, `.children`, `.connector` assertions (Q API gone)
       - Use isinstance checks and attribute access on Predicate types instead
    5. Rewrite TestQueryChaining tests:
       - Replace Q(...) filter objects with field operators or direct Predicate construction
       - Verify _filters is correct Predicate type
    6. Update integration tests (TestQueryFetchIntegration, TestModelCentricWorkflow):
       - Replace Q(country="US") with Sales.country == "US" (field operators)
       - Replace Q(revenue__gt=500) with Sales.revenue > 500
       - Verify the full pipeline still works with Predicate filters
    7. Remove all Q imports; ensure no Q references remain in test_query.py

    **jaffle-shop test_mock_queries.py changes:**
    1. Replace `from cubano import Q` with field operator usage
    2. In TestFiltering:
       - Replace `.where(Q(is_food_order=True))` with `.where(Orders.is_food_order == True)`
       - Replace `.where(Q(order_total__gt=50))` with `.where(Orders.order_total > 50)`
    3. Remove Q import entirely

    **jaffle-shop test_warehouse_queries.py changes:**
    1. Check if Q is used; if so, replace with field operators
    2. Remove Q import if present

    Run targeted tests first: `uv run pytest tests/unit/test_query.py -v`
    Then run full suite: `uv run --extra dev pytest`
  </action>
  <verify>
    `uv run pytest tests/unit/test_query.py -v` all tests pass.
    `uv run --extra dev pytest` full suite passes (all quality gates).
    `uv run basedpyright` full project passes.
    `uv run ruff check` passes.
    `uv run ruff format --check` passes.
    Grep for "from cubano.filters import Q" or "from cubano import Q" returns 0 results in src/ and tests/.
  </verify>
  <done>
    All test files updated from Q to Predicate-based API.
    Full test suite passes with 0 failures.
    No Q references remain in source or test code.
    jaffle-shop tests use field operators.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite filtering.md and update queries.md</name>
  <files>docs/src/guides/filtering.md, docs/src/guides/queries.md</files>
  <action>
    **filtering.md — complete rewrite:**
    The guide currently documents Q objects and string-keyed lookup expressions.
    Rewrite entirely to document the new Field method-based filter API.

    New structure:
    1. Title: "Filtering" (drop "with Q")
    2. Intro: Cubano uses Python operators and Field methods for filter conditions. Filters compose via &, |, ~ operators.
    3. Sample model (same Sales model)
    4. "Comparison operators" section:
       - `Sales.revenue > 1000`, `Sales.revenue >= 500`, `Sales.revenue < 100`, etc.
       - Show all 6 operators with examples
    5. "Named filter methods" section:
       - `.between(lo, hi)` with example
       - `.in_(values)` with example
       - `.isnull()` with example
       - `.like(pattern)` and `.ilike(pattern)` with examples
       - `.startswith()`, `.endswith()`, case-insensitive variants
       - `.iexact()` with example
    6. "Composition" section:
       - AND: `(a) & (b)` or multiple `.where()` calls
       - OR: `(a) | (b)`
       - NOT: `~(a)`
    7. "Complex nesting" section with combined example
    8. "Multiple .where() calls" section (conditional filter building pattern)
    9. "Escape hatch" section: `field.lookup(CustomLookup, value)` for extension
    10. Operator precedence warning (same as current)

    Remove ALL references to Q(), Q objects, string-keyed lookups (__gt, __lt, etc.).

    **queries.md — update .where() section:**
    1. Find the `.where(condition)` section (section 3 of "The 8 query methods")
    2. Update signature description: accepts Predicate objects (from field operators) or None
    3. Replace `from cubano import Q` and `Q(country="US")` examples with `Sales.country == "US"`
    4. Show varargs: `.where(Sales.country == "US", Sales.revenue > 1000)`
    5. Keep rest of queries.md unchanged (metrics, dimensions, order_by, limit, etc.)

    Run: `uv run mkdocs build --strict` if available, or just verify files are valid markdown.
  </action>
  <verify>
    Grep for "Q(" in filtering.md returns 0 results.
    Grep for "Q(" in queries.md returns 0 results (in the .where section).
    Grep for "from cubano import Q" in docs/ returns 0 results.
    filtering.md contains `.between(`, `.in_(`, `.like(`, `.isnull()` examples.
    queries.md .where section shows field operator syntax.
  </verify>
  <done>
    filtering.md completely rewritten for Field method-based filter API.
    queries.md .where() section updated with field operator examples.
    No Q references remain in user-facing documentation.
    All documented filter methods match the actual API.
  </done>
</task>

</tasks>

<verification>
ALL quality gates must pass after this plan:
1. `uv run basedpyright` — 0 errors (strict mode, full project)
2. `uv run ruff check` — 0 errors
3. `uv run ruff format --check` — all files formatted
4. `uv run --extra dev pytest` — all tests pass (unit + doctests)
5. No references to Q class in src/, tests/, docs/, or cubano-jaffle-shop/
6. `from cubano import Predicate` works (public API)
7. `from cubano import Q` raises ImportError (Q removed)
</verification>

<success_criteria>
- Full test suite green (all unit tests + doctests pass)
- basedpyright strict mode passes on entire project
- Q completely removed from public API, source, tests, and docs
- Predicate exported from cubano package
- where() accepts varargs with None filtering
- filtering.md documents Field methods as primary filter API
- jaffle-shop tests use field operators
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-implement-filter-lookup-system-and-where-clause-compiler/13.1-04-SUMMARY.md`
</output>

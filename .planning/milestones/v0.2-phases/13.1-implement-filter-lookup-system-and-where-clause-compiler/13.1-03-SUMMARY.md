---
phase: 13.1-implement-filter-lookup-system-and-where-clause-compiler
plan: 03
subsystem: sql-compiler
tags: [where-clause, bind-params, pattern-matching, parameterized-sql, dialect-placeholder]

# Dependency graph
requires:
  - phase: 13.1-01
    provides: "Predicate tree IR: Predicate, And, Or, Not, Lookup[T], 15 core lookup subclasses"
provides:
  - "WHERE clause compiler: _compile_predicate() pattern-matching all Predicate node types"
  - "Parameterized SQL output: build_select_with_params() returning (sql_template, params)"
  - "Dialect.placeholder property: %s for Snowflake/Mock, ? for Databricks"
  - "Inline rendering: render_inline() for display/debugging"
  - "Bind params in execute(): SnowflakeEngine and DatabricksEngine pass params to cursor"
affects: [13.1-04-integration]

# Tech tracking
tech-stack:
  added: []
  patterns: [structural-pattern-matching-compiler, bind-param-pipeline, inline-rendering]

key-files:
  created: []
  modified:
    - src/cubano/engines/sql.py
    - src/cubano/engines/snowflake.py
    - src/cubano/engines/databricks.py
    - tests/unit/test_sql.py

key-decisions:
  - "match/case structural pattern matching for predicate tree compilation -- clean, exhaustive, matches Python 3.11+ requirement"
  - "build_select() delegates to build_select_with_params() + render_inline() for backward compat (str return type unchanged)"
  - "Empty In([]) compiles to '1 = 0' (always false, Django precedent) -- safe SQL, no empty IN clause"
  - "cast('object', node) to work around basedpyright strict Lookup[Unknown] type narrowing in catch-all match case"
  - "v0.2: Do NOT escape % and _ in LIKE wildcard values -- deferred to v0.3 per RESEARCH.md"

patterns-established:
  - "Predicate compilation pipeline: _compile_predicate() -> (sql_fragment, params) -> build_select_with_params() -> (full_sql, all_params)"
  - "Dialect.placeholder drives all parameterized placeholder styles per backend"
  - "render_inline() for display/debugging replaces placeholders with repr(param)"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-02-22
---

# Phase 13.1 Plan 03: WHERE Clause Compiler Summary

**Pattern-matching WHERE clause compiler converting Predicate trees to parameterized SQL with dialect-specific placeholders and bind params in engine execute()**

## Performance

- **Duration:** 6 min
- **Started:** 2026-02-22T08:13:15Z
- **Completed:** 2026-02-22T08:19:43Z
- **Tasks:** 1 (TDD: RED + GREEN)
- **Files modified:** 4

## Accomplishments
- Complete WHERE clause compiler using match/case on all 15 lookup types + 3 composite nodes (And, Or, Not)
- Parameterized SQL pipeline: build_select_with_params() returns (sql_template, params) with no literal values in SQL
- Dialect.placeholder abstract property on ABC with %s (Snowflake/Mock) and ? (Databricks) implementations
- SnowflakeEngine.execute() and DatabricksEngine.execute() now pass bind params to cursor.execute()
- render_inline() for backward-compatible build_select() returning inline-rendered str
- WHERE 1=1 placeholder completely replaced with real predicate compilation
- 96 tests pass (54 existing + 42 new) covering all lookup types, composites, edge cases, parameterization

## Task Commits

Each task was committed atomically:

1. **TDD RED: Failing tests for WHERE clause compiler** - `96bc012` (test)
2. **TDD GREEN: WHERE clause compiler implementation** - `f1c0d2d` (feat)

_Note: No REFACTOR phase needed -- implementation is clean and minimal._

## Files Created/Modified
- `src/cubano/engines/sql.py` - Added Dialect.placeholder property, _compile_predicate(), build_select_with_params(), render_inline(), rewrote _build_where_clause() and build_select()
- `src/cubano/engines/snowflake.py` - Updated execute() to use build_select_with_params() and pass bind params to cursor.execute(sql, params)
- `src/cubano/engines/databricks.py` - Updated execute() to use build_select_with_params() and pass bind params to cursor.execute(sql, parameters=params)
- `tests/unit/test_sql.py` - Added 42 new tests: TestDialectPlaceholder, TestWhereClauseCompiler, TestBuildSelectWithParams, TestRenderInline, TestBuildSelectBackwardCompat

## Decisions Made
- Used match/case structural pattern matching (Python 3.10+) for clean, exhaustive predicate compilation
- build_select() now delegates to build_select_with_params() + render_inline() -- same return type (str), no breaking change
- Empty In([]) compiles to "1 = 0" (always false), following Django/SQLAlchemy precedent
- cast("object", node) workaround for basedpyright strict mode's Lookup[Unknown] type narrowing in catch-all case
- StartsWith/EndsWith wildcard values (%/_ escaping) deferred to v0.3 per RESEARCH.md

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- basedpyright strict mode narrowed the catch-all match case to `Lookup[Unknown]`, causing `reportUnknownMemberType` on `type(node).__name__`. Resolved with `cast("object", node)` to erase the partially-unknown generic parameter.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- WHERE clause compiler is ready for integration in Plan 04
- Plan 04 will wire query.py to use Predicate instead of Q, update __init__.py exports, and fix remaining references
- MockEngine.to_sql() already works (uses build_select() which now renders inline)

## Self-Check: PASSED

- FOUND: src/cubano/engines/sql.py
- FOUND: src/cubano/engines/snowflake.py
- FOUND: src/cubano/engines/databricks.py
- FOUND: tests/unit/test_sql.py
- FOUND: 13.1-03-SUMMARY.md
- FOUND: 96bc012 (RED commit)
- FOUND: f1c0d2d (GREEN commit)

---
*Phase: 13.1-implement-filter-lookup-system-and-where-clause-compiler*
*Completed: 2026-02-22*

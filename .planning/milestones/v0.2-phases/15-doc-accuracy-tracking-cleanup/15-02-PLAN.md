---
phase: 15-doc-accuracy-tracking-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/integration/test_queries.py
  - src/cubano/cli/codegen.py
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "All 25 REQUIREMENTS.md checkboxes show [x] (complete)"
    - "test_queries.py has no stale WHERE 1=1 placeholder comment"
    - "codegen.py has no stale stub reference to plan 09-03"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement tracking with all boxes checked"
      contains: "- [x] **CODEGEN-01**"
    - path: "tests/integration/test_queries.py"
      provides: "Clean module docstring without stale comments"
    - path: "src/cubano/cli/codegen.py"
      provides: "Accurate inline comment"
  key_links: []
---

<objective>
Update REQUIREMENTS.md to check all 25 v0.2 requirement boxes and remove stale comments from two source files.

Purpose: The v0.2 milestone audit found 11 unchecked requirement boxes (INT-02-05, CODEGEN-01-08) despite those features being complete, plus 2 stale comments referencing work that has since been done (WHERE compiler and codegen stub).

Output: Updated REQUIREMENTS.md, test_queries.py, codegen.py.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-doc-accuracy-tracking-cleanup/15-RESEARCH.md
@.planning/REQUIREMENTS.md
@tests/integration/test_queries.py
@src/cubano/cli/codegen.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check all REQUIREMENTS.md boxes and remove stale source comments</name>
  <files>
    .planning/REQUIREMENTS.md
    tests/integration/test_queries.py
    src/cubano/cli/codegen.py
  </files>
  <action>
**REQUIREMENTS.md — check 11 boxes:**

Change these 11 lines from `- [ ]` to `- [x]`:
- Line 13: `INT-02`
- Line 14: `INT-03`
- Line 15: `INT-04`
- Line 16: `INT-05`
- Line 21: `CODEGEN-01`
- Line 22: `CODEGEN-02`
- Line 23: `CODEGEN-03`
- Line 24: `CODEGEN-04`
- Line 25: `CODEGEN-05`
- Line 26: `CODEGEN-06`
- Line 27: `CODEGEN-07`
- Line 28: `CODEGEN-08`

Also update the Traceability table at the bottom — change "Pending" to "Complete" for INT-02 through INT-05 and CODEGEN-01 through CODEGEN-08.

**test_queries.py — remove stale comment (lines 15-16):**

Remove these two lines from the module docstring:
```
Note: WHERE clause filtering is not yet compiled to SQL (WHERE 1=1 placeholder in sql.py).
Filter integration tests are deferred until the Q-object SQL compiler is implemented.
```

The WHERE clause compiler was implemented in Phase 13.1. This comment is now false and misleading.

**codegen.py — fix stale stub comment (line 74):**

Change:
```python
    # Generate SQL (stub: implemented in 09-03)
```
to:
```python
    # Generate SQL for all discovered models
```

The function is fully implemented since Phase 09-03. The stub reference is stale.
  </action>
  <verify>
1. `uv run basedpyright` — passes (no type changes)
2. `uv run ruff check` — passes (no lint changes)
3. `uv run --extra dev pytest` — passes (no behavior changes)
4. Grep REQUIREMENTS.md for `- [ ]` — should return zero matches in the v0.2 section
5. Grep test_queries.py for `WHERE 1=1` — should return zero matches
6. Grep codegen.py for `stub:` — should return zero matches
  </verify>
  <done>
- All 25 REQUIREMENTS.md v0.2 boxes are `[x]`
- Traceability table shows all requirements as "Complete"
- test_queries.py module docstring has no stale WHERE compiler comment
- codegen.py has descriptive comment instead of stale stub reference
  </done>
</task>

</tasks>

<verification>
1. Count `- [x]` in REQUIREMENTS.md v0.2 section = 25
2. Count `- [ ]` in REQUIREMENTS.md v0.2 section = 0
3. No "WHERE 1=1" or "not yet compiled" text in test_queries.py
4. No "stub:" text in codegen.py
5. All quality gates pass (typecheck, lint, tests)
</verification>

<success_criteria>
- All 25 v0.2 requirement checkboxes marked complete
- Traceability table fully updated
- Both stale comments removed/replaced
- All quality gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-doc-accuracy-tracking-cleanup/15-02-SUMMARY.md`
</output>

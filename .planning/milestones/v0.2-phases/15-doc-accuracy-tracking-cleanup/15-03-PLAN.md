---
phase: 15-doc-accuracy-tracking-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/conftest.py
  - .github/workflows/docs.yml
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "3 orphaned fixtures removed from integration conftest (test_schema_name, snowflake_connection, databricks_connection)"
    - "docs.yml uses actions/checkout@v6 consistent with ci.yml and other workflows"
    - "All existing tests still pass after fixture removal"
  artifacts:
    - path: "tests/integration/conftest.py"
      provides: "Clean conftest with only active fixtures"
    - path: ".github/workflows/docs.yml"
      provides: "CI workflow with consistent action versions"
  key_links:
    - from: "tests/integration/conftest.py"
      to: "tests/integration/test_queries.py"
      via: "pytest fixture injection"
      pattern: "backend_engine|snowflake_engine|databricks_engine"
---

<objective>
Remove 3 orphaned fixtures from integration conftest and align docs.yml checkout action version.

Purpose: The integration conftest contains 3 fixtures (`test_schema_name`, `snowflake_connection`, `databricks_connection`) from Phase 8 that were superseded by Phase 12's engine fixtures. They are dead code — no test references them. The docs.yml workflow uses `checkout@v4` while all other workflows use `@v6`.

Output: Cleaned conftest.py, updated docs.yml.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-doc-accuracy-tracking-cleanup/15-RESEARCH.md
@tests/integration/conftest.py
@.github/workflows/docs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned fixtures and align docs.yml checkout version</name>
  <files>
    tests/integration/conftest.py
    .github/workflows/docs.yml
  </files>
  <action>
**conftest.py — remove 3 orphaned fixtures (lines 72-191):**

Delete the following 3 fixture functions entirely:
1. `test_schema_name` (lines 72-89) — generates unique schema names, replaced by inline uuid in engine fixtures
2. `snowflake_connection` (lines 92-141) — creates raw Snowflake connection, replaced by `snowflake_engine` fixture
3. `databricks_connection` (lines 143-191) — creates raw Databricks connection, replaced by `databricks_engine` fixture

These fixtures only reference each other and `snowflake_credentials`/`databricks_credentials`. No test file imports or requests them. The `snowflake_credentials` and `databricks_credentials` fixtures they depend on are still used by the Phase 12 engine fixtures — do NOT remove those.

After removal, ensure there is a clean blank line between the `databricks_credentials` fixture (ending at line ~69-70) and the `TEST_DATA` section header comment (currently at line ~194).

Also remove the `uuid` import from the top of the file — it was only used by `test_schema_name`. The `uuid` import at line 10 is also used by `snowflake_engine` and `databricks_engine`, so check: grep the file for `uuid.uuid4` OUTSIDE of the deleted fixtures. If still used, keep the import. (It IS still used in `snowflake_engine` line ~271 and `databricks_engine` line ~377, so KEEP the uuid import.)

**docs.yml — update checkout version (line 28):**

Change:
```yaml
        uses: actions/checkout@v4
```
to:
```yaml
        uses: actions/checkout@v6
```

This matches `ci.yml`, `release.yml`, and other workflow files.
  </action>
  <verify>
1. `uv run --extra dev pytest` — all tests pass (no fixture-not-found errors)
2. `uv run --extra dev pytest --collect-only tests/integration/` — collects without errors
3. `uv run basedpyright` — passes
4. `uv run ruff check` — passes
5. Grep conftest.py for `test_schema_name` — zero matches
6. Grep conftest.py for `def snowflake_connection` — zero matches
7. Grep conftest.py for `def databricks_connection` — zero matches
8. Grep docs.yml for `checkout@v4` — zero matches
  </verify>
  <done>
- 3 orphaned fixtures removed from tests/integration/conftest.py
- docs.yml uses actions/checkout@v6
- All tests pass (no fixture dependencies broken)
- All quality gates pass
  </done>
</task>

</tasks>

<verification>
1. `pytest --collect-only tests/integration/` succeeds (no broken fixture references)
2. Full test suite passes
3. conftest.py has no `test_schema_name`, `snowflake_connection`, or `databricks_connection` fixtures
4. docs.yml has `checkout@v6`
5. Typecheck and lint pass
</verification>

<success_criteria>
- 3 orphaned fixtures removed without breaking any tests
- docs.yml checkout version aligned with other workflows
- All quality gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-doc-accuracy-tracking-cleanup/15-03-SUMMARY.md`
</output>

---
phase: 15-doc-accuracy-tracking-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/how-to/filtering.md
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "filtering.md iexact SQL example shows ILIKE operator matching actual compiler output"
    - "filtering.md AND composition SQL example shows outer parentheses matching actual compiler output"
  artifacts:
    - path: "docs/src/how-to/filtering.md"
      provides: "Accurate SQL examples for iexact and AND composition"
      contains: "ILIKE"
  key_links:
    - from: "docs/src/how-to/filtering.md"
      to: "src/cubano/engines/sql.py"
      via: "SQL examples match compiler output"
      pattern: "ILIKE"
---

<objective>
Fix two SQL examples in `docs/src/how-to/filtering.md` that don't match actual compiler output from `src/cubano/engines/sql.py`.

Purpose: DOCS-04 requires filtering documentation to accurately reflect the library's behavior. Two SQL examples are wrong: iexact shows `LOWER(...) =` but the compiler emits `ILIKE`, and the AND composition example omits outer parentheses that the compiler produces.

Output: Updated `filtering.md` with correct SQL examples.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-doc-accuracy-tracking-cleanup/15-RESEARCH.md
@docs/src/how-to/filtering.md
@src/cubano/engines/sql.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix iexact and AND composition SQL examples in filtering.md</name>
  <files>docs/src/how-to/filtering.md</files>
  <action>
Fix two SQL examples in `docs/src/how-to/filtering.md`:

**Fix 1 — iexact SQL (lines 241-251):**
Change the Snowflake tab SQL from:
```sql
WHERE LOWER("country") = 'united states'
```
to:
```sql
WHERE "country" ILIKE 'united states'
```

Change the Databricks tab SQL from:
```sql
WHERE LOWER(`country`) = 'united states'
```
to:
```sql
WHERE `country` ILIKE 'united states'
```

This matches `src/cubano/engines/sql.py` line ~460: `case IExact(field_name=f, value=v): return f"{q(f)} ILIKE {ph}", [v]`

**Fix 2 — AND composition SQL (lines 303-313):**
Change the Snowflake tab SQL from:
```sql
WHERE "country" = 'US' AND "revenue" > 500
```
to:
```sql
WHERE ("country" = 'US' AND "revenue" > 500)
```

Change the Databricks tab SQL from:
```sql
WHERE `country` = 'US' AND `revenue` > 500
```
to:
```sql
WHERE (`country` = 'US' AND `revenue` > 500)
```

This matches `src/cubano/engines/sql.py` lines ~392-395: `case And(left=left, right=right): return f"({l_sql} AND {r_sql})", l_params + r_params`

Do NOT touch the complex nested AND/OR examples later in the file — those already show correct parenthesization.
  </action>
  <verify>
Run `uv run mkdocs build --strict` — must pass with no errors. Manually confirm the two fixed SQL examples visually match the compiler output patterns described above.
  </verify>
  <done>
- iexact SQL tabs show `ILIKE` instead of `LOWER(...) =`
- AND composition SQL tabs show outer parentheses `(... AND ...)`
- `mkdocs build --strict` passes
  </done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` passes (docs build validates markdown structure)
2. Grep `filtering.md` for `LOWER` — should return zero matches (old pattern removed)
3. Grep `filtering.md` for `ILIKE` — should find the corrected iexact example
4. The AND example SQL lines contain opening `(` before the first condition
</verification>

<success_criteria>
- filtering.md iexact SQL shows `ILIKE` operator (not `LOWER(...) =`)
- filtering.md AND composition SQL shows outer parens (not bare `AND`)
- Docs build passes with `--strict`
</success_criteria>

<output>
After completion, create `.planning/phases/15-doc-accuracy-tracking-cleanup/15-01-SUMMARY.md`
</output>

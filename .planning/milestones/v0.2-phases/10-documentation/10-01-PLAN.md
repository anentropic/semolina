---
phase: 10-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - mkdocs.yml
  - docs/index.md
  - docs/changelog.md
  - scripts/gen_ref_pages.py
autonomous: true
requirements: [DOCS-02, DOCS-06, DOCS-07]

must_haves:
  truths:
    - "`mkdocs serve` starts without errors and the site is browsable at localhost:8000"
    - "API reference pages are auto-generated for all public Cubano modules"
    - "Material theme loads with dark/light mode toggle and copy-to-clipboard on code blocks"
    - "Left sidebar navigation is grouped into Home, Getting Started, Guides, API Reference sections"
    - "`mkdocs build --strict` exits 0 with no warnings"
  artifacts:
    - path: "mkdocs.yml"
      provides: "MkDocs site configuration with Material theme, plugins, nav"
      contains: "mkdocstrings"
    - path: "docs/index.md"
      provides: "Landing page with card grid linking to key sections"
      contains: "grid cards"
    - path: "scripts/gen_ref_pages.py"
      provides: "Auto-generates API reference pages from src/ modules"
      contains: "mkdocs_gen_files"
    - path: "pyproject.toml"
      provides: "docs dependency group with mkdocs, material, mkdocstrings, gen-files, literate-nav"
      contains: "mkdocs-material"
  key_links:
    - from: "scripts/gen_ref_pages.py"
      to: "docs/reference/"
      via: "mkdocs-gen-files plugin at build time"
      pattern: "mkdocs_gen_files\\.open"
    - from: "mkdocs.yml plugins"
      to: "scripts/gen_ref_pages.py"
      via: "gen-files plugin scripts list"
      pattern: "gen-files"
    - from: "mkdocs.yml nav"
      to: "docs/reference/ SUMMARY.md"
      via: "literate-nav nav_file"
      pattern: "literate-nav"
---

<objective>
Configure MkDocs with Material theme, auto-generated API reference, and the full plugin stack so the docs site builds and serves locally with zero warnings.

Purpose: Foundation for all documentation. Without a working MkDocs config and auto-generated API reference, guides cannot be previewed or built in CI.
Output: Working `mkdocs serve` / `mkdocs build --strict`, auto-generated `reference/` pages for all Cubano modules, landing page with card grid navigation.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation/10-CONTEXT.md
@.planning/phases/10-documentation/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docs dependency group and install MkDocs stack</name>
  <files>pyproject.toml</files>
  <action>
Add a `[dependency-groups]` `docs` section to `pyproject.toml`. The existing `dev` group is already present under `[dependency-groups]`. Add:

```toml
[dependency-groups]
dev = [
    # existing dev deps unchanged
]
docs = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.7.0",
    "mkdocstrings[python]>=0.26.0",
    "mkdocs-gen-files>=0.5.0",
    "mkdocs-literate-nav>=0.6.0",
    "mkdocs-section-index>=0.3.0",
]
```

Then install: `uv sync --group docs`

Do NOT add docs deps to `[project] dependencies` (they are dev-only, not published to PyPI). Do NOT add them to the `dev` group either — keep docs separate for CI separation.
  </action>
  <verify>`uv sync --group docs` exits 0; `uv run --group docs mkdocs --version` prints a version</verify>
  <done>docs dependency group installed; `mkdocs --version` works</done>
</task>

<task type="auto">
  <name>Task 2: Create mkdocs.yml with Material theme, plugins, and nav</name>
  <files>mkdocs.yml, scripts/gen_ref_pages.py</files>
  <action>
Create `mkdocs.yml` at the repo root with the following configuration. Use the exact structure from the research doc (RESEARCH.md section 2) with these adjustments for Cubano:

```yaml
site_name: Cubano
site_description: A Pythonic ORM for querying data warehouse semantic views
site_url: https://anentropic.github.io/cubano/
repo_url: https://github.com/anentropic/cubano
repo_name: anentropic/cubano
edit_uri: edit/main/docs/

theme:
  name: material
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: indigo
      accent: indigo
      toggle:
        icon: material/brightness-4
        name: Switch to light mode
  features:
    - navigation.instant
    - navigation.sections
    - navigation.top
    - navigation.tracking
    - toc.follow
    - content.code.copy
    - content.code.annotate
    - search.suggest
    - search.highlight

extra:
  search:
    provider: algolia
    lang: en
  algolia:
    application_id: !ENV DOCSEARCH_APP_ID
    api_key: !ENV DOCSEARCH_API_KEY
    index_name: cubano

plugins:
  - search
  - gen-files:
      scripts:
        - scripts/gen_ref_pages.py
  - literate-nav:
      nav_file: SUMMARY.md
  - section-index
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            docstring_style: google
            docstring_section_style: table
            show_source: true
            show_root_heading: true
            show_root_toc_entry: true
            show_symbol_type_heading: true
            show_symbol_type_toc: true
            members_order: source
            filters:
              - "!^_"
            merge_init_into_class: true
            show_signature_annotations: true
            separate_signature: true

markdown_extensions:
  - admonition
  - attr_list
  - md_in_html
  - pymdownx.details
  - pymdownx.highlight:
      anchor_linenums: true
      line_spans: __span
      pygments_lang_class: true
  - pymdownx.inlinehilite
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.snippets
  - toc:
      permalink: true

nav:
  - Home: index.md
  - Getting Started:
      - Installation: guides/installation.md
      - First Query: guides/first-query.md
  - Guides:
      - Defining Models: guides/models.md
      - Building Queries: guides/queries.md
      - Filtering with Q: guides/filtering.md
      - Ordering & Limiting: guides/ordering.md
      - Backends:
          - Overview: guides/backends/overview.md
          - Snowflake: guides/backends/snowflake.md
          - Databricks: guides/backends/databricks.md
      - Codegen CLI: guides/codegen.md
  - API Reference: reference/
  - Changelog: changelog.md
```

**DocSearch note:** The `extra.algolia` block above enables DocSearch by Algolia (the LOCKED decision: "Search: DocSearch by Algolia for powerful search-first navigation, free tier for open source"). Obtain a DocSearch app ID and API key by applying at https://docsearch.algolia.com (free for open source projects). Store these as GitHub repository secrets named `DOCSEARCH_APP_ID` and `DOCSEARCH_API_KEY` — they are injected into the build environment in plan 10-04 Task 1. During local `mkdocs serve`, set these env vars in your shell or use a `.env` file (not committed). The standard `- search` plugin entry is kept alongside the algolia config — Material theme uses `extra.search.provider: algolia` to switch to DocSearch at runtime while the standard search plugin handles indexing fallback.

**IMPORTANT:** The nav references guide pages that don't exist yet (they are created in plan 10-03). To allow `mkdocs build --strict` to pass before the guide pages exist, add a `docs/` directory with stub Markdown files for each guide path that appears in the nav. Each stub is a single line: `# [Page Title] (coming soon)`. This prevents "Missing nav page" warnings from failing the strict build.

Create stubs for:
- `docs/guides/installation.md`
- `docs/guides/first-query.md`
- `docs/guides/models.md`
- `docs/guides/queries.md`
- `docs/guides/filtering.md`
- `docs/guides/ordering.md`
- `docs/guides/backends/overview.md`
- `docs/guides/backends/snowflake.md`
- `docs/guides/backends/databricks.md`
- `docs/guides/codegen.md`

Create `scripts/gen_ref_pages.py` (create the `scripts/` directory if needed):

```python
"""Generate API reference pages for all Cubano modules."""

from pathlib import Path

import mkdocs_gen_files

src = Path("src")
nav = mkdocs_gen_files.Nav()

for path in sorted(src.rglob("*.py")):
    module_path = path.relative_to(src).with_suffix("")
    doc_path = path.relative_to(src).with_suffix(".md")
    full_doc_path = Path("reference", doc_path)

    parts = tuple(module_path.parts)

    # Skip __init__, __main__, conftest, and private modules
    if parts[-1] in ("__init__", "__main__", "conftest"):
        continue
    if any(part.startswith("_") for part in parts):
        continue

    nav[parts] = doc_path.as_posix()

    with mkdocs_gen_files.open(full_doc_path, "w") as fd:
        ident = ".".join(parts)
        fd.write(f"# `{ident}`\n\n::: {ident}\n")

    mkdocs_gen_files.set_edit_path(full_doc_path, path)

with mkdocs_gen_files.open("reference/SUMMARY.md", "w") as nav_file:
    nav_file.writelines(nav.build_literate_nav())
```
  </action>
  <verify>`uv run --group docs mkdocs build --strict` exits 0 (stubs satisfy nav, API reference pages generated)</verify>
  <done>`mkdocs build` succeeds; `site/reference/` directory contains generated pages for cubano modules</done>
</task>

<task type="auto">
  <name>Task 3: Create landing page (docs/index.md) and changelog stub</name>
  <files>docs/index.md, docs/changelog.md</files>
  <action>
Create `docs/index.md` as the landing page. Use Material card grid syntax (requires `attr_list` and `md_in_html` extensions, both enabled in mkdocs.yml):

```markdown
# Cubano

A Pythonic ORM for querying data warehouse semantic views — Snowflake and Databricks.

<div class="grid cards" markdown>

-   :material-clock-fast:{ .lg .middle } **5-minute quickstart**

    ---
    Install Cubano and run your first query in minutes.

    [:octicons-arrow-right-24: Getting Started](guides/installation.md)

-   :material-database:{ .lg .middle } **Semantic view models**

    ---
    Define typed models over Snowflake semantic views and Databricks metric views.

    [:octicons-arrow-right-24: Defining Models](guides/models.md)

-   :material-filter:{ .lg .middle } **Fluent query builder**

    ---
    Chain `.metrics()`, `.dimensions()`, `.filter()`, `.order_by()`, `.limit()`.

    [:octicons-arrow-right-24: Building Queries](guides/queries.md)

-   :material-api:{ .lg .middle } **API Reference**

    ---
    Auto-generated from docstrings — always up to date.

    [:octicons-arrow-right-24: Reference](reference/)

</div>

## Quick Example

```python
from cubano import SemanticView, Metric, Dimension, Query, register
from cubano.engines.mock import MockEngine

class Sales(SemanticView, view="sales"):
    revenue = Metric()
    country = Dimension()

register("default", MockEngine())

results = (Query()
    .metrics(Sales.revenue)
    .dimensions(Sales.country)
    .limit(10)
    .fetch())

for row in results:
    print(row.country, row.revenue)
```

Cubano works identically with Snowflake and Databricks — swap the engine, keep your queries.
```

Create `docs/changelog.md` as a minimal stub:

```markdown
# Changelog

## v0.2.0 (unreleased)

- Codegen CLI: generate warehouse-native CREATE SEMANTIC VIEW SQL from Cubano models
- Integration testing infrastructure for Snowflake and Databricks
- Comprehensive documentation site with auto-generated API reference

## v0.1.0

- Initial release: SemanticView model definitions, fluent Query builder, Snowflake and Databricks backends
```
  </action>
  <verify>`uv run --group docs mkdocs build --strict` still exits 0; `site/index.html` contains "grid cards"; `site/changelog/index.html` exists</verify>
  <done>Landing page and changelog exist; `mkdocs build --strict` passes with all three tasks complete</done>
</task>

</tasks>

<verification>
After all three tasks:

1. `uv run --group docs mkdocs build --strict` exits 0 with no errors
2. `site/reference/` directory exists and contains generated .html pages
3. `site/index.html` contains "grid cards" content
4. `uv run --group docs mkdocs serve` starts at localhost:8000 (interrupt with Ctrl+C after confirming it starts)
5. API reference page for `cubano.query` module is present in `site/reference/`
6. `uv run basedpyright` still passes (scripts/gen_ref_pages.py is not in the pyright include path, so no action needed)
7. `uv run ruff check` passes
</verification>

<success_criteria>
- `mkdocs build --strict` exits 0: all nav pages exist, all mkdocstrings references resolve
- API reference auto-generated from src/ modules (DOCS-02 satisfied)
- Material theme configured with dark/light mode, copy-to-clipboard, sidebar navigation (DOCS-06 satisfied)
- gen-files + literate-nav + section-index + mkdocstrings all active (DOCS-07 satisfied)
- No regressions in existing quality gates (basedpyright, ruff, pytest)
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-01-SUMMARY.md`
</output>

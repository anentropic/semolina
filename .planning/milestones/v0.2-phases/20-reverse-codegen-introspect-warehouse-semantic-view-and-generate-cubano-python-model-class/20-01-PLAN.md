---
phase: 20-reverse-codegen-introspect-warehouse-semantic-view-and-generate-cubano-python-model-class
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/cubano/codegen/introspector.py
  - src/cubano/codegen/type_map.py
  - src/cubano/engines/base.py
  - src/cubano/engines/mock.py
  - tests/unit/codegen/test_introspector.py
  - tests/unit/codegen/test_type_map.py
autonomous: true
requirements:
  - CODEGEN-REVERSE

must_haves:
  truths:
    - "IntrospectedField and IntrospectedView frozen dataclasses exist with correct fields"
    - "Snowflake JSON type names map to Python annotation strings (FIXED→int, TEXT→str, REAL→float, etc.)"
    - "Databricks type names map to Python annotation strings (bigint→int, string→str, double→float, etc.)"
    - "Types without clean Python equivalents (GEOGRAPHY, VARIANT, ARRAY, etc.) return None to trigger TODO comment"
    - "FIXED with scale>0 returns 'float'; scale=0 returns 'int'"
    - "Engine ABC declares introspect() as an abstract method with IntrospectedView return type"
    - "MockEngine.introspect() raises NotImplementedError (test stub, not used by CLI)"
  artifacts:
    - path: "src/cubano/codegen/introspector.py"
      provides: "IntrospectedField, IntrospectedView frozen dataclasses"
      exports: ["IntrospectedField", "IntrospectedView"]
    - path: "src/cubano/codegen/type_map.py"
      provides: "SQL type → Python annotation mapping for Snowflake and Databricks"
      exports: ["snowflake_json_type_to_python", "databricks_type_to_python"]
    - path: "src/cubano/engines/base.py"
      provides: "Engine ABC with abstract introspect() method"
      contains: "def introspect"
    - path: "tests/unit/codegen/test_introspector.py"
      provides: "Tests for IntrospectedField/IntrospectedView construction and validation"
    - path: "tests/unit/codegen/test_type_map.py"
      provides: "Tests for all Snowflake and Databricks type mappings"
  key_links:
    - from: "src/cubano/engines/base.py"
      to: "src/cubano/codegen/introspector.py"
      via: "TYPE_CHECKING import of IntrospectedView"
      pattern: "IntrospectedView"
    - from: "src/cubano/engines/mock.py"
      to: "src/cubano/engines/base.py"
      via: "Engine ABC abstract method"
      pattern: "def introspect"
---

<objective>
Build the foundational data types and type-mapping functions for Phase 20's reverse codegen.

Purpose: Everything downstream (engine introspection implementations, Python renderer, CLI) depends on these shared types. Getting them right first with TDD ensures a stable contract.
Output: IntrospectedField/IntrospectedView dataclasses, SQL→Python type maps, Engine ABC updated with introspect() abstract method, MockEngine stub.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-reverse-codegen-introspect-warehouse-semantic-view-and-generate-cubano-python-model-class/20-CONTEXT.md
@.planning/phases/20-reverse-codegen-introspect-warehouse-semantic-view-and-generate-cubano-python-model-class/20-RESEARCH.md
@src/cubano/engines/base.py
@src/cubano/engines/mock.py
@src/cubano/codegen/models.py
</context>

<feature>
  <name>Introspection IR + Type Mapping + Engine ABC</name>
  <files>
    src/cubano/codegen/introspector.py,
    src/cubano/codegen/type_map.py,
    src/cubano/engines/base.py,
    src/cubano/engines/mock.py,
    tests/unit/codegen/test_introspector.py,
    tests/unit/codegen/test_type_map.py
  </files>
  <behavior>
    IntrospectedField: frozen dataclass with name (str), field_type ("metric"|"dimension"|"fact"), data_type (str), description (str, default "")
    IntrospectedView: frozen dataclass with view_name (str), class_name (str), fields (list[IntrospectedField])

    snowflake_json_type_to_python(type_json: dict) -> str | None:
    - {"type": "FIXED", "scale": 0} -> "int"
    - {"type": "FIXED", "scale": 2} -> "float"
    - {"type": "TEXT"} -> "str"
    - {"type": "REAL"} -> "float"
    - {"type": "BOOLEAN"} -> "bool"
    - {"type": "DATE"} -> "datetime.date"
    - {"type": "TIMESTAMP_LTZ"} -> "datetime.datetime"
    - {"type": "TIMESTAMP_NTZ"} -> "datetime.datetime"
    - {"type": "TIMESTAMP_TZ"} -> "datetime.datetime"
    - {"type": "TIME"} -> "datetime.time"
    - {"type": "BINARY"} -> "bytes"
    - {"type": "ARRAY"} -> None  (signals TODO)
    - {"type": "OBJECT"} -> None
    - {"type": "VARIANT"} -> None
    - {"type": "GEOGRAPHY"} -> None
    - {"type": "GEOMETRY"} -> None
    - unknown type -> None

    databricks_type_to_python(type_obj: dict) -> str | None:
    - {"name": "string"} -> "str"
    - {"name": "bigint"} -> "int"
    - {"name": "int"} -> "int"
    - {"name": "smallint"} -> "int"
    - {"name": "tinyint"} -> "int"
    - {"name": "long"} -> "int"
    - {"name": "double"} -> "float"
    - {"name": "float"} -> "float"
    - {"name": "decimal"} -> "float"
    - {"name": "boolean"} -> "bool"
    - {"name": "date"} -> "datetime.date"
    - {"name": "timestamp"} -> "datetime.datetime"
    - {"name": "timestamp_ntz"} -> "datetime.datetime"
    - {"name": "binary"} -> "bytes"
    - {"name": "array"} -> None
    - {"name": "map"} -> None
    - {"name": "struct"} -> None
    - {"name": "variant"} -> None
    - unknown name -> None

    Engine.introspect(view_name: str) -> IntrospectedView: @abstractmethod
    MockEngine.introspect(view_name: str) -> IntrospectedView: raises NotImplementedError("MockEngine does not support introspection")
  </behavior>
  <implementation>
    RED: Create test files with all test cases -> run pytest -> MUST fail (modules don't exist yet)
    GREEN: Create introspector.py, type_map.py; update base.py and mock.py -> run pytest -> MUST pass
    REFACTOR: Check docstrings, type annotations, clean up

    introspector.py: Two frozen dataclasses. Import from __future__ import annotations. No external deps.
    type_map.py: Two private dict constants + two public functions. Handle case-insensitive lookups (uppercase for Snowflake, lowercase for Databricks). No external deps.
    base.py: Add `from __future__ import annotations` guard. Add abstract introspect() with TYPE_CHECKING import of IntrospectedView. Keep existing to_sql/execute abstract methods intact.
    mock.py: Add introspect() method that raises NotImplementedError.

    basedpyright note: IntrospectedView in base.py return type annotation must be quoted string or use TYPE_CHECKING import to avoid circular at runtime.
  </implementation>
</feature>

<verification>
<automated>uv run --extra dev pytest tests/unit/codegen/test_introspector.py tests/unit/codegen/test_type_map.py -v && uv run basedpyright src/cubano/codegen/introspector.py src/cubano/codegen/type_map.py src/cubano/engines/base.py src/cubano/engines/mock.py</automated>
<manual>Run `uv run --extra dev pytest tests/ -x` to confirm no existing tests broken by Engine ABC change</manual>
</verification>

<success_criteria>
- All new tests pass (RED → GREEN cycle completed)
- basedpyright reports 0 errors on new and modified files
- `uv run --extra dev pytest tests/` passes (MockEngine now requires implementing introspect — confirm test_engines.py updated or that MockEngine's stub satisfies the ABC)
- `uv run ruff check src/cubano/codegen/introspector.py src/cubano/codegen/type_map.py`  passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-reverse-codegen-introspect-warehouse-semantic-view-and-generate-cubano-python-model-class/20-01-SUMMARY.md`
</output>

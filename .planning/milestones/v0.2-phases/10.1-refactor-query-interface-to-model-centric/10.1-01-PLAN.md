---
phase: 10.1-refactor-query-interface-to-model-centric
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cubano/fields.py
  - src/cubano/models.py
  - src/cubano/query.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can start queries via model class method: Users.query() returns a Query instance"
    - "Field attributes access (Users.revenue, Users.country) returns Field descriptor objects"
    - "Reserved method names (query, metrics, dimensions, where, execute) prevent namespace collisions"
    - "Field descriptors work identically at class level and in query builder methods"
  artifacts:
    - path: "src/cubano/fields.py"
      provides: "Updated Field.__set_name__ with expanded reserved names list"
      contains: "reserved ="
      min_lines: 5
    - path: "src/cubano/models.py"
      provides: "SemanticView.query() class method for model-centric entry point"
      contains: "@classmethod"
      min_lines: 10
    - path: "src/cubano/query.py"
      provides: "Query._model tracking for field validation"
      contains: "_model"
      min_lines: 5
  key_links:
    - from: "src/cubano/models.py"
      to: "src/cubano/query.py"
      via: "SemanticView.query() creates and returns Query instance"
      pattern: "Query\\("
    - from: "src/cubano/fields.py"
      to: "src/cubano/models.py"
      via: "Field.__set_name__ validation blocks reserved names"
      pattern: "reserved ="

---

<objective>
Implement model-centric query entry point and namespace management. Establish the foundation for the refactored API by adding SemanticView.query() as the primary entry point and implementing reserved method name validation to prevent namespace collisions between field names and builder methods.

Purpose: This plan provides the critical infrastructure for Phase 10.1 — allowing users to start queries naturally (Users.query()) while preventing the namespace collision problem that plagues flexible descriptor-based approaches.

Output: Users can invoke Model.query() to create queries; Field.__set_name__ prevents reserved field names from causing conflicts.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-refactor-query-interface-to-model-centric/10.1-CONTEXT.md
@.planning/phases/10.1-refactor-query-interface-to-model-centric/10.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Field.__set_name__ with reserved method names</name>
  <files>src/cubano/fields.py</files>
  <action>
Update Field.__set_name__ validation to include builder method names as reserved:

1. Locate the existing `reserved` set in Field.__set_name__ (currently: {"keys", "values", "items", "get", "pop", "update", "clear"})

2. Expand reserved set to include Phase 10.1 builder methods:
   - "query" (entry point)
   - "metrics" (field selection)
   - "dimensions" (field selection)
   - "where" (filtering)
   - "filter" (internal, but reserve for consistency)
   - "order_by" (ordering)
   - "limit" (limiting)
   - "execute" (eager execution)
   - "to_sql" (SQL generation)
   - "using" (engine selection)

3. Improve error message to suggest alternatives: "Did you mean: 'user_query'? Or use source mapping: query_field = Metric(source='query')" (source mapping exists in fields.py for column aliasing)

4. Add comment explaining why these are reserved: "# Query builder methods that must be available on all models"

5. Run tests to ensure validation works: `uv run pytest tests/test_models.py -xvs` and `uv run pytest tests/test_fields.py -xvs`

Reference existing code patterns:
- Field.__set_name__ already validates keywords and soft keywords (lines 68-77)
- Current reserved set is at line 80
- ValueError message pattern shown at line 82
  </action>
  <verify>
- Field class raises ValueError when field named 'query' is defined: `class Users(SemanticView, view='users'): query = Metric()` throws error
- Error message includes suggestion about alternative naming
- All existing tests pass (test_models.py, test_fields.py)
- Run: `uv run basedpyright` and `uv run ruff check` pass
  </verify>
  <done>
Field.__set_name__ prevents all reserved method names from being used as field names with clear error messages and helpful suggestions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SemanticView.query() class method for model-centric entry point</name>
  <files>src/cubano/models.py</files>
  <action>
Add a class method to SemanticView that creates and returns a Query instance bound to the model:

1. Import Query in models.py (add to imports at top: `from .query import Query`)

2. Add `query` class method to SemanticView class (after __init_subclass__):
   ```python
   @classmethod
   def query(cls, using: str | None = None) -> Query:
       """
       Create a query for this semantic view.

       Entry point for model-centric query construction. Returns a Query
       instance bound to this model, enabling fluent method chaining:

           Users.query().metrics(Users.revenue).dimensions(Users.country).execute()

       Args:
           using: Optional engine name (defaults to 'default')

       Returns:
           Query instance bound to this model's view
       """
       q = Query(_using=using)
       object.__setattr__(q, '_model', cls)
       return q
   ```

3. Add doctest example to SemanticView class docstring showing basic usage:
   ```python
   >>> class Orders(SemanticView, view='orders'):
   ...     total = Metric()
   ...     region = Dimension()
   >>> q = Orders.query()
   >>> q._model is Orders
   True
   ```

4. Run tests: `uv run pytest tests/test_models.py -xvs` and new test for .query() method behavior

5. Run doctest: `uv run pytest --doctest-modules src/cubano/models.py -xvs`

Note: The _model attribute is used internally for field validation in later plans. This is private API (underscore prefix).
  </action>
  <verify>
- SemanticView has a .query() class method
- Calling Model.query() returns a Query instance with _model set to the model class
- Calling Model.query(using='warehouse') sets _using correctly
- Method signature is: `query(cls, using: str | None = None) -> Query`
- Docstring includes usage example
- Type checks pass: `uv run basedpyright`
- All tests pass: `uv run pytest tests/test_models.py -xvs`
  </verify>
  <done>
SemanticView.query() is the primary entry point for model-centric queries, returning a Query instance bound to the model for field validation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Query dataclass to track model reference and validate field ownership</name>
  <files>src/cubano/query.py</files>
  <action>
Extend the Query dataclass to track which model it's bound to, enabling field validation:

1. Add _model field to Query dataclass (with default None):
   ```python
   _model: type | None = field(default=None, init=False, repr=False)
   ```
   - Use init=False so Query() constructor doesn't require a model parameter
   - Use repr=False to keep debug output clean
   - Dataclass field allows assignment via object.__setattr__ even with frozen=True

2. Add field ownership validation to Query.metrics() method:
   - Before adding fields, check if _model is set
   - If set, verify all fields have owner == _model
   - If field is from different model, raise TypeError with helpful message:
     ```
     "Cannot mix fields from different models in one query. "
     "Expected fields from {model_name}, got field '{field_name}' from {other_model}"
     ```

3. Add same validation to Query.dimensions() method

4. Keep Query() internal constructor available (for backward compatibility / advanced use)

5. Add docstring note: "Typically created via Model.query(), not directly via Query()"

6. Run tests: `uv run pytest tests/test_query.py -xvs`

7. Run type checks: `uv run basedpyright`

Note: Field already stores .owner set by __set_name__, so checking owner is straightforward.
  </action>
  <verify>
- Query dataclass has _model: type | None field with init=False
- Query._model defaults to None and can be set without violating frozen dataclass
- Query.metrics() validates field ownership when _model is set
- Query.dimensions() validates field ownership when _model is set
- Mixing fields from different models raises TypeError with clear message
- Backward compatible: Query() still works without _model (for internal use)
- All existing tests pass
- Type checks pass: `uv run basedpyright`
  </verify>
  <done>
Query tracks which model it belongs to and validates that all fields originate from that model, preventing silent mixing of fields from different models.
  </done>
</task>

</tasks>

<verification>
All tasks must complete before moving to Plan 02:
1. Field.__set_name__ rejects reserved method names with helpful error messages
2. SemanticView.query() creates Query instances bound to the model
3. Query validates field ownership when _model is set
4. No regressions in existing tests (test_models.py, test_query.py, test_fields.py)
5. Type checking passes: basedpyright strict mode
6. Linting passes: ruff check + ruff format --check
</verification>

<success_criteria>
- Users can invoke Model.query() to start model-centric queries
- Reserved method names are enforced at field definition time with clear error messages
- Query instances remember which model they're bound to
- Field ownership is validated to prevent mixing fields from different models
- Zero backward compatibility issues with existing internal Query() usage
- All quality gates pass: typecheck (basedpyright), lint (ruff check), format (ruff format)
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-refactor-query-interface-to-model-centric/10.1-01-SUMMARY.md` documenting:
- Reserved method names list (for documentation)
- Model.query() signature and usage
- Field ownership validation behavior
- Any deviations from LOCKED decisions
- Next plan dependencies
</output>

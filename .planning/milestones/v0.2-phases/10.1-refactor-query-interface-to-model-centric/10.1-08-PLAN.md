---
phase: 10.1-refactor-query-interface-to-model-centric
plan: 08
type: execute
wave: 2
depends_on: ["10.1-07"]
gap_closure: true
files_modified:
  - src/cubano/engines/*.py
  - tests/test_query.py
  - tests/*.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "All internal code in src/cubano/engines/ uses Query.execute() instead of .fetch()"
    - "All test code uses Query.execute() (via Result object) instead of .fetch()"
    - "No test or internal code calls .filter() (use .where() instead)"
    - "No grep results for `.filter(` or `.fetch(` in src/ and tests/ directories"
    - "All 456+ tests pass with new API calls only"
    - "Typecheck, lint, format, and doctest all pass without warnings"
  artifacts:
    - path: "src/cubano/engines/mock.py"
      provides: "No .fetch() calls in docstring examples"
      not_contains: "fetch()"
    - path: "src/cubano/engines/snowflake.py"
      provides: "No .fetch() calls in docstring examples"
      not_contains: "fetch()"
    - path: "src/cubano/engines/databricks.py"
      provides: "No .fetch() calls in docstring examples"
      not_contains: "fetch()"
    - path: "src/cubano/engines/sql.py"
      provides: "No .filter() or .fetch() calls"
      not_contains: "filter()"
    - path: "tests/test_query.py"
      provides: "All tests use .execute() or new API"
      not_contains: ".fetch()"
    - path: "tests/"
      provides: "No .filter() or .fetch() calls in test files"
      not_contains: "\.filter("
  key_links:
    - from: "tests/"
      to: "src/cubano/"
      via: "Test code exercises new API only"
      pattern: "\.execute\\(\\)"

---

<objective>
Update all internal code and tests to use the new Query API (Query.execute(), Query.where()) instead of old methods (Query.fetch(), Query.filter()). Ensure complete removal of old API usage throughout the codebase — no references to deprecated methods should remain.

Purpose: With Plan 10.1-07 removing old methods from the Query class, this plan updates all call sites to use the new API. This ensures:
1. All internal docstring examples use new API
2. All test code uses new API
3. No code breaks after old methods are removed
4. Clean migration path with no backward compatibility

Output: Complete codebase migration to new API; all tests passing; no references to old methods anywhere.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-refactor-query-interface-to-model-centric/10.1-VERIFICATION.md
@.planning/phases/10.1-refactor-query-interface-to-model-centric/10.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docstring examples in src/cubano/engines/ to use new API</name>
  <files>src/cubano/engines/mock.py, src/cubano/engines/snowflake.py, src/cubano/engines/databricks.py, src/cubano/engines/sql.py</files>
  <action>
Search for and replace all docstring examples that use old Query API in engine files:

1. **In each engine file**, find docstring examples showing Query() usage:

   Search pattern: Look for triple-quoted examples containing `.fetch()` or `.filter(`

2. **Replace .fetch() with .execute():**
   - Old: `results = Query().metrics(Sales.revenue).fetch()`
   - New: `result = Sales.query().metrics(Sales.revenue).execute()`
   - Note: Also update variable names for clarity (results → result, indicates Result object not list)

3. **Replace .filter(Q(...)) with .where(...):**
   - Old: `Query().filter(Q(country='US'))`
   - New: `Sales.query().where(Sales.country == 'US')`
   - This shows modern operator-based filtering

4. **Replace direct Query() with Model.query():**
   - Old: `query = Query().metrics(Sales.revenue)`
   - New: `query = Sales.query().metrics(Sales.revenue)`
   - Ensures all examples follow model-centric pattern

5. Files to update:
   - src/cubano/engines/mock.py
   - src/cubano/engines/snowflake.py
   - src/cubano/engines/databricks.py
   - src/cubano/engines/sql.py

Verification:
- `grep -n "\.fetch()" src/cubano/engines/*.py` returns 0 results in examples
- `grep -n "\.filter(" src/cubano/engines/*.py` returns 0 results in examples
- All docstring examples use Sales.query() or Model.query() pattern
- Examples compile and execute (no syntax errors)
  </action>
</task>

<task type="auto">
  <name>Task 2: Update test_query.py to remove .filter() and .fetch() test cases</name>
  <files>tests/test_query.py</files>
  <action>
Remove or update all test cases in test_query.py that directly test .filter() or .fetch() methods:

1. **Identify test classes/functions to update:**
   - Search for "class TestFilter" or "def test_filter" → these test .filter() method
   - Search for "class TestFetch" or "def test_fetch" → these test .fetch() method
   - These tests should be REMOVED (not updated) since the methods no longer exist

2. **Remove these test sections entirely:**
   - `class TestFilterMethod` (tests Query.filter() specifically)
   - `def test_filter_with_q_object`
   - `def test_multiple_filters_and_together`
   - `def test_filter_type_validation`
   - Any test with "filter" in the name that tests the method directly

   - `class TestFetchMethod` or `class TestFetchExecution` (tests Query.fetch())
   - `def test_fetch_with_registry`
   - `def test_fetch_with_no_metrics_raises_error`
   - Any test with "fetch" in the name that tests the method directly

3. **Update remaining tests that use .fetch() for setup/action:**
   - Tests that call .fetch() in test logic (not testing .fetch() itself) should change:
     - Old: `results = query.fetch()`
     - New: `result = query.execute(); results = list(result)`
     - Or: Change to iterate over Result directly (Result is iterable)

4. **Update tests that use .filter() in setup/action:**
   - Tests that call .filter() in test logic should change:
     - Old: `query.filter(Q(country='US'))`
     - New: `query.where(Q(country='US'))`

5. **Search and replace pass:**
   ```bash
   grep -n "\.fetch()" tests/test_query.py
   grep -n "\.filter(" tests/test_query.py
   ```
   Update all found instances to new API

Verification:
- `grep -n "\.fetch()" tests/test_query.py` returns 0 matches
- `grep -n "\.filter(" tests/test_query.py` returns 0 matches
- Test file still has 380+ lines (tests removed but not extensively rewritten)
- All test functions pass: `uv run pytest tests/test_query.py -v`
  </action>
</task>

<task type="auto">
  <name>Task 3: Search and replace old API in all test files</name>
  <files>tests/*.py</files>
  <action>
Perform comprehensive search and replace across all test files to update old API calls:

1. **Search all test files for old API usage:**
   ```bash
   grep -r "\.filter(" tests/ --include="*.py" | grep -v "# " | head -20
   grep -r "\.fetch()" tests/ --include="*.py" | grep -v "# " | head -20
   grep -r "Query()" tests/ --include="*.py" | grep -v "# " | head -20
   ```

2. **For each match found, apply these transformations:**

   **Replace .filter(Q(...)) → .where(...):**
   - Context: Most filter calls should become where calls
   - Example: `.filter(Q(country='US'))` → `.where(Q(country='US'))`
   - Or better (modern syntax): `.filter(Q(country='US'))` → `.where(Sales.country == 'US')`

   **Replace .fetch() → .execute():**
   - Context: Any test that calls .fetch() to get results
   - Example: `results = query.fetch()` → `result = query.execute(); results = list(result)`
   - Note: Result is iterable, so `for row in result:` still works
   - Note: Result has len(), indexing support, so existing assertions work

   **Replace Query() → Model.query():**
   - Context: Direct Query() constructor calls
   - Example: `Query().metrics(Sales.revenue)` → `Sales.query().metrics(Sales.revenue)`
   - Ensure correct model is used (Sales, Users, etc. from conftest)

3. **Files to check:**
   - tests/test_query.py (already done in Task 2, but verify)
   - tests/test_fields.py
   - tests/test_results.py
   - tests/test_models.py
   - tests/test_engines.py (if exists)
   - tests/test_filters.py (if exists)
   - Any integration tests

4. **Manual verification after replacements:**
   - Syntax errors: `python -m py_compile tests/*.py`
   - Type checking: `uv run basedpyright tests/`
   - Lint: `uv run ruff check tests/`

Verification:
- `grep -r "\.filter(" tests/ --include="*.py"` returns 0 results
- `grep -r "\.fetch()" tests/ --include="*.py"` returns 0 results
- All files compile without syntax errors
  </action>
</task>

<task type="auto">
  <name>Task 4: Run full test suite and quality gates</name>
  <files>src/cubano/*.py, tests/*.py</files>
  <action>
Run complete test suite and all quality gates to ensure migration is complete and successful:

1. **Run full pytest suite:**
   ```bash
   uv run --extra dev pytest -v
   ```
   Expected: All tests pass (450+ tests), 0 failures
   Note: May have 6 skipped tests (expected)

2. **Typecheck strict mode:**
   ```bash
   uv run basedpyright
   ```
   Expected: 0 errors, 0 warnings in src/cubano/

3. **Lint check:**
   ```bash
   uv run ruff check src/ tests/
   ```
   Expected: 0 errors, 0 warnings

4. **Format check:**
   ```bash
   uv run ruff format --check src/ tests/
   ```
   Expected: All files already formatted
   If fails: Apply with `uv run ruff format src/ tests/`

5. **Doctest validation:**
   ```bash
   uv run pytest src/ --doctest-modules -v
   ```
   Expected: All docstring examples pass (16 doctests)

6. **Final verification: No old API in codebase:**
   ```bash
   grep -r "\.filter(" src/ tests/ --include="*.py" | grep -v "# deprecated\|# old api"
   grep -r "\.fetch()" src/ tests/ --include="*.py" | grep -v "# internal\|# old api"
   grep -r "def filter(" src/ --include="*.py"
   grep -r "def fetch(" src/ --include="*.py"
   ```
   All should return 0 results

Verification criteria:
- pytest: 450+ tests pass, 0 failures
- basedpyright: 0 errors
- ruff check: 0 errors
- ruff format: All files formatted
- doctests: All pass
- No references to old API methods found
  </action>
</task>

</tasks>


---
phase: 10.1-refactor-query-interface-to-model-centric
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/cubano/query.py
  - src/cubano/__init__.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Query class no longer exposes public Query() constructor (private or removed from __init__)"
    - "Query.filter() method is completely removed from source code (not deprecated, not present)"
    - "Query.fetch() method is completely removed from source code (not deprecated, not present)"
    - "Query.execute() is the only public execution method available"
    - "SemanticView.query() remains the only documented entry point for query creation"
  artifacts:
    - path: "src/cubano/query.py"
      provides: "Query class without .filter() and .fetch() methods"
      contains: "def execute"
      not_contains: "def filter"
      min_lines: 300
    - path: "src/cubano/query.py"
      provides: "Query class without .fetch() method"
      contains: "def execute"
      not_contains: "def fetch"
      min_lines: 300
    - path: "src/cubano/__init__.py"
      provides: "Query not exported from public API"
      not_contains: "Query"
  key_links:
    - from: "src/cubano/models.py"
      to: "src/cubano/query.py"
      via: "SemanticView.query() creates Query instances internally"
      pattern: "def query"

---

<objective>
Remove old procedural Query API methods and make Query constructor private. Complete the Phase 10.1 refactor by eliminating backward compatibility code and enforcing model-centric query construction via SemanticView.query() as the only public entry point.

Purpose: Phase 10.1 explicitly requires NO backward compatibility — the old Query(), .filter(), and .fetch() API should not exist at all. This plan removes them from the codebase entirely.

Output: Query class with only new API methods (.where(), .execute()); old methods completely removed; Query() constructor inaccessible to users.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-refactor-query-interface-to-model-centric/10.1-VERIFICATION.md
@.planning/phases/10.1-refactor-query-interface-to-model-centric/10.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Query.filter() method completely</name>
  <files>src/cubano/query.py</files>
  <action>
Remove the Query.filter() method from src/cubano/query.py entirely (not mark deprecated, fully delete):

1. Locate the filter() method (lines ~182-211 in current version)

2. Delete the entire method definition and docstring:
   - Method signature: `def filter(self, condition: Any) -> Query:`
   - Full docstring explaining deprecated status
   - Implementation that validates Q object and combines filters

3. Remove any references to .filter() in the Query class docstring at the top:
   - Current line ~35 mentions: ".filter() — internal filtering (replaced by .where())"
   - Remove or update this reference

4. Verify no line in the file contains "def filter"

Verification:
- `grep -n "def filter" src/cubano/query.py` returns no results
- `grep -n "\.filter()" src/cubano/query.py` returns no results (except in examples showing it's removed)
- Query class docstring does not reference .filter()
  </action>
</task>

<task type="auto">
  <name>Task 2: Remove Query.fetch() method completely</name>
  <files>src/cubano/query.py</files>
  <action>
Remove the Query.fetch() method from src/cubano/query.py entirely (not mark deprecated, fully delete):

1. Locate the fetch() method (lines ~347-372 in current version)

2. Delete the entire method definition and docstring:
   - Method signature: `def fetch(self) -> list[Any]:`
   - Full docstring explaining execution behavior
   - Implementation that resolves engine and returns Row list

3. Update Query.execute() docstring which currently says:
   - "Wrapper around .fetch() that returns results in a Result object"
   - Change to: "Execute query and return results in a Result object immediately"

4. Update Query.execute() implementation to NOT call self.fetch():
   - Current implementation: `rows = self.fetch(); return Result(rows)`
   - New implementation should directly resolve engine, execute, wrap:
     ```python
     from .registry import get_engine
     from .results import Row, Result

     self._validate_for_execution()
     engine = get_engine(self._using)
     raw_results = engine.execute(self)
     rows = [Row(data) for data in raw_results]
     return Result(rows)
     ```

5. Remove any references to .fetch() in the Query class docstring and other methods:
   - Line ~35: ".fetch() returns List[Row] (internal/backward compatibility)"
   - Line ~307: "Called by .fetch() and .to_sql()"
   - Update to: "Called by .execute() and .to_sql()"

6. Verify no line in the file contains "def fetch"

Verification:
- `grep -n "def fetch" src/cubano/query.py` returns no results
- Query.execute() calls engine directly, not .fetch()
- Query.using() docstring references now point to .execute()
  </action>
</task>

<task type="auto">
  <name>Task 3: Make Query() constructor private or inaccessible</name>
  <files>src/cubano/query.py, src/cubano/__init__.py</files>
  <action>
Make Query() constructor private so users cannot instantiate it directly:

**Option A: Prefix with underscore (Pythonic private)**
1. Rename class `Query` to `_Query` in src/cubano/query.py
2. Update all internal imports:
   - src/cubano/models.py: Update SemanticView.query() to return _Query
   - src/cubano/results.py: Update any Query references to _Query
   - Any engine files: Update to use _Query

3. Update docstrings that show Query() examples to show _Query()

4. Verify: `grep -n "^class Query" src/cubano/query.py` returns no results

**Option B: Use __init_subclass__ or metaclass to prevent instantiation (advanced)**
- Add runtime check in __init__ to prevent direct instantiation
- Allow only SemanticView.query() to create instances
- (Option B is more complex; prefer Option A)

**Recommended: Use Option A** (rename to _Query)

After renaming:
1. Update src/cubano/__init__.py to NOT export Query:
   - Remove "Query" from __all__ if present
   - Remove any imports of Query

2. Verify Query is not accessible from public API:
   - `python -c "from cubano import Query"` should raise ImportError
   - `python -c "from cubano.query import _Query; print(_Query)"` should work (internal only)

Verification:
- Query renamed to _Query in all source files
- Public imports (src/cubano/__init__.py) do not export Query
- SemanticView.query() still works (returns _Query instance)
- `from cubano import Query` raises ImportError
  </action>
</task>

<task type="auto">
  <name>Task 4: Run quality gates and verify removals</name>
  <files>src/cubano/*.py</files>
  <action>
Verify that all API removals are complete and quality gates still pass:

1. **Search for old API references in source:**
   ```bash
   grep -r "def filter(" src/cubano/ --include="*.py"
   grep -r "def fetch(" src/cubano/ --include="*.py"
   ```
   Both should return 0 matches

2. **Check for Query() direct instantiation in src/:**
   ```bash
   grep -r "Query()" src/cubano/ --include="*.py"
   ```
   Should only find matches in docstrings/comments showing examples (and those should be updated to _Query() or removed)

3. **Typecheck:**
   ```bash
   uv run basedpyright
   ```
   Must pass with 0 errors (Query → _Query rename may cause initial failures; fix all references)

4. **Lint:**
   ```bash
   uv run ruff check src/
   ```
   Must pass with 0 errors

5. **Format:**
   ```bash
   uv run ruff format --check src/
   ```
   Must pass (apply fixes with `uv run ruff format src/` if needed)

Verification criteria:
- Zero matches for "def filter(" and "def fetch("
- Typecheck passes with 0 errors
- Lint passes with 0 errors
- All tests still compile/import successfully (test updates come in Plan 10.1-08)
  </action>
</task>

</tasks>


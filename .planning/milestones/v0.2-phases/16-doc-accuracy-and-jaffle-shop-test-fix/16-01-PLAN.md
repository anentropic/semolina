---
phase: 16-doc-accuracy-and-jaffle-shop-test-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/how-to/filtering.md
  - cubano-jaffle-shop/tests/test_warehouse_queries.py
autonomous: true
requirements:
  - DOCS-04
  - INT-02

must_haves:
  truths:
    - "filtering.md complex nested WHERE SQL example includes outer AND parentheses matching compiler output"
    - "filtering.md escape hatch section references SQLBuilder._compile_predicate(), not 'engine's'"
    - "cubano-jaffle-shop test_filter_comparison_greater_than uses .metrics() for lifetime_spend (not .dimensions())"
  artifacts:
    - path: "docs/src/how-to/filtering.md"
      provides: "Corrected SQL examples and escape hatch reference"
      contains: "(("
    - path: "cubano-jaffle-shop/tests/test_warehouse_queries.py"
      provides: "Corrected metric field usage in filter test"
      contains: ".metrics(Customers.lifetime_spend)"
  key_links:
    - from: "docs/src/how-to/filtering.md"
      to: "src/cubano/engines/sql.py"
      via: "SQL examples matching compiler output"
      pattern: "\\(\\(.*AND"
    - from: "cubano-jaffle-shop/tests/test_warehouse_queries.py"
      to: "cubano-jaffle-shop/src/cubano_jaffle_shop/jaffle_models.py"
      via: "field type matches query method"
      pattern: "\\.metrics\\(Customers\\.lifetime_spend\\)"
---

<objective>
Fix three non-blocking audit gaps: two SQL inaccuracies in filtering.md and one test bug in the jaffle-shop example project.

Purpose: Close the final three v0.2 milestone audit gaps (DOCS-04-NESTED-PARENS, DOCS-04-ESCAPE-HATCH, JAFFLE-SHOP-DIMENSIONS-BUG) so documentation matches compiler output and the example project uses the correct query API for Metric fields.

Output: Corrected `docs/src/how-to/filtering.md` and `cubano-jaffle-shop/tests/test_warehouse_queries.py`.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-doc-accuracy-and-jaffle-shop-test-fix/16-RESEARCH.md
@docs/src/how-to/filtering.md
@cubano-jaffle-shop/tests/test_warehouse_queries.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix filtering.md SQL examples and escape hatch reference</name>
  <files>docs/src/how-to/filtering.md</files>
  <action>
Make three edits to docs/src/how-to/filtering.md:

**Edit 1 — DOCS-04-NESTED-PARENS (Snowflake tab, around line 397-398):**

Change the Snowflake SQL example in the "Build complex nested conditions" section from:

```sql
WHERE ("country" = 'US' OR "country" = 'CA')
    AND NOT ("revenue" < 100)
```

To (add outer parentheses wrapping the AND expression):

```sql
WHERE (("country" = 'US' OR "country" = 'CA')
    AND NOT ("revenue" < 100))
```

**Edit 2 — DOCS-04-NESTED-PARENS (Databricks tab, around line 404-405):**

Change the Databricks SQL example in the same section from:

```sql
WHERE (`country` = 'US' OR `country` = 'CA')
    AND NOT (`revenue` < 100)
```

To (add outer parentheses):

```sql
WHERE ((`country` = 'US' OR `country` = 'CA')
    AND NOT (`revenue` < 100))
```

**Edit 3 — DOCS-04-ESCAPE-HATCH (around line 472-473):**

Change:

```
to your engine's `_compile_predicate()` method.
```

To:

```
to `SQLBuilder._compile_predicate()`.
```

The reason for edits 1-2: `SQLBuilder._compile_predicate()` always wraps AND nodes in outer parentheses (confirmed by `tests/unit/test_sql.py` test_compile_and). The docs must match. Both Snowflake and Databricks tabs must be updated together.

The reason for edit 3: `_compile_predicate()` lives on `SQLBuilder` (src/cubano/engines/sql.py line 369), not on any Engine class.

IMPORTANT: Preserve MkDocs Material 4-space indentation within `===` tabbed blocks.
  </action>
  <verify>
Run `uv run mkdocs build --strict` from the project root — must complete with no errors or warnings.

Visually confirm:
- Lines ~397-398 show `WHERE (("country" = 'US' OR "country" = 'CA')`
- Lines ~404-405 show `WHERE ((`country` = 'US' OR `country` = 'CA')`
- Line ~473 shows `` to `SQLBuilder._compile_predicate()`. ``
  </verify>
  <done>
filtering.md complex nested WHERE SQL examples include outer AND parentheses matching compiler output in both Snowflake and Databricks tabs, and the escape hatch section references `SQLBuilder._compile_predicate()` instead of "your engine's".
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix jaffle-shop test to use .metrics() for lifetime_spend</name>
  <files>cubano-jaffle-shop/tests/test_warehouse_queries.py</files>
  <action>
In `cubano-jaffle-shop/tests/test_warehouse_queries.py`, find the `test_filter_comparison_greater_than` method (line ~282).

Change:

```python
.dimensions(Customers.lifetime_spend)
```

To:

```python
.metrics(Customers.lifetime_spend)
```

The reason: `lifetime_spend` is declared as `Metric()` in `jaffle_models.py` line 53. Using `.dimensions()` on a Metric field generates incorrect SQL because metrics require aggregation wrapping (AGG/MEASURE). The test must use `.metrics()` to match the field type.
  </action>
  <verify>
Run `cd /Users/paul/Documents/Dev/Personal/cubano/cubano-jaffle-shop && uv run pytest tests/ -m mock -v` — all mock tests must pass.

NOTE: Must run from within `cubano-jaffle-shop/` directory (separate package with its own pyproject.toml).
  </verify>
  <done>
`test_filter_comparison_greater_than` uses `.metrics(Customers.lifetime_spend)` instead of `.dimensions(Customers.lifetime_spend)`, correctly matching the Metric field type.
  </done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` passes (validates filtering.md Markdown structure intact)
2. `cd cubano-jaffle-shop && uv run pytest tests/ -m mock -v` passes (validates jaffle-shop test fix)
3. `uv run --extra dev pytest` passes (validates no regressions in main test suite)
4. Visual inspection confirms all three edits match the corrected text from 16-RESEARCH.md
</verification>

<success_criteria>
- filtering.md nested WHERE SQL shows outer AND parentheses in both Snowflake and Databricks tabs
- filtering.md escape hatch references `SQLBuilder._compile_predicate()`, not "your engine's"
- jaffle-shop test_filter_comparison_greater_than uses `.metrics(Customers.lifetime_spend)`
- All quality gates pass: mkdocs build, pytest (main + jaffle-shop mock), typecheck, lint, format
</success_criteria>

<output>
After completion, create `.planning/phases/16-doc-accuracy-and-jaffle-shop-test-fix/16-01-SUMMARY.md`
</output>

---
phase: 13-docs-accuracy-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-ci-example-updates/11-VERIFICATION.md
autonomous: true
requirements:
  - DOCS-10

must_haves:
  truths:
    - "Phase 11 VERIFICATION.md exists at .planning/phases/11-ci-example-updates/11-VERIFICATION.md"
    - "VERIFICATION.md confirms CI has two separate pytest steps (unit tests + doctests)"
    - "Doctest command `uv run pytest src/ --doctest-modules -v` passes locally before VERIFICATION.md is written"
    - "DOCS-10 requirement is listed as SATISFIED with evidence in the VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/11-ci-example-updates/11-VERIFICATION.md"
      provides: "Formal verification of Phase 11 CI doctest enforcement"
      contains: "DOCS-10"
  key_links:
    - from: ".planning/phases/11-ci-example-updates/11-VERIFICATION.md"
      to: ".github/workflows/ci.yml"
      via: "Evidence references specific lines from ci.yml"
      pattern: "ci\\.yml"
---

<objective>
Create the missing Phase 11 VERIFICATION.md by running doctest commands locally, inspecting the CI workflow, and documenting evidence for each Phase 11 success criterion.

Purpose: Phase 11 has no VERIFICATION.md — the DOCS-10 requirement (doctest enforcement in CI) remains formally unverified. This plan closes that audit gap.
Output: `.planning/phases/11-ci-example-updates/11-VERIFICATION.md` with VERIFIED status and evidence table.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-docs-accuracy-verification/13-CONTEXT.md
@.planning/phases/13-docs-accuracy-verification/13-RESEARCH.md
@.planning/phases/11-ci-example-updates/11-01-SUMMARY.md
@.planning/phases/11-ci-example-updates/11-02-SUMMARY.md
@.planning/phases/12-warehouse-testing-syrupy/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run verification checks and create Phase 11 VERIFICATION.md</name>
  <files>.planning/phases/11-ci-example-updates/11-VERIFICATION.md</files>
  <action>
    Verify Phase 11 by running all required checks, then write the VERIFICATION.md.

    STEP 1 — Inspect CI workflow (do not modify):
    Read `.github/workflows/ci.yml` and confirm:
    - Two separate pytest steps exist: "Run unit tests" (`uv run pytest tests/ -n auto -v --snapshot-warn-unused`) and "Run doctests" (`uv run pytest src/ --doctest-modules -v`)
    - No merged `pytest tests/ src/` form exists in the file

    STEP 2 — Run unit tests locally:
    ```
    uv run pytest tests/ -n auto -v
    ```
    Capture the summary line (N passed, N failed, etc.) and exit code. If exit code is non-zero and failures are pre-existing (not introduced by Phase 11), note that; do not block on pre-existing failures like `test_snowflake_engine.py`.

    STEP 3 — Run doctests locally (LOCKED DECISION: block if failures):
    ```
    uv run pytest src/ --doctest-modules -v
    ```
    If ANY doctest fails (exit code non-zero), fix the failing doctest BEFORE writing VERIFICATION.md. Report the actual item count (do not assume 16 or 26 — use the actual output). If 6 items are skipped, that is expected (warehouse-dependent doctests).

    STEP 4 — Read Phase 11 CONTEXT.md at `.planning/phases/11-ci-example-updates/11-CONTEXT.md` to extract the Phase 11 success criteria.

    STEP 5 — Create VERIFICATION.md following the Phase 12 format exactly (the template):
    - Frontmatter: `phase: 11-ci-example-updates`, `verified: <ISO timestamp>`, `status: VERIFIED`, `score: N/N must-haves verified`, `re_verification: true`
    - Opening header: Phase 11 CI & Example Updates Verification Report
    - Fields: Phase Goal (from Phase 11 CONTEXT.md), Verified date, Status, Re-verification note ("Yes — verified in Phase 13 gap closure")
    - Observable Truths table: one row per Phase 11 success criterion, Status column = VERIFIED, Evidence column = short command output or file excerpt
      The truths to verify (from Phase 11 CONTEXT.md):
      1. CI test command runs unit tests (pytest tests/) separately from doctests (pytest src/ --doctest-modules)
      2. cubano-jaffle-shop example uses Model-centric API (Model.query(), .execute(), no deprecated Query)
      3. All tests pass including doctest validation
      4. DOCS-10 requirement complete (separate CI step for doctest enforcement)
    - Required Artifacts table: list PLAN.md files as source, verify deliverables exist
    - Key Link Verification table: ci.yml → doctest step linkage
    - Requirements Coverage table: DOCS-10 row with status SATISFIED and evidence
    - Gaps Summary section: "No automated gaps found" if all truths verified

    Evidence format for Observable Truths (match Phase 12 pattern):
    - Use backtick inline code for command outputs
    - Include file line references (e.g., "`.github/workflows/ci.yml` lines 109-113: `uv run pytest src/ --doctest-modules -v`")
    - Include actual doctest count from local run

    Use Write tool to create the file. Follow the exact frontmatter schema from Phase 12 VERIFICATION.md.
  </action>
  <verify>
    1. File exists: `ls .planning/phases/11-ci-example-updates/11-VERIFICATION.md`
    2. File has required frontmatter: `grep "status: VERIFIED" .planning/phases/11-ci-example-updates/11-VERIFICATION.md`
    3. File references DOCS-10: `grep "DOCS-10" .planning/phases/11-ci-example-updates/11-VERIFICATION.md`
    4. File references SATISFIED: `grep "SATISFIED" .planning/phases/11-ci-example-updates/11-VERIFICATION.md`
    5. Doctests pass: `uv run pytest src/ --doctest-modules -v` exits 0
  </verify>
  <done>
    `.planning/phases/11-ci-example-updates/11-VERIFICATION.md` exists with `status: VERIFIED` frontmatter, observable truths table with evidence for all Phase 11 success criteria, and DOCS-10 listed as SATISFIED. Doctest command passes locally before file is written.
  </done>
</task>

</tasks>

<verification>
Run after plan completion:
1. `ls .planning/phases/11-ci-example-updates/11-VERIFICATION.md` — file exists
2. `grep "status:" .planning/phases/11-ci-example-updates/11-VERIFICATION.md` — shows "VERIFIED"
3. `grep "DOCS-10" .planning/phases/11-ci-example-updates/11-VERIFICATION.md` — requirement mentioned
4. `uv run pytest src/ --doctest-modules -v` — exits 0 (all doctests pass or are expected-skip)
</verification>

<success_criteria>
Phase 11 VERIFICATION.md exists, has VERIFIED status, documents evidence for each success criterion including the CI two-step pytest configuration, and formally marks DOCS-10 as SATISFIED.
</success_criteria>

<output>
After completion, create `.planning/phases/13-docs-accuracy-verification/13-01-SUMMARY.md`
</output>

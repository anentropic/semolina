---
phase: 07.1-ci-cd-setup-with-github-actions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
autonomous: true

must_haves:
  truths:
    - "Dependabot scans dev dependencies weekly"
    - "GitHub Actions dependencies are tracked and updated"
    - "Security updates run automatically with higher priority"
  artifacts:
    - path: ".github/dependabot.yml"
      provides: "Dependabot configuration for pip and GitHub Actions"
      contains: "package-ecosystem:"
  key_links:
    - from: ".github/dependabot.yml"
      to: "dev dependencies"
      via: "pip ecosystem with development grouping"
      pattern: "dependency-type.*development"
---

<objective>
Update Dependabot configuration to track dev dependencies and GitHub Actions.

Purpose: Automated security and version updates for dependencies
Output: Updated .github/dependabot.yml with proper pip and GitHub Actions tracking
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07.1-ci-cd-setup-with-github-actions/07.1-CONTEXT.md
@.planning/phases/07.1-ci-cd-setup-with-github-actions/07.1-RESEARCH.md
@.github/dependabot.yml
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Update Dependabot config for dev dependencies grouping</name>
  <files>.github/dependabot.yml</files>
  <action>
Update the existing `.github/dependabot.yml` file to properly group dev dependencies:

Current config groups all python dependencies together. Update to distinguish development dependencies from production dependencies (though Cubano has zero production deps).

Replace the pip ecosystem configuration with:

```yaml
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    groups:
      dev-dependencies:
        dependency-type: "development"
```

Keep the existing github-actions ecosystem configuration unchanged.

**Rationale:**
- Cubano has zero runtime dependencies (dependencies = [])
- Optional dependencies (databricks, snowflake) are user-installed, not in CI
- Only dev dependencies need tracking (basedpyright, pytest, ruff, etc.)
- Group dev deps to reduce PR noise
- Weekly schedule balances freshness with review burden

Per user decision: Use GitHub Dependabot for dependency vulnerability scanning (no additional security tools).
  </action>
  <verify>
Check updated Dependabot config:
```bash
cat .github/dependabot.yml
```

Verify:
- [ ] pip ecosystem uses dependency-type: "development" grouping
- [ ] github-actions ecosystem unchanged
- [ ] Weekly schedule retained
- [ ] No minimum/maximum version constraints
  </verify>
  <done>
Dependabot config updated at `.github/dependabot.yml` with proper dev dependency grouping. GitHub Actions dependencies already tracked.
  </done>
</task>

</tasks>

<verification>
Manual verification after merge to main:

1. Navigate to GitHub repository → Insights → Dependency graph → Dependabot
2. Verify two ecosystems appear: pip and github-actions
3. Wait for first Dependabot run (or trigger manually)
4. Verify Dependabot PRs group dev dependencies together
5. Confirm security updates run separately from weekly version updates

Expected behavior:
- Weekly PRs for grouped dev dependency updates
- Immediate PRs for security vulnerabilities (separate from weekly schedule)
- GitHub Actions updates as separate PRs

Per user decision: Weekly update frequency, no additional security scanning tools.
</verification>

<success_criteria>
- [ ] .github/dependabot.yml updated with development dependency grouping
- [ ] pip ecosystem configured with weekly schedule
- [ ] github-actions ecosystem unchanged
- [ ] Configuration valid (no YAML syntax errors)
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ci-cd-setup-with-github-actions/07.1-04-SUMMARY.md`
</output>

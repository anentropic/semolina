---
phase: 06-databricks-backend
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/test_databricks_engine.py
autonomous: true

must_haves:
  truths:
    - "DatabricksEngine can be instantiated without Databricks connection"
    - "Lazy import raises helpful error when databricks-sql-connector missing"
    - "to_sql() generates correct SQL with MEASURE() and backticks"
    - "execute() uses context managers for connection lifecycle"
    - "Databricks errors are caught and translated to RuntimeError"
    - "Results are mapped from tuples to dicts with correct column names"
  artifacts:
    - path: "tests/test_databricks_engine.py"
      provides: "Comprehensive unit tests for DatabricksEngine"
      min_lines: 200
      contains: "class TestDatabricksEngine"
  key_links:
    - from: "tests/test_databricks_engine.py"
      to: "unittest.mock"
      via: "Mock Databricks connector for testing"
      pattern: "Mock.*databricks\\.sql"
    - from: "TestDatabricksEngineExecute"
      to: "DatabricksEngine.execute()"
      via: "test connection lifecycle and result mapping"
      pattern: "test_.*execute"
---

<objective>
Create comprehensive unit tests for DatabricksEngine using unittest.mock to simulate Databricks connector behavior.

Purpose: Verify DatabricksEngine connection management, SQL generation, error handling, and result mapping without requiring real Databricks workspace access.

Output: Full test coverage for DatabricksEngine including lazy import, context managers, error translation, and result mapping.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-databricks-backend/06-RESEARCH.md
@.planning/phases/06-databricks-backend/06-01-SUMMARY.md
@.planning/phases/05-snowflake-backend/05-02-SUMMARY.md
@src/cubano/engines/databricks.py
@tests/test_engines.py
@tests/test_snowflake_engine.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive tests for DatabricksEngine</name>
  <files>tests/test_databricks_engine.py</files>
  <action>
Create tests/test_databricks_engine.py with comprehensive test coverage. Mirror test_snowflake_engine.py structure with Databricks-specific adaptations.

**Test structure:**

1. **TestDatabricksEngineInit** (lazy import and initialization):
   - test_init_stores_connection_params: Verify __init__ stores params without creating connection
   - test_init_creates_databricks_dialect: Verify self.dialect is DatabricksDialect instance
   - test_lazy_import_raises_helpful_error: Mock missing databricks.sql, verify ImportError message includes installation instructions

2. **TestDatabricksEngineToSQL** (SQL generation delegation):
   - test_to_sql_delegates_to_sqlbuilder: Verify to_sql() uses SQLBuilder(DatabricksDialect)
   - test_to_sql_generates_measure_syntax: Verify metrics wrapped in MEASURE()
   - test_to_sql_quotes_identifiers_with_backticks: Verify identifiers use backticks
   - test_to_sql_escapes_backticks: Verify field names with backticks are escaped (e.g., 'my`field' → `my``field`)
   - test_to_sql_handles_unity_catalog_three_part_names: Verify catalog.schema.view → `catalog`.`schema`.`view`

3. **TestDatabricksEngineExecute** (execution with mocked connector):
   - test_execute_uses_context_manager_for_connection: Mock databricks.sql.connect, verify called with connection params, verify __enter__ and __exit__ called
   - test_execute_uses_context_manager_for_cursor: Mock cursor, verify __enter__ and __exit__ called
   - test_execute_calls_cursor_execute_with_sql: Mock cursor.execute, verify called with SQL from to_sql()
   - test_execute_maps_tuples_to_dicts: Mock cursor.description and cursor.fetchall, verify tuples mapped to dicts with correct column names
   - test_execute_returns_list_of_dicts: Verify return type is list[dict[str, Any]]
   - test_execute_handles_empty_results: Mock cursor.fetchall() returning [], verify returns empty list

4. **TestDatabricksEngineErrorHandling**:
   - test_database_error_translated_to_runtime_error: Mock cursor.execute raising DatabaseError, verify RuntimeError raised with error message
   - test_operational_error_translated_to_runtime_error: Mock cursor.execute raising OperationalError, verify RuntimeError raised with error message
   - test_generic_error_translated_to_runtime_error: Mock cursor.execute raising Error, verify RuntimeError raised with error message
   - test_original_exception_chained: Verify RuntimeError has __cause__ set to original exception (from e pattern)

5. **TestDatabricksEngineIntegration** (end-to-end with mocks):
   - test_full_query_execution_flow: Mock full connection → cursor → execute → fetchall flow, verify complete pipeline works
   - test_multiple_queries_same_engine: Create engine once, execute multiple queries, verify connection params reused
   - test_unity_catalog_three_part_name_query: Mock query with catalog.schema.view, verify SQL generation and execution

**Mock patterns:**

Use unittest.mock.patch to mock databricks.sql:
```python
from unittest.mock import MagicMock, patch
from databricks.sql.exc import DatabaseError, OperationalError, Error

@patch('cubano.engines.databricks.databricks.sql')
def test_execute_uses_context_manager(mock_sql):
    mock_conn = MagicMock()
    mock_sql.connect.return_value.__enter__.return_value = mock_conn
    # ... assertions
```

Mock cursor.description:
```python
mock_cursor.description = [('revenue',), ('country',)]
mock_cursor.fetchall.return_value = [(100, 'US'), (200, 'CA')]
# Expected: [{'revenue': 100, 'country': 'US'}, {'revenue': 200, 'country': 'CA'}]
```

**Test data reuse:**
- Use Sales model from conftest.py
- Use Query().metrics(Sales.revenue).dimensions(Sales.country) pattern
- Verify SQL matches expected Databricks syntax from Phase 3 (MEASURE() and backticks)

**Key differences from test_snowflake_engine.py:**
- Mock databricks.sql instead of snowflake.connector
- Use databricks.sql.exc exceptions (DatabaseError, OperationalError, Error) instead of ProgrammingError, DatabaseError
- Verify MEASURE() wrapping instead of AGG()
- Verify backtick quoting instead of double quotes
- Add Unity Catalog three-part naming tests
- Connection params use server_hostname, http_path, access_token instead of account, warehouse, password

**Quality requirements:**
- Each test should be focused and test one thing
- Use descriptive test names following test_<what>_<expected_behavior> pattern
- Use pytest fixtures for common setup (engine with mocked connector)
- Follow existing test file conventions from tests/test_engines.py and tests/test_snowflake_engine.py
- Comprehensive docstrings for test classes explaining what aspect is tested

Run quality gates after test creation to ensure all tests pass.
  </action>
  <verify>
- File exists: tests/test_databricks_engine.py
- uv run basedpyright tests/test_databricks_engine.py — 0 errors
- uv run ruff check tests/test_databricks_engine.py — All checks passed
- uv run ruff format --check tests/test_databricks_engine.py — Formatted correctly
- uv run --extra dev pytest tests/test_databricks_engine.py — All new tests pass
- uv run --extra dev pytest — All tests pass (no regressions)
- At least 15 tests in test_databricks_engine.py
- Test coverage includes: init, to_sql, execute, error handling, integration, Unity Catalog
  </verify>
  <done>
Comprehensive test suite for DatabricksEngine with 15+ tests covering initialization, SQL generation delegation, connection lifecycle with context managers, result mapping, error translation, Unity Catalog three-part naming, and end-to-end integration flows. All tests pass with no regressions.
  </done>
</task>

</tasks>

<verification>
**Overall Test Coverage:**

1. Initialization:
   - Connection params stored without creating connection
   - DatabricksDialect created and stored
   - Lazy import error handling

2. SQL Generation:
   - Delegation to SQLBuilder verified
   - MEASURE() wrapping verified
   - Backtick identifier quoting verified
   - Backtick escaping verified
   - Unity Catalog three-part naming verified

3. Execution:
   - Context manager usage for connection verified
   - Context manager usage for cursor verified
   - cursor.execute called with correct SQL
   - Results mapped from tuples to dicts
   - Empty results handled correctly

4. Error Handling:
   - DatabaseError caught and translated
   - OperationalError caught and translated
   - Error (generic) caught and translated
   - Original exception chained (from e)
   - Error messages include helpful context

5. Integration:
   - Full query flow tested end-to-end
   - Multiple queries on same engine verified
   - Connection params reused correctly
   - Unity Catalog queries work end-to-end

**Quality Gates:**
- basedpyright: 0 errors
- ruff check: All passed
- ruff format: Formatted correctly
- pytest: All new tests pass, no regressions
</verification>

<success_criteria>
1. tests/test_databricks_engine.py exists with 15+ tests
2. All tests use unittest.mock to simulate Databricks connector
3. Test coverage includes init, to_sql, execute, errors, integration, Unity Catalog
4. Context manager usage verified (connection and cursor)
5. Result mapping verified (tuples → dicts with correct column names)
6. Error translation verified (DatabaseError, OperationalError, Error → RuntimeError)
7. Lazy import error handling verified
8. Unity Catalog three-part naming verified
9. All quality gates pass
10. No test regressions in existing test suite
11. Tests follow existing conventions from test_engines.py and test_snowflake_engine.py
</success_criteria>

<output>
After completion, create `.planning/phases/06-databricks-backend/06-02-SUMMARY.md`
</output>

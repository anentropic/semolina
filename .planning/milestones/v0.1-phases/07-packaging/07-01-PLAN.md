---
phase: 07-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-packaging/07-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Wheel and source distribution build successfully without errors"
    - "Wheel contains py.typed marker for type checking support"
    - "Distribution metadata includes correct version, dependencies, and extras"
    - "RECORD file is valid and matches wheel contents"
  artifacts:
    - path: "dist/cubano-0.1.0-py3-none-any.whl"
      provides: "Built wheel distribution"
      min_size_kb: 10
    - path: "dist/cubano-0.1.0.tar.gz"
      provides: "Built source distribution"
      min_size_kb: 5
  key_links:
    - from: "pyproject.toml"
      to: "dist/cubano-*.whl"
      via: "uv build backend"
      pattern: "cubano-0.1.0"
    - from: "src/cubano/py.typed"
      to: "wheel contents"
      via: "automatic inclusion"
      pattern: "cubano/py.typed"
---

<objective>
Build distributions and verify packaging configuration is correct

Purpose: Ensure the project builds successfully with uv-build and produces well-formed distributions containing all necessary files (source code, py.typed marker, metadata) with proper structure and valid RECORD files. Validates PKG-01 (zero dependencies), PKG-02/PKG-03 (extras declared), and PKG-05 (py.typed marker).

Output: Verified wheel and source distributions ready for installation testing
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-packaging/07-RESEARCH.md

# Configuration
@pyproject.toml

# Source files that should be in distribution
@src/cubano/__init__.py
@src/cubano/py.typed
@src/cubano/engines/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Build distributions with uv</name>
  <files>
    dist/cubano-0.1.0-py3-none-any.whl
    dist/cubano-0.1.0.tar.gz
  </files>
  <action>
Run `uv build` to create wheel and source distributions. This builds using the uv-build backend configured in pyproject.toml.

Expected outputs:
- `dist/cubano-0.1.0-py3-none-any.whl` - wheel distribution
- `dist/cubano-0.1.0.tar.gz` - source distribution

If build fails, capture error output for debugging.
  </action>
  <verify>
Build completes without errors. Both wheel and sdist exist in dist/ directory:
```bash
uv build
ls -lh dist/cubano-0.1.0-py3-none-any.whl dist/cubano-0.1.0.tar.gz
```
  </verify>
  <done>
Both distributions exist in dist/ directory with reasonable file sizes (wheel >10KB, sdist >5KB)
  </done>
</task>

<task type="auto">
  <name>Inspect wheel contents and verify structure</name>
  <files>
    .planning/phases/07-packaging/wheel-contents.txt
  </files>
  <action>
Extract and verify wheel contents to ensure correct package structure:

1. List wheel contents with `unzip -l dist/cubano-0.1.0-py3-none-any.whl`
2. Verify critical files are present:
   - `cubano/__init__.py` - main package
   - `cubano/py.typed` - type marker (PKG-05)
   - All source modules (fields.py, query.py, models.py, filters.py, results.py, registry.py)
   - `cubano/engines/__init__.py`, `cubano/engines/base.py`, `cubano/engines/sql.py`, `cubano/engines/mock.py`, `cubano/engines/snowflake.py`, `cubano/engines/databricks.py`
   - `cubano-0.1.0.dist-info/METADATA` - package metadata
   - `cubano-0.1.0.dist-info/WHEEL` - wheel metadata
   - `cubano-0.1.0.dist-info/RECORD` - file checksums

3. Extract and inspect METADATA file for:
   - Name: cubano
   - Version: 0.1.0
   - Requires-Python: >=3.11
   - Dependencies: (empty - zero dependencies, PKG-01)
   - Provides-Extra: databricks, snowflake (PKG-02, PKG-03)
   - Requires-Dist entries with `extra == "databricks"` and `extra == "snowflake"`

Save full wheel listing to `.planning/phases/07-packaging/wheel-contents.txt` for SUMMARY reference.
  </action>
  <verify>
All expected files present in wheel. Verify py.typed specifically:
```bash
unzip -l dist/cubano-0.1.0-py3-none-any.whl | grep py.typed
# Expected: cubano/py.typed

# Verify metadata
unzip -p dist/cubano-0.1.0-py3-none-any.whl cubano-0.1.0.dist-info/METADATA | grep -E "(Name:|Version:|Requires-Python:|Provides-Extra:|Requires-Dist:)"
```
  </verify>
  <done>
Wheel contains all source files, py.typed marker, correct metadata with version 0.1.0, zero core dependencies, and both extras (databricks, snowflake) with their dependencies. Full contents logged to wheel-contents.txt.
  </done>
</task>

<task type="auto">
  <name>Validate distribution quality with twine</name>
  <files>
    .planning/phases/07-packaging/twine-check.txt
  </files>
  <action>
Install twine and validate distributions:

1. Install twine: `uv pip install twine`
2. Run validation: `twine check dist/*`

This checks for:
- Valid metadata (PEP 621 compliance)
- RECORD file integrity (2026 PyPI requirement - critical)
- README rendering for PyPI (if applicable)
- Wheel structure correctness

Save twine output to `.planning/phases/07-packaging/twine-check.txt`.

If any warnings or errors appear, document them in the SUMMARY with recommended fixes.
  </action>
  <verify>
Twine validation passes:
```bash
twine check dist/*
# Expected: "PASSED" for both wheel and sdist
```
  </verify>
  <done>
Both distributions pass twine validation with no errors or warnings. RECORD file is valid (2026 PyPI compliance). Results saved to twine-check.txt.
  </done>
</task>

</tasks>

<verification>
Manual inspection checklist:
- [ ] `uv build` completes without errors
- [ ] Wheel file exists: `dist/cubano-0.1.0-py3-none-any.whl`
- [ ] Source distribution exists: `dist/cubano-0.1.0.tar.gz`
- [ ] `py.typed` marker present in wheel
- [ ] METADATA shows zero core dependencies
- [ ] METADATA shows databricks and snowflake extras with correct dependencies
- [ ] twine check passes with "PASSED" status
</verification>

<success_criteria>
Build system produces valid distributions that:
1. Contain all source modules and py.typed marker
2. Declare correct metadata (version, Python requirement, extras)
3. Have zero core dependencies (enabling `pip install cubano` without backend libraries)
4. Include properly formatted extras for databricks and snowflake
5. Pass PyPI validation checks (twine, RECORD integrity)
</success_criteria>

<output>
After completion, create `.planning/phases/07-packaging/07-01-SUMMARY.md` documenting:
- Build process and outputs
- Wheel contents verification (key files present)
- Metadata validation results
- Any issues found and resolutions
</output>

---
phase: 05-snowflake-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cubano/engines/snowflake.py
  - src/cubano/engines/__init__.py
autonomous: true

must_haves:
  truths:
    - "SnowflakeEngine connects to Snowflake warehouse with connection parameters"
    - "SnowflakeEngine generates SQL with AGG(metric) wrapper for metrics"
    - "snowflake-connector-python imports lazily (only when SnowflakeEngine instantiated)"
    - "Connection lifecycle uses context managers for proper cleanup"
    - "Snowflake errors are translated to helpful RuntimeError messages"
  artifacts:
    - path: "src/cubano/engines/snowflake.py"
      provides: "SnowflakeEngine class with connection management"
      min_lines: 150
      exports: ["SnowflakeEngine"]
    - path: "src/cubano/engines/__init__.py"
      provides: "Public API export for SnowflakeEngine"
      contains: "SnowflakeEngine"
  key_links:
    - from: "src/cubano/engines/snowflake.py"
      to: "snowflake.connector"
      via: "lazy import in __init__"
      pattern: "import snowflake\\.connector"
    - from: "SnowflakeEngine.to_sql()"
      to: "SQLBuilder + SnowflakeDialect"
      via: "SQL generation (reuse Phase 3)"
      pattern: "SQLBuilder.*SnowflakeDialect"
    - from: "SnowflakeEngine.execute()"
      to: "snowflake.connector.connect()"
      via: "context manager"
      pattern: "with snowflake\\.connector\\.connect"
---

<objective>
Implement SnowflakeEngine with lazy driver import, connection management, and error handling.

Purpose: Enable queries to execute against Snowflake Semantic Views with proper AGG() syntax, connection lifecycle, and helpful error messages.

Output: Production-ready SnowflakeEngine class integrated into public API, fully reusing Phase 3 SQL generation architecture.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-snowflake-backend/05-RESEARCH.md
@.planning/phases/03-sql-generation-mock-backend/03-01-SUMMARY.md
@.planning/phases/04-execution-results/04-03-SUMMARY.md
@src/cubano/engines/base.py
@src/cubano/engines/sql.py
@src/cubano/engines/mock.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SnowflakeEngine with lazy import and connection management</name>
  <files>src/cubano/engines/snowflake.py</files>
  <action>
Create src/cubano/engines/snowflake.py implementing SnowflakeEngine class:

**Structure:**
- Inherit from Engine ABC (from cubano.engines.base)
- Use TYPE_CHECKING for type hints to avoid import at module load
- Lazy import snowflake.connector in __init__ with helpful ImportError

**__init__ method:**
- Accept **connection_params: Any (dictionary of connection parameters)
- Store params in self._connection_params for execute() time
- Lazy import snowflake.connector with clear error message if missing:
  "snowflake-connector-python is required for SnowflakeEngine. Install with: pip install cubano[snowflake]"
- Initialize self.dialect = SnowflakeDialect() (reuse from sql.py)
- Do NOT create connection here (connection creation deferred to execute())

**to_sql(query: Query) -> str:**
- Reuse SQLBuilder from sql.py with SnowflakeDialect
- Pattern: SQLBuilder(self.dialect).build_select(query)
- This generates SQL with AGG() wrapping and double-quoted identifiers
- No new code needed — just delegation to existing Phase 3 implementation

**execute(query: Query) -> list[dict[str, Any]]:**
- Import snowflake.connector (already imported in __init__, available in scope)
- Use context manager for connection: with snowflake.connector.connect(**self._connection_params) as conn:
- Nested context manager for cursor: with conn.cursor() as cur:
- Generate SQL: sql = self.to_sql(query)
- Execute: cur.execute(sql)
- Get column names from cursor.description: columns = [desc[0] for desc in cur.description]
- Fetch results: rows = cur.fetchall()
- Map tuples to dicts: return [dict(zip(columns, row)) for row in rows]
- Wrap in try-except to catch snowflake.connector.errors (ProgrammingError, DatabaseError)
- Translate to RuntimeError with helpful message including error code and SQL state

**Error handling:**
- Import from snowflake.connector.errors: ProgrammingError, DatabaseError
- Catch ProgrammingError (SQL syntax, invalid object): raise RuntimeError(f"Snowflake query failed: {e.msg} (Error {e.errno}, SQL State {e.sqlstate})") from e
- Catch DatabaseError (connection, permission): raise RuntimeError(f"Snowflake database error: {e.msg}") from e

**Docstrings:**
- Class docstring explaining connection parameters, AGG() syntax, and context manager pattern
- Include example showing connection parameter dict
- Document that account parameter needs region suffix (e.g., "xy12345.us-east-1")
- Each method with comprehensive Args/Returns/Raises sections
- Follow D213 style (summary on second line after opening quotes)

**Imports:**
```python
from __future__ import annotations

from typing import TYPE_CHECKING, Any

from cubano.engines.base import Engine
from cubano.engines.sql import SnowflakeDialect, SQLBuilder

if TYPE_CHECKING:
    import snowflake.connector
    from snowflake.connector.connection import SnowflakeConnection

    from cubano.query import Query
```

Follow project conventions: 100 char line length, strict type checking, comprehensive docstrings.
  </action>
  <verify>
- File exists: src/cubano/engines/snowflake.py
- uv run basedpyright src/cubano/engines/snowflake.py — 0 errors
- uv run ruff check src/cubano/engines/snowflake.py — All checks passed
- uv run ruff format --check src/cubano/engines/snowflake.py — Formatted correctly
- SnowflakeEngine class exists with __init__, to_sql, execute methods
- Lazy import pattern verified (TYPE_CHECKING guard + import in __init__)
  </verify>
  <done>
SnowflakeEngine class exists with lazy import, connection management via context managers, error translation from Snowflake exceptions to RuntimeError, and SQL generation delegating to SQLBuilder(SnowflakeDialect).
  </done>
</task>

<task type="auto">
  <name>Task 2: Export SnowflakeEngine in public API and run quality gates</name>
  <files>src/cubano/engines/__init__.py</files>
  <action>
Update src/cubano/engines/__init__.py to export SnowflakeEngine:

**Add to imports:**
```python
from cubano.engines.snowflake import SnowflakeEngine
```

**Add to __all__ list:**
```python
__all__ = [
    "Engine",
    "Dialect",
    "SnowflakeDialect",
    "DatabricksDialect",
    "MockDialect",
    "MockEngine",
    "SnowflakeEngine",  # NEW
]
```

**Run all quality gates:**
- uv run basedpyright — strict mode, 0 errors
- uv run ruff check — all files pass
- uv run ruff format --check — all files formatted
- uv run --extra dev pytest — all existing tests pass (no new tests in this task)

Note: Tests are in Plan 02, this task only ensures the implementation doesn't break existing tests.
  </action>
  <verify>
- src/cubano/engines/__init__.py includes SnowflakeEngine in imports and __all__
- from cubano.engines import SnowflakeEngine works (no ImportError)
- uv run basedpyright — 0 errors
- uv run ruff check — All checks passed
- uv run ruff format --check — All files formatted
- uv run --extra dev pytest — All tests pass (no regressions)
  </verify>
  <done>
SnowflakeEngine is exported from cubano.engines public API, all quality gates pass, no test regressions.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**

1. SnowflakeEngine class structure:
   - Inherits from Engine ABC
   - Uses lazy import pattern (TYPE_CHECKING + import in __init__)
   - Stores connection params without creating connection
   - Reuses SnowflakeDialect and SQLBuilder from Phase 3

2. Connection management:
   - Context managers (with statements) for connection and cursor
   - No connection in __init__ (deferred to execute())
   - Proper cleanup guaranteed by context managers

3. Error handling:
   - Catches ProgrammingError and DatabaseError
   - Translates to RuntimeError with helpful messages
   - Includes error code, SQL state, and message from Snowflake

4. SQL generation:
   - Delegates to SQLBuilder(SnowflakeDialect).build_select(query)
   - Generates AGG() wrapping for metrics
   - Uses double-quoted identifiers
   - No new SQL generation logic (reuse Phase 3)

5. Result mapping:
   - Uses cursor.description for column names
   - Maps tuples to list[dict[str, Any]]
   - Consistent with MockEngine return format

6. Quality gates:
   - basedpyright strict mode: 0 errors
   - ruff check: All passed
   - ruff format: All formatted
   - pytest: All tests pass
</verification>

<success_criteria>
1. SnowflakeEngine class exists in src/cubano/engines/snowflake.py
2. Lazy import prevents ImportError for users without snowflake-connector-python
3. Connection parameters stored but connection deferred to execute() time
4. Context managers guarantee connection cleanup
5. SQL generation delegates to existing SQLBuilder + SnowflakeDialect
6. Results mapped from tuples to dicts using cursor.description
7. Snowflake errors translated to helpful RuntimeError messages
8. SnowflakeEngine exported from cubano.engines public API
9. All quality gates pass (basedpyright, ruff, pytest)
10. No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-snowflake-backend/05-01-SUMMARY.md`
</output>

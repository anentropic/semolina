---
phase: 13-docs-accuracy-verification
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/guides/warehouse-testing.md
autonomous: true
requirements:
  - TEST-VCR
  - DOCS-09

must_haves:
  truths:
    - "warehouse-testing.md references `tests/integration/conftest.py` (not `tests/conftest.py`)"
    - "warehouse-testing.md references `tests/integration/test_queries.py` (not `tests/test_snapshot_queries.py`)"
    - "warehouse-testing.md references `TEST_DATA` constant (not `SNAPSHOT_TEST_DATA`)"
    - "warehouse-testing.md references `tests/integration/__snapshots__/` (not `tests/__snapshots__/`)"
    - "warehouse-testing.md uses `DATABRICKS_SERVER_HOSTNAME` env var (not `DATABRICKS_HOST`)"
    - "After edits, no grep hits for the 5 wrong values remain in the file"
  artifacts:
    - path: "docs/src/guides/warehouse-testing.md"
      provides: "Accurate warehouse testing developer guide"
      contains: "tests/integration/conftest.py"
  key_links:
    - from: "docs/src/guides/warehouse-testing.md"
      to: "tests/integration/conftest.py"
      via: "File path references in guide match actual project structure"
      pattern: "tests/integration/conftest\\.py"
---

<objective>
Fix five categories of inaccuracies in `warehouse-testing.md` — all introduced when the test directory was restructured from a flat `tests/` layout to `tests/integration/` subfolder and when constants were renamed during Phase 12.

Purpose: The warehouse testing guide is currently non-functional for new contributors — wrong file paths, wrong constant names, wrong env var name mean no step in the Quick Start works as documented.
Output: Corrected `docs/src/guides/warehouse-testing.md` where all references match actual project files.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-docs-accuracy-verification/13-CONTEXT.md
@.planning/phases/13-docs-accuracy-verification/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 5 categories of inaccuracies in warehouse-testing.md</name>
  <files>docs/src/guides/warehouse-testing.md</files>
  <action>
    Read `docs/src/guides/warehouse-testing.md` in full first.

    Then make ALL of the following corrections (all 5 categories — fixing only some leaves the guide broken):

    CATEGORY 1 — Wrong conftest location (line 17 and other occurrences):
    - `tests/conftest.py` → `tests/integration/conftest.py`
    - Verify after: `grep "tests/conftest.py" docs/src/guides/warehouse-testing.md` → 0 matches

    CATEGORY 2 — Wrong test file name (~lines 43, 82, 175 and other occurrences):
    - `tests/test_snapshot_queries.py` → `tests/integration/test_queries.py`
    - Also update recording commands: `pytest --snapshot-update tests/test_snapshot_queries.py` → `uv run pytest tests/integration/test_queries.py --snapshot-update`
    - Verify after: `grep "test_snapshot_queries" docs/src/guides/warehouse-testing.md` → 0 matches

    CATEGORY 3 — Wrong constant name (~lines 54-57, 153, 169, 215, 217 and other occurrences):
    - `SNAPSHOT_TEST_DATA` → `TEST_DATA`
    - Verify after: `grep "SNAPSHOT_TEST_DATA" docs/src/guides/warehouse-testing.md` → 0 matches

    CATEGORY 4 — Wrong snapshot directory (~lines 92, 100, 183, 225 and other occurrences):
    - `tests/__snapshots__/` → `tests/integration/__snapshots__/`
    - Update `git diff` examples accordingly: `git diff tests/__snapshots__/` → `git diff tests/integration/__snapshots__/`
    - Verify after: `grep "tests/__snapshots__/" docs/src/guides/warehouse-testing.md` → 0 matches

    CATEGORY 5 — Wrong Databricks env var (~lines 137, 285 and other occurrences):
    - `DATABRICKS_HOST` → `DATABRICKS_SERVER_HOSTNAME`
    - Verify after: `grep "DATABRICKS_HOST[^N]" docs/src/guides/warehouse-testing.md` → 0 matches

    IMPORTANT: Read the actual file for exact line numbers — the research provides approximate references. Make all corrections in a single Write operation after reading the full file content. Do not miss occurrences outside the approximate line ranges.

    After editing, do a final verification grep for each wrong value:
    - `grep "tests/conftest\.py" docs/src/guides/warehouse-testing.md` → 0 results
    - `grep "test_snapshot_queries" docs/src/guides/warehouse-testing.md` → 0 results
    - `grep "SNAPSHOT_TEST_DATA" docs/src/guides/warehouse-testing.md` → 0 results
    - `grep "tests/__snapshots__/" docs/src/guides/warehouse-testing.md` → 0 results
    - `grep "DATABRICKS_HOST[^N]" docs/src/guides/warehouse-testing.md` → 0 results

    Cross-reference against actual files to confirm corrections:
    - `ls tests/integration/conftest.py` — should exist
    - `ls tests/integration/test_queries.py` — should exist
    - `grep "TEST_DATA" tests/integration/conftest.py` — should show the constant
    - `ls tests/integration/__snapshots__/` — should exist
    - `grep "server_hostname" src/cubano/testing/credentials.py` — should show DATABRICKS_SERVER_HOSTNAME
  </action>
  <verify>
    All 5 wrong values must return 0 grep hits after editing:
    1. `grep "tests/conftest\.py" docs/src/guides/warehouse-testing.md` → 0
    2. `grep "test_snapshot_queries" docs/src/guides/warehouse-testing.md` → 0
    3. `grep "SNAPSHOT_TEST_DATA" docs/src/guides/warehouse-testing.md` → 0
    4. `grep "tests/__snapshots__/" docs/src/guides/warehouse-testing.md` → 0
    5. `grep "DATABRICKS_HOST[^N]" docs/src/guides/warehouse-testing.md` → 0
    6. `grep "tests/integration/conftest\.py" docs/src/guides/warehouse-testing.md` → at least 1 match
    7. `grep "tests/integration/test_queries\.py" docs/src/guides/warehouse-testing.md` → at least 1 match
    8. `grep "TEST_DATA" docs/src/guides/warehouse-testing.md` → at least 1 match
    9. `grep "tests/integration/__snapshots__/" docs/src/guides/warehouse-testing.md` → at least 1 match
    10. `grep "DATABRICKS_SERVER_HOSTNAME" docs/src/guides/warehouse-testing.md` → at least 1 match
  </verify>
  <done>
    All 5 categories of inaccuracies are corrected in `warehouse-testing.md`. No wrong values remain. All corrected values match actual project files and constants as confirmed by grep.
  </done>
</task>

</tasks>

<verification>
Run after plan completion:
1. `grep "tests/conftest\.py" docs/src/guides/warehouse-testing.md` → 0 results
2. `grep "test_snapshot_queries" docs/src/guides/warehouse-testing.md` → 0 results
3. `grep "SNAPSHOT_TEST_DATA" docs/src/guides/warehouse-testing.md` → 0 results
4. `grep "tests/__snapshots__/" docs/src/guides/warehouse-testing.md` → 0 results
5. `grep "DATABRICKS_HOST[^N]" docs/src/guides/warehouse-testing.md` → 0 results
6. `grep "tests/integration/conftest\.py" docs/src/guides/warehouse-testing.md` → matches found
</verification>

<success_criteria>
`warehouse-testing.md` is accurate: all 5 categories of stale references corrected, all file paths and constant names match the actual test infrastructure, Databricks env var uses the SDK convention (`DATABRICKS_SERVER_HOSTNAME`).
</success_criteria>

<output>
After completion, create `.planning/phases/13-docs-accuracy-verification/13-03-SUMMARY.md`
</output>

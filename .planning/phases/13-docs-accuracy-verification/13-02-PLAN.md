---
phase: 13-docs-accuracy-verification
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/guides/filtering.md
autonomous: true
requirements:
  - DOCS-04

must_haves:
  truths:
    - "filtering.md lookup table uses `__ge` and `__le` (not `__gte`/`__lte`)"
    - "All occurrences of `__gte` and `__lte` in docs/ are corrected"
    - "Light review of all v0.2 user-facing docs finds and fixes any factual inaccuracies (wrong API names, wrong commands, stale references)"
    - "No references to old Query() API, .filter(), or .fetch() remain in docs after review"
  artifacts:
    - path: "docs/src/guides/filtering.md"
      provides: "Accurate Q-object lookup suffix table"
      contains: "__ge"
  key_links:
    - from: "docs/src/guides/filtering.md"
      to: "src/cubano/fields.py"
      via: "Lookup suffix table matches Field.__ge__ and Field.__le__ implementations"
      pattern: "__ge|__le"
---

<objective>
Fix the `filtering.md` lookup suffix table (two wrong cells: `__gte`/`__lte` must become `__ge`/`__le`) and perform a light accuracy review of all v0.2 user-facing docs, fixing any factual inaccuracies found.

Purpose: The lookup suffix table in filtering.md documents operators that differ from what `fields.py` actually generates — users writing Q objects from the docs get a KeyError at runtime. The light review catches any additional staleness from the Phase 10.1 API refactor.
Output: Corrected `filtering.md` and any additional doc fixes found during the light accuracy review.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-docs-accuracy-verification/13-CONTEXT.md
@.planning/phases/13-docs-accuracy-verification/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix filtering.md lookup table and correct all __gte/__lte occurrences in docs</name>
  <files>docs/src/guides/filtering.md</files>
  <action>
    STEP 1 — Find all wrong suffixes in docs:
    Run: `grep -rn "__gte\|__lte" docs/`
    This will show every occurrence across all docs files. Fix ALL of them (not just the table).

    STEP 2 — Fix `docs/src/guides/filtering.md`:
    Read the file first. The lookup suffix table (around lines 39-49) has two wrong rows:
    - Row with `__gte` → change to `__ge` in the suffix column AND change `revenue__gte=500` to `revenue__ge=500` in the example column
    - Row with `__lte` → change to `__le` in the suffix column AND change `revenue__lte=999` to `revenue__le=999` in the example column
    - DO NOT change `__gt` or `__lt` rows — those are already correct

    Also check the "Multiple `.where()` calls" section near the end of the file for `Q(revenue__gte=min_revenue)` — fix to `Q(revenue__ge=min_revenue)` if present.

    STEP 3 — Fix any other docs files found in step 1 with `__gte`/`__lte` occurrences.

    CRITICAL: Only fix `__gte` → `__ge` and `__lte` → `__le`. Do not change `__gt` or `__lt`. The source of truth is `src/cubano/fields.py`: `Field.__ge__` produces `__ge`, `Field.__le__` produces `__le`, `Field.__gt__` produces `__gt`, `Field.__lt__` produces `__lt`.
  </action>
  <verify>
    1. `grep -rn "__gte\|__lte" docs/` → 0 matches
    2. `grep -n "__ge\|__le" docs/src/guides/filtering.md` → shows corrected rows
    3. `grep -n "__gt\|__lt" docs/src/guides/filtering.md` → still shows correct __gt and __lt rows (unchanged)
  </verify>
  <done>
    No `__gte` or `__lte` occurrences remain anywhere in `docs/`. The filtering.md table has `__ge` and `__le` rows with matching example expressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Light accuracy review of all v0.2 user-facing docs</name>
  <files>
    docs/src/guides/first-query.md
    docs/src/guides/models.md
    docs/src/guides/queries.md
    docs/src/guides/ordering.md
    docs/src/guides/installation.md
    docs/src/guides/backends/snowflake.md
    docs/src/guides/backends/databricks.md
    docs/src/guides/backends/overview.md
    docs/src/guides/codegen.md
    docs/src/index.md
    docs/src/changelog.md
  </files>
  <action>
    Read each file listed. For each file, check ONLY for factual inaccuracies (accuracy review, not style or prose improvements — those go in Phase 14):

    ACCURACY CHECKS (fix if found):
    a) Any reference to old procedural `Query()` constructor — should be `Model.query()`
    b) Any reference to `.filter()` method — should be `.where()` (kept as alias but `.where()` is the documented API)
    c) Any reference to `.fetch()` method — should be `.execute()`
    d) Any wrong CLI command syntax (e.g., wrong flag names, wrong subcommand names)
    e) Any wrong file path references that don't exist in the actual project
    f) Any wrong import paths (e.g., importing from a module that no longer exports that name)
    g) Any wrong environment variable names (compare against `src/cubano/testing/credentials.py` if needed)

    SKIP (do not fix in this plan):
    - Style improvements, tone issues, phrasing preferences
    - Missing content or sections (new content goes in Phase 14)
    - Formatting preferences

    For each inaccuracy found: fix it in place. Document what was changed in the task summary.

    If no inaccuracies found in a file: note "clean" in the summary. Do not add content.

    Quick-4 task already updated docs for the API refactor, so the old Query() API is likely already gone — the check is a safety net.
  </action>
  <verify>
    1. `grep -rn "Query()" docs/src/` → 0 matches (no standalone Query() constructor calls)
    2. `grep -rn "\.fetch()" docs/src/` → 0 matches (no deprecated .fetch() usage)
    3. `uv run mkdocs build --strict` passes (if mkdocs is installed in the docs group)
  </verify>
  <done>
    All v0.2 user-facing docs reviewed. Any factual inaccuracies found are fixed. A record of what was found and fixed (or "clean" for each file) is included in the plan summary.
  </done>
</task>

</tasks>

<verification>
Run after plan completion:
1. `grep -rn "__gte\|__lte" docs/` → 0 results
2. `grep -n "__ge\|__le" docs/src/guides/filtering.md` → shows corrected rows present
3. `grep -rn "Query()" docs/src/` → 0 results (no old constructor)
4. `grep -rn "\.fetch()" docs/src/` → 0 results (no deprecated method)
</verification>

<success_criteria>
filtering.md lookup suffix table is accurate (matches `fields.py` operator implementations). No `__gte`/`__lte` occurrences remain anywhere in docs/. All v0.2 user-facing docs reviewed for accuracy; any factual inaccuracies found are fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-docs-accuracy-verification/13-02-SUMMARY.md`
</output>

---
phase: 46-async-query-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .github/workflows/ci.yml
  - tests/unit/test_async_packaging.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ASYNC-04, ASYNC-05]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "adbc-poolhouse >=1.6.1 is the installed base pin and `_resolve_tuning` is importable from `adbc_poolhouse._pool_factory`, so `DuckDBConfig(database=':memory:', pool_size=1)` builds a pool of 1 rather than 5"
    - "`from adbc_poolhouse import create_async_pool, close_async_pool` succeeds in the project venv"
    - "`import trio` succeeds in the project venv (the Trio half of the D-17 matrix can run)"
    - "The full root pytest suite is green against poolhouse >=1.6.1 — RESEARCH Assumption A1 is closed by an executed run, not by reasoning"
    - "`uv run ruff check src/semolina` reports no TID251 finding, and the rule is proven armed: a throwaway module in `src/semolina/` that imports asyncio makes ruff exit non-zero"
    - "A file under `tests/` may import anyio and asyncio without a TID251 finding (D-14)"
    - "A base `pip install semolina` (no extras) imports `semolina` without anyio present in `sys.modules`"
    - "`.planning/REQUIREMENTS.md` ASYNC-04 and `.planning/ROADMAP.md` Phase 46 success criterion 4 both name `adbc-poolhouse[async]>=1.6.1` with a one-line evidence note citing the `_resolve_tuning` regression boundary"
  artifacts:
    - path: "pyproject.toml"
      provides: "async extra, all-extra inclusion, dev trio pin, poolhouse floor bump, TID251 Posture A gate"
      contains: "flake8-tidy-imports"
    - path: "tests/unit/test_async_packaging.py"
      provides: "ASYNC-04 packaging assertions plus the subprocess base-import no-anyio assertion"
    - path: ".github/workflows/ci.yml"
      provides: "packaging-smoke step asserting a base install pulls no anyio"
      contains: "anyio"
  key_links:
    - from: "pyproject.toml"
      to: "src/semolina/"
      via: "ruff TID251 banned-api entries for asyncio and anyio, scoped by per-file-ignores to exclude tests/"
      pattern: "banned-api"
    - from: "pyproject.toml"
      to: ".github/workflows/ci.yml"
      via: "the all extra must include async because every CI test job syncs with --extra all"
      pattern: "extra all"
  prohibitions:
    - requirement_id: ASYNC-05
      category: transparency
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT ship a Posture A gate that can pass vacuously — a gate that greens because the rule code is absent from the ruff select list, because no file is in scope, or because the ban is evaded by a dynamic module lookup, certifies nothing while reading as green"
---

<objective>
Land the packaging and lint foundation the whole phase stands on: bump the adbc-poolhouse
floor so the async surface exists at all, declare the `semolina[async]` extra, add Trio to
the dev group, arm the ruff TID251 Posture A gate, and amend the two planning artifacts
whose text names the superseded `>=1.5.0` floor.

Nothing else in Phase 46 can import, run, or be tested until this plan lands (D-11): the
installed poolhouse is 1.3.1 and has no `_async` subpackage at all.

Purpose: unblock every later plan, and make ASYNC-05's automated check real rather than
nominal — the gate is proven armed by an executed fail-first probe, not asserted.
Output: a venv that can build async pools under both loop backends, a lint rule that fails
the build on an asyncio/anyio import in library code, an ASYNC-04 packaging test, a CI
assertion that a base install stays anyio-free, and corrected requirement text.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-async-query-surface/46-CONTEXT.md
@.planning/phases/46-async-query-surface/46-RESEARCH.md
@.planning/phases/46-async-query-surface/46-PATTERNS.md
@.planning/phases/46-async-query-surface/46-VALIDATION.md
@CLAUDE.md
</context>

<environment_note>
`uv`, `prek`, `basedpyright`, and `git commit` panic under the command sandbox in this
project. Run every `uv`/`prek`/`basedpyright` invocation in this plan with the command
sandbox disabled.
</environment_note>

<tasks>

<task type="auto">
  <name>Task 1: Bump the adbc-poolhouse floor to 1.6.1, declare the async extra and the Trio dev dep, and close Assumption A1 with a full-suite run</name>
  <reversibility rating="costly">The bump changes sync `pool_size` resolution for five existing `DuckDBConfig(database=":memory:", pool_size=1)` call sites — undoing it means re-reasoning about every DuckDB pool test, not just reverting one line.</reversibility>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (current base pin at line 11, `[project.optional-dependencies]` at 27-51, `[dependency-groups] dev` at 57-69)
    - .planning/phases/46-async-query-surface/46-RESEARCH.md — Finding 1 (the `_resolve_tuning` regression boundary and the five affected call sites), Finding 6 (trio is not vendored by anyio), Pitfall 4 (`all` and CI `--extra all`)
    - .planning/phases/46-async-query-surface/46-PATTERNS.md — the `pyproject.toml` section (extra pattern with an explanatory comment)
    - tests/unit/test_pool.py (line 367 reasons explicitly about `pool_size=1` semantics and must be re-read after the bump)
    - tests/unit/test_duckdb_engine.py, tests/unit/test_query.py, tests/conftest.py, src/semolina/conftest.py (the other `DuckDBConfig(..., pool_size=1)` call sites)
  </read_first>
  <action>
    Per D-11, amended to `>=1.6.1` by the user on the evidence in RESEARCH Finding 1.

    In `pyproject.toml`: raise the base `[project] dependencies` pin from
    `adbc-poolhouse>=1.3.1` to `adbc-poolhouse>=1.6.1`; add a new
    `[project.optional-dependencies]` entry keyed `async` holding
    `adbc-poolhouse[async]>=1.6.1`, with a short comment stating that the floor is 1.6.1
    (not 1.5.0) because `create_async_pool` before 1.6.0 hard-coded `pool_size=5` and
    ignored the config's own value; change `all` from
    `semolina[snowflake,databricks,duckdb]` to
    `semolina[snowflake,databricks,duckdb,async]` per the user-resolved Q3 — every CI test
    job syncs with `--extra all`, so excluding async means the async tests never run in CI;
    add `trio>=0.33.0` to `[dependency-groups] dev` in alphabetical position, because
    `adbc-poolhouse[async]` declares only `anyio>=4.13` and anyio does not vendor Trio.

    Sync the lockfile so `uv.lock` records the new resolution.

    Then close RESEARCH Assumption A1 by execution, not by argument: run the full root
    pytest suite. Before the bump, all five `DuckDBConfig(database=":memory:",
    pool_size=1)` call sites silently received a pool of 5; after it they receive 1. Read
    `tests/unit/test_pool.py` around its `pool_size=1` reasoning and adjust any test whose
    assertion depended on the old five-connection behaviour, treating each adjustment as a
    correctness fix and recording it in the SUMMARY. Do not pin back to 1.5.x to avoid a
    failure.

    Do not add async runtime code, the lint rule, or the CI step here — those are Tasks 2
    and 3.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import importlib.metadata as m; v=m.version('adbc-poolhouse'); assert tuple(int(x) for x in v.split('.')[:3]) >= (1,6,1), v; from adbc_poolhouse._pool_factory import _resolve_tuning; print('ok', v)"` prints `ok` and a version at or above 1.6.1
    - `uv run python -c "from adbc_poolhouse import create_async_pool, close_async_pool; print('async surface ok')"` succeeds
    - `uv run python -c "import trio; print(trio.__version__)"` prints a version at or above 0.33.0
    - `uv run python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); od=d['project']['optional-dependencies']; assert od['async']==['adbc-poolhouse[async]>=1.6.1'], od['async']; assert any('async' in s for s in od['all']), od['all']; assert any(s.startswith('adbc-poolhouse>=1.6.1') for s in d['project']['dependencies']), d['project']['dependencies']; print('pyproject ok')"` prints `pyproject ok`
    - `uv run pytest tests/unit/test_pool.py tests/unit/test_duckdb_engine.py tests/unit/test_query.py -x -q` exits 0
    - `uv run pytest -q` exits 0 (full root suite, the A1 closure)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>poolhouse >=1.6.1 and trio are installed, the `async` extra and `all` inclusion are declared, `uv.lock` is updated, and the full root suite is green against the new floor with every A1-affected test adjustment recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Arm the TID251 Posture A lint gate scoped to src/semolina, and prove it fails first</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (`[tool.ruff]` at 103-106, `[tool.ruff.lint]` select at 109, ignore list at 110-117)
    - .pre-commit-config.yaml (the ruff hook pins rev v0.9.6, while the dev dependency is ruff>=0.15.1 — the config must satisfy both)
    - .planning/phases/46-async-query-surface/46-RESEARCH.md — Code Examples §5 (the exact executed config and its output on both ruff versions), Pitfall 6 (TID251 is import-graph based, not textual, and the residual dynamic-import gap)
    - .planning/phases/46-async-query-surface/46-CONTEXT.md — D-13, D-14
  </read_first>
  <action>
    Per D-13 and D-14, using the config RESEARCH executed against both ruff versions in
    play (0.15.x dev dependency and the v0.9.6 pre-commit pin).

    In `pyproject.toml`, append `TID` to the `[tool.ruff.lint]` select list, which currently
    reads `E, F, W, I, UP, B, SIM, TCH, D`. Add a `[tool.ruff.lint.flake8-tidy-imports.banned-api]`
    table with exactly two keys, `"asyncio"` and `"anyio"`, each carrying a `msg`. The
    asyncio message names Posture A and ASYNC-05 and directs the reader to await
    adbc-poolhouse primitives instead; the anyio message names Posture A and ASYNC-05 and
    directs the reader to a bare `async def` with neutral awaits. Add a
    `[tool.ruff.lint.per-file-ignores]` entry mapping `"tests/**"` to `["TID251"]`, with a
    comment noting D-14: the Trio half of the loop matrix requires test code to import
    anyio freely.

    Then prove the gate is not vacuous. Write a throwaway single-line module under
    `src/semolina/` whose only statement imports the standard-library event-loop module,
    run ruff restricted to the TID251 rule over just that file, capture the exit status,
    delete the throwaway file, and assert the captured status was non-zero. Delete the
    throwaway file even if ruff behaves unexpectedly — it must not survive the task. Record
    in the SUMMARY that the residual evasion path is a dynamic module lookup by string
    name, which is import-graph invisible and deliberately not defended against (Task 3
    amends the roadmap wording to match).

    Do not add an ignore, a noqa, or a per-file-ignore for any file under `src/semolina/`.
  </action>
  <acceptance_criteria>
    - `uv run ruff check src/semolina` exits 0
    - `uv run python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb'))['tool']['ruff']['lint']; assert 'TID' in d['select'], d['select']; b=d['flake8-tidy-imports']['banned-api']; assert set(b)=={'asyncio','anyio'}, sorted(b); assert all(v['msg'].strip() for v in b.values()); assert d['per-file-ignores']['tests/**']==['TID251']; print('tid config ok')"` prints `tid config ok`
    - Fail-first proof: `printf 'import asyncio\n' > src/semolina/_tid_probe.py; uv run ruff check --no-cache --select TID251 src/semolina/_tid_probe.py; RC=$?; rm -f src/semolina/_tid_probe.py; test "$RC" -ne 0` exits 0
    - Scope proof: `printf 'import anyio\nimport asyncio\n' > tests/unit/_tid_scope_probe.py; uv run ruff check --no-cache --select TID251 tests/unit/_tid_scope_probe.py; RC=$?; rm -f tests/unit/_tid_scope_probe.py; test "$RC" -eq 0` exits 0
    - `test ! -e src/semolina/_tid_probe.py && test ! -e tests/unit/_tid_scope_probe.py` exits 0
    - `prek run --all-files` exits 0 (the pinned v0.9.6 ruff hook accepts the same config)
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check src/semolina &amp;&amp; printf 'import asyncio\n' > src/semolina/_tid_probe.py; uv run ruff check --no-cache --select TID251 src/semolina/_tid_probe.py; RC=$?; rm -f src/semolina/_tid_probe.py; test "$RC" -ne 0</automated>
  </verify>
  <done>TID251 is armed over `src/semolina/`, exempt under `tests/`, accepted by both ruff versions, and proven to fail on a real violation and pass on a test-tree import.</done>
</task>

<task type="auto">
  <name>Task 3: Add the ASYNC-04 packaging test, the CI base-install no-anyio assertion, and amend the superseded version floor in REQUIREMENTS.md and ROADMAP.md</name>
  <files>tests/unit/test_async_packaging.py, .github/workflows/ci.yml, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .github/workflows/ci.yml (the `packaging-smoke` job at 149-172, and the four test jobs at 34, 55, 76, 107 that sync with `--extra all`)
    - pyproject.toml (as amended by Tasks 1 and 2)
    - .planning/REQUIREMENTS.md (ASYNC-04 at line 15)
    - .planning/ROADMAP.md (Phase 46 section: the `**Depends on**` line and success criterion 4)
    - .planning/phases/46-async-query-surface/46-RESEARCH.md — Pitfall 3 (poolhouse resolves async entry points lazily, and why a module-level import defeats it), Pitfall 6 (the residual dynamic-import gap in ROADMAP SC4's literal wording), Open Question 2
    - .planning/phases/46-async-query-surface/46-PATTERNS.md — the `.github/workflows/ci.yml` section
  </read_first>
  <action>
    Three edits, all serving ASYNC-04.

    First, create `tests/unit/test_async_packaging.py`. It asserts the packaging contract by
    parsing `pyproject.toml` with `tomllib`: the base `dependencies` list carries an
    `adbc-poolhouse>=1.6.1` entry; the `async` optional-dependency group is exactly
    `adbc-poolhouse[async]>=1.6.1`; the `all` group's requirement string names `async`; the
    dev dependency group carries a `trio` pin. It also asserts the Pitfall 3 lazy-import
    contract in a way that holds even though anyio is installed in this dev venv: spawn a
    child interpreter with `sys.executable`, have it import `semolina` and then report
    whether `anyio` is present in its `sys.modules`, and assert absence. Give the module the
    `unit` marker used elsewhere in the suite and a module docstring naming ASYNC-04. Keep
    the test names containing the token `packaging` so `pytest -k packaging` selects them,
    matching the 46-VALIDATION.md verification row.

    Second, extend the existing `packaging-smoke` job in `.github/workflows/ci.yml` with a
    step in the same shape as its current two steps: create a second clean venv, install the
    project with no extras into it, and run a one-liner in that interpreter that imports
    `semolina` and asserts `importlib.util.find_spec('anyio')` is None. Keep the existing
    `[duckdb]` extra step untouched.

    Third, amend the two planning artifacts whose text names the superseded floor, following
    the Phase 42/43 precedent of amending criteria on evidence. In
    `.planning/REQUIREMENTS.md`, change ASYNC-04's `adbc-poolhouse[async]>=1.5.0` to
    `>=1.6.1` and append a one-line evidence note: the `_resolve_tuning` helper that makes
    `create_async_pool` honour the config's own `pool_size` landed in 1.6.0, so 1.5.x would
    silently build a five-connection pool for `DuckDBConfig(database=":memory:",
    pool_size=1)`. In `.planning/ROADMAP.md`, make the same `>=1.5.0` to `>=1.6.1` change in
    Phase 46 success criterion 4 with the same one-line note, update the `**Depends on**`
    line's reference to the poolhouse version that carries the async stack, and reword
    criterion 4's automated-check clause so it describes what TID251 actually enforces —
    no asyncio or anyio in the import graph of `src/semolina/` — rather than a textual
    reference scan, noting in one clause that a dynamic module lookup by string name is
    outside the rule's reach.

    Use scoped `Edit` replacements on both planning files. Do not rewrite either file
    wholesale — `.planning/ROADMAP.md` carries every other phase's entry.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_async_packaging.py -x -q` exits 0
    - `uv run pytest tests/unit -k packaging -q` collects at least one test and exits 0
    - `uv run python -c "import subprocess,sys; r=subprocess.run([sys.executable,'-c','import semolina,sys; print(\"anyio\" in sys.modules)'],capture_output=True,text=True); assert r.returncode==0, r.stderr; assert r.stdout.strip()=='False', r.stdout"` exits 0
    - `grep -c '1\.6\.1' .planning/REQUIREMENTS.md` reports at least 1, and `grep -n 'ASYNC-04' .planning/REQUIREMENTS.md` shows the amended line naming `>=1.6.1`
    - `grep -n '1\.6\.1' .planning/ROADMAP.md` shows the amended Phase 46 success criterion 4
    - `grep -cE '^### Phase (46|47|48|49|50):' .planning/ROADMAP.md` reports 5 — every v0.7 phase entry survived the scoped edits (verified as 5 at plan time; use `Edit`, never a whole-file `Write`, on this file)
    - `uv run python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); s=d['jobs']['packaging-smoke']['steps']; assert any('anyio' in str(st) for st in s), [st.get('name') for st in s]; print('ci step ok')"` prints `ci step ok`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit -k packaging -q</automated>
  </verify>
  <done>The packaging test passes, the CI smoke job asserts a base install stays anyio-free, and both planning artifacts name `>=1.6.1` with an evidence note while every other roadmap phase entry is intact.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is created by Phase 46 and does not exist in the codebase before it.
Source-grounding drift checks must treat these as new, not as unresolved references.

**Config keys, extras, and lint rules (this plan)**
- `pyproject.toml` `[project.optional-dependencies]` extra named `async`
- `pyproject.toml` `all` extra gains `semolina[...,async]`
- `pyproject.toml` `[dependency-groups] dev` gains `trio`
- `pyproject.toml` `[tool.ruff.lint] select` gains `TID`
- `pyproject.toml` `[tool.ruff.lint.flake8-tidy-imports.banned-api]` keys `"asyncio"` and `"anyio"`
- `pyproject.toml` `[tool.ruff.lint.per-file-ignores]` entry `"tests/**"`
- ruff rule id `TID251` (newly in force for this project)
- `.github/workflows/ci.yml` `packaging-smoke` step asserting a base install pulls no anyio

**Python symbols (later plans in this phase)**
- `semolina.engines.abase.AsyncEngine` and its `connect`, `aexecute`, `dispose`
- `semolina.acursor.AsyncSemolinaCursor` and its `__aiter__`, `__anext__`, `aclose`,
  `__aenter__`, `__aexit__`, `__del__`, `__repr__`, `_column_names`, `fetchall_rows`,
  `fetchone_row`, `fetchmany_rows`, `fetchall`, `fetchone`, `fetchmany`,
  `fetch_arrow_table`, `fetch_record_batch`, `description`, `rowcount`
- `semolina.config.create_async_engine`
- `semolina.registry.register_async_engine`, `get_async_engine`, `unregister_async_engine`,
  `_async_engines`
- `semolina.query._Query.aexecute`
- `semolina.__init__` exports: `AsyncSemolinaCursor`, `create_async_engine`,
  `get_async_engine`, `register_async_engine`, `unregister_async_engine`

**New file paths (later plans in this phase)**
- `src/semolina/engines/abase.py`, `src/semolina/acursor.py`
- `tests/unit/test_async_engine.py`, `tests/unit/test_async_cursor.py`,
  `tests/unit/test_async_query.py`, `tests/unit/test_async_cancel.py`,
  `tests/unit/test_async_packaging.py`, `tests/unit/test_asyncio_trio_matrix.py`
- `tests/integration/test_async_queries.py`
- `tests/integration/cassettes/async_single_metric_snowflake/`,
  `tests/integration/cassettes/async_single_metric_databricks/`

**New test fixtures (later plans in this phase)**
- `async_duckdb_engine`, `async_duckdb_file_engine` (`tests/conftest.py`)
- `anyio_backend` (module-local, in each async test module)
- `snowflake_async_engine`, `databricks_async_engine` (`tests/integration/conftest.py`)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → project venv | A new optional dependency and a raised floor cross into the build |
| library source → lint gate | The gate is the only automated enforcement of Posture A; a vacuous gate is indistinguishable from a passing one without a fail-first proof |

## STRIDE Threat Register (ASVS L1, blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-SC | Tampering | `adbc-poolhouse[async]`, `anyio`, `trio` installs | high | mitigate | RESEARCH § Package Legitimacy Audit verified all three independently against the PyPI JSON API (release history + repository URL) and read the poolhouse wheel source directly; verdicts OK, none `[ASSUMED]`/`[SUS]`/`[SLOP]`, so no blocking legitimacy checkpoint is warranted. `anyio` is declared by poolhouse's own `[async]` extra rather than chosen by Semolina; `trio` is dev-only. `uv.lock` records hashes for the resolved set. |
| T-46-06 | Information Disclosure | `pyproject.toml` banned-api `msg` strings | low | accept | The two messages are static literals naming Posture A and ASYNC-05; they interpolate no config, path, or credential. |
| T-46-07 | Repudiation | ruff TID251 gate | high | mitigate | A gate that greens without inspecting anything is the failure mode this plan defends against. Task 2 machine-proves fail-first by writing a real violation into `src/semolina/`, confirming a non-zero ruff exit, and deleting it; and proves the D-14 scope carve-out by confirming the same imports under `tests/` are clean. The residual dynamic-lookup evasion path is recorded and the roadmap wording is amended to match what the rule enforces, rather than left as an overclaim. |
</threat_model>

<probe_ledger>
## Probe Ledger — Flagged Assumptions (phase-level, authoritative)

No SPEC exists for Phase 46, so the deterministic edge probe ran over the seven phase
requirement IDs and the prohibition recall ran in-prompt. **All 9 edge rows returned
`unresolved`**, and the six `unclassified` rows must stay `unresolved` — an `unclassified`
row is never auto-promoted to `backstop`. Each is therefore surfaced here as an explicit
flagged assumption, never a silent drop. This is the single authoritative ledger for the
phase; Plans 02-07 point at it rather than re-listing rows.

**No-silent-drop equality: 9 probe-surfaced edge rows == 0 authored into `must_haves.truths`
as probe-derived predicates + 9 surfaced as flagged assumptions below.**

| # | Requirement | Probe category | Status | Flagged assumption the plan set proceeds on |
|---|-------------|----------------|--------|---------------------------------------------|
| 1 | ASYNC-01 | unclassified | unresolved | The probe could not classify a shape axis for "await returns the same result surface". The plan set proceeds on 46-VALIDATION.md's explicit criteria instead (rows for `test_async_engine.py` and its concurrency variant), and on RESEARCH Finding 10's reading of "same surface" as same shape + same `Row` type with awaited fetches. If a boundary case the probe would have caught exists, it is unsurfaced. |
| 2 | ASYNC-02 | unclassified | unresolved | Same as above for the query-builder twin; covered instead by the `test_async_query.py` row in 46-VALIDATION.md. Unsurfaced probe edges remain possible. |
| 3 | ASYNC-03 | unclassified | unresolved | Streaming boundary conditions (first batch, zero-row batch, exhausted reader, re-iteration) are taken from the sync analog's contract at `cursor.py:238-285` rather than from a probe classification. |
| 4 | ASYNC-04 | unclassified | unresolved | Packaging has no data-shape axis the probe recognises; covered by this plan's explicit criteria. |
| 5 | ASYNC-05 | unclassified | unresolved | The probe surfaced no shape edge for the lint gate; the gate's own vacuity risk is instead carried as a kept prohibition on this plan and machine-proven fail-first in Task 2. |
| 6 | ASYNC-06 | unclassified | unresolved | Cancellation timing edges are not shape edges. RESEARCH Assumption A3 (a long-running DuckDB query is a reliable cancellation substrate under both backends) is the live risk here and is carried as an explicit assumption on Plan 05. |
| 7 | TOOL-01 | adjacency | unresolved | "When two things are exactly equal or just touch, do they merge, collide, or separate?" has no meaningful reading for a single-key planning-config edit. Recorded, not resolved. |
| 8 | TOOL-01 | empty | unresolved | "What is the result for empty, single-element, or null input?" likewise has no meaningful reading for a single-key edit. Recorded, not resolved. |
| 9 | TOOL-01 | ordering | unresolved | "When elements compare equal, is output order specified and stable?" is not a data-ordering question here, but the phase does carry a real *commit*-ordering constraint (D-18), which is expressed as a kept prohibition on Plan 07 rather than resolved from this row. |

Additional non-probe assumption carried into execution: RESEARCH Assumption A1 (the five
existing `DuckDBConfig(..., pool_size=1)` call sites still pass once the floor honours
`pool_size=1`) is **not** left as an assumption — Task 1 closes it with an executed
full-suite run.

**Prohibition recall (in-prompt, spec-less fallback §B).** Stage 1 over-produced across the
seven requirements; seven candidates survived the Stage 2 precision pass and are authored
descriptor-less into the owning plan's `must_haves.prohibitions`, each disposing
flagged-unverified with no `check_*` descriptor wired:

| # | Requirement | Category | Owning plan |
|---|-------------|----------|-------------|
| 1 | ASYNC-01 | transparency — a blocking call presented as async | 46-02 |
| 2 | ASYNC-03 | transparency — full materialization behind a streaming interface | 46-02 |
| 3 | ASYNC-01 | safety — widening the warehouse connection budget or concurrency bound | 46-02 |
| 4 | ASYNC-05 | transparency — a Posture A gate that can pass vacuously | 46-01 (this plan) |
| 5 | ASYNC-06 | transparency — a cancellation test that greens on abandonment alone | 46-05 |
| 6 | ASYNC-03 | transparency — documenting leak-safety parity the async cursor does not have | 46-06 |
| 7 | TOOL-01 | safety — re-arming branch auto-switching before the phase's commits land | 46-07 |

ASYNC-02 and ASYNC-04 yielded zero kept prohibitions. That is the correct precision outcome,
not an omission: ASYNC-02's strongest candidate (a wrong-kind engine resolving silently) is
already an explicit written design decision, D-05, and ASYNC-04's (anyio becoming a hard
dependency of a base install) is already the requirement's own literal text. 46-04-PLAN.md
records its deliberate zero.

Canon-referral drops, recorded as breadcrumbs rather than minted: credential leakage in error
messages and in cassettes is canon secrets and OWASP territory — covered by
`/gsd-secure-phase` and by the `<threat_model>` rows T-46-05 and T-46-06 across this plan set,
not minted as prohibitions.
</probe_ledger>

<verification>
- `uv run pytest -q` green (the A1 closure run, Task 1)
- `uv run pytest tests/unit -k packaging -q` green
- `uv run ruff check src/semolina` clean, and the Task 2 fail-first probe exits non-zero
- `prek run --all-files` green (proves the config satisfies the pinned v0.9.6 ruff hook as
  well as the 0.15.x dev dependency)
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` both name `>=1.6.1`; the roadmap
  still lists all five v0.7 phase entries
</verification>

<success_criteria>
- adbc-poolhouse >=1.6.1 and trio are resolved in `uv.lock` and importable
- `semolina[async]` is a declared extra; `all` includes it; a base install pulls no anyio
- TID251 bans asyncio and anyio in `src/semolina/` only, and is proven to fail on a real
  violation rather than assumed to
- The full root suite is green against the new floor, with any A1-driven test adjustment
  recorded in the SUMMARY
- Both planning artifacts carry the corrected floor plus a one-line evidence note
</success_criteria>

<output>
Create `.planning/phases/46-async-query-surface/46-01-SUMMARY.md` when done
</output>

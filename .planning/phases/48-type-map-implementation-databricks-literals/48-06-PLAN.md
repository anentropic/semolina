---
phase: 48-type-map-implementation-databricks-literals
plan: 06
type: execute
wave: 4
depends_on: ["48-02", "48-03", "48-05"]
files_modified:
  - docs/src/explanation/type-fidelity.rst
  - docs/src/how-to/codegen.rst
  - .planning/WINDOWS.md
  - .planning/todos/pending/
autonomous: true
requirements: [TYPE-03, TYPE-04, TYPE-05, TYPE-06, TYPE-07, DBX-04]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/src/explanation/type-fidelity.rst` no longer tells the reader to trust the runtime value over the annotation in a generated model — the `.. note::` at lines 152-165 describes a temporary state that Phase 48 ends, and it is rewritten rather than left standing as a false claim."
    - "The page states D-04's `TIMESTAMP_NS` caveat plainly: without pandas the value truncates to microseconds and raises `ValueError` on sub-microsecond input, so the `datetime.datetime` annotation is a documented over-approximation rather than an exact description."
    - "The page explains D-02: running `semolina codegen` and then `semolina codegen --check` on the same view can report drift wherever the metadata route and the probe route still disagree, and that this is the finding rather than a bug."
    - "`docs/src/how-to/codegen.rst` § 'Handle TODO comments' no longer lists VARIANT among the types that get `Any` — TYPE-06 makes it a `JsonValue` union."
    - "The how-to's exit-code list-table carries a `5` row whose wording matches the Rich epilog in `src/semolina/cli/__init__.py` — the two are duplicates and must not disagree."
    - "The how-to has a new imperative-verb-first section documenting `--check`: what it compares, that it fetches no rows, the `--model` requirement, the exit codes, and that the reported route tells you whether it probed or fell back to metadata."
    - "`just docs-build` (`sphinx-build -W`, strict) exits 0 — every docs edit builds warning-free."
    - "`.planning/todos/pending/2026-08-12-verify-databricks-zero-row-fallback.md` is still open and referenced from the docs' Databricks limitation, rather than quietly closed by a phase that could not verify it."
    - "No documentation claim in this phase asserts behaviour against a live Databricks workspace (D-09)."
  artifacts:
    - docs/src/explanation/type-fidelity.rst
    - docs/src/how-to/codegen.rst
    - .planning/WINDOWS.md
  key_links:
    - "The exit-code table exists twice — `src/semolina/cli/__init__.py`'s Rich epilog and `docs/src/how-to/codegen.rst:293-317` — and the two move together."
    - "`47-DECISIONS.md` is normative and `type-fidelity.rst` is derived from it one-directionally. The page carries no `.planning` link, so the two cannot form a citation cycle; keep it that way."
    - "The `type-fidelity.rst` edit changes a substantive claim, so CLAUDE.md's 'minor fixes' carve-out does not apply and the docs skill is mandatory."
  prohibitions:
    - "Phase 48 must not modify src/semolina/cursor.py, src/semolina/acursor.py, or src/semolina/results.py. The Decimal policy is annotation-only; the annotation is corrected to the value, never the reverse."
    - "Do not edit .planning/phases/47-type-fidelity-probe-decision-doc/47-DECISIONS.md. It is normative, dated, and was approved at a blocking human checkpoint; Phase 48 changes code, not the decision."
    - "Do not add a `.planning/` link to `docs/src/explanation/type-fidelity.rst`. The derivation is one-directional by design."
    - "Do not document a Databricks `--check` capability. Its route is unverified and broken window 2 is still open (D-09)."
    - "Do not close broken window 2 or the Databricks zero-row-fallback todo. Nothing in this phase verified them."
---

<objective>
Bring the two documentation pages this phase falsifies back into line, then run the phase
gate.

`docs/src/explanation/type-fidelity.rst` currently tells readers, in a `.. note::`, that
generated annotations do not agree with the values described on the page and that they should
"trust the value you get at runtime over the annotation in a generated model". Phase 48 is the
thing that note is waiting for; leaving it standing ships a false statement.
`docs/src/how-to/codegen.rst` lists VARIANT among the types that get `Any`, and its exit-code
table has no row for the drift code `--check` now returns.

Purpose: this is the last plan in the phase, so it is also where the phase gate runs — both
test suites, the strict typecheck, the strict docs build, and the type-fidelity artifact's
staleness check — and where the broken-window ledger and pending todos are reconciled with what
the phase actually verified.

Output: a rewritten explanation note that describes the new state honestly (including the
`TIMESTAMP_NS` caveat and the metadata-vs-probe divergence), a how-to that documents `--check`
and corrects its VARIANT claim, a green `sphinx-build -W`, and a reconciled ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/semolina-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-type-map-implementation-databricks-literals/48-CONTEXT.md
@.planning/phases/48-type-map-implementation-databricks-literals/48-PATTERNS.md
@.planning/phases/47-type-fidelity-probe-decision-doc/47-DECISIONS.md
@.planning/phases/48-type-map-implementation-databricks-literals/48-03-SUMMARY.md
@.planning/phases/48-type-map-implementation-databricks-literals/48-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files created by **Phase 48**, all documented by this plan:

- `semolina.JsonValue` — public type alias exported from `src/semolina/types.py` (48-03)
- `semolina.codegen.arrow_map.arrow_type_to_python` (48-04)
- `semolina.codegen.probe.probe_schema` / `ProbeResult` / `ROUTE_EXECUTE_SCHEMA` /
  `ROUTE_ZERO_ROW` / `NOT_IMPLEMENTED_ERRORS` (48-04)
- `semolina.codegen.model_reader.read_committed_model` / `CommittedModel` / `CommittedField` (48-05)
- `semolina.codegen.annotation_check.check_view` / `ViewCheckReport` / `FieldCheckRow` /
  `ROUTE_METADATA` (48-05)
- `semolina.codegen.python_renderer.metric_annotation` (48-05)
- `semolina.cli.codegen.EXIT_ANNOTATION_DRIFT` (= 5), the `--check` flag and the `--model`
  option (48-05)
- `IntrospectedField.raw_type` (48-01)
- New test modules: `tests/unit/test_scope_fence.py`, `tests/unit/test_public_surface.py`,
  `tests/unit/codegen/test_arrow_map.py`, `tests/unit/codegen/test_model_reader.py`,
  `tests/unit/codegen/test_annotation_check.py`

This plan itself creates no new source symbol. It documents the above and reconciles
`.planning/WINDOWS.md` and `.planning/todos/pending/`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the type-fidelity explanation's temporary note (Explanation)</name>
  <files>docs/src/explanation/type-fidelity.rst</files>
  <read_first>
    - @.claude/skills/semolina-docs-author/SKILL.md (mandatory per CLAUDE.md — this edit changes a substantive claim, so the "minor fixes" carve-out does not apply)
    - docs/src/explanation/type-fidelity.rst (whole page — especially the surrounding prose at lines 141-150 whose voice this edit must match, the `.. note::` at 152-165, and the `See also` section at 167-171)
    - .planning/phases/47-type-fidelity-probe-decision-doc/47-DECISIONS.md (normative — this page is derived from it one-directionally; § "Derived documentation" states the direction)
    - .planning/phases/47-type-fidelity-probe-decision-doc/47-TYPE-FIDELITY.md (as regenerated by 48-01 and 48-03 — the measured evidence the page describes)
    - .planning/phases/48-type-map-implementation-databricks-literals/48-CONTEXT.md § "D-02" and § "D-04"
    - .planning/phases/48-type-map-implementation-databricks-literals/48-03-SUMMARY.md (the annotation contract as it actually shipped)
    - .planning/WINDOWS.md (broken window 3 — the pandas-dependent `TIMESTAMP_NS` row)
  </read_first>
  <action>
Load the docs skill first and apply its Diataxis classification, voice, and humanizer pass.
This page is Explanation: background and reasoning, no step-by-step instructions, second
person, short declaratives, `See also` links at the bottom. The surrounding prose at lines
141-150 is the voice to match ("It is a good estimate. It is simply not the same source as the
query itself.").

The `.. note::` at lines 152-165 says, verbatim, that "The Python annotations that
``semolina codegen`` writes into a generated model are a separate matter, and they do not all
agree with that yet", names `int`/`float`/`float`/`Any` for Snowflake `NUMBER`, Databricks
`decimal` and DuckDB `DECIMAL`, and tells the reader to "trust the value you get at runtime
over the annotation in a generated model". Every one of those claims is now false. Rewrite the
block rather than deleting it — the page still needs to say what the relationship between
annotation and value IS, it just changed. State that a decimal column now annotates
`decimal.Decimal` on all three backends, that metric annotations are `T | None`, and that the
raw warehouse type is preserved as a comment above any field whose annotation does not name it.

Add two things the page did not need before and does now:

**The `TIMESTAMP_NS` caveat (D-04).** A DuckDB `TIMESTAMP_NS` column annotates
`datetime.datetime`, which is a sound over-approximation rather than an exact description: the
value arrives as a `pandas.Timestamp` (a `datetime.datetime` subclass) when pandas is
importable; without pandas it truncates to microsecond resolution, and pyarrow raises
`ValueError` when the value carries sub-microsecond precision. Say that pandas is not a
Semolina dependency and arrives only transitively. Do not soften it into "may vary by
environment" — name the two behaviours and the error.

**The `--check` divergence (D-02).** Explain, as background rather than instruction, that
codegen builds a model from warehouse introspection metadata while `semolina codegen --check`
resolves annotations from the query-time result schema, that 47-DECISIONS.md Decision 3 makes
the result schema primary and metadata a labelled fallback, and that the two can therefore
still disagree — so `--check` immediately after `codegen` can legitimately report drift. That
divergence is this page's own central finding surfacing in a tool, not a defect. Link to the
how-to for the mechanics; the *why* stays here and the *how* goes in Task 2.

Keep the `See also` section and add a cross-reference to the new `--check` how-to section.
Add no `.planning/` link: `47-DECISIONS.md` is normative and this page is derived from it
one-directionally, and the page carrying no link back is what keeps the two from forming a
citation cycle.

Run `just docs-build` (`sphinx-build -W`, strict) and fix every warning before committing.
  </action>
  <verify>
    <automated>just docs-build</automated>
  </verify>
  <acceptance_criteria>
    - `just docs-build` exits 0
    - `grep -c 'trust the value you get at runtime over the annotation' docs/src/explanation/type-fidelity.rst` is 0
    - `grep -c 'they do not all agree with that yet' docs/src/explanation/type-fidelity.rst` is 0
    - `grep -c 'TIMESTAMP_NS' docs/src/explanation/type-fidelity.rst` is at least 1
    - `grep -ci 'pandas' docs/src/explanation/type-fidelity.rst` is at least 1
    - `grep -c 'howto-codegen' docs/src/explanation/type-fidelity.rst` is at least 1
    - `grep -c '.planning' docs/src/explanation/type-fidelity.rst` is 0
    - `grep -ci 'decimal.Decimal' docs/src/explanation/type-fidelity.rst` is at least 1
  </acceptance_criteria>
  <done>The page describes the post-Phase-48 relationship between annotation and value, states the `TIMESTAMP_NS` environment dependence including the `ValueError`, explains why `--check` can disagree with `codegen`, and builds warning-free under `-W`.</done>
</task>

<task type="auto">
  <name>Task 2: Document `--check` and correct the VARIANT claim (How-to)</name>
  <files>docs/src/how-to/codegen.rst</files>
  <read_first>
    - @.claude/skills/semolina-docs-author/SKILL.md (mandatory — the `--check` section is new-page-equivalent content)
    - docs/src/how-to/codegen.rst (whole page — especially § "Understand field type mapping" at 251-273, § "Handle TODO comments" at 275-291, and § "Exit codes" at 293-317 with its `list-table` and `.. tip::`)
    - src/semolina/cli/__init__.py (as edited by 48-05 — the Rich epilog exit-code table this page duplicates; the two must say the same thing)
    - src/semolina/cli/codegen.py (as edited by 48-05 — the shipped `--check` / `--model` surface and `EXIT_ANNOTATION_DRIFT`)
    - .planning/phases/48-type-map-implementation-databricks-literals/48-05-SUMMARY.md (the CLI surface as it actually shipped, and the D-09 narrowing)
    - .planning/phases/48-type-map-implementation-databricks-literals/48-03-SUMMARY.md (the annotation contract, for the type-mapping table)
    - .planning/todos/pending/2026-08-12-verify-databricks-zero-row-fallback.md
  </read_first>
  <action>
Load the docs skill first. This page is How-to: goal-oriented, imperative-verb-first headings
(`Handle TODO comments`, `Understand field type mapping`, `Override the SQL column name with
source=`), illustrative snippets rather than full runnable programs, reader supplies setup.

Four edits.

**Correct the VARIANT claim.** § "Handle TODO comments" (lines 275-291) opens "When a field's
SQL type has no clean Python equivalent (GEOGRAPHY, VARIANT, ARRAY, MAP, STRUCT), codegen types
the field as ``Any``". VARIANT no longer belongs in that list — TYPE-06 maps it to a
`JsonValue` union. Remove it from the parenthetical and add a short paragraph pointing at the
new behaviour: a VARIANT column annotates `semolina.JsonValue`, which is the recursive
`str | int | float | bool | None | list | dict` union, and codegen adds `JsonValue` to the
`from semolina import ...` line for you. Say plainly that on Databricks the value arrives as
JSON *text* rather than a parsed `dict`, so `JsonValue` is honest-but-loose there.

**Document the raw-type comment.** The same section says the TODO comment "carries the
warehouse's own type descriptor verbatim". Extend it: for a field whose annotation does NOT
name its warehouse type — a `DECIMAL(10,2)` that becomes `decimal.Decimal`, a `UUID` or `JSON`
or `ENUM` that becomes `str` — codegen now emits the raw warehouse type as a comment above the
field, so the precision, scale, or enum members are not lost when the `TODO:` disappears.

**Add the exit-code row.** Append a `5` row to the `list-table` at lines 293-317, matching the
existing `* - \`\`N\`\`` / `- Meaning` shape. Its wording must match the Rich epilog string
48-05 added to `src/semolina/cli/__init__.py` — the table is duplicated between the two and a
divergence is a defect. Keep the existing `.. tip::` about exit code 2 and extend it, or add a
second tip, noting that exit 5 means "drift found" while exit 1 means "the tool broke", which
is why they are separate codes.

**Add the `--check` section.** New section, imperative-verb-first heading (e.g.
`Check a committed model for drift`), placed after § "Handle TODO comments" and before
§ "Exit codes". Follow the page's own section pattern at lines 251-273: an orienting sentence,
a code block or `list-table`, then a paragraph explaining why the design is what it is. Cover:
the invocation shape (`semolina codegen --check --model path/to/models.py my_view --backend duckdb --database analytics.db`);
that `--model` is required with `--check` and its absence exits 2; that the comparison is
per-field against the warehouse's query-time **result schema**, not against regenerated source;
that it fetches no rows; that the report goes to stderr and stdout stays empty; that each row
names the route (`execute-schema`, `zero-row`, `metadata`) so a run that fell back to warehouse
metadata is visible; and that exit 5 means drift.

State the Databricks limitation honestly in that section and do not overstate it: `--check` is
verified against DuckDB live and, for the comparison core, against a committed Snowflake
recording. On Databricks the driver does not answer `ExecuteSchema` and the zero-row fallback
has not been confirmed against a live metric-view planner, so `--check` there is unverified.
Do not claim it works and do not claim it fails. Do NOT close the tracking todo.

Add a `See also` cross-reference back to the type-fidelity explanation for the *why*.

Run `just docs-build` and fix every warning before committing.
  </action>
  <verify>
    <automated>just docs-build</automated>
  </verify>
  <acceptance_criteria>
    - `just docs-build` exits 0
    - `grep -c 'GEOGRAPHY, VARIANT, ARRAY, MAP, STRUCT' docs/src/how-to/codegen.rst` is 0
    - `grep -c 'JsonValue' docs/src/how-to/codegen.rst` is at least 2
    - `grep -c '\-\-check' docs/src/how-to/codegen.rst` is at least 3
    - `grep -c '\-\-model' docs/src/how-to/codegen.rst` is at least 2
    - The exit-code `list-table` contains a row for ``5``, and its meaning text matches the corresponding epilog string in `src/semolina/cli/__init__.py` word for word
    - `grep -ci 'execute-schema' docs/src/how-to/codegen.rst` is at least 1
    - `grep -ci 'databricks' docs/src/how-to/codegen.rst` is at least 1 and the surrounding sentence says the Databricks `--check` route is unverified
    - `test -f .planning/todos/pending/2026-08-12-verify-databricks-zero-row-fallback.md` succeeds (the todo is not closed)
  </acceptance_criteria>
  <done>The how-to documents `--check` end to end, carries the new exit code in wording identical to the CLI epilog, no longer lists VARIANT among the `Any` types, explains the raw-type comment, states the Databricks limitation without overclaiming, and builds warning-free under `-W`.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — run every check and reconcile the ledger</name>
  <files>.planning/WINDOWS.md, .planning/todos/pending/</files>
  <read_first>
    - .planning/phases/48-type-map-implementation-databricks-literals/48-VALIDATION.md (the requirement-to-test contract and the Wave 0 checklist this gate closes out)
    - .planning/phases/48-type-map-implementation-databricks-literals/48-01-SUMMARY.md through 48-05-SUMMARY.md (every deviation, `[ASSUMED]` marker, and evidence limit recorded during the phase)
    - .planning/WINDOWS.md (the markdown table AND the trailing JSON block AND the frontmatter counters — all three must stay in step)
    - .planning/todos/pending/ (the full listing, in particular `2026-08-12-verify-databricks-zero-row-fallback.md` and the two Snowflake/Databricks recording todos)
    - justfile (the `test`, `docs-build`, and `type-fidelity` recipes)
  </read_first>
  <action>
Run the full phase gate and record the result. Every command below must be executed, not
assumed:

  - `just test` — BOTH suites. `just test` runs the root `uv run pytest` and then the
    `semolina-jaffle-shop` suite; `uv run pytest` alone misses half, and jaffle-shop's fixtures
    declare `DECIMAL(10,2)` / `DECIMAL(12,2)` columns that this phase's map change touches.
  - `prek run --all-files` — ruff lint+format, basedpyright strict, shellcheck.
  - `just docs-build` — `sphinx-build -W`, strict.
  - `uv run python tests/type_fidelity_probe.py --check` — exit 0, under an environment synced
    with `uv sync --dev --extra all` (broken window 3: a plain `--dev` sync flips the pandas row
    to `not measured` and turns the staleness guard red for an environment reason).
  - `git diff --name-only <phase-base>..HEAD | grep -E 'src/semolina/(cursor|acursor|results)\.py'`
    — must produce no output. Also run `uv run pytest tests/unit/test_scope_fence.py -x` with
    `SEMOLINA_SCOPE_FENCE_BASE` set to the phase's base commit so the gate actually runs rather
    than skipping.
  - `git grep -n '# type: ignore' -- src/semolina` — the count must not exceed the pre-phase
    count; record both numbers.

Then reconcile `.planning/WINDOWS.md` with what the phase actually verified:

  - **Broken window 2** (the zero-row fallback has never fired against a refusing driver) stays
    OPEN unless 48-04's `go/statement.go` re-read and 48-05's work between them actually made it
    fire. If 48-04 found the Databricks driver now implements `ExecuteSchema`, update the
    entry's description with that finding and its date but do NOT mark it fixed — the fallback
    branch is still unexercised.
  - **Broken window 3** (the pandas-dependent artifact row) stays OPEN; this phase did not fix it.
    D-04 documented it instead, and Task 1 wrote that documentation — note the doc reference in
    the entry.
  - **Broken window 4** (the two artifact tables kept column-disjoint by convention only) is
    untouched by this phase.
  - The **DuckDB `INTERVAL`** entry 48-03 added stays open (D-06).
  - Any new deviation recorded in a plan SUMMARY that has no ledger entry gets one now.
  Prefer the `gsd-tools windows` entry point. If hand-editing, update the markdown table, the
  trailing JSON block, and the frontmatter `open_count` / `total_count` together — the three
  representations gate `/gsd-ship` and a partial edit corrupts the count silently.

Reconcile `.planning/todos/pending/`:
  - `2026-08-12-verify-databricks-zero-row-fallback.md` stays pending; append a dated line
    recording 48-04's driver re-read finding so the next person does not repeat it.
  - The Snowflake and Databricks recording todos stay pending — this phase deliberately planned
    no recording, and the evidence limits are recorded in 48-03's summary instead.
  - Add one new todo if 48-03's `[ASSUMED]` Databricks `interval` answer still has no
    measurement: a Databricks interval column needs a cassette before that annotation can be
    called measured.

Finally, cross-check `48-VALIDATION.md`'s requirement-to-test contract against what was built:
tick the Wave 0 checklist, and for any row whose named test path or command no longer matches
reality (notably the `tests/unit/codegen/test_probe.py` circularity row, superseded by
`tests/unit/test_type_fidelity_table.py` in 48-04), correct the row and note the correction in
this plan's summary. Set `nyquist_compliant: true` in its frontmatter only if every requirement
row has a green automated command.
  </action>
  <verify>
    <automated>just test &amp;&amp; prek run --all-files &amp;&amp; just docs-build &amp;&amp; uv run python tests/type_fidelity_probe.py --check</automated>
  </verify>
  <acceptance_criteria>
    - `just test` exits 0 (root suite AND jaffle-shop suite both reported)
    - `prek run --all-files` exits 0
    - `just docs-build` exits 0
    - `uv run python tests/type_fidelity_probe.py --check` exits 0
    - `git diff --name-only <phase-base>..HEAD | grep -E 'src/semolina/(cursor|acursor|results)\.py'` produces no output
    - `SEMOLINA_SCOPE_FENCE_BASE=<phase-base> uv run pytest tests/unit/test_scope_fence.py -x` exits 0 and reports 1 passed, 0 skipped
    - `git grep -c '# type: ignore' -- src/semolina` is not greater than the recorded pre-phase count
    - `.planning/WINDOWS.md` frontmatter `open_count` equals the number of rows with `status: open` in BOTH the markdown table and the trailing JSON block
    - `test -f .planning/todos/pending/2026-08-12-verify-databricks-zero-row-fallback.md` succeeds
    - `48-VALIDATION.md` has no row naming a test path that does not exist on disk
  </acceptance_criteria>
  <done>Both test suites, the strict typecheck, the strict docs build, the artifact staleness check and the scope-fence gate all pass; the broken-window ledger and pending todos match what the phase actually verified; and `48-VALIDATION.md`'s contract names only tests that exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> user action | A published instruction is executed by readers against their own warehouses; a wrong instruction is a wrong action at scale |
| documentation -> credential handling | Example invocations in a how-to are copied verbatim, including anything that looks like a credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-26 | Information disclosure | `docs/src/how-to/codegen.rst` examples | medium | mitigate | The `--check` examples take `--backend`, `--database` and `--model`. Use obviously-placeholder values (`analytics.db`, `path/to/models.py`) and never a token, password, account identifier, or hostname — the page already documents `DUCKDB_DATABASE` as the env-var route and that stays the recommended shape (ASVS V7). |
| T-48-27 | Spoofing | documenting an unverified Databricks capability | high | mitigate | Documenting `--check` as working on Databricks would give users false assurance about a route nobody has run: the driver does not answer `ExecuteSchema` and the `WHERE 1=0` wrapper is unconfirmed against a live metric-view planner. The section states the limitation explicitly, the tracking todo stays open, and broken window 2 stays open. |
| T-48-28 | Tampering | the duplicated exit-code table | medium | mitigate | The table exists in `src/semolina/cli/__init__.py`'s epilog and in the how-to. A silent divergence would send scripted callers to the wrong code. The acceptance criteria require word-for-word agreement between the two. |
| T-48-29 | Repudiation | `.planning/WINDOWS.md` reconciliation | medium | mitigate | The ledger's markdown table, JSON block, and frontmatter counters must stay in step; `open_count` gates `/gsd-ship`, so a partial hand-edit could let the project ship with an unrecorded open defect. Prefer the `gsd-tools windows` entry point and verify the count three ways in the acceptance criteria. |
| T-48-30 | Information disclosure | a `--check` report in a CI log | low | accept | Documented behaviour, mitigated in 48-05 (T-48-22): the report carries field names, annotation strings and routes only. This page states that the run fetches no rows so readers know what a captured report can contain. |
| T-48-SC | Tampering | package installs | low | accept | This plan installs no packages. `48-RESEARCH.md` § "Package Legitimacy Audit" records zero added dependencies for the phase. |
</threat_model>

<verification>
- `just test` green (both suites)
- `prek run --all-files` clean
- `just docs-build` clean under `-W`
- `uv run python tests/type_fidelity_probe.py --check` exits 0
- The scope-fence test runs (not skips) and passes
- `47-DECISIONS.md` appears in no commit of this phase
- Broken windows 2 and 3 are still open; the Databricks zero-row-fallback todo is still pending
</verification>

<success_criteria>
- Neither documentation page carries a claim this phase falsified
- `--check` is documented with its exit codes, its no-rows guarantee, its route reporting, and its Databricks limitation
- The exit-code table agrees word for word between the CLI epilog and the how-to
- `TIMESTAMP_NS`'s environment dependence and the metadata-vs-probe divergence are both explained rather than hidden (D-04, D-02)
- The phase gate ran every check and the ledger matches what was verified
</success_criteria>

<output>
Create `.planning/phases/48-type-map-implementation-databricks-literals/48-06-SUMMARY.md` when done.
Record in it: the gate results with actual command output counts (tests passed per suite, prek
result, sphinx result, artifact `--check` result); the pre- and post-phase `# type: ignore`
counts; the final `.planning/WINDOWS.md` state with each entry's disposition and why; every
todo left pending and why; and the list of `48-VALIDATION.md` rows corrected.
</output>

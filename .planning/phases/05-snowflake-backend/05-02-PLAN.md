---
phase: 05-snowflake-backend
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/test_snowflake_engine.py
autonomous: true

must_haves:
  truths:
    - "SnowflakeEngine can be instantiated without Snowflake connection"
    - "Lazy import raises helpful error when snowflake-connector-python missing"
    - "to_sql() generates correct SQL with AGG() and double quotes"
    - "execute() uses context managers for connection lifecycle"
    - "Snowflake errors are caught and translated to RuntimeError"
    - "Results are mapped from tuples to dicts with correct column names"
  artifacts:
    - path: "tests/test_snowflake_engine.py"
      provides: "Comprehensive unit tests for SnowflakeEngine"
      min_lines: 200
      contains: "class TestSnowflakeEngine"
  key_links:
    - from: "tests/test_snowflake_engine.py"
      to: "unittest.mock"
      via: "Mock Snowflake connector for testing"
      pattern: "Mock.*snowflake\\.connector"
    - from: "TestSnowflakeEngineExecute"
      to: "SnowflakeEngine.execute()"
      via: "test connection lifecycle and result mapping"
      pattern: "test_.*execute"
---

<objective>
Create comprehensive unit tests for SnowflakeEngine using unittest.mock to simulate Snowflake connector behavior.

Purpose: Verify SnowflakeEngine connection management, SQL generation, error handling, and result mapping without requiring real Snowflake warehouse access.

Output: Full test coverage for SnowflakeEngine including lazy import, context managers, error translation, and result mapping.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-snowflake-backend/05-RESEARCH.md
@.planning/phases/05-snowflake-backend/05-01-SUMMARY.md
@src/cubano/engines/snowflake.py
@tests/test_engines.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive tests for SnowflakeEngine</name>
  <files>tests/test_snowflake_engine.py</files>
  <action>
Create tests/test_snowflake_engine.py with comprehensive test coverage:

**Test structure:**

1. **TestSnowflakeEngineInit** (lazy import and initialization):
   - test_init_stores_connection_params: Verify __init__ stores params without creating connection
   - test_init_creates_snowflake_dialect: Verify self.dialect is SnowflakeDialect instance
   - test_lazy_import_raises_helpful_error: Mock missing snowflake.connector, verify ImportError message includes installation instructions

2. **TestSnowflakeEngineToSQL** (SQL generation delegation):
   - test_to_sql_delegates_to_sqlbuilder: Verify to_sql() uses SQLBuilder(SnowflakeDialect)
   - test_to_sql_generates_agg_syntax: Verify metrics wrapped in AGG()
   - test_to_sql_quotes_identifiers: Verify identifiers use double quotes
   - test_to_sql_escapes_quotes: Verify field names with quotes are escaped (e.g., 'my"field' → "my""field")

3. **TestSnowflakeEngineExecute** (execution with mocked connector):
   - test_execute_uses_context_manager_for_connection: Mock snowflake.connector.connect, verify called with connection params, verify __enter__ and __exit__ called
   - test_execute_uses_context_manager_for_cursor: Mock cursor, verify __enter__ and __exit__ called
   - test_execute_calls_cursor_execute_with_sql: Mock cursor.execute, verify called with SQL from to_sql()
   - test_execute_maps_tuples_to_dicts: Mock cursor.description and cursor.fetchall, verify tuples mapped to dicts with correct column names
   - test_execute_returns_list_of_dicts: Verify return type is list[dict[str, Any]]
   - test_execute_handles_empty_results: Mock cursor.fetchall() returning [], verify returns empty list

4. **TestSnowflakeEngineErrorHandling**:
   - test_programming_error_translated_to_runtime_error: Mock cursor.execute raising ProgrammingError, verify RuntimeError raised with error message, errno, sqlstate
   - test_database_error_translated_to_runtime_error: Mock cursor.execute raising DatabaseError, verify RuntimeError raised with error message
   - test_original_exception_chained: Verify RuntimeError has __cause__ set to original exception (from e pattern)

5. **TestSnowflakeEngineIntegration** (end-to-end with mocks):
   - test_full_query_execution_flow: Mock full connection → cursor → execute → fetchall flow, verify complete pipeline works
   - test_multiple_queries_same_engine: Create engine once, execute multiple queries, verify connection params reused

**Mock patterns:**

Use unittest.mock.patch to mock snowflake.connector:
```python
from unittest.mock import MagicMock, patch
from snowflake.connector.errors import ProgrammingError, DatabaseError

@patch('cubano.engines.snowflake.snowflake.connector')
def test_execute_uses_context_manager(mock_connector):
    mock_conn = MagicMock()
    mock_connector.connect.return_value.__enter__.return_value = mock_conn
    # ... assertions
```

Mock cursor.description:
```python
mock_cursor.description = [('revenue',), ('country',)]
mock_cursor.fetchall.return_value = [(100, 'US'), (200, 'CA')]
# Expected: [{'revenue': 100, 'country': 'US'}, {'revenue': 200, 'country': 'CA'}]
```

**Test data reuse:**
- Use Sales model from conftest.py
- Use Query().metrics(Sales.revenue).dimensions(Sales.country) pattern
- Verify SQL matches expected Snowflake syntax from Phase 3

**Quality requirements:**
- Each test should be focused and test one thing
- Use descriptive test names following test_<what>_<expected_behavior> pattern
- Use pytest fixtures for common setup (engine with mocked connector)
- Follow existing test file conventions from tests/test_engines.py
- Comprehensive docstrings for test classes explaining what aspect is tested

Run quality gates after test creation to ensure all tests pass.
  </action>
  <verify>
- File exists: tests/test_snowflake_engine.py
- uv run basedpyright tests/test_snowflake_engine.py — 0 errors
- uv run ruff check tests/test_snowflake_engine.py — All checks passed
- uv run ruff format --check tests/test_snowflake_engine.py — Formatted correctly
- uv run --extra dev pytest tests/test_snowflake_engine.py — All new tests pass
- uv run --extra dev pytest — All tests pass (no regressions)
- At least 15 tests in test_snowflake_engine.py
- Test coverage includes: init, to_sql, execute, error handling, integration
  </verify>
  <done>
Comprehensive test suite for SnowflakeEngine with 15+ tests covering initialization, SQL generation delegation, connection lifecycle with context managers, result mapping, error translation, and end-to-end integration flows. All tests pass with no regressions.
  </done>
</task>

</tasks>

<verification>
**Overall Test Coverage:**

1. Initialization:
   - Connection params stored without creating connection
   - SnowflakeDialect created and stored
   - Lazy import error handling

2. SQL Generation:
   - Delegation to SQLBuilder verified
   - AGG() wrapping verified
   - Double quote identifier quoting verified
   - Quote escaping verified

3. Execution:
   - Context manager usage for connection verified
   - Context manager usage for cursor verified
   - cursor.execute called with correct SQL
   - Results mapped from tuples to dicts
   - Empty results handled correctly

4. Error Handling:
   - ProgrammingError caught and translated
   - DatabaseError caught and translated
   - Original exception chained (from e)
   - Error messages include helpful context

5. Integration:
   - Full query flow tested end-to-end
   - Multiple queries on same engine verified
   - Connection params reused correctly

**Quality Gates:**
- basedpyright: 0 errors
- ruff check: All passed
- ruff format: Formatted correctly
- pytest: All new tests pass, no regressions
</verification>

<success_criteria>
1. tests/test_snowflake_engine.py exists with 15+ tests
2. All tests use unittest.mock to simulate Snowflake connector
3. Test coverage includes init, to_sql, execute, errors, integration
4. Context manager usage verified (connection and cursor)
5. Result mapping verified (tuples → dicts with correct column names)
6. Error translation verified (ProgrammingError, DatabaseError → RuntimeError)
7. Lazy import error handling verified
8. All quality gates pass
9. No test regressions in existing test suite
10. Tests follow existing conventions from test_engines.py
</success_criteria>

<output>
After completion, create `.planning/phases/05-snowflake-backend/05-02-SUMMARY.md`
</output>

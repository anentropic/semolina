---
phase: 42-codegen-field-type-inference
plan: 02
type: execute
wave: 1
depends_on: ["42-01"]
files_modified:
  - src/semolina/codegen/python_renderer.py
autonomous: true
requirements: [DKGEN-05]
must_haves:
  truths:
    - "_field_class_for maps metric→Metric, dimension→Dimension, fact→Fact via an explicit dict"
    - "_field_class_for raises ValueError on any unrecognized role instead of silently returning Dimension"
    - "The Plan 01 raise-path test now passes (RED→GREEN)"
    - "The existing DuckDB e2e snapshot stays byte-identical (strict change is a no-op for known roles)"
  artifacts:
    - path: "src/semolina/codegen/python_renderer.py"
      provides: "strict _field_class_for + module-level _ROLE_TO_CLASS map"
      contains: "_ROLE_TO_CLASS"
  key_links:
    - from: "src/semolina/codegen/python_renderer.py:_build_model_context"
      to: "_field_class_for"
      via: "direct call at line ~109; raise must propagate (not caught)"
      pattern: "_field_class_for\\(f\\.field_type\\)"
---

<objective>
Make `_field_class_for` strict: replace the catch-all `return "Dimension"` with an explicit `_ROLE_TO_CLASS` dict lookup that raises `ValueError` on any unrecognized role. This turns the Plan 01 raise-path test GREEN (the fix half of the bug-fix-first sequence) and enforces the locked "no unknown case" invariant — schema drift fails loudly instead of mislabeling a column.

Purpose: Per locked CONTEXT.md decision, the generator must fail loudly on an unrecognized warehouse role rather than silently defaulting to Dimension. This is the only production code change in the phase.
Output: A strict `_field_class_for` with `Raises: ValueError` documented; all codegen tests green including the regression-guard DuckDB snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-codegen-field-type-inference/42-CONTEXT.md
@.planning/phases/42-codegen-field-type-inference/42-RESEARCH.md
@.planning/phases/42-codegen-field-type-inference/42-PATTERNS.md

<interfaces>
<!-- Exact change site. Extracted from codebase at HEAD f1a218e. -->

From src/semolina/codegen/python_renderer.py:
- Current `_field_class_for` (lines 65-79): `if field_type == "metric": return "Metric"` / `if field_type == "fact": return "Fact"` / `return "Dimension"` (catch-all = the silent-coercion bug).
- Caller `_build_model_context` (line 109): `field_class=_field_class_for(f.field_type)` — do NOT wrap in try/except; the raise must propagate.
- Module constant precedent: `_DATETIME_TYPES = frozenset({...})` at line 21 — place `_ROLE_TO_CLASS` near it.
- `IntrospectedField.field_type` is typed `Literal["metric","dimension","fact"]` (introspector.py:40), so an out-of-Literal value is a contract violation → ValueError is the idiomatic choice (RESEARCH lines 238-251: no fitting Semolina error class exists; ABC docstrings already cite ValueError).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make _field_class_for strict (raise on unrecognized role)</name>
  <files>src/semolina/codegen/python_renderer.py</files>
  <read_first>
    - src/semolina/codegen/python_renderer.py:1-120 (current _field_class_for at 65-79, caller at 109, _DATETIME_TYPES constant at 21)
    - .planning/phases/42-codegen-field-type-inference/42-PATTERNS.md (target shape, lines 46-74)
    - .planning/phases/42-codegen-field-type-inference/42-RESEARCH.md (exception-type recommendation lines 238-268; anti-pattern "do not catch the raise" lines 208-209)
    - tests/unit/codegen/test_python_renderer.py (the Plan 01 raise-path test this change must turn green)
  </read_first>
  <behavior>
    - _field_class_for("metric") == "Metric"; ("dimension") == "Dimension"; ("fact") == "Fact" (existing behavior preserved for known roles).
    - _field_class_for("widget") raises ValueError matching "Unrecognized field role" (Plan 01 test goes GREEN).
    - The DuckDB e2e snapshot (only known roles) renders byte-identical — strict lookup returns the same strings.
  </behavior>
  <action>
    Add a module-level constant `_ROLE_TO_CLASS = {"metric": "Metric", "dimension": "Dimension", "fact": "Fact"}` near `_DATETIME_TYPES` (line 21). Rewrite `_field_class_for(field_type: str) -> str` to `try: return _ROLE_TO_CLASS[field_type]` / `except KeyError: raise ValueError(f"Unrecognized field role: {field_type!r}") from None`. Keep the Google-style docstring; add a `Raises:` section documenting the ValueError for unrecognized roles. Do NOT modify `_build_model_context` — the raise must propagate from line 109. No `# type: ignore`; the function stays `str -> str`. Line length 100; D213 (summary on second line). The keys are lowercase because both engines normalize role strings to lowercase before the renderer sees them.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/codegen/ -x</automated>
  </verify>
  <acceptance_criteria>
    - src/semolina/codegen/python_renderer.py contains `_ROLE_TO_CLASS` at module level and `raise ValueError(f"Unrecognized field role:` in `_field_class_for`.
    - The function docstring has a `Raises:` section naming ValueError.
    - `_build_model_context` is unchanged (no try/except around the call) — grep `_field_class_for(f.field_type)` still present, no surrounding `try`.
    - `uv run pytest tests/unit/codegen/ -x` exits 0 (all codegen tests green, including the Plan 01 raise-path test now GREEN and the DuckDB snapshot still passing without `--snapshot-update`).
    - `git diff tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` shows NO change to the DuckDB block after this change.
  </acceptance_criteria>
  <done>_field_class_for is strict and raises ValueError on unrecognized roles; raise-path test is GREEN; DuckDB snapshot byte-identical; no production code other than the renderer touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| warehouse role string → field class name | The normalized lowercase role string (`metric`/`dimension`/`fact`) crosses into the renderer. The strict-raise invariant lives exactly here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | Tampering / Spoofing | `_field_class_for` receiving a role outside its Literal contract (schema drift, malformed metadata) | mitigate | Strict dict lookup raises ValueError on any unrecognized role — the generator fails loudly instead of silently mislabeling a column as Dimension (the prior catch-all behavior). This is the security-relevant hardening of the phase: it removes a silent-coercion path. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate needed. |
</threat_model>

<verification>
- `uv run pytest tests/unit/codegen/ -x` → all green (raise-path test now passes; SF/DB snapshots pass; DuckDB snapshot unchanged).
- `git diff src/semolina/codegen/python_renderer.py` shows only the `_ROLE_TO_CLASS` constant + strict `_field_class_for` rewrite + docstring `Raises:`.
- `prek run --all-files` clean (ruff + basedpyright strict; no `# type: ignore`).
</verification>

<success_criteria>
- Strict mapping with explicit dict; ValueError on unrecognized role; raise propagates to the caller.
- RED→GREEN sequence complete (Plan 01 test now passes).
- DuckDB regression guard byte-identical.
</success_criteria>

<output>
Create `.planning/phases/42-codegen-field-type-inference/42-02-SUMMARY.md` when done.
</output>

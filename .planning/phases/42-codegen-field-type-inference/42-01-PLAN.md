---
phase: 42-codegen-field-type-inference
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/codegen/test_python_renderer.py
  - tests/unit/codegen/test_codegen_e2e.py
  - tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr
autonomous: true
requirements: [DKGEN-05]
must_haves:
  truths:
    - "_field_class_for('widget') raises ValueError BEFORE any renderer change (RED, bug-fix-first)"
    - "Snowflake offline introspect→render snapshot emits Metric, Dimension, and Fact field classes"
    - "Databricks offline introspect→render snapshot emits Metric and Dimension only (no Fact, intentional)"
    - "All three new tests run fully offline via sys.modules connector mocks (no live warehouse, no credentials)"
  artifacts:
    - path: "tests/unit/codegen/test_python_renderer.py"
      provides: "raise-path unit test test_field_class_for_unrecognized_role_raises"
      contains: "pytest.raises(ValueError"
    - path: "tests/unit/codegen/test_codegen_e2e.py"
      provides: "Snowflake + Databricks offline codegen snapshot tests"
      contains: "render_and_format"
    - path: "tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr"
      provides: "new committed .ambr snapshot blocks for SF + Databricks codegen output"
      contains: "Fact["
  key_links:
    - from: "tests/unit/codegen/test_codegen_e2e.py"
      to: "semolina.engines.snowflake.SnowflakeEngine.introspect"
      via: "sys.modules connector mock + cursor.fetchall"
      pattern: "fetchall\\.return_value"
    - from: "tests/unit/codegen/test_codegen_e2e.py"
      to: "semolina.engines.databricks.DatabricksEngine.introspect"
      via: "sys.modules connector mock + cursor.fetchone"
      pattern: "fetchone\\.return_value"
---

<objective>
Create the three Wave 0 failing tests that define the phase's acceptance contract, in bug-fix-first order: the strict-raise unit test (RED against the still-lenient renderer) plus the two offline Snowflake/Databricks codegen snapshot tests (criteria 2 and 3). No production code changes in this plan.

Purpose: Per CLAUDE.md bug-fix discipline, the failing raise-path test MUST be committed before the `_field_class_for` change (Plan 02). The SF/Databricks snapshot tests close the only real coverage gap in the phase: DuckDB already has an e2e snapshot, the other two backends do not.
Output: Two test files extended (one new unit test, two new snapshot tests) and a new committed `.ambr` snapshot block set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-codegen-field-type-inference/42-CONTEXT.md
@.planning/phases/42-codegen-field-type-inference/42-RESEARCH.md
@.planning/phases/42-codegen-field-type-inference/42-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase at HEAD f1a218e. Do not re-explore. -->

From src/semolina/codegen/python_renderer.py:
- `def render_and_format(views: list[IntrospectedView]) -> str` (line 251) — runs render_views + ruff format/isort; matches the DuckDB e2e precedent. Falls back to raw source if ruff absent.
- `def render_views(views: list[IntrospectedView]) -> str` (line 123) — subprocess-free Jinja alternative.
- `def _field_class_for(field_type: str) -> str` (line 65) — CURRENTLY lenient: returns "Dimension" for any unknown role. The raise-path test targets this; it must FAIL when run against this current body ("widget" → "Dimension", no raise).

From semolina.engines.snowflake:
- `SnowflakeEngine(account=..., user=..., password=...)`; `.introspect(view_name) -> IntrospectedView`. Reads `SHOW COLUMNS IN VIEW`; `kind` arrives UPPERCASE, engine `.lower()`s it before building IntrospectedField. Fixtures use UPPERCASE METRIC/DIMENSION/FACT.

From semolina.engines.databricks:
- `DatabricksEngine(server_hostname=..., http_path=..., access_token=...)`; `.introspect(view_name) -> IntrospectedView`. Reads `DESCRIBE TABLE EXTENDED ... AS JSON`; maps `is_measure` True→"metric", False→"dimension". No Fact concept.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the strict-raise RED unit test (bug-fix-first)</name>
  <files>tests/unit/codegen/test_python_renderer.py</files>
  <read_first>
    - tests/unit/codegen/test_python_renderer.py (existing TestRenderViews style, imports at lines 9-11; pytest not yet imported)
    - src/semolina/codegen/python_renderer.py:65-79 (current lenient _field_class_for — the test must fail against this)
    - .planning/phases/42-codegen-field-type-inference/42-PATTERNS.md (raise-path test section, lines 78-104)
  </read_first>
  <behavior>
    - test_field_class_for_unrecognized_role_raises: calling `_field_class_for("widget")` raises ValueError with message matching "Unrecognized field role".
    - This test MUST FAIL right now (current body returns "Dimension" for "widget", no raise) — that is the RED state. Do NOT change python_renderer.py in this plan.
  </behavior>
  <action>
    In tests/unit/codegen/test_python_renderer.py, add a module-level test function `test_field_class_for_unrecognized_role_raises` adjacent to the `TestRenderViews` class. Import `pytest` at module top (not yet present) and import `_field_class_for` from `semolina.codegen.python_renderer` (match the file's convention of importing render_* inside the test body if preferred, but `_field_class_for` import is fine at top or in-body). The test uses `pytest.raises(ValueError, match="Unrecognized field role")` around `_field_class_for("widget")`. Use the role string "widget" as the unrecognized value. Add a D213-style one-line docstring stating it raises rather than defaulting to Dimension.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/codegen/test_python_renderer.py::test_field_class_for_unrecognized_role_raises -x 2>&1 | grep -q "1 failed" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/codegen/test_python_renderer.py contains `pytest.raises(ValueError` and the string "Unrecognized field role".
    - `import pytest` present at module top.
    - The new test FAILS against the unchanged renderer (RED) — verified by the grep "1 failed" above. python_renderer.py is unchanged.
  </acceptance_criteria>
  <done>The raise-path test exists and is RED against the current lenient _field_class_for; production code untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add offline Snowflake codegen snapshot test</name>
  <files>tests/unit/codegen/test_codegen_e2e.py, tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr</files>
  <read_first>
    - tests/unit/codegen/test_codegen_e2e.py (DuckDB e2e structure + syrupy SnapshotAssertion usage)
    - tests/unit/test_snowflake_engine.py:47-76 (sys.modules autouse mock fixture), 575-642 (cursor description/fetchall rows, _make_cursor_row helper)
    - .planning/phases/42-codegen-field-type-inference/42-PATTERNS.md (Snowflake codegen E2E section, lines 108-168)
    - .planning/phases/42-codegen-field-type-inference/42-RESEARCH.md (Pitfall 1 lines 301-307: do NOT route through CliRunner/_resolve_backend — credentials loader trips offline)
  </read_first>
  <action>
    In tests/unit/codegen/test_codegen_e2e.py, add `test_codegen_snowflake_field_types(snapshot)`. Drive `SnowflakeEngine.introspect("sales_view")` → `render_and_format([view])` → `assert ... == snapshot`. Do NOT use CliRunner or _resolve_backend (those call SnowflakeCredentials.load()). Copy the sys.modules connector-mock seam from test_snowflake_engine.py into this module (do not import across test modules). Set `mock_cursor.description = [("column_name",),("kind",),("data_type",),("comment",)]` and `mock_cursor.fetchall.return_value` to three rows lifted verbatim from test_snowflake_engine.py:608-612: revenue/METRIC/`{"type":"FIXED","scale":0}` (→ Metric[int]), country/DIMENSION/`{"type":"TEXT"}` (→ Dimension[str]), date_key/FACT/`{"type":"DATE"}` (→ Fact[datetime.date]). Construct `SnowflakeEngine(account="test", user="user", password="pass")`. Wire `patch("snowflake.connector.connect")` → `__enter__` → `cursor().__enter__` → mock_cursor. Use the same syrupy `snapshot: SnapshotAssertion` parameter shape as the DuckDB test.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/codegen/test_codegen_e2e.py -k snowflake -x --snapshot-update && uv run pytest tests/unit/codegen/test_codegen_e2e.py -k snowflake -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/codegen/test_codegen_e2e.py defines `test_codegen_snowflake_field_types` calling `render_and_format` and asserting `== snapshot`.
    - Test does NOT reference CliRunner, _resolve_backend, SnowflakeCredentials, env vars, or `.semolina.toml`.
    - After `--snapshot-update`, `tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` contains `Fact[` and `Metric[` and `Dimension[` under the snowflake test block.
    - `uv run pytest tests/unit/codegen/test_codegen_e2e.py -k snowflake -x` exits 0 (green) without `--snapshot-update`.
  </acceptance_criteria>
  <done>Snowflake codegen snapshot test passes offline; new .ambr block committed demonstrating Metric/Dimension/Fact emission.</done>
</task>

<task type="auto">
  <name>Task 3: Add offline Databricks codegen snapshot test (no Fact)</name>
  <files>tests/unit/codegen/test_codegen_e2e.py, tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr</files>
  <read_first>
    - tests/unit/codegen/test_codegen_e2e.py (structure; the just-added Snowflake test as a sibling pattern)
    - tests/unit/test_databricks_engine.py:28-73 (sys.modules mock + _create_mock_databricks helper), 838-892 (schema_json fetchone payload, _make_schema_json helper)
    - .planning/phases/42-codegen-field-type-inference/42-PATTERNS.md (Databricks codegen E2E section, lines 172-205)
    - .planning/phases/42-codegen-field-type-inference/42-CONTEXT.md (Databricks no-Fact constraint, lines 85-88)
  </read_first>
  <action>
    In tests/unit/codegen/test_codegen_e2e.py, add `test_codegen_databricks_field_types(snapshot)` with a docstring stating Databricks emits Metric/Dimension only and the absence of Fact is intentional (Databricks has no native Fact type), so reviewers do not read it as a bug. Drive `DatabricksEngine.introspect("sales_view")` → `render_and_format([view])` → `assert == snapshot`. Copy the Databricks sys.modules mock seam (`_create_mock_databricks()` + `patch.dict(sys.modules, {...})`) into this module. Note the Databricks cursor returns a single JSON string in a 1-tuple: `mock_cursor.fetchone.return_value = (schema_json,)` where schema_json is `json.dumps({"columns": [...]})` with two columns lifted from test_databricks_engine.py:850-855: revenue/`is_measure=True`/`{"name":"double"}` (→ Metric[float]), country/`is_measure=False`/`{"name":"string"}` (→ Dimension[str]). Construct `DatabricksEngine(server_hostname="test", http_path="/sql/1.0/warehouses/abc", access_token="token")`. Wire `mock_sql.connect` → `__enter__` → `cursor().__enter__` → mock_cursor. Do NOT add a Fact row.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/codegen/test_codegen_e2e.py -k databricks -x --snapshot-update && uv run pytest tests/unit/codegen/test_codegen_e2e.py -k databricks -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/codegen/test_codegen_e2e.py defines `test_codegen_databricks_field_types` with a docstring noting the no-Fact absence is intentional.
    - Test does NOT reference CliRunner, _resolve_backend, DatabricksCredentials, or credentials.
    - After `--snapshot-update`, the databricks `.ambr` block contains `Metric[` and `Dimension[` and does NOT contain `Fact[`.
    - `uv run pytest tests/unit/codegen/ -x` exits 0 EXCEPT the still-RED raise-path test from Task 1 (which stays red until Plan 02). The two snapshot tests are green.
  </acceptance_criteria>
  <done>Databricks codegen snapshot test passes offline emitting only Metric/Dimension; .ambr block committed; no-Fact intent documented in the test docstring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| warehouse metadata → generated Python source | Column/role identifiers from warehouse metadata flow into rendered source strings. In this plan the metadata is fully synthetic (hand-crafted fixtures), so no untrusted input is exercised here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | warehouse identifiers rendered into Python source | accept | Identifiers already flow through the existing `normalize_identifier`/type-mapping paths in the engines and renderer (out of scope to change here). This plan only adds tests over synthetic, non-attacker-controlled fixtures — no new injection surface. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH confirms no dependency changes; pytest + syrupy already present). No legitimacy gate needed. |
</threat_model>

<verification>
- `uv run pytest tests/unit/codegen/test_python_renderer.py::test_field_class_for_unrecognized_role_raises -x` → FAILS (RED, expected — fixed in Plan 02).
- `uv run pytest tests/unit/codegen/test_codegen_e2e.py -k "snowflake or databricks" -x` → PASSES.
- `git status` shows a new/updated `tests/unit/codegen/__snapshots__/test_codegen_e2e.ambr` with snowflake + databricks blocks.
</verification>

<success_criteria>
- Raise-path test committed RED before any renderer change (bug-fix discipline satisfied).
- SF snapshot demonstrates Metric/Dimension/Fact; Databricks snapshot demonstrates Metric/Dimension with no Fact.
- All new tests run offline (no credentials, no live warehouse).
</success_criteria>

<output>
Create `.planning/phases/42-codegen-field-type-inference/42-01-SUMMARY.md` when done.
</output>

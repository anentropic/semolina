---
phase: 42-codegen-field-type-inference
plan: 03
type: execute
wave: 2
depends_on: ["42-02"]
files_modified:
  - docs/src/how-to/codegen.rst
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DKGEN-05]
must_haves:
  truths:
    - "The codegen how-to documents per-role field-type emission across all three backends"
    - "The how-to states the Databricks no-Fact constraint and the new strict-raise-on-unrecognized-role behaviour"
    - "ROADMAP Phase 42 criterion 4 and REQUIREMENTS DKGEN-05 wording no longer claim a Field() fallback; they state every column resolves to a concrete role and an unrecognized role raises"
    - "DKGEN-05 is marked complete in REQUIREMENTS (checkbox [x] + Traceability table Complete)"
    - "PROJECT.md Key Decisions records the per-backend metadata-query paths and the strict-raise decision"
  artifacts:
    - path: "docs/src/how-to/codegen.rst"
      provides: "amended field-type section: 3-backend per-role emission + Databricks no-Fact + strict-raise note"
      contains: "raise"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DKGEN-05 marked [x] + Traceability Complete + reworded (no Field() fallback)"
      contains: "DKGEN-05"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row for metadata-query paths + strict-raise"
      contains: "DESCRIBE SEMANTIC VIEW"
  key_links:
    - from: ".planning/ROADMAP.md Phase 42 criterion 4"
      to: ".planning/REQUIREMENTS.md DKGEN-05"
      via: "matching reworded text (concrete role / raise, not Field() fallback)"
      pattern: "raise"
---

<objective>
Close out the phase: amend the codegen how-to to document per-role emission across all three backends plus the Databricks no-Fact note and the new strict-raise behaviour; rewrite ROADMAP Phase 42 criterion 4 and REQUIREMENTS DKGEN-05 from "Field() fallback preserved" to "every column resolves to a concrete role; unrecognized role raises"; mark DKGEN-05 complete; and record the per-backend metadata-query paths + strict-raise decision in PROJECT.md Key Decisions.

Purpose: The locked decision removed the Field() fallback concept entirely, so the success-criterion and requirement text that still describes it is now wrong and must be corrected before close. Documentation and traceability are part of the phase's definition of done (criterion 5).
Output: Amended how-to (Sphinx -W clean), corrected ROADMAP + REQUIREMENTS, DKGEN-05 closed, PROJECT.md decision logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/semolina-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-codegen-field-type-inference/42-CONTEXT.md
@.planning/phases/42-codegen-field-type-inference/42-RESEARCH.md
@.planning/phases/42-codegen-field-type-inference/42-PATTERNS.md
@docs/src/how-to/codegen.rst

<close_out_targets>
<!-- Exact locations from RESEARCH (verified at HEAD f1a218e). Re-confirm line numbers before editing — Plan 02 did not touch these files but confirm anyway. -->
- docs/src/how-to/codegen.rst — field-type section ~lines 211-251 (worked example 211-219; `.. note::` Databricks-no-Fact 221-225; list-table mapping 230-240). Amend, do not rewrite the page.
- .planning/ROADMAP.md — Phase 42 success criterion 4 at **line 152** (current text: "Existing `Field()` fallback behaviour is preserved for columns whose role cannot be determined; behaviour is documented in the codegen how-to.").
- .planning/ROADMAP.md — Phase 42 `**Plans**` checklist items at **lines 155-157** (three `- [ ]` lines to flip to `- [x]`).
- .planning/ROADMAP.md — Progress table Phase 42 row at **line 182** (current: `| 42 | v0.5 | 0/0 | Not started | - |`).
- .planning/REQUIREMENTS.md — DKGEN-05 checkbox at line 23 ([ ]→[x]); Traceability table DKGEN-05 row at line 67 (Pending→Complete). Reword DKGEN-05 if its text implies a Field() fallback (current line 23 does not, but confirm against the rewritten criterion).
- .planning/PROJECT.md — Key Decisions section starts at line 122.
</close_out_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend the codegen how-to (semolina-docs-author skill MANDATORY)</name>
  <files>docs/src/how-to/codegen.rst</files>
  <read_first>
    - .claude/skills/semolina-docs-author/SKILL.md (Diataxis how-to classification + humanizer pass — MANDATORY)
    - docs/src/how-to/codegen.rst:205-254 (existing field-type section: worked example, Databricks no-Fact note, list-table mapping)
    - .planning/phases/42-codegen-field-type-inference/42-RESEARCH.md (Doc Amendment Target lines 386-410)
    - .planning/phases/42-codegen-field-type-inference/42-PATTERNS.md (doc amend section lines 236-248)
  </read_first>
  <action>
    Load the semolina-docs-author skill and apply its how-to classification + humanizer pass to the changed prose only (this is a targeted <50% amendment). In docs/src/how-to/codegen.rst field-type section: (1) make per-role emission explicit for all three backends — confirm Snowflake and Databricks are named alongside DuckDB, not just DuckDB; (2) verify/lightly expand the existing Databricks no-Fact `.. note::` so it reads as an intentional constraint (Databricks metric views support only metric vs dimension); (3) add NEW prose documenting strict-raise: an unrecognized warehouse role string makes codegen fail loudly (raises) rather than silently mislabel the column as a Dimension. Keep second-person voice, warm-but-efficient tone. Use the existing list-table/note RST constructs; if adding a dialect-specific example use sphinx-design tab-set with `:sync-group: warehouse`. Do not create a new page.
  </action>
  <verify>
    <automated>just docs-build</automated>
  </verify>
  <acceptance_criteria>
    - docs/src/how-to/codegen.rst field-type section names all three backends (DuckDB, Snowflake, Databricks) for per-role emission.
    - The section contains new prose describing the strict-raise / fail-loudly behaviour for an unrecognized role (grep the file for "raise" or "fail" in the field-type section).
    - The Databricks no-Fact constraint is stated as intentional.
    - `just docs-build` (sphinx-build -W, strict) exits 0 — no warnings.
  </acceptance_criteria>
  <done>How-to documents 3-backend per-role emission, the Databricks no-Fact constraint, and the new strict-raise behaviour; Sphinx -W build is clean; humanizer pass applied to changed prose.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite criterion 4 / DKGEN-05 wording and close DKGEN-05 traceability</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md:144-157 (Phase 42 section; criterion 4 at line 152; Plans checklist at lines 155-157)
    - .planning/ROADMAP.md:173-184 (Progress table; Phase 42 row at line 182)
    - .planning/REQUIREMENTS.md:22-23 (DKGEN-05 definition), 61-68 (Traceability table), 70-77 (Coverage footer)
    - .planning/phases/42-codegen-field-type-inference/42-CONTEXT.md:52-56 (the locked criterion-4 rewrite wording)
    - .planning/phases/42-codegen-field-type-inference/42-RESEARCH.md:412-419 (Close-Out Targets table)
  </read_first>
  <action>
    Make four edits in .planning/ROADMAP.md and the DKGEN-05 close-out in .planning/REQUIREMENTS.md, keeping the two files' wording mutually consistent.

    (a) ROADMAP line 152 — replace Phase 42 success criterion 4. Current text: "Existing `Field()` fallback behaviour is preserved for columns whose role cannot be determined; behaviour is documented in the codegen how-to." Rewrite to match the locked decision (CONTEXT.md:52-56): every column resolves to a concrete role (Metric/Dimension/Fact) across all three backends; an unrecognized role string raises `ValueError` (fails loudly) rather than silently defaulting to Dimension; behaviour is documented in the codegen how-to. The rewritten line MUST NOT contain the substring `Field()` fallback behaviour is preserved and SHOULD contain the phrase "resolves to a concrete role".

    (b) ROADMAP lines 155-157 — flip the three Phase 42 `**Plans**` checklist items from `- [ ]` to `- [x]` (42-01, 42-02, 42-03).

    (c) ROADMAP line 182 — update the Progress table Phase 42 row from `| 42 | v0.5 | 0/0 | Not started | - |` to `| 42 | v0.5 | 3/3 | Complete | 2026-06-09 |` (3 plans, today's date).

    (d) .planning/REQUIREMENTS.md — change the DKGEN-05 checkbox (line 23) from `[ ]` to `[x]`; if the DKGEN-05 description implies a Field() fallback, reword to match (current text describes inference across three backends — keep that, ensure it does not claim a fallback). Change the Traceability table DKGEN-05 row (line 67) from `Pending` to `Complete`. Update the REQUIREMENTS footer "Last updated" line noting DKGEN-05 closed at Phase 42 + criterion-4 rewrite.
  </action>
  <verify>
    <automated>if grep -q "Field()\` fallback behaviour is preserved" .planning/ROADMAP.md; then echo "FAIL: stale criterion-4 text still present"; exit 1; fi; grep -q "resolves to a concrete role" .planning/ROADMAP.md && grep -q "| 42 | v0.5 | 3/3 | Complete | 2026-06-09 |" .planning/ROADMAP.md && grep -q "\[x\] \*\*DKGEN-05\*\*" .planning/REQUIREMENTS.md && grep -q "| DKGEN-05 | Phase 42 | Complete |" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md line 152 no longer contains the string ``Field()` fallback behaviour is preserved` and now contains "resolves to a concrete role" + the raise-on-unrecognized statement.
    - .planning/ROADMAP.md Phase 42 Plans checklist (lines 155-157) shows all three items as `- [x]`.
    - .planning/ROADMAP.md Progress table Phase 42 row (line 182) reads `| 42 | v0.5 | 3/3 | Complete | 2026-06-09 |`.
    - .planning/REQUIREMENTS.md DKGEN-05 checkbox is `[x]`.
    - .planning/REQUIREMENTS.md Traceability row reads `| DKGEN-05 | Phase 42 | Complete |`.
    - ROADMAP criterion 4 text and REQUIREMENTS DKGEN-05 text are mutually consistent (both describe raise-on-unrecognized, neither claims a Field() fallback).
  </acceptance_criteria>
  <done>Criterion 4 reworded (line 152), Plans checklist flipped to [x] (lines 155-157), Progress row updated (line 182); DKGEN-05 reworded if needed and marked complete in both the checkbox and the Traceability table; the deterministic grep verify passes.</done>
</task>

<task type="auto">
  <name>Task 3: Record metadata-query paths + strict-raise in PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md:122-175 (Key Decisions section format — follow the existing row/entry style)
    - .planning/phases/42-codegen-field-type-inference/42-CONTEXT.md:95-102 (close-out decision: what to log)
    - .planning/phases/42-codegen-field-type-inference/42-RESEARCH.md:412-419 (Close-Out Targets)
  </read_first>
  <action>
    In .planning/PROJECT.md Key Decisions, add an entry (matching the section's existing format — table row or bulleted decision, whichever the section uses) recording two related decisions from Phase 42: (1) the per-backend semantic-view metadata-query paths used for field-role inference — DuckDB `DESCRIBE SEMANTIC VIEW`, Snowflake `SHOW COLUMNS IN VIEW` (`kind`), Databricks `DESCRIBE TABLE EXTENDED ... AS JSON` (`is_measure`); and (2) the strict-raise-on-unrecognized-role decision in `_field_class_for` (fail loudly on schema drift rather than silently coercing to Dimension), noting Databricks has no native Fact type. Reference Phase 42. Keep it concise and consistent with surrounding entries.
  </action>
  <verify>
    <automated>grep -q "DESCRIBE SEMANTIC VIEW" .planning/PROJECT.md && grep -q "SHOW COLUMNS IN VIEW" .planning/PROJECT.md && grep -q "DESCRIBE TABLE EXTENDED" .planning/PROJECT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md Key Decisions contains the three metadata-query paths (DuckDB DESCRIBE SEMANTIC VIEW, Snowflake SHOW COLUMNS IN VIEW, Databricks DESCRIBE TABLE EXTENDED).
    - The entry mentions the strict-raise-on-unrecognized-role decision and the Databricks no-Fact constraint.
    - The entry references Phase 42 and matches the surrounding Key Decisions format.
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions records the per-backend metadata-query paths and the strict-raise decision, attributed to Phase 42.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only edits documentation and planning/tracking files; no executable surface and no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Information disclosure | how-to documents warehouse metadata-query paths | accept | The documented query paths (`DESCRIBE SEMANTIC VIEW` etc.) are public warehouse DDL syntax, not secrets. No sensitive information exposed. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. No legitimacy gate needed. |
</threat_model>

<verification>
- `just docs-build` → exits 0 (Sphinx -W strict, no warnings).
- `grep "\[x\] \*\*DKGEN-05\*\*" .planning/REQUIREMENTS.md` → matches.
- `grep "DESCRIBE TABLE EXTENDED" .planning/PROJECT.md` → matches.
- ROADMAP criterion 4 (line 152) contains "resolves to a concrete role" and no longer contains the "Field()` fallback behaviour is preserved" string; Progress row (line 182) reads `3/3 | Complete | 2026-06-09`; ROADMAP criterion 4 and REQUIREMENTS DKGEN-05 text are consistent.
</verification>

<success_criteria>
- How-to amended (3-backend per-role + Databricks no-Fact + strict-raise), Sphinx -W clean, humanizer applied.
- Criterion 4 + DKGEN-05 reworded to match the locked decision; DKGEN-05 closed in checkbox + Traceability; Plans checklist + Progress row updated.
- PROJECT.md Key Decisions logs the metadata-query paths + strict-raise.
</success_criteria>

<output>
Create `.planning/phases/42-codegen-field-type-inference/42-03-SUMMARY.md` when done.
</output>

---
phase: 24-v02-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cubano/codegen/models.py
  - .planning/REQUIREMENTS.md
  - .planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-PLAN.md
  - .planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-SUMMARY.md
autonomous: true
requirements:
  - TECH-DEBT-DEAD-CODE
  - TECH-DEBT-REQUIREMENTS-BOOKKEEPING

must_haves:
  truths:
    - "src/cubano/codegen/models.py does not exist (deleted)"
    - "REQUIREMENTS.md Codegen section has a supersession annotation noting CODEGEN-01–08 describe deleted forward codegen"
    - "REQUIREMENTS.md DOCS-03 says .where() not .filter()"
    - "REQUIREMENTS.md DOCS-04 says Predicate composition not Q-objects"
    - "19-01-PLAN.md frontmatter has requirements: []"
    - "19-01-SUMMARY.md frontmatter has requirements-completed: []"
    - "All tests still pass after deletion (no callers existed)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Annotated CODEGEN section, corrected DOCS-03 and DOCS-04 descriptions"
      contains: "SUPERSEDED"
    - path: ".planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-PLAN.md"
      provides: "Corrected requirements frontmatter"
      contains: "requirements: []"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 20-04"
      via: "CODEGEN-01–08 supersession annotation"
      pattern: "SUPERSEDED"
---

<objective>
Delete the dead `src/cubano/codegen/models.py` file (ModelData and FieldData dataclasses with
zero callers since Phase 20 teardown) and correct four stale text entries in REQUIREMENTS.md
and two planning file frontmatter fields.

Purpose: Stale code and stale requirement descriptions create confusion. CODEGEN-01–08 describe
forward codegen that was intentionally deleted in Phase 20-04. DOCS-03 references `.filter()`
(renamed to `.where()` in Phase 10.1). DOCS-04 references Q-objects (replaced by Predicate
in Phase 13.1). Phase 19 plan incorrectly claims DOCS-04 as its requirement.

Output: Deleted file, annotated REQUIREMENTS.md Codegen section, corrected DOCS-03/04
descriptions, corrected Phase 19 frontmatter.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-PLAN.md
@.planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete codegen/models.py dead code</name>
  <files>src/cubano/codegen/models.py</files>
  <action>
    Delete the file `src/cubano/codegen/models.py`.

    Confirmed dead: grep across the entire codebase finds zero imports of
    `cubano.codegen.models` or `from cubano.codegen.models import` anywhere
    outside the file itself (confirmed by research). The file contains only
    `FieldData` and `ModelData` frozen dataclasses that were superseded when
    Phase 20 replaced forward codegen with reverse codegen.

    After deletion, verify the codegen package still works:
    - `src/cubano/codegen/__init__.py` — keep (empty package init)
    - `src/cubano/codegen/introspector.py` — keep
    - `src/cubano/codegen/python_renderer.py` — keep
    - `src/cubano/codegen/type_map.py` — keep
    - `src/cubano/codegen/templates/` — keep

    Run `uv run basedpyright` to confirm no remaining references to the deleted module.
    Run `uv run --extra dev pytest` to confirm no tests import from it.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/cubano && python -c "import cubano.codegen.introspector; print('OK')" && uv run --extra dev pytest tests/ -x -q 2>&1 | tail -5</automated>
    <manual>Confirm src/cubano/codegen/models.py no longer exists. Confirm uv run basedpyright reports 0 errors.</manual>
  </verify>
  <done>File deleted, all tests pass, basedpyright reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate REQUIREMENTS.md CODEGEN section and fix DOCS-03/04</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Make four targeted edits to `.planning/REQUIREMENTS.md`:

    **Edit 1 — Add supersession annotation to Codegen section.**
    Find the `### Codegen` section heading. Add the following HTML comment block
    immediately AFTER the heading line and BEFORE the first `- [x] **CODEGEN-01**` line:

    ```
    <!-- SUPERSEDED: CODEGEN-01–08 describe forward codegen (Python → SQL/YAML) that was
    intentionally deleted in Phase 20-04 (reverse codegen teardown). The currently shipped
    behavior is reverse codegen (warehouse → Python), tracked as CODEGEN-REVERSE and
    CODEGEN-WAREHOUSE in the Traceability table below. -->
    ```

    **Edit 2 — Fix DOCS-03 description.**
    Find the line:
    ```
    - [x] **DOCS-03**: Query language guide: `.metrics()`, `.dimensions()`, `.filter()`, `.order_by()`, `.limit()`
    ```
    Change `.filter()` to `.where()`:
    ```
    - [x] **DOCS-03**: Query language guide: `.metrics()`, `.dimensions()`, `.where()`, `.order_by()`, `.limit()`
    ```

    **Edit 3 — Fix DOCS-04 description.**
    Find the line:
    ```
    - [x] **DOCS-04**: Query language guide: Q-objects and AND/OR composition
    ```
    Change to reflect Phase 13.1 Predicate system:
    ```
    - [x] **DOCS-04**: Query language guide: Predicate composition with `&`, `|`, `~` operators
    ```

    After edits, verify the markdown is well-formed (no broken list items).
  </action>
  <verify>
    <automated>grep -n "SUPERSEDED\|\.where()\|Predicate composition" /Users/paul/Documents/Dev/Personal/cubano/.planning/REQUIREMENTS.md</automated>
    <manual>Confirm all three edits are present: supersession comment in Codegen section, .where() in DOCS-03, "Predicate composition" in DOCS-04.</manual>
  </verify>
  <done>REQUIREMENTS.md has supersession annotation in Codegen section, DOCS-03 says .where(), DOCS-04 says "Predicate composition with &, |, ~".</done>
</task>

<task type="auto">
  <name>Task 3: Fix Phase 19 plan and summary frontmatter</name>
  <files>
    .planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-PLAN.md
    .planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-SUMMARY.md
  </files>
  <action>
    **In `19-01-PLAN.md`:**
    Find the frontmatter block at the top of the file. Find the line:
    ```yaml
    requirements:
      - DOCS-04
    ```
    Change to:
    ```yaml
    requirements: []
    ```
    DOCS-04 is Predicate composition (unrelated to Fact field documentation). Phase 19 has
    no formal REQUIREMENTS.md entry; it was an informally added phase.

    **In `19-01-SUMMARY.md`:**
    The summary file also has an incorrect `requirements-completed` field. Find the frontmatter
    block and locate the `requirements-completed` line (it may be `requirements-completed: [DOCS-04]`
    or similar). Change it to:
    ```yaml
    requirements-completed: []
    ```
    If the SUMMARY.md does not already have a `requirements-completed` field in its frontmatter,
    add `requirements-completed: []` as a new line after the last existing frontmatter field
    (before the closing `---`).
  </action>
  <verify>
    <automated>grep -n "requirements" /Users/paul/Documents/Dev/Personal/cubano/.planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-PLAN.md && grep -n "requirements" /Users/paul/Documents/Dev/Personal/cubano/.planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-SUMMARY.md</automated>
    <manual>Confirm 19-01-PLAN.md has requirements: [] and 19-01-SUMMARY.md has requirements-completed: []</manual>
  </verify>
  <done>19-01-PLAN.md has requirements: [], 19-01-SUMMARY.md has requirements-completed: [].</done>
</task>

</tasks>

<verification>
```bash
# No callers of deleted module
uv run basedpyright 2>&1 | grep -c "error" || echo "0 errors"

# All tests pass
uv run --extra dev pytest -q 2>&1 | tail -5

# REQUIREMENTS.md changes present
grep -c "SUPERSEDED\|\.where()\|Predicate composition" .planning/REQUIREMENTS.md

# Phase 19 frontmatter corrected
grep "requirements" .planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-PLAN.md
grep "requirements" .planning/phases/19-document-fact-field-type-for-databricks-and-snowflake-users/19-01-SUMMARY.md
```
</verification>

<success_criteria>
- `src/cubano/codegen/models.py` does not exist
- `uv run basedpyright` 0 errors (no dangling import references)
- `uv run --extra dev pytest` all pass
- REQUIREMENTS.md Codegen section has `<!-- SUPERSEDED: ... -->` annotation
- REQUIREMENTS.md DOCS-03 contains `.where()` (not `.filter()`)
- REQUIREMENTS.md DOCS-04 contains "Predicate composition" (not "Q-objects")
- `19-01-PLAN.md` has `requirements: []`
- `19-01-SUMMARY.md` has `requirements-completed: []`
</success_criteria>

<output>
After completion, create `.planning/phases/24-v02-tech-debt-cleanup/24-02-SUMMARY.md`
</output>

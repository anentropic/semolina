---
phase: 22-fix-codegen-md-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/how-to/codegen.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The backend table in codegen.md shows `SHOW COLUMNS IN VIEW` for Snowflake (not `SHOW COLUMNS IN SEMANTIC VIEW`)"
    - "The credentials paragraph in codegen.md shows `SNOWFLAKE_ACCOUNT` (not `CUBANO_SNOWFLAKE_ACCOUNT`)"
  artifacts:
    - path: "docs/src/how-to/codegen.md"
      provides: "Accurate codegen how-to guide"
      contains: "SHOW COLUMNS IN VIEW"
  key_links:
    - from: "docs/src/how-to/codegen.md"
      to: "src/cubano/engines/snowflake.py:322"
      via: "SQL string matching actual cur.execute() call"
      pattern: "SHOW COLUMNS IN VIEW"
    - from: "docs/src/how-to/codegen.md"
      to: "src/cubano/testing/credentials.py:51"
      via: "env_prefix matching SettingsConfigDict(env_prefix='SNOWFLAKE_')"
      pattern: "SNOWFLAKE_ACCOUNT"
---

<objective>
Fix two factual inaccuracies in `docs/src/how-to/codegen.md`:
1. The "Introspects via" column for Snowflake shows `SHOW COLUMNS IN SEMANTIC VIEW` — the actual SQL executed is `SHOW COLUMNS IN VIEW` (confirmed in `src/cubano/engines/snowflake.py:322`).
2. The credentials paragraph cites `CUBANO_SNOWFLAKE_ACCOUNT` — the actual env prefix is `SNOWFLAKE_` (confirmed in `src/cubano/testing/credentials.py:51`, `env_prefix="SNOWFLAKE_"`), making the correct var `SNOWFLAKE_ACCOUNT`.

Purpose: Accurate docs prevent users from debugging phantom SQL commands or looking for wrong environment variable names.
Output: `docs/src/how-to/codegen.md` with both strings corrected; docs build still passes.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix two accuracy errors in codegen.md</name>
  <files>docs/src/how-to/codegen.md</files>
  <action>
    Make exactly two targeted string replacements in `docs/src/how-to/codegen.md`:

    1. In the backend table (around line 40), change:
       `| \`snowflake\` | Snowflake Semantic Views | \`SHOW COLUMNS IN SEMANTIC VIEW\` |`
       to:
       `| \`snowflake\` | Snowflake Semantic Views | \`SHOW COLUMNS IN VIEW\` |`

    2. In the credentials paragraph (around line 44), change:
       `(for example, \`CUBANO_SNOWFLAKE_ACCOUNT\` for Snowflake).`
       to:
       `(for example, \`SNOWFLAKE_ACCOUNT\` for Snowflake).`

    Do NOT alter anything else in the file — no prose rewrites, no reformatting, no new sections.

    Verification of correctness:
    - SQL command: `src/cubano/engines/snowflake.py` line 322 runs `cur.execute(f"SHOW COLUMNS IN VIEW {qualified_name}")` — the word "SEMANTIC" is absent.
    - Env var: `src/cubano/testing/credentials.py` line 51 sets `env_prefix="SNOWFLAKE_"` — the prefix has no `CUBANO_` component, so `SNOWFLAKE_ACCOUNT` is the correct variable name.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -5</automated>
    <manual>Open docs/src/how-to/codegen.md and confirm line 40 reads `SHOW COLUMNS IN VIEW` and line 44 reads `SNOWFLAKE_ACCOUNT`.</manual>
  </verify>
  <done>
    - `SHOW COLUMNS IN SEMANTIC VIEW` does not appear anywhere in codegen.md
    - `CUBANO_SNOWFLAKE_ACCOUNT` does not appear anywhere in codegen.md
    - `SHOW COLUMNS IN VIEW` appears in the backend table row for `snowflake`
    - `SNOWFLAKE_ACCOUNT` appears in the credentials paragraph
    - `uv run mkdocs build --strict` exits 0
  </done>
</task>

</tasks>

<verification>
```bash
# Confirm neither stale string is present
grep -n "SHOW COLUMNS IN SEMANTIC VIEW\|CUBANO_SNOWFLAKE_ACCOUNT" docs/src/how-to/codegen.md
# Expected: no output (both removed)

# Confirm corrected strings are present
grep -n "SHOW COLUMNS IN VIEW\|SNOWFLAKE_ACCOUNT" docs/src/how-to/codegen.md
# Expected: two matching lines

# Full docs build passes
uv run mkdocs build --strict
```
</verification>

<success_criteria>
- `grep "SHOW COLUMNS IN SEMANTIC VIEW" docs/src/how-to/codegen.md` returns nothing
- `grep "CUBANO_SNOWFLAKE_ACCOUNT" docs/src/how-to/codegen.md` returns nothing
- `grep "SHOW COLUMNS IN VIEW" docs/src/how-to/codegen.md` returns the Snowflake table row
- `grep "SNOWFLAKE_ACCOUNT" docs/src/how-to/codegen.md` returns the credentials paragraph line
- `uv run mkdocs build --strict` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/22-fix-codegen-md-accuracy/22-01-SUMMARY.md` using the summary template.
</output>

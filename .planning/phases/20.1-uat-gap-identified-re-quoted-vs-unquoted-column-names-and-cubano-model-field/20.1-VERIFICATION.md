---
phase: 20.1-uat-gap-identified-re-quoted-vs-unquoted-column-names-and-cubano-model-field
verified: 2026-02-25T06:30:00Z
status: passed
score: 26/26 must-haves verified
re_verification:
  previous_status: passed
  previous_score: 22/22
  note: "Previous verification covered plans 01-03 only. Plans 04-05 were executed after the first verification. This re-verification covers all five plans."
  gaps_closed:
    - "Generated Python model code passes ruff isort (I001) — template reordered stdlib-first, format_with_ruff() runs two-pass ruff pipeline"
    - "Exit codes are presented in a formatted table in cubano codegen --help output — Rich paragraph-per-row with color markup"
    - "Metric[int](), Dimension[str](), Fact[float]() typed subscript syntax documented as the recommended API in models.md"
    - "Untyped Metric(), Dimension(), Fact() documented as valid shorthand in new 'Type the subscript' subsection"
  gaps_remaining: []
  regressions: []
gaps: []
human_verification: []
---

# Phase 20.1: UAT Gap Fixes Verification Report

**Phase Goal:** Fix UAT gaps from Phase 20 live testing: Pythonic lowercase field names from introspection, Generic[T] on Field for type-safe Metric[int]() codegen output, source= parameter for SQL column name overrides, and distinct CLI exit codes per error category.

**Verified:** 2026-02-25T06:30:00Z

**Status:** PASSED

**Re-verification:** Yes — after gap closure. Previous VERIFICATION.md (22/22) covered plans 01-03 only. Plans 04 and 05 were executed subsequently to close four additional UAT gaps. This report covers all five plans.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Field, Metric, Dimension, Fact are Generic[T] — Metric[int]() is a valid expression at runtime and statically | VERIFIED | `class Field(Generic[T])` in fields.py:78; `Metric[int]()` constructs without error; isinstance(Metric[int](), Metric) returns True |
| 2 | Field.__init__ accepts source: str | None = None — stored as self.source | VERIFIED | fields.py:117 — `def __init__(self, source: str | None = None):`; self.source set at line 135 |
| 3 | Field.__repr__ shows source= when set, omits it when None | VERIFIED | fields.py:252-254 — conditional source= in repr string |
| 4 | __hash__ is preserved on Field despite __eq__ override | VERIFIED | fields.py:378 — `__hash__ = object.__hash__` present |
| 5 | isinstance(Metric[int](), Metric) returns True — Generic does not break isinstance | VERIFIED | Live check confirmed True in plan 01 verification |
| 6 | SnowflakeDialect.normalize_identifier('order_id') returns 'ORDER_ID' | VERIFIED | sql.py:235 — `return name.upper()`; test_sql.py:868 confirms |
| 7 | DatabricksDialect.normalize_identifier('ORDER_ID') returns 'order_id' | VERIFIED | sql.py:298-311 — `return name.lower()`; test_sql.py:875 confirms |
| 8 | MockDialect.normalize_identifier passes through unchanged | VERIFIED | sql.py:372-388 — identity transform; test_sql.py:882-884 confirms |
| 9 | SQL builder SELECT uses normalize_identifier when field.source is None, uses field.source verbatim when set | VERIFIED | sql.py:437-455 — _resolve_col_name; used in _build_select_clause at lines 704 and 710 |
| 10 | SQL builder WHERE clause uses normalize_identifier on field_name from predicates | VERIFIED | sql.py:498-567 — every predicate branch applies dialect.normalize_identifier(f) before quoting |
| 11 | SnowflakeEngine.introspect() stores lowercased column name in IntrospectedField.name | VERIFIED | snowflake.py:344 — `python_name = original_col_name.lower()` |
| 12 | SnowflakeEngine.introspect() emits source_name only when normalize would not round-trip | VERIFIED | snowflake.py:345-348 — source_name set only when normalized_back != original_col_name |
| 13 | python_renderer emits Metric[int]() with correct type subscript | VERIFIED | python_model.py.jinja2:19 — `{{ field.field_class }}[{{ field.data_type }}]()`; test_python_renderer.py:29 confirms |
| 14 | python_renderer emits source='...' when IntrospectedField.source_name is set | VERIFIED | python_model.py.jinja2:16-17 — `{% if field.source_name %}...source="{{ field.source_name }}"...` |
| 15 | python_renderer emits from typing import Any when Any is used | VERIFIED | python_model.py.jinja2:4-6 — `{% if needs_any %} from typing import Any {% endif %}` |
| 16 | CubanoViewNotFoundError and CubanoConnectionError exist in cubano.engines.base | VERIFIED | base.py:19-24 — both classes defined, subclass RuntimeError |
| 17 | CLI exits with code 2 for invalid backend, 3 for view not found, 4 for connection error, 1 for unexpected error | VERIFIED | codegen.py:23-25 — constants defined; lines 97-114 — ordered exception catches with correct exit codes |
| 18 | Exit codes are documented in BOTH the --help text (as formatted list) and the codegen how-to guide | VERIFIED | __init__.py:19-29 — Rich paragraph-per-row epilog with color markup; codegen.md:159-172 — exit codes table |
| 19 | DatabricksEngine.introspect() raises CubanoViewNotFoundError for DatabaseError and CubanoConnectionError for OperationalError | VERIFIED | databricks.py:354-362 — split except blocks with typed exceptions |
| 20 | SnowflakeEngine.introspect() raises typed exceptions | VERIFIED | snowflake.py:366-374 — ProgrammingError → CubanoViewNotFoundError, DatabaseError → CubanoConnectionError |
| 21 | source= usage pattern documented in codegen how-to | VERIFIED | codegen.md:174-193 — "Override the SQL column name with source=" section with code example |
| 22 | Generated Python model code has stdlib imports before cubano import (isort I001 compliant) | VERIFIED | python_model.py.jinja2 lines 1-7: `{% if needs_datetime %} import datetime {% endif %}`, `{% if needs_any %} from typing import Any {% endif %}`, then `from cubano import ...` — stdlib-first ordering confirmed |
| 23 | format_with_ruff() runs two-pass ruff pipeline: ruff format then ruff check --fix --select I | VERIFIED | python_renderer.py:207-248 — first pass ruff format (lines 208-219), second pass ruff check --fix --select I (lines 224-248); independent fallback on each pass |
| 24 | Exit codes are presented one per line in cubano codegen --help with Rich color markup | VERIFIED | __init__.py:22-28 — `\n\n` between each exit code row; green/yellow/red color convention; Rich markup mode active |
| 25 | Metric[int](), Dimension[str](), Fact[float]() typed subscript syntax documented as the recommended API in models.md | VERIFIED | models.md:15-19 "Create a model" shows typed forms as primary examples; all subsection examples use typed syntax |
| 26 | Untyped Metric(), Dimension(), Fact() documented as valid shorthand with note on tradeoffs | VERIFIED | models.md:185-186 "Type the subscript" subsection: "`Metric()` is shorthand for `Metric[Any]()` — valid Python, just without type narrowing" |

**Score:** 26/26 truths verified

---

## Required Artifacts

### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/cubano/fields.py` | Generic[T] Field with source= parameter | VERIFIED | `class Field(Generic[T])` at line 78; `source: str | None = None` at line 117; `__hash__ = object.__hash__` at line 378 |
| `tests/unit/test_fields.py` | Tests for Generic behavior and source= param | VERIFIED | TestFieldGeneric, TestFieldSourceParam, TestFieldHashPreserved classes present |

### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/cubano/engines/sql.py` | normalize_identifier on Dialect + _resolve_col_name on SQLBuilder | VERIFIED | Abstract normalize_identifier at line 147; concrete impls on all 3 dialects; _resolve_col_name at line 437 |
| `src/cubano/codegen/introspector.py` | IntrospectedField with source_name field | VERIFIED | `source_name: str | None = None` at line 43 |
| `src/cubano/codegen/templates/python_model.py.jinja2` | Jinja2 template emitting Metric[T]() with optional source= | VERIFIED | `{{ field.field_class }}[{{ field.data_type }}]()` at line 19; source= branch at lines 16-17 |

### Plan 03 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/cubano/engines/base.py` | CubanoViewNotFoundError, CubanoConnectionError exception classes | VERIFIED | Both classes at lines 19-24 |
| `src/cubano/engines/databricks.py` | DatabricksEngine.introspect() raises typed exceptions | VERIFIED | Split except blocks with CubanoViewNotFoundError and CubanoConnectionError |
| `src/cubano/cli/codegen.py` | Distinct exit codes per error category | VERIFIED | Constants EXIT_INVALID_BACKEND=2, EXIT_VIEW_NOT_FOUND=3, EXIT_CONNECTION_ERROR=4 |
| `docs/src/how-to/codegen.md` | Exit codes table + source= section | VERIFIED | Exit codes table at lines 159-172; source= section at lines 174-193 |

### Plan 04 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/cubano/codegen/templates/python_model.py.jinja2` | Stdlib imports (datetime, typing) before cubano import | VERIFIED | Lines 1-7: needs_datetime block, needs_any block, then `from cubano import SemanticView, Metric, Dimension, Fact` — stdlib-first order confirmed |
| `src/cubano/codegen/python_renderer.py` | format_with_ruff() runs ruff check --fix --select I after ruff format | VERIFIED | Two subprocess.run calls at lines 208-248; second call uses `["uv", "run", "ruff", "check", "--fix", "--select", "I", ...]` |
| `src/cubano/cli/__init__.py` | Exit codes epilog formatted as one Rich paragraph per code row | VERIFIED | Lines 22-28: `\n\n` separator between each row; Rich color markup [green], [yellow], [red] per severity |
| `tests/unit/codegen/test_python_renderer.py` | Tests for isort pass and fallback behavior | VERIFIED | test_isort_pass_applied_after_format, test_isort_fallback_returns_formatted_on_failure present; existing tests updated for two subprocess calls |
| `tests/unit/codegen/test_cli.py` | Import assertion matches ruff-isort-sorted alphabetical order | VERIFIED | Line 183: `from cubano import Dimension, Fact, Metric, SemanticView` (alphabetical, isort-sorted) |

### Plan 05 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docs/src/how-to/models.md` | Typed subscript syntax as primary examples; "Type the subscript" subsection; codegen.md cross-reference | VERIFIED | Lines 15-19: typed forms in "Create a model"; line 167: "## Type the subscript" subsection; line 260: codegen.md cross-reference in See also |

---

## Key Link Verification

### Plan 01 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/cubano/fields.py` | `typing.Generic` | `class Field(Generic[T])` | WIRED | fields.py:13 imports Generic; line 78 uses it in class declaration |
| `src/cubano/fields.py` | `Field.__init__` | `source: str | None = None parameter` | WIRED | Confirmed at fields.py:117 and stored at line 135 |

### Plan 02 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/cubano/engines/sql.py` | `Field.source` | `_resolve_col_name: field.source if not None, else dialect.normalize_identifier(field.name)` | WIRED | _resolve_col_name at sql.py:437-455; used in SELECT (lines 704, 710) and ORDER BY (line 805) |
| `src/cubano/engines/snowflake.py` | `src/cubano/codegen/introspector.py` | `IntrospectedField(name=lowercased, source_name=original_if_needed)` | WIRED | snowflake.py:343-356 constructs IntrospectedField with python_name and source_name |
| `src/cubano/codegen/python_renderer.py` | `src/cubano/codegen/templates/python_model.py.jinja2` | `_FieldContext with data_type and source_name fields` | WIRED | _FieldContext has data_type and source_name; renderer passes needs_any flag; template uses all three |

### Plan 03 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/cubano/engines/snowflake.py` | `src/cubano/engines/base.py` | `raises CubanoViewNotFoundError when view not found` | WIRED | Import at line 13; raised at line 369 |
| `src/cubano/engines/databricks.py` | `src/cubano/engines/base.py` | `raises CubanoViewNotFoundError for DatabaseError; CubanoConnectionError for OperationalError` | WIRED | Import at line 13; raised at lines 357 and 362 |
| `src/cubano/cli/codegen.py` | `src/cubano/engines/base.py` | `except CubanoViewNotFoundError → raise typer.Exit(code=3)` | WIRED | codegen.py:93 imports both; lines 106-114 catch with correct exit codes |

### Plan 04 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `python_model.py.jinja2` | ruff isort I001 | stdlib imports (datetime, typing) appear before third-party (cubano) | WIRED | Template lines 1-7: conditional stdlib blocks then `from cubano import ...`; `import datetime` and `from typing import Any` both precede cubano import |
| `format_with_ruff()` | ruff check --fix --select I | second subprocess.run call after ruff format | WIRED | python_renderer.py:224-248 — second subprocess.run with `["uv", "run", "ruff", "check", "--fix", "--select", "I", ...]` |

### Plan 05 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `docs/src/how-to/models.md` | `codegen.md` | See also cross-reference (codegen output uses typed subscripts) | WIRED | models.md:260 — "Generating models from warehouse views (codegen.md) -- codegen output uses typed subscripts" |

---

## Requirements Coverage

No requirement IDs declared in any plan frontmatter (`requirements: []` in all five plans). No REQUIREMENTS.md entries map to this phase. No orphaned requirements to report.

---

## Anti-Patterns Found

None. Same findings as the previous verification report apply. The `TODO:` strings in source files are intentional design constructs (codegen emits TODO comments for unmapped warehouse types). No placeholder or stub patterns introduced by plans 04-05.

| File | Pattern | Severity | Verdict |
|------|---------|----------|---------|
| snowflake.py:333, databricks.py:337 | `"TODO: ..."` in data_type | Info | Intentional design — codegen emits TODO comments for unmapped warehouse types |
| sql.py (multiple) | `placeholder` | Info | SQL parameter placeholder terminology, not a code stub |

---

## Human Verification Required

None. All behaviors are testable programmatically and tests pass.

The following items could optionally be human-verified but are not blockers:

1. **Live Snowflake introspection with uppercase column**: Confirm `SHOW COLUMNS` result for a standard `ORDER_ID` column yields `IntrospectedField(name='order_id', source_name=None)` against a real warehouse.

2. **Live Snowflake introspection with quoted-lowercase column**: Confirm `"order_id"` (case-preserved) yields `IntrospectedField(name='order_id', source_name='order_id')`.

3. **`cubano codegen --help` terminal rendering**: Confirm the Rich color markup (green/yellow/red) renders in a color-capable terminal and each exit code appears on its own line.

---

## Quality Gates

| Gate | Result |
|------|--------|
| `uv run pytest tests/ -x -q` | 728 passed, 0 failed |
| `uv run basedpyright` | 0 errors, 0 warnings, 0 notes |
| `uv run ruff check` | All checks passed |
| `uv run ruff format --check` | 56 files already formatted |
| `uv run mkdocs build --strict` | Documentation built successfully |

All five quality gates pass.

---

## Gaps Summary

No gaps. All 26 observable truths are verified across all five plans, all artifacts are substantive and wired, all key links are connected, and all five quality gates pass.

The four UAT gaps found in `20.1-UAT.md` after plans 01-03:
- isort I001 in generated code — CLOSED by plan 04 (template reordered, two-pass format_with_ruff())
- CLI --help exit codes as formatted list — CLOSED by plan 04 (Rich paragraph-per-row with double-newline)
- Typed subscript syntax documentation — CLOSED by plan 05 (models.md updated with typed forms as primary examples and new "Type the subscript" subsection)
- Field.__init__ "multiple values for source" — Confirmed NOT A BUG (test authoring error; descriptor protocol design is correct; no code fix needed)

Phase 20.1 fully achieves its goal across all five plans.

---

_Verified: 2026-02-25T06:30:00Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 20.1-uat-gap-identified-re-quoted-vs-unquoted-column-names-and-cubano-model-field
plan: "05"
type: execute
wave: 4
depends_on: ["20.1-03"]
files_modified:
  - docs/src/how-to/models.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Metric[int](), Dimension[str](), Fact[float]() typed subscript syntax is documented as the recommended API"
    - "Untyped Metric(), Dimension(), Fact() is documented as valid shorthand (not wrong), with a note that typed subscripts give IDE autocomplete"
  artifacts:
    - path: "docs/src/how-to/models.md"
      provides: "Models how-to guide with typed subscript syntax in the primary examples"
      contains: "Metric[int]()"
  key_links:
    - from: "docs/src/how-to/models.md"
      to: "codegen.md"
      via: "See also / cross-reference (codegen output uses typed subscripts)"
      pattern: "codegen.md"
---

<objective>
Document `Metric[int]()`, `Dimension[str]()`, `Fact[float]()` typed subscript syntax
as the recommended way to declare fields in Cubano models. Currently `models.md` only
shows untyped `Metric()`, `Dimension()`, `Fact()` — users who see typed subscripts in
codegen output or tutorials will not know this is the standard API.

Purpose: Users reaching the models how-to after running `cubano codegen` will see
`Metric[int]()` in the generated output and need the guide to explain what the subscript
means and why to use it.

Output:
- `docs/src/how-to/models.md` updated with typed subscript syntax as primary examples
- Untyped forms documented as valid shorthand with explicit note on tradeoffs
- Docs build continues to pass `mkdocs build --strict`
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
@.claude/skills/cubano-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/src/how-to/models.md
@docs/src/how-to/codegen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed subscript syntax to models how-to guide</name>
  <files>
    docs/src/how-to/models.md
  </files>
  <action>
    Update `docs/src/how-to/models.md` to document the typed Generic subscript syntax.
    This is a targeted addition — do NOT rewrite existing content. Apply the Cubano
    docs-author skill: Diataxis how-to format, warm-but-efficient tone, second person.

    **Changes required:**

    1. **"Create a model" section** — Show typed syntax as the primary form. Replace
       the opening example:
       ```python
       # BEFORE (current):
       revenue = Metric()
       cost = Metric()
       country = Dimension()

       # AFTER:
       revenue = Metric[float]()
       cost = Metric[float]()
       country = Dimension[str]()
       unit_price = Fact[float]()
       ```
       Add a concise inline note (1-2 sentences) that the type parameter gives IDE
       autocomplete when you assign the field's value in application code. Do not
       explain Generic typing in detail — assume Python proficiency.

    2. **"Choose field types" table and subsections** — Update the `Metric`, `Dimension`,
       and `Fact` subsection examples to use typed syntax. Each subsection has a code
       block showing field declarations — update these. Keep SQL tabs unchanged (SQL
       output does not change).

    3. **Add a new subsection "Type the subscript"** between "Choose field types" and
       "Put it together". Keep it brief (max 8-10 lines of prose + code). Content:

       ```markdown
       ## Type the subscript

       Annotate each field with the Python type of the underlying warehouse column.
       The subscript is optional but recommended — it lets your IDE infer the type
       when you access query results:

       ```python
       revenue = Metric[float]()   # float column
       order_id = Metric[int]()    # integer column
       country = Dimension[str]()  # string column
       created_at = Dimension[datetime.date]()  # date column (requires import datetime)
       ```

       When the column type has no clean Python equivalent (GEOGRAPHY, VARIANT, ARRAY),
       use `Metric[Any]()` and import `Any` from `typing`. The `cubano codegen` command
       emits a `# TODO:` comment for these cases.

       You can also omit the subscript entirely. `Metric()` is shorthand for
       `Metric[Any]()` — valid Python, just without type narrowing.
       ```

    4. **"Put it together" section** — Update the final complete example to use typed
       syntax throughout:
       ```python
       class Orders(SemanticView, view="orders"):
           """Order-level metrics and dimensions."""

           total_revenue = Metric[float]()
           order_count = Metric[int]()
           country = Dimension[str]()
           product_category = Dimension[str]()
           unit_price = Fact[float]()  # raw price, not aggregated
       ```

    5. **"Access field descriptors" and "Model immutability" sections** — These show
       field access patterns, not declarations. Leave them unchanged.

    6. **"Add field docstrings for codegen" section** — The example shows `Metric()`
       without a subscript. Update to `Metric[float]()` for consistency.

    **Do NOT:**
    - Change any SQL tab content (SQL output is unaffected by subscripts)
    - Explain Generic typing internals
    - Add long warnings about untyped usage (it's valid, just less informative)
    - Rewrite sections not listed above

    **Humanizer pass** before saving: Read the changed sections aloud mentally.
    Every sentence should sound like something a senior engineer would write in a
    Stripe or FastAPI doc — not corporate, not over-hedged.
  </action>
  <verify>
    <automated>
      uv run mkdocs build --strict 2>&amp;&amp;
      uv run --extra dev pytest src/cubano/ --doctest-modules -q
    </automated>
    <manual>
      Open docs/src/how-to/models.md and verify:
      - "Create a model" example shows Metric[float](), Dimension[str]()
      - "Type the subscript" subsection exists and is clear
      - "Put it together" uses typed syntax throughout
    </manual>
  </verify>
  <done>
    - models.md "Create a model" example uses typed subscripts
    - "Type the subscript" subsection added and concise
    - "Put it together" complete example uses typed subscripts
    - mkdocs build --strict passes (0 errors)
    - Doctests still pass
  </done>
</task>

</tasks>

<verification>
```bash
uv run mkdocs build --strict
uv run --extra dev pytest src/cubano/ --doctest-modules -q
```

Confirm `Metric[int]()` appears in models.md and the new subsection is clear and brief.
</verification>

<success_criteria>
- `docs/src/how-to/models.md` shows `Metric[int]()` / `Dimension[str]()` / `Fact[float]()` as the primary example syntax
- "Type the subscript" subsection explains the syntax in under 10 prose lines
- Untyped `Metric()` documented as valid shorthand (not removed, not discouraged)
- `mkdocs build --strict` passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-uat-gap-identified-re-quoted-vs-unquoted-column-names-and-cubano-model-field/20.1-05-SUMMARY.md`
</output>

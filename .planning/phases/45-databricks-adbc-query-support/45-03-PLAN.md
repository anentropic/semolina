---
phase: 45-databricks-adbc-query-support
plan: 03
type: execute
wave: 2
depends_on: ["45-01", "45-02"]
files_modified:
  - tests/integration/cassettes/integration/test_queries
autonomous: false
requirements: [DBX-03]
user_setup:
  - service: databricks
    why: "Live cassette recording needs real Databricks credentials, a warm SQL Warehouse, and the Foundry ADBC driver — cannot run in CI"
    env_vars:
      - name: DATABRICKS_HOST
        source: "Databricks workspace (or [connections.databricks] in .semolina.toml)"
      - name: DATABRICKS_HTTP_PATH
        source: "Databricks SQL Warehouse -> Connection details"
      - name: DATABRICKS_TOKEN
        source: "Databricks -> Settings -> Developer -> Access tokens"
    dashboard_config:
      - task: "Start the SQL Warehouse and let any paused Free-Edition workload resume (first compute can take ~20min — connect is instant once warm)"
        location: "Databricks SQL Warehouses"
      - task: "Install the Foundry-distributed Databricks ADBC driver into the venv (absent from PyPI)"
        location: "ADBC Driver Foundry"
must_haves:
  truths:
    - "All 7 Databricks integration tests replay GREEN from committed cassettes with no live warehouse"
    - "The recorded test_filtered_by_dimension Databricks cassette shows the inlined literal `'US'` and an empty params file"
    - "The 7 Snowflake cassettes still replay GREEN (no regression from the dialect change)"
  artifacts:
    - path: "tests/integration/cassettes/integration/test_queries"
      provides: "7 recorded *_databricks_engine_/adbc_driver_manager.dbapi/ cassette dirs (000_query.sql, 000_params.json, 000_result.arrow)"
      contains: "databricks_engine"
  key_links:
    - from: "tests/integration/test_queries.py::test_filtered_by_dimension (databricks_engine)"
      to: "recorded cassette 000_query.sql + 000_params.json"
      via: "pytest-adbc-replay matches driver-received SQL"
      pattern: "databricks_engine"
---

<objective>
Record and commit the 7 Databricks integration cassettes so `pytest tests/integration
-k databricks` replays GREEN in CI (DBX-03). Only Snowflake cassettes exist today; the
7 Databricks `test_queries.py` tests are known-failing `CassetteMissError`. With the
dialect fix (Plan 01) and the poolhouse DSN fix (Plan 02) landed, recording over a live
warehouse will now capture inline-literal Databricks SQL against a resolvable view.

Purpose: Prove both blocker fixes end-to-end over real ADBC and give CI a permanent,
credential-free Databricks replay alongside Snowflake.

Output: 7 `*_databricks_engine_/adbc_driver_manager.dbapi/` cassette dirs committed
under `tests/integration/cassettes/integration/test_queries/`, with the WHERE cassette
showing inlined `'US'` + empty params.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-databricks-adbc-query-support/45-RESEARCH.md
@.planning/phases/45-databricks-adbc-query-support/45-PATTERNS.md
@.planning/phases/45-databricks-adbc-query-support/45-VALIDATION.md
@.planning/phases/45-databricks-adbc-query-support/45-01-SUMMARY.md
@.planning/phases/45-databricks-adbc-query-support/45-02-SUMMARY.md

<read_first>
- tests/integration/conftest.py:239-354 — `databricks_engine` fixture: record branch (:263-338, creates the temp schema + sales_data table + sales_view metric view, registers an engine with the schema, drops on teardown) and replay branch (:339-354). Recording is already fully wired.
- tests/integration/conftest.py:357-371 — `backend_engine` parametrized fixture (`snowflake_engine`, `databricks_engine`); per-backend cassette dirs from node id
- tests/integration/test_queries.py:49-60 — the shared unqualified `Sales(view="sales_view")` model (MUST stay unqualified — the DSN schema now resolves it)
- tests/integration/test_queries.py:170-186 — `test_filtered_by_dimension` (the WHERE test that exercises the bind-param blocker; expects `[(1500,150,"US")]`)
- tests/integration/cassettes/integration/test_queries/test_filtered_by_dimension_snowflake_engine_/ — the Snowflake cassette layout to replicate (driver subdir, 000_query.sql / 000_params.json / 000_result.arrow); Databricks records under driver subdir `adbc_driver_manager.dbapi`
- scripts/spike_databricks_adbc_introspect.py + diagnose_databricks.py (project root) — offline diagnostics to re-verify the warehouse/driver before recording
</read_first>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 (checkpoint:human-action): Record the 7 Databricks cassettes over a live warehouse</name>
  <action>Pause for the operator to run the live cassette recording: with Databricks creds + a warm SQL Warehouse + the Foundry ADBC driver present, and after confirming the Plan 02 DSN fix is consumed, run `uv run pytest --adbc-record=once tests/integration -k databricks`. This cannot run in CI; it produces the 7 cassette dirs Task 1 then verifies and commits.</action>
  <what-built>
    Plan 01 made Databricks `.where()` inline literals; Plan 02 made the poolhouse DSN
    carry catalog/schema. The `databricks_engine` record fixture (conftest :263-338) is
    already wired to build the temp schema + metric view and record. This step runs the
    live recording — it needs the operator's credentials, a warm SQL Warehouse, and the
    Foundry ADBC driver, none of which exist in CI.
  </what-built>
  <how-to-verify>
    1. Confirm `[connections.databricks]` in `.semolina.toml` (or `DATABRICKS_HOST/HTTP_PATH/TOKEN` env) is set, and the Foundry Databricks ADBC driver is installed in the venv.
    2. Verify Plan 02 landed: `uv run python -c "from adbc_poolhouse import DatabricksConfig; from pydantic import SecretStr; print(DatabricksConfig(host='h',http_path='/p',token=SecretStr('t'),catalog='main',schema='s').to_adbc_kwargs()['uri'])"` ends with `?catalog=main&schema=s`. (RESEARCH Pitfall 3: recording before the poolhouse fix lands captures `TABLE_OR_VIEW_NOT_FOUND`.)
    3. Start the SQL Warehouse; if Free-Edition, wait for the paused workload to resume (~20min first compute; connect is instant once warm). Optionally run `python diagnose_databricks.py` to re-verify.
    4. Record: `uv run pytest --adbc-record=once tests/integration -k databricks`. The fixture creates the temp schema/view, records, and drops the schema on teardown.
    5. Confirm 7 `*_databricks_engine_/adbc_driver_manager.dbapi/` cassette dirs now exist with `000_query.sql`, `000_params.json`, `000_result.arrow`.
  </how-to-verify>
  <resume-signal>Type "recorded" once the 7 Databricks cassettes exist on disk, or describe the failure (e.g. warehouse cold, driver missing, view not found)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Verify recorded cassette content, replay green, and commit cassettes</name>
  <files>tests/integration/cassettes/integration/test_queries</files>
  <action>
    Implements DBX-03. After the operator records (previous step), assert the recorded
    content is correct, replay it offline, and commit. Verify the
    `test_filtered_by_dimension_databricks_engine_` cassette's `000_query.sql` shows the
    literal-inlined form (`` `country` = 'US' ``, `MEASURE(\`revenue\`)`, backtick
    lowercase names) and its `000_params.json` is `[]` (proves Plan 01 end-to-end). The
    other 6 tests have no WHERE so their params were already `[]`. Then run the offline
    replay (no warehouse) for both backends to prove no regression, and commit the 7 new
    cassette dirs. Do NOT change `Sales(view="sales_view")` or qualify it per-backend.
  </action>
  <verify>
    <automated>uv run pytest tests/integration -k "databricks or snowflake"</automated>
  </verify>
  <acceptance_criteria>
    - artifact: 7 `*_databricks_engine_/adbc_driver_manager.dbapi/` cassette dirs exist under tests/integration/cassettes/integration/test_queries/, each with 000_query.sql + 000_params.json + 000_result.arrow
    - source: `test_filtered_by_dimension_databricks_engine_/.../000_query.sql` contains `` `country` = 'US' `` and `MEASURE(` and no `?`; its `000_params.json` is `[]`
    - behavior: `uv run pytest tests/integration -k databricks` is 7/7 green on replay with no live warehouse
    - behavior: `uv run pytest tests/integration -k snowflake` still 7/7 green (no regression from the dialect change)
    - command: `uv run pytest tests/integration -k "databricks or snowflake"` passes (14/14)
  </acceptance_criteria>
  <done>The 7 Databricks cassettes are recorded, content-verified (inlined WHERE literal + empty params), replay 7/7 green offline alongside the 7 Snowflake cassettes, and are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Databricks creds -> recorded cassettes | Recording connects with a real token and writes result artifacts to disk that get committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Information Disclosure | token / live creds during recording | mitigate | Recording uses `warehouse_config("databricks")` (token stays SecretStr); the cassette stores driver-received SQL + arrow results, not the DSN/token. Confirm no `000_*` file embeds the token before committing. |
| T-45-03b | Tampering (supply chain) | Foundry ADBC driver install | accept | Operator-installed from the ADBC Driver Foundry (not PyPI); used only at record time, not on the replay/CI path. No new PyPI package added this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `uv run pytest tests/integration -k databricks` — 7/7 green on offline replay.
- `uv run pytest tests/integration -k snowflake` — 7/7 green (no regression).
- `uv run pytest tests/integration` — all backends green (phase gate before `/gsd-verify-work`).
- The WHERE cassette `000_query.sql` shows inlined `'US'` + `MEASURE()`; `000_params.json` == `[]`.
- No cassette file embeds the token.
</verification>

<success_criteria>
- DBX-03: all 7 Databricks tests replay GREEN from committed cassettes with no live warehouse.
- The recorded WHERE SQL proves Plan 01 (inline literal + empty params) and Plan 02 (resolvable unqualified view) end-to-end.
- Snowflake cassettes unchanged and still green.
</success_criteria>

<output>
Create `.planning/phases/45-databricks-adbc-query-support/45-03-SUMMARY.md` when done.
</output>

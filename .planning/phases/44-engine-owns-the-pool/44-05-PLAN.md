---
phase: 44-engine-owns-the-pool
plan: 05
type: execute
wave: 4
depends_on: ["44-03"]
files_modified:
  - tests/integration/conftest.py
  - tests/integration/test_queries.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Integration fixtures build Engines via create_engine and register them via register('name', engine) — no bare (pool, dialect) tuples"
    - "Recorded Snowflake cassettes replay GREEN after the redesign (recorded SQL byte-identical; only connection plumbing moved)"
    - "Record-mode DDL setup still works (native connectors retained for DDL only, not the library path)"
  artifacts:
    - path: "tests/integration/conftest.py"
      provides: "create_engine + register(engine) fixtures; native DDL setup preserved for record mode"
      contains: "create_engine"
  key_links:
    - from: "tests/integration/conftest.py"
      to: "semolina.create_engine + register"
      via: "fixture builds Engine then register('name', engine)"
      pattern: "create_engine\\("
    - from: "tests/integration/test_queries.py"
      to: "recorded cassettes"
      via: "pytest-adbc-replay matches on the unchanged generated SQL"
      pattern: "fetchall_rows|execute"
---

<objective>
Migrate the integration test fixtures to the Engine API and PROVE the recorded Snowflake
cassettes still replay green after the redesign. This is the explicit cassette-stays-green gate
(RESEARCH Pitfall 1 / A3): the generated query SQL is byte-identical post-refactor (only the
connection plumbing moved), so pytest-adbc-replay — which matches on the SQL string the driver
receives — must still match. If anything drifted in Plans 02–03, this plan surfaces it.

Purpose: The integration cassettes are the only persisted artifact that could break from this
refactor (RESEARCH Runtime State Inventory). They are keyed by test node + recorded SQL, both
unchanged — but that must be VERIFIED by running replay, not assumed.
Output: integration conftest fixtures on the Engine API (native connectors kept ONLY for
record-mode DDL setup per RESEARCH Open Q1), and a green `pytest tests/integration` replay run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-engine-owns-the-pool/44-CONTEXT.md
@.planning/phases/44-engine-owns-the-pool/44-RESEARCH.md
@.planning/phases/44-engine-owns-the-pool/44-PATTERNS.md
@.planning/phases/44-engine-owns-the-pool/44-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan produces no new public symbols. It migrates the integration test fixtures to the
phase-wide artifacts (`create_engine`, `register("name", engine)`) and adds the cassette-replay
verification gate.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate integration fixtures to create_engine + register(engine), keep native DDL</name>
  <files>tests/integration/conftest.py</files>
  <read_first>
    - tests/integration/conftest.py:82-177 (the fixtures building create_pool + register(pool, dialect=...) at 144-145, 172-173) and the record-mode DDL setup using native snowflake.connector / databricks.sql (113-138, 224-255) + the *_connect_kwargs usage around 91,102,209,221
    - src/semolina/config.py (create_engine + warehouse_config from Plans 02-03)
    - src/semolina/registry.py (register(engine) + get_engine + reset close_pool from Plan 02)
    - 44-PATTERNS.md "tests/integration/conftest.py" row + "Registry/fixture migration" shared pattern + 44-RESEARCH.md Open Q1 (keep native DDL, smallest blast radius)
  </read_first>
  <action>
    Replace the fixture pool construction (`create_pool(config)` + `register(name, pool,
    dialect=...)` at conftest.py ~144-145,172-173) with `engine = create_engine(config)` followed
    by `register("name", engine)` (2-arg). Use `reset()`-based teardown (now closes pools through
    the engine, Plan 02). KEEP the record-mode DDL setup on the native connectors
    (`snowflake.connector` / `databricks.sql`) — DDL is not part of the library path and only runs
    when recording (RESEARCH Open Q1). If the DDL setup imported `snowflake_connect_kwargs` /
    `databricks_connect_kwargs` (deleted in Plan 03), replace those calls with direct native
    `connect(**...)` kwargs assembled inline from `warehouse_config(backend)` (the native
    connectors stay; only the deleted helper goes). Leave the recorded cassettes and the
    `adbc_*`/`adbc_scrub_keys` pytest config untouched — do NOT re-record. The Databricks fixtures
    remain gated by the known recording hang (do not attempt live Databricks recording).
  </action>
  <verify>
    <automated>grep -q 'create_engine' tests/integration/conftest.py && grep -c 'register(.*pool.*dialect=' tests/integration/conftest.py | grep -qx 0 && echo OK-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/conftest.py` builds engines via `create_engine(` and registers via 2-arg `register("` (grep both)
    - `grep -c 'register(.*pool.*dialect=' tests/integration/conftest.py` == 0 (no 3-arg register left)
    - `grep -c 'snowflake_connect_kwargs\|databricks_connect_kwargs' tests/integration/conftest.py` == 0 (deleted helpers no longer imported)
    - the record-mode DDL setup still references the native connectors (`snowflake.connector` / `databricks.sql`) — these are intentionally retained (grep present)
    - recorded cassette files under `tests/integration/cassettes/` are unchanged (no new/modified cassette in the diff)
  </acceptance_criteria>
  <done>Integration fixtures use create_engine + register(engine); deleted *_connect_kwargs replaced with inline native kwargs for DDL only; cassettes untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the cassette replay gate and confirm green</name>
  <files>tests/integration/test_queries.py</files>
  <read_first>
    - tests/integration/test_queries.py (the replay-mode query tests that match recorded SQL)
    - pyproject.toml `[tool.pytest.ini_options]` adbc_* keys (adbc_cassette_dir, adbc_scrub_keys, replay default mode)
    - 44-RESEARCH.md Pitfall 1 (cassette drift) + A3 (replay-green assumption to verify) + Runtime State Inventory (cassettes keyed by node + recorded SQL)
  </read_first>
  <action>
    Run `pytest tests/integration` in the default replay mode and confirm every Snowflake query
    test passes against the existing cassettes — this PROVES the generated SQL is byte-identical
    post-refactor (the cassette-stays-green gate, RESEARCH Pitfall 1). If any test fails with a
    cassette mismatch, the connection refactor leaked into the SQL builder output (a regression
    introduced in Plan 02/03) — STOP and report the exact SQL diff; do NOT re-record to paper over
    it (re-recording needs live Snowflake creds that are absent, A3). The fix in that case is to
    restore the builder output, not to regenerate cassettes. Databricks integration tests stay
    skipped/gated by the recording hang (do not attempt them live). Record the replay result in
    the SUMMARY.
  </action>
  <verify>
    <automated>pytest tests/integration -k snowflake 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration -k snowflake` exits 0 with no cassette-mismatch failures
    - No cassette files were added or modified during the run (replay mode, not record)
    - If any mismatch occurred, the SUMMARY documents the SQL diff and the regression is fixed (not masked by re-recording)
    - `just test && pytest tests/integration` (the wave-merge sampling command from 44-VALIDATION.md) is green end-to-end
  </acceptance_criteria>
  <done>Snowflake integration cassettes replay green against the refactored connection path, proving the generated SQL is unchanged; no re-recording performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| record-mode DDL → live warehouse | Native connectors hold real creds during recording; replay uses scrubbed placeholders |
| recorded cassettes → repo | Cassettes persist generated SQL + Arrow results; must contain no live secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08 | Information Disclosure | secrets leaking into (re)recorded cassettes | mitigate | This plan runs replay only (no re-record); `adbc_scrub_keys` + placeholder-cred replay config already scrub keys (unchanged) — verify no new cassette is written |
| T-44-09 | Information Disclosure | inline native DDL kwargs assembled from warehouse_config | mitigate | DDL kwargs read SecretStr-wrapped values via the existing config; secrets are only used to connect, never logged/committed |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs; phase adds zero new external packages (44-RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest tests/integration -k snowflake` GREEN in replay mode; no cassette files modified.
- `tests/integration/conftest.py` uses create_engine + 2-arg register; no deleted *_connect_kwargs imports; native DDL retained.
- `just test && pytest tests/integration` green end-to-end.
- `prek run --all-files` passes on the edited integration files.
</verification>

<success_criteria>
- Integration fixtures fully migrated to the Engine API (D2/D4) with record-mode DDL preserved on native connectors (RESEARCH Open Q1).
- The cassette-stays-green gate passes: recorded Snowflake SQL replays identically through the refactored connection path (RESEARCH Pitfall 1 / A3) — proving the redesign did not touch SQL builder output.
- No cassette re-recording performed; Databricks recording remains gated by the known hang.
</success_criteria>

<output>
Create `.planning/phases/44-engine-owns-the-pool/44-05-SUMMARY.md` when done (record the replay result).
</output>

---
phase: 44-engine-owns-the-pool
plan: 04
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - src/semolina/engines/databricks.py
  - tests/unit/test_databricks_engine.py
  - scripts/spike_databricks_adbc_introspect.py
autonomous: false
requirements: []
user_setup:
  - service: databricks
    why: "Live Databricks ADBC introspection cannot be validated without the Foundry-distributed ADBC driver (not on PyPI) and a working warehouse; the recording also hangs on cold start"
    env_vars:
      - name: DATABRICKS_HOST
        source: "Databricks workspace → SQL Warehouse connection details"
      - name: DATABRICKS_HTTP_PATH
        source: "Databricks workspace → SQL Warehouse connection details"
      - name: DATABRICKS_TOKEN
        source: "Databricks workspace → User Settings → Developer → Access tokens"
    dashboard_config:
      - task: "Install the Foundry Databricks ADBC driver into the dev/CI venv"
        location: "ADBC Driver Foundry (Columnar) — not PyPI"
      - task: "Have a SQL Warehouse running with at least one metric view to introspect"
        location: "Databricks workspace → SQL Warehouses"
must_haves:
  truths:
    - "Either: Databricks introspection runs DESCRIBE TABLE EXTENDED ... AS JSON over the engine's ADBC pool (validated by spike); OR Databricks introspection raises a clearly-marked NotImplementedError with a TODO (documented fallback)"
    - "The Snowflake+DuckDB critical path (Plan 03) is never blocked by the Databricks driver/credential gate"
  artifacts:
    - path: "scripts/spike_databricks_adbc_introspect.py"
      provides: "Standalone (non-pytest) spike validating DESCRIBE TABLE EXTENDED AS JSON over ADBC vs native"
    - path: "src/semolina/engines/databricks.py"
      provides: "ADBC-pool introspection OR a marked NotImplementedError fallback"
  key_links:
    - from: "src/semolina/engines/databricks.py::introspect"
      to: "self.connect() ADBC cursor (or NotImplementedError)"
      via: "DESCRIBE TABLE EXTENDED {view} AS JSON over the owned pool"
      pattern: "DESCRIBE TABLE EXTENDED|NotImplementedError"
---

<objective>
Resolve the one genuinely-unvalidated branch of the redesign: Databricks metric-view
introspection over ADBC (D3). This is gated behind a live spike because the Foundry Databricks
ADBC driver is NOT on PyPI / not installed and the recording hangs (RESEARCH Pitfall 5, A2;
STATE blocker). The plan delivers ONE of two outcomes — validated ADBC introspection, or a
clearly-marked `NotImplementedError`/TODO fallback — without ever blocking the Snowflake+DuckDB
critical path (Plan 03 runs in parallel and ships independently).

Purpose: Databricks is "just SQL" (DESCRIBE TABLE EXTENDED ... AS JSON), so it very likely works
over ADBC, but it has never been run that way. The human checkpoint decides driver-availability
and validates the live result; the executor then either wires the real path or installs the
fallback.
Output: a standalone spike script, the Databricks introspect() rewired onto ADBC (or a marked
TODO fallback), and the Databricks unit test driven by an ADBC-cursor mock.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-engine-owns-the-pool/44-CONTEXT.md
@.planning/phases/44-engine-owns-the-pool/44-RESEARCH.md
@.planning/phases/44-engine-owns-the-pool/44-PATTERNS.md
@.planning/phases/44-engine-owns-the-pool/44-VALIDATION.md
</context>

<artifacts_this_phase_produces>
- `scripts/spike_databricks_adbc_introspect.py` — standalone script (NOT pytest) running
  `DESCRIBE TABLE EXTENDED {view} AS JSON` over both native `databricks.sql` and an ADBC pool,
  comparing the single-cell JSON output. Mirrors the validated Snowflake spike shape.
- `DatabricksEngine.introspect()` — EITHER rewired onto `self.connect()` (ADBC) with the JSON
  parser unchanged and error translation to `adbc_driver_manager.Error`; OR a marked
  `NotImplementedError("Databricks ADBC introspection pending Foundry driver — see Phase 44")`.
- `tests/unit/test_databricks_engine.py` — ADBC-cursor-seam mock feeding the JSON the parser
  expects (or a test asserting the NotImplementedError fallback if descoped).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the standalone Databricks ADBC-introspection spike script</name>
  <files>scripts/spike_databricks_adbc_introspect.py</files>
  <read_first>
    - src/semolina/engines/databricks.py (full — __init__ 106; introspect with native databricks.sql.connect 326-330, DESCRIBE TABLE EXTENDED ... AS JSON 330-332, JSON parser 333-360, error catch 362-375)
    - 44-CONTEXT.md <specifics> (the validated Snowflake spike at /tmp/claude/spike_adbc_introspect.py — mirror its compare-native-vs-ADBC structure)
    - 44-RESEARCH.md "Databricks introspection command (the SPIKE target)" + Pitfall 5 (Foundry driver not installed) + Open Q3 (decouple introspection validation from recording)
    - src/semolina/config.py (warehouse_config("databricks") → DatabricksConfig; create_engine from Plan 02)
  </read_first>
  <action>
    Create `scripts/spike_databricks_adbc_introspect.py` as a standalone runnable script (argparse
    or hard-coded view name; reads creds from `.semolina.toml` / `DATABRICKS_*` env via
    `warehouse_config("databricks")`). It must: (a) build an ADBC pool via
    `create_engine(warehouse_config("databricks"))` and run
    `DESCRIBE TABLE EXTENDED {view} AS JSON` over `engine.connect()`; (b) run the same SQL via the
    native `databricks.sql.connect` path for comparison; (c) parse both single-cell JSON results
    and assert structural equality (same columns, `is_measure` flags, comments). The script must
    FAIL FAST with a clear message if the Foundry ADBC driver is absent (catch the
    `create_pool` ImportError / "ADBC driver 'databricks' not found") so the human checkpoint can
    read it. Do NOT use pytest-adbc-replay (no recording — decouple from the hang, Open Q3).
    Keep it import-light and clearly commented as a one-off validation tool. No `# type: ignore`.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/spike_databricks_adbc_introspect.py').read()); print('OK-parses')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike_databricks_adbc_introspect.py` exists and parses as valid Python
    - It references `DESCRIBE TABLE EXTENDED` and `AS JSON` (grep both)
    - It builds the ADBC path via `create_engine` and a native comparison via `databricks.sql` (grep both)
    - It handles the missing-Foundry-driver ImportError with a human-readable message (grep `ADBC driver` or `ImportError` handling)
    - It does NOT import pytest or pytest_adbc_replay (grep count 0)
  </acceptance_criteria>
  <done>A standalone, runnable Databricks ADBC-introspection spike exists that compares ADBC vs native DESCRIBE TABLE EXTENDED AS JSON and fails fast when the Foundry driver is missing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human checkpoint — acquire the Foundry driver and validate (or descope)</name>
  <what-built>
    A standalone Databricks ADBC-introspection spike script
    (`scripts/spike_databricks_adbc_introspect.py`) that compares `DESCRIBE TABLE EXTENDED ... AS
    JSON` over an ADBC pool against the native `databricks.sql` path. It fails fast if the
    Foundry-distributed Databricks ADBC driver is not installed.
  </what-built>
  <how-to-verify>
    This step requires a human because the Foundry Databricks ADBC driver is not on PyPI and the
    live warehouse credentials + a running SQL Warehouse are not available in the agent session
    (RESEARCH Pitfall 5 / A2; STATE blocker: recording hangs on cold start).

    1. Decide whether the Foundry Databricks ADBC driver can be installed in this dev/CI venv now.
       - If NOT available: reply "fallback" — the executor will install the marked
         `NotImplementedError`/TODO in `DatabricksEngine.introspect()` (Task 3 path B) and ship
         Snowflake+DuckDB on the new API. This is an explicitly-approved descope (RESEARCH
         Environment Availability fallback).
    2. If the driver IS available: install it, ensure a SQL Warehouse is running with at least one
       metric view, set `DATABRICKS_HOST` / `DATABRICKS_HTTP_PATH` / `DATABRICKS_TOKEN` (or
       `[connections.databricks]` in `.semolina.toml`), then run:
       `python scripts/spike_databricks_adbc_introspect.py <schema.metric_view>`
    3. Confirm the script prints that the ADBC and native JSON results are structurally identical
       (same columns, `is_measure` flags, comments). Reply "validated" and paste the comparison
       summary, OR reply "fallback" if it cannot be run.
  </how-to-verify>
  <resume-signal>Reply "validated" (+ paste the ADBC-vs-native comparison) to wire the real ADBC path, or "fallback" to install the NotImplementedError/TODO descope.</resume-signal>
  <action>HUMAN GATE — no autonomous action. The executor pauses here; the operator either installs the Foundry Databricks ADBC driver and runs the spike to validate ("validated"), or approves the descope ("fallback"). The executor proceeds to Task 3 on the chosen branch.</action>
  <verify>
    <human-check>Operator replies "validated" (with the ADBC-vs-native comparison summary) or "fallback".</human-check>
  </verify>
  <done>Operator has decided Path A (validated ADBC) or Path B (descope); the decision and any comparison output are recorded for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Databricks introspect() to ADBC (if validated) or install the marked fallback</name>
  <files>src/semolina/engines/databricks.py, tests/unit/test_databricks_engine.py</files>
  <read_first>
    - src/semolina/engines/databricks.py (full — introspect native path 326-375 to swap or descope)
    - tests/unit/test_databricks_engine.py (the Plan 01 RED ADBC-cursor-seam mock)
    - src/semolina/engines/snowflake.py (Plan 03 ADBC introspect rewrite — the sibling pattern to mirror if validated)
    - 44-PATTERNS.md "engines/databricks.py::introspect()" (SPIKE-GATED) + "Error translation" shared pattern
  </read_first>
  <action>
    Branch on the checkpoint outcome.
    PATH A ("validated"): mirror the Snowflake ADBC introspect rewrite (Plan 03) —
    `DatabricksEngine` receives pool+dialect via the base `__init__`; rewrite `introspect()` to
    `with self.connect() as conn: cur = conn.cursor()` running
    `DESCRIBE TABLE EXTENDED {view_name} AS JSON` (SQL + JSON parser unchanged); retarget the
    `except` from `databricks.sql.exc.*` to `adbc_driver_manager.Error`, still raising
    `SemolinaViewNotFoundError`/`SemolinaConnectionError`; remove the native `databricks.sql`
    import from the introspect path. Update `test_databricks_engine.py` so its ADBC-cursor mock
    feeds the JSON the parser expects, and assert the parsed `IntrospectedView`.
    PATH B ("fallback"): make `DatabricksEngine.introspect()` raise
    `NotImplementedError("Databricks ADBC introspection pending the Foundry ADBC driver — see
    Phase 44 / 44-RESEARCH.md")` with a clear TODO comment; keep the construction wired to
    pool+dialect (so `create_engine`/execution still works for Databricks). Change
    `test_databricks_engine.py` to assert the `NotImplementedError` (and remove the now-irrelevant
    introspection-result assertions). Record the chosen path in the SUMMARY. No `# type: ignore`.
  </action>
  <verify>
    <automated>pytest tests/unit/test_databricks_engine.py 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - PATH A: `databricks.py` introspect uses `self.connect()`, contains `DESCRIBE TABLE EXTENDED` + `AS JSON`, no `databricks.sql.connect(` in the introspect path, catches `adbc_driver_manager.Error`; test_databricks_engine.py asserts a parsed `IntrospectedView`
    - PATH B: `databricks.py` introspect raises `NotImplementedError` with a Phase-44 TODO; test_databricks_engine.py asserts `pytest.raises(NotImplementedError)`
    - In EITHER path `pytest tests/unit/test_databricks_engine.py` exits 0
    - `just test` passes
    - The SUMMARY states which path (A/B) was taken and why
  </acceptance_criteria>
  <done>Databricks introspection is either validated ADBC or a clearly-marked NotImplementedError fallback; the Databricks unit test is GREEN for whichever path was chosen; the choice is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .semolina.toml / DATABRICKS_* env → spike script | Databricks PAT (SecretStr) crosses into the spike/config here |
| view name → DESCRIBE SQL | Metric-view identifier flows into `DESCRIBE TABLE EXTENDED {view} AS JSON` via f-string (pre-existing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Information Disclosure | spike script printing config/results | mitigate | The spike must never print the PAT/token; build the config via `warehouse_config` (SecretStr stays wrapped) and print only the JSON column metadata being compared |
| T-44-07 | Tampering | identifier injection in DESCRIBE SQL | accept | Pre-existing f-string pattern carried over unchanged; redesign does not widen the surface (V5) |
| T-44-SC | Tampering | Foundry ADBC driver acquisition (out-of-band, not PyPI) | mitigate | Driver install is a human-checkpoint step (blocking-human); the executor never silently installs an unverified package — driver source confirmed by the operator before use |
</threat_model>

<verification>
- `pytest tests/unit/test_databricks_engine.py` GREEN for the chosen path (A or B).
- `scripts/spike_databricks_adbc_introspect.py` parses and is runnable; it fails fast with a clear message when the Foundry driver is absent.
- `just test` passes (the Databricks branch does not regress the Snowflake+DuckDB suite).
- `prek run --all-files` passes (ruff + basedpyright strict, no new `# type: ignore`).
- The Snowflake+DuckDB critical path (Plan 03) is unaffected regardless of the path chosen here.
</verification>

<success_criteria>
- Databricks introspection over ADBC is either validated and wired (Path A) or shipped as a clearly-marked NotImplementedError/TODO (Path B) — both satisfy D3 given the documented Foundry-driver constraint.
- The decision is gated behind a blocking human checkpoint (driver availability + live validation) and never blocks the Snowflake+DuckDB ship.
- The standalone spike exists for future re-validation when the driver + warehouse are available.
</success_criteria>

<output>
Create `.planning/phases/44-engine-owns-the-pool/44-04-SUMMARY.md` when done (state Path A or B).
</output>

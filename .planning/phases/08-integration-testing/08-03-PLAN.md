---
phase: 08-integration-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - cubano-jaffle-shop/tests/conftest.py
  - cubano-jaffle-shop/tests/test_mock_queries.py
  - cubano-jaffle-shop/tests/fixtures/mock_data.py
autonomous: true
requirements:
  - INT-02
  - INT-03

must_haves:
  truths:
    - "Mock tests validate query builder logic (metrics, dimensions, filters) without warehouse"
    - "Mock tests verify ORDER BY and LIMIT behavior using realistic fixture data"
    - "Mock engine returns fixture data matching expected field types (int, str, datetime, Decimal)"
    - "Tests run fast locally (< 1 second) providing immediate feedback on query logic"
  artifacts:
    - path: "cubano-jaffle-shop/tests/conftest.py"
      provides: "Mock engine fixtures with jaffle-shop data loaded"
      min_lines: 40
      contains: "@pytest.fixture"
    - path: "cubano-jaffle-shop/tests/test_mock_queries.py"
      provides: "Mock-based query validation tests"
      min_lines: 80
      contains: "@pytest.mark.mock"
    - path: "cubano-jaffle-shop/tests/fixtures/mock_data.py"
      provides: "Realistic jaffle-shop fixture data"
      min_lines: 50
      contains: "orders_data"
  key_links:
    - from: "cubano-jaffle-shop/tests/test_mock_queries.py"
      to: "MockEngine"
      via: "mock_engine fixture"
      pattern: "mock_engine\\.execute"
    - from: "cubano-jaffle-shop/tests/conftest.py"
      to: "cubano-jaffle-shop/tests/fixtures/mock_data.py"
      via: "fixture data import"
      pattern: "from.*fixtures.*import"
    - from: "cubano-jaffle-shop/tests/test_mock_queries.py"
      to: "cubano_jaffle_shop.jaffle_models"
      via: "Orders, Customers, Products imports"
      pattern: "from cubano_jaffle_shop\\.jaffle_models import"
---

<objective>
Validate query builder logic with fast mock tests using realistic jaffle-shop fixture data.

**Purpose:** Provide developers with immediate feedback on query construction, field combinations, filtering, and ordering without expensive warehouse connections. Mock tests catch SQL generation bugs and validate query API behavior.

**Output:**
- Mock fixture data matching jaffle-shop schema (orders, customers, products)
- Mock-based tests for field combinations (metrics, dimensions, facts)
- Mock-based tests for ORDER BY and LIMIT behavior
- Foundation for warehouse test comparison (Plan 08-04)
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-testing/08-CONTEXT.md
@.planning/phases/08-integration-testing/08-RESEARCH.md
@.planning/phases/08-integration-testing/08-01-SUMMARY.md

@cubano-jaffle-shop/pyproject.toml
@cubano-jaffle-shop/src/cubano_jaffle_shop/jaffle_models.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create realistic jaffle-shop mock fixture data</name>
  <files>
cubano-jaffle-shop/tests/fixtures/__init__.py
cubano-jaffle-shop/tests/fixtures/mock_data.py
  </files>
  <action>
Create cubano-jaffle-shop/tests/fixtures/ package with realistic test data:

**cubano-jaffle-shop/tests/fixtures/__init__.py:**
Export fixture data for test imports.

**cubano-jaffle-shop/tests/fixtures/mock_data.py:**

Provide realistic fixture data matching jaffle-shop models:

1. **orders_data: list[dict]** — 10-15 order records
   - Fields: revenue (Decimal), order_count (int), tax_paid (Decimal), order_cost (Decimal), ordered_at (datetime), order_total_dim (Decimal), is_food_order (bool), is_drink_order (bool), customer_order_number (int)
   - Include variety: food orders (is_food_order=True), drink orders (is_drink_order=True), mixed orders
   - Include edge cases: minimum order (single item), maximum order (multiple items), nulls if realistic
   - Use realistic values: order_total 10-100 range, ordered_at spanning 2024-01 to 2024-12

2. **customers_data: list[dict]** — 5-7 customer records
   - Fields: customers (int), count_lifetime_orders (int), lifetime_spend_pretax (Decimal), lifetime_spend (Decimal), customer_name (str), customer_type (str), first_ordered_at (datetime), last_ordered_at (datetime | None)
   - Include variety: new customers (1 order), regular customers (5-10 orders), VIP customers (20+ orders)
   - Include edge case: customer with no orders (last_ordered_at=None)

3. **products_data: list[dict]** — 8-10 product records
   - Fields: product_name (str), product_type (str), product_description (str), is_food_item (bool), is_drink_item (bool), product_price (Decimal)
   - Include variety: food items, drink items, various price points (5-20 range)

Use Python's datetime and Decimal types for realistic type matching. Include docstrings describing the fixture data purpose.

Pattern: Realistic data sizes (10-15 rows) mimic warehouse results. Edge cases (nulls, extremes) validate query handling. Type matching (Decimal for money, datetime for timestamps) catches type coercion bugs.
  </action>
  <verify>
uv run python -c "from cubano_jaffle_shop.tests.fixtures.mock_data import orders_data, customers_data, products_data; print(f'Orders: {len(orders_data)}, Customers: {len(customers_data)}, Products: {len(products_data)}')" --project=/Users/paul/Documents/Dev/Personal/cubano/cubano-jaffle-shop
  </verify>
  <done>
Fixture data defined with realistic types (Decimal, datetime), 10-15 order records, 5-7 customer records, 8-10 product records, edge cases included
  </done>
</task>

<task type="auto">
  <name>Task 2: Create mock engine fixtures for jaffle-shop tests</name>
  <files>cubano-jaffle-shop/tests/conftest.py</files>
  <action>
Create cubano-jaffle-shop/tests/conftest.py with mock engine fixtures:

1. **orders_engine fixture (scope="function"):**
   - Creates MockEngine instance
   - Loads orders_data into "orders" view: engine.load("orders", orders_data)
   - Returns engine
   - Docstring: "MockEngine with orders fixture data loaded."

2. **customers_engine fixture (scope="function"):**
   - Same pattern for customers_data into "customers" view
   - Docstring: "MockEngine with customers fixture data loaded."

3. **products_engine fixture (scope="function"):**
   - Same pattern for products_data into "products" view
   - Docstring: "MockEngine with products fixture data loaded."

4. **jaffle_engine fixture (scope="function"):**
   - Creates MockEngine instance
   - Loads all fixture data: orders, customers, products views
   - Returns engine for multi-model queries
   - Docstring: "MockEngine with all jaffle-shop fixture data loaded."

Import fixture data from cubano_jaffle_shop.tests.fixtures.mock_data.

Function scope ensures each test gets fresh engine (no state leakage between tests).

Pattern: Separate fixtures per view allow focused tests. jaffle_engine provides comprehensive fixture for integration-style mock tests.
  </action>
  <verify>
uv run pytest --collect-only cubano-jaffle-shop/tests/ 2>&1 | grep -q "orders_engine" &&
uv run basedpyright cubano-jaffle-shop/tests/conftest.py
  </verify>
  <done>
Mock engine fixtures defined, function-scoped for isolation, fixture data loaded into views, imports successful, typechecking passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Write mock-based query validation tests</name>
  <files>cubano-jaffle-shop/tests/test_mock_queries.py</files>
  <action>
Create cubano-jaffle-shop/tests/test_mock_queries.py with @pytest.mark.mock tests:

**Test structure:** Group tests by concern (field combinations, ordering, limiting, filtering).

1. **TestFieldCombinations class:**
   - test_single_metric: Query().metrics(Orders.order_total) → validate result contains order_total field
   - test_multiple_metrics: Query().metrics(Orders.order_total, Orders.order_count) → validate both fields present
   - test_metric_with_dimension: Query().metrics(Orders.order_total).dimensions(Orders.ordered_at) → validate both fields
   - test_dimension_only: Query().dimensions(Orders.is_food_order) → validate dimension field
   - Use orders_engine fixture, execute queries, assert fields in results

2. **TestOrdering class:**
   - test_order_by_metric_desc: Query().metrics(Orders.order_total).order_by(Orders.order_total.desc()).limit(5) → validate descending order (totals sorted high to low)
   - test_order_by_dimension_asc: Query().dimensions(Customers.customer_name).order_by(Customers.customer_name.asc()) → validate ascending order (names alphabetical)
   - Use customers_engine fixture, execute queries, assert sorted order

3. **TestLimiting class:**
   - test_limit_results: Query().dimensions(Products.product_name).limit(3) → assert len(results) == 3
   - test_limit_larger_than_data: Query().dimensions(Products.product_name).limit(100) → assert len(results) == len(products_data) (no error)
   - Use products_engine fixture, execute queries, assert result counts

4. **TestFiltering class:**
   - test_filter_boolean: Query().dimensions(Orders.is_food_order).filter(Q(is_food_order=True)) → assert all results have is_food_order=True
   - test_filter_comparison: Query().metrics(Orders.order_total).filter(Q(order_total__gt=50)) → assert all results have order_total > 50 (if MockEngine supports filtering)
   - Note: If MockEngine doesn't implement filter evaluation (returns raw fixture data), document limitation in test docstring and mark as xfail with reason="MockEngine doesn't evaluate filters yet"

Mark all tests with @pytest.mark.mock decorator.

Import: from cubano import Query, Q; from cubano_jaffle_shop.jaffle_models import Orders, Customers, Products

Pattern: Tests validate query API usage and expected field presence. MockEngine validates SQL generation; actual result evaluation is warehouse-only (Plan 08-04).
  </action>
  <verify>
uv run pytest cubano-jaffle-shop/tests/test_mock_queries.py -v -m mock &&
uv run basedpyright cubano-jaffle-shop/tests/test_mock_queries.py
  </verify>
  <done>
Mock tests pass, marked with @pytest.mark.mock, test field combinations, ordering, limiting, typechecking passes, execution time < 1 second
  </done>
</task>

</tasks>

<verification>
## Mock Test Execution

```bash
# Run mock tests only
uv run pytest cubano-jaffle-shop/tests/test_mock_queries.py -v -m mock
# Should complete in < 1 second, all tests pass

# Verify test count
uv run pytest cubano-jaffle-shop/tests/test_mock_queries.py --collect-only | grep "test session starts" -A 20
# Should show 8+ test cases collected
```

## Fixture Data Validation

```bash
# Verify fixture data types
uv run python -c "
from cubano_jaffle_shop.tests.fixtures.mock_data import orders_data
from decimal import Decimal
from datetime import datetime
row = orders_data[0]
assert isinstance(row['order_total_dim'], Decimal), 'order_total_dim should be Decimal'
assert isinstance(row['ordered_at'], datetime), 'ordered_at should be datetime'
print('Fixture types validated')
" --project=/Users/paul/Documents/Dev/Personal/cubano/cubano-jaffle-shop
```

## Query API Coverage

```bash
# Verify all jaffle-shop models are tested
uv run pytest cubano-jaffle-shop/tests/test_mock_queries.py --collect-only -v | grep -E "(Orders|Customers|Products)"
# Should show tests covering all three models
```

## Type Safety

```bash
uv run basedpyright cubano-jaffle-shop/tests/
# Should pass with no errors

uv run ruff check cubano-jaffle-shop/tests/
# Should pass with no errors
```
</verification>

<success_criteria>
**Must be TRUE:**
1. Mock tests execute in < 1 second providing fast feedback
2. Tests validate field combinations (single metric, multiple metrics, metric + dimension, dimension only)
3. Tests validate ORDER BY behavior with realistic sorted data
4. Tests validate LIMIT behavior (within range, exceeding data size)
5. Fixture data uses realistic types (Decimal for money, datetime for timestamps, bool for flags)
6. All tests marked with @pytest.mark.mock for selective execution
7. Mock engine fixtures are function-scoped preventing test interference

**Files created:**
- cubano-jaffle-shop/tests/fixtures/mock_data.py with orders_data, customers_data, products_data
- cubano-jaffle-shop/tests/conftest.py with mock engine fixtures
- cubano-jaffle-shop/tests/test_mock_queries.py with @pytest.mark.mock tests

**Quality gates:**
- uv run basedpyright cubano-jaffle-shop/tests/ passes
- uv run pytest cubano-jaffle-shop/tests/test_mock_queries.py passes
- Mock tests complete in < 1 second
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-testing/08-03-SUMMARY.md` documenting:
- Mock fixture data structure and realistic types
- Mock test coverage (field combinations, ordering, limiting)
- MockEngine limitations (filter evaluation) and workarounds
- Performance characteristics (< 1 second execution)
- Examples of running mock tests with pytest -m mock
</output>

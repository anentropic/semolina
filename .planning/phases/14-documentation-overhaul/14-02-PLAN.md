---
phase: 14-documentation-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - docs/src/index.md
  - docs/src/tutorials/index.md
  - docs/src/tutorials/installation.md
  - docs/src/tutorials/first-query.md
  - docs/src/explanation/index.md
  - docs/src/explanation/semantic-views.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "Getting started tutorial walks through install, model definition, engine registration, first query, and reading results with runnable code"
    - "Tutorial code examples include imports and expected output"
    - "Explanation page explains what a semantic view is without giving step-by-step instructions"
    - "Explanation page links to Snowflake and Databricks vendor docs for semantic view setup"
    - "Home page index.md links point to correct new tutorial/how-to paths"
    - "All rewritten prose passes humanizer tone review (warm but efficient, second person)"
  artifacts:
    - path: "docs/src/tutorials/installation.md"
      provides: "Tutorial: install Cubano and verify setup"
    - path: "docs/src/tutorials/first-query.md"
      provides: "Tutorial: define model, register engine, run query, read results"
    - path: "docs/src/explanation/semantic-views.md"
      provides: "Explanation: what semantic views are and where Cubano fits"
    - path: "docs/src/index.md"
      provides: "Landing page with updated card links and quick example"
  key_links:
    - from: "docs/src/tutorials/installation.md"
      to: "docs/src/tutorials/first-query.md"
      via: "end-of-page pointer"
      pattern: "first-query"
    - from: "docs/src/explanation/semantic-views.md"
      to: "docs/src/tutorials/installation.md"
      via: "See also link"
      pattern: "tutorials/installation"
---

<objective>
Rewrite tutorial pages (installation, first query), the explanation page (semantic views), and the home page landing using the diataxis-documentation skill for structure and the humanizer skill for tone.

Purpose: Transform tutorials from "guide-style" to proper Diataxis tutorials (learning-oriented, hands-on, runnable code) and create a focused explanation page. Establish the writing voice for the entire documentation site.

Output: 4-6 rewritten/new markdown files with Diataxis-correct structure and human-sounding prose.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-overhaul/14-CONTEXT.md
@.planning/phases/14-documentation-overhaul/14-RESEARCH.md
@.planning/phases/14-documentation-overhaul/14-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tutorial pages using diataxis tutorial rules</name>
  <files>
    docs/src/tutorials/installation.md
    docs/src/tutorials/first-query.md
    docs/src/tutorials/index.md
  </files>
  <action>
    **Prerequisite: Load skills.** Read the diataxis-documentation skill:
    1. Read `~/.claude/skills/diataxis-documentation/SKILL.md`
    2. Read `~/.claude/skills/diataxis-documentation/references/tutorials.md` for tutorial-specific rules

    **Workflow for each tutorial page:**

    1. Read the existing page content
    2. Extract all code examples verbatim (these are preserved unchanged per user decision)
    3. Rewrite ALL prose from scratch following diataxis tutorial rules:
       - Learning-oriented: teach by doing, not by explaining
       - Step-by-step: numbered steps where the user does something at each step
       - Concrete outcomes: "you should see..." after each code block
       - Runnable: code works if pasted with right imports (already true, preserve this)
       - Show imports and expected output (per user decision)
       - No SQL output in tutorials (per user decision: SQL shown in how-to guides only)
    4. Apply humanizer tone review: second person ("you"), warm but efficient, like FastAPI/Stripe docs
    5. Read `~/.claude/skills/humanizer/SKILL.md` and scan rewritten prose for AI patterns. Fix any found.

    **installation.md specifics:**
    - Audience: data/analytics engineers who already have semantic views, comfortable with Python
    - Framing: "You already have a semantic view in your warehouse. Cubano lets you query it from Python."
    - Steps: install package, verify installation, brief mention of extras (snowflake/databricks)
    - End with clear pointer: "Next: [Your first query](first-query.md)"
    - Keep concise -- installation is short

    **first-query.md specifics:**
    - Walk through: define a model, register a MockEngine, build a query, execute, read results
    - Each step: brief context, code block, "you should see..." verification
    - Code examples preserved from existing page
    - End with "See also" links to how-to guides for deeper topics (models, queries, filtering)

    **tutorials/index.md:**
    - Brief section landing page (10-15 lines)
    - 2-3 sentences: what tutorials are and who they're for
    - Links to installation and first-query
    - Tone: inviting but not wordy

    **Writing constraints (from CONTEXT.md locked decisions):**
    - Perspective: second person ("you")
    - Tone: warm but efficient -- friendly without being wordy
    - Assume user already has semantic views defined in their warehouse
    - Focus on where Cubano fits, not what a semantic view is (that's in explanation/)
  </action>
  <verify>
    Run `uv run mkdocs build --strict` -- must succeed.
    Read each file and verify:
    - installation.md has step-by-step format with concrete actions
    - first-query.md has runnable code with imports and expected output
    - Neither tutorial shows SQL output (SQL is for how-to guides only)
    - All prose uses "you" (second person)
    - No AI vocabulary words from humanizer checklist (delve, crucial, enhance, landscape, etc.)
    - Both tutorials end with "See also" or "Next" cross-references
  </verify>
  <done>
    Two tutorial pages rewritten with diataxis tutorial structure: learning-oriented, step-by-step, runnable code with expected output. Prose is warm-but-efficient second person. Section index page links to both tutorials.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create explanation page and update home page</name>
  <files>
    docs/src/explanation/semantic-views.md
    docs/src/explanation/index.md
    docs/src/index.md
  </files>
  <action>
    **Prerequisite: Load skills.** Read:
    1. `~/.claude/skills/diataxis-documentation/references/explanation.md` for explanation-specific rules
    2. `~/.claude/skills/humanizer/SKILL.md` for tone review

    **semantic-views.md: "What is a semantic view?"**

    Per user decisions, this page must:
    - Be a brief elevator pitch, NOT exhaustive
    - Assume users already have semantic views in their warehouse
    - Focus on where Cubano fits into the picture and target use cases
    - Link out to warehouse vendor docs for semantic view setup

    Structure (per RESEARCH.md outline):
    1. One-paragraph definition of semantic views (the concept) -- what they are, why they exist
    2. How Snowflake (CREATE SEMANTIC VIEW) and Databricks (Metric Views) implement them -- 2-3 sentences each, not a tutorial
    3. Where Cubano fits: a Python ORM that mirrors your existing warehouse views as typed models -- this is the core message
    4. Link to Snowflake docs: https://docs.snowflake.com/en/sql-reference/sql/create-semantic-view
    5. Link to Databricks docs: https://docs.databricks.com/en/sql/language-manual/sql-ref-syntax-ddl-create-metric-view.html
    6. "See also" links to tutorials and how-to guides

    **Critical: Explanation pages explain concepts, they do NOT give instructions.** No "do this next" language. No step-by-step. If the page starts sounding like a tutorial, link to the tutorial instead.

    Length target: 40-70 lines. Brief and focused.

    **explanation/index.md:**
    - Brief section landing (10-15 lines)
    - 2-3 sentences: background concepts for understanding Cubano
    - Link to semantic-views.md

    **index.md (home page) update:**
    - Read the existing `docs/src/index.md`
    - Update the feature card links to point to new paths:
      - Getting Started card: `tutorials/installation.md`
      - Defining Models card: `how-to/models.md`
      - Building Queries card: `how-to/queries.md`
      - API Reference card: `reference/` (unchanged)
    - Rewrite the card descriptions if needed to match the warm-but-efficient tone
    - Keep the Quick Example code block unchanged
    - Consider adding a brief tagline that frames Cubano for the target audience (engineers who already have semantic views)

    **Humanizer pass:** After writing all content, re-read each page and check against humanizer patterns. Fix:
    - Any "Additionally", "crucial", "delve", "enhance", "landscape", "testament" etc.
    - Any promotional language ("groundbreaking", "powerful", "stunning")
    - Any rule-of-three patterns
    - Any em dash overuse (prefer commas or periods)
    - Any inline-header vertical lists with bold+colon pattern (unless genuinely useful for docs scannability)
  </action>
  <verify>
    Run `uv run mkdocs build --strict` -- must succeed.
    Read semantic-views.md and verify:
    - Contains links to Snowflake and Databricks vendor docs
    - Does NOT contain step-by-step instructions
    - Explains where Cubano fits (ORM for existing semantic views)
    - Has "See also" section at bottom
    - Length: 40-70 lines
    Read index.md and verify:
    - Card links point to tutorials/ and how-to/ paths (not guides/)
    - Quick Example code block preserved unchanged
    No AI vocabulary words in any page.
  </verify>
  <done>
    "What is a semantic view?" explanation page created: brief, concept-focused, links to vendor docs and Cubano tutorials. Home page updated with correct links and consistent tone. Explanation section index page links to semantic-views.md.
  </done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` succeeds
2. All tutorial pages have runnable code with imports and expected output
3. Explanation page does not contain step-by-step instructions
4. All pages use second-person ("you") perspective
5. No AI vocabulary words from humanizer checklist appear in rewritten prose
6. Home page cards link to correct new paths
7. Cross-references work: tutorials link forward, explanation links to tutorials
</verification>

<success_criteria>
- Installation tutorial: step-by-step with concrete "you should see" outcomes
- First query tutorial: complete working example from model definition through result reading
- Semantic views explanation: brief, concept-focused, vendor doc links, no instructions
- Home page: updated links, consistent tone
- All prose: warm-but-efficient second person, no AI patterns
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-overhaul/14-02-SUMMARY.md`
</output>

---
phase: 03-sql-generation-mock-backend
plan: 05
type: execute
wave: 1
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - src/cubano/engines/mock.py
  - tests/conftest.py
  - tests/test_engines.py
  - tests/test_sql.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "MockEngine constructor accepts no fixtures parameter"
    - "Test fixtures are injected via pytest fixtures, not constructor"
    - "All existing tests use pytest fixtures for test data"
    - "Backward compatibility maintained in test structure"
    - "MockEngine public API is clean and decoupled from testing concerns"
  artifacts:
    - path: "src/cubano/engines/mock.py"
      provides: "Refactored MockEngine with fixtures removed from constructor"
      exports: ["MockEngine.__init__", "MockEngine.to_sql", "MockEngine.execute"]
    - path: "tests/conftest.py"
      provides: "Pytest fixtures for test data injection"
      exports: ["sales_fixtures", "sales_engine"]
    - path: "tests/test_engines.py"
      provides: "Updated engine tests using pytest fixtures"
      min_lines: 450
    - path: "tests/test_sql.py"
      provides: "Updated SQL tests using pytest fixtures"
      min_lines: 480
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_engines.py"
      via: "fixture injection"
      pattern: "@pytest.fixture"
    - from: "tests/conftest.py"
      to: "tests/test_sql.py"
      via: "fixture injection"
      pattern: "@pytest.fixture"
    - from: "tests/test_engines.py"
      to: "src/cubano/engines/mock.py"
      via: "MockEngine usage"
      pattern: "MockEngine\\(\\)"
    - from: "src/cubano/engines/mock.py"
      to: "tests/test_engines.py"
      via: "test coverage"
      pattern: "def test_.*fixtures"
---

<objective>
Refactor MockEngine's public API to remove testing concerns (fixtures parameter) and use pytest fixtures for test data injection instead. This decouples testing logic from the engine's core design.

Purpose: Clean public API that doesn't expose testing concerns. Fixtures passed to constructor couples internal testing details into the API contract. Moving to pytest fixtures isolates test infrastructure from production code.

Output:
- Refactored MockEngine.__init__() with no fixtures parameter
- Central conftest.py with reusable test fixtures
- Updated test_engines.py and test_sql.py using fixture injection
- All tests passing with new fixture strategy
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@/Users/paul/Documents/Dev/Personal/cubano/.planning/phases/03-sql-generation-mock-backend/03-04-SUMMARY.md
</execution_context>

<context>
@/Users/paul/Documents/Dev/Personal/cubano/src/cubano/engines/mock.py
@/Users/paul/Documents/Dev/Personal/cubano/tests/test_engines.py
@/Users/paul/Documents/Dev/Personal/cubano/tests/test_sql.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create central conftest.py with reusable pytest fixtures</name>
  <files>tests/conftest.py</files>
  <action>
Create tests/conftest.py with pytest fixtures for test data injection:

1. Create fixture `sales_model` returning Sales SemanticView class (already defined in test files, should be centralized)
2. Create fixture `sales_fixtures` returning dict[str, list[dict[str, Any]]] with standard test data:
   - 'sales_view': [
       {'revenue': 1000, 'cost': 100, 'country': 'US', 'region': 'West'},
       {'revenue': 2000, 'cost': 200, 'country': 'CA', 'region': 'West'},
       {'revenue': 500, 'cost': 50, 'country': 'US', 'region': 'East'},
     ]
3. Create fixture `sales_engine(sales_fixtures)` returning MockEngine instance configured with sales_fixtures
4. Document each fixture with docstring explaining its purpose and typical usage
5. Import necessary types: from typing import Any; from cubano import SemanticView, Metric, Dimension; from cubano.engines.mock import MockEngine

Type annotations: Ensure all fixtures are properly type-hinted (e.g., `-> dict[str, list[dict[str, Any]]]`)
  </action>
  <verify>
- File exists at tests/conftest.py with 50-70 lines
- pytest can find fixtures: `uv run pytest --fixtures | grep "sales_"`
- Fixtures are importable and callable
  </verify>
  <done>
- conftest.py created with sales_model, sales_fixtures, sales_engine fixtures
- Each fixture has type hints and docstring
- Fixtures follow pytest conventions (no imports needed in test files)
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor MockEngine.__init__ to remove fixtures parameter</name>
  <files>src/cubano/engines/mock.py</files>
  <action>
Refactor MockEngine class:

1. Change __init__ signature from `__init__(self, fixtures: dict[str, list[dict[str, Any]]] | None = None)` to `__init__(self)` with no parameters
2. Remove the docstring parameter documentation for fixtures (keep just the class docstring)
3. Remove `self.fixtures = fixtures or {}` line (now no internal fixtures storage)
4. Update to_sql() to work without internal fixtures (already doesn't use them, just passes through to SQLBuilder)
5. Update execute() to raise NotImplementedError or descriptive error: "MockEngine.execute() not available in gap closure phase. Use pytest fixtures for test data."
   - Rationale: In Phase 3, execute() returns raw fixtures. In Phase 4+, real backend engines will implement filtering/aggregation. For testing, pytest fixtures are cleaner.
6. Update class docstring to remove the Example section showing fixtures parameter (replace with example using pytest fixtures)
7. Keep all other functionality intact: to_sql(), SQLBuilder integration, MockDialect

Imports: No changes needed (already imports Any, Engine, MockDialect, SQLBuilder)

Test approach: After this refactor, tests will be broken temporarily (expected). Task 3 fixes them.
  </action>
  <verify>
- File syntax is valid: `python -m py_compile src/cubano/engines/mock.py`
- Class still inherits from Engine
- to_sql() method signature unchanged
- execute() method has clear error message about pytest fixtures
- No references to self.fixtures remain in __init__
  </verify>
  <done>
- MockEngine.__init__(self) signature with no parameters
- Fixtures parameter completely removed from API
- execute() raises NotImplementedError with helpful message
- Class docstring updated with pytest fixture example
- to_sql() continues to work as before
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test_engines.py to use pytest fixtures</name>
  <files>tests/test_engines.py</files>
  <action>
Update tests/test_engines.py to use fixtures from conftest:

1. Remove all local fixture definitions (current @pytest.fixture methods in test classes)
2. Remove local Sales model definition at module level (move to conftest, import from there)
3. Update TestMockEngineInit class tests:
   - `test_mock_engine_with_no_fixtures()` -> Change to test MockEngine() initialization without fixtures parameter
   - `test_mock_engine_with_fixtures()` -> DELETE (no longer valid, fixtures not in constructor)
   - `test_mock_engine_has_dialect()` -> Keep as-is (dialect still exists)
   - `test_mock_engine_multiple_fixtures()` -> DELETE
4. Update TestMockEngineExecute class:
   - Replace `@pytest.fixture def sales_fixtures(self)` with parameter injection: `def test_...(self, sales_fixtures)`
   - Replace all `MockEngine(fixtures=sales_fixtures)` with `sales_engine` fixture parameter
   - Keep all assertion logic, only change how fixtures are provided
5. Update TestMockEngineIntegration class:
   - Replace `@pytest.fixture def sales_fixtures(self)` with parameter injection
   - Replace all `MockEngine(fixtures=sales_fixtures)` with `sales_engine` fixture parameter
6. Update TestENG01_MockEngineWithoutWarehouse:
   - Delete tests that pass fixtures to constructor
   - Update remaining tests to not pass fixtures parameter
7. Keep all other tests as-is (they test behavior, not fixture initialization)

Minimize churn: Only modify lines that reference fixtures or MockEngine constructor. Keep all test logic identical.
  </action>
  <verify>
- File has 400+ lines (existing tests preserved)
- No `MockEngine(fixtures=...)` calls remain (only `MockEngine()`)
- No local `@pytest.fixture def sales_fixtures` methods
- Test class structure unchanged (only method bodies updated)
- `uv run pytest tests/test_engines.py -v` passes all tests
  </verify>
  <done>
- All test_engines.py tests use pytest fixtures or MockEngine()
- Fixture injection via parameters (pytest magic)
- No local fixture definitions remain
- 400+ lines maintained, high test coverage
- All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 4: Update test_sql.py to use pytest fixtures from conftest</name>
  <files>tests/test_sql.py</files>
  <action>
Update tests/test_sql.py to use fixtures from conftest:

1. Remove local Sales model definition (use from conftest)
2. Note: test_sql.py doesn't currently use MockEngine fixtures in constructor (tests Dialect and SQLBuilder directly)
3. Update any tests that use Query with Sales model:
   - Ensure Sales is imported from conftest or defined locally if needed
4. TestSQLBuilderSelectClause, TestSQLBuilderFromClause, etc. - these test SQL generation, not MockEngine, so minimal changes
5. Add sales_fixtures parameter to any integration tests that might benefit from standard test data (optional, keep minimal)

Key insight: test_sql.py tests the SQL layer (Dialect, SQLBuilder), not engine fixture injection. Changes here are minimal.

Import: `from conftest import Sales` (or keep local definition - check if necessary to avoid circular imports)
  </action>
  <verify>
- File syntax is valid: `python -m py_compile tests/test_sql.py`
- All tests still reference Sales model correctly
- `uv run pytest tests/test_sql.py -v` passes all tests
- No new test failures introduced
  </verify>
  <done>
- test_sql.py updated to use conftest Sales model if applicable
- SQL generation tests still cover all scenarios
- 480+ lines maintained
- All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify refactored code against quality gates</name>
  <files>src/cubano/engines/mock.py</files>
  <action>
Run full quality checks on refactored code:

1. Typecheck: `uv run basedpyright src/cubano/engines/mock.py tests/conftest.py tests/test_engines.py tests/test_sql.py`
   - Fix any type errors from removed fixtures parameter
   - Ensure Any, dict, list annotations are correct in conftest
2. Lint: `uv run ruff check src/cubano/engines/mock.py tests/conftest.py tests/test_engines.py tests/test_sql.py`
   - Fix import ordering, naming conventions
3. Format: `uv run ruff format --check src/cubano/engines/mock.py tests/conftest.py tests/test_engines.py tests/test_sql.py`
   - Apply formatting: `uv run ruff format src/cubano/engines/mock.py tests/conftest.py tests/test_engines.py tests/test_sql.py`
4. Tests: `uv run --extra dev pytest tests/test_engines.py tests/test_sql.py -v`
   - All tests must pass
   - No new warnings introduced

If failures occur, fix in-place. Expected: All should pass (refactoring preserves behavior, just changes fixture injection strategy).
  </action>
  <verify>
- `uv run basedpyright ...` returns 0 errors
- `uv run ruff check ...` returns 0 errors
- `uv run ruff format --check ...` shows no formatting issues
- `uv run --extra dev pytest tests/test_engines.py tests/test_sql.py -v` shows all tests passing
  </verify>
  <done>
- All quality gates pass (typecheck, lint, format, tests)
- MockEngine API refactoring complete and verified
- Code is production-ready
  </done>
</task>

</tasks>

<verification>
Complete MockEngine API refactoring:

1. **API Cleanliness:**
   - `MockEngine()` constructor accepts no parameters (tested in Task 5)
   - No fixtures leakage into public API
   - to_sql() works independently as before

2. **Test Infrastructure:**
   - pytest fixtures centralized in conftest.py (Task 1)
   - All tests use fixture injection (Task 3, 4)
   - No local fixture duplication across test files

3. **Backward Compatibility:**
   - Test structure preserved (same test classes, assertions)
   - Test data identical (sales_fixtures dict unchanged)
   - Only injection method changed (constructor -> pytest fixtures)

4. **Quality Assurance:**
   - Type checking passes
   - Linting passes
   - Formatting consistent
   - All 50+ existing tests still pass
</verification>

<success_criteria>
- MockEngine.__init__() has zero parameters (fixtures removed)
- conftest.py exists with sales_model, sales_fixtures, sales_engine fixtures
- test_engines.py uses pytest fixtures exclusively, no local @pytest.fixture methods
- test_sql.py updated to import Sales from conftest or uses local definition
- All 450+ test cases in test_engines.py pass
- All 480+ test cases in test_sql.py pass
- typecheck, lint, format all pass
- No `# type: ignore` comments needed
- MockEngine public API is clean and decoupled from testing concerns
</success_criteria>

<output>
After completion, create `.planning/phases/03-sql-generation-mock-backend/03-05-SUMMARY.md` with:
- Refactoring summary (what changed, why)
- API before/after comparison
- Testing strategy (pytest fixtures)
- Files modified (mock.py, conftest.py, test_engines.py, test_sql.py)
- Test results (all 450+ tests passing)
- Quality gate status (typecheck, lint, format all pass)
</output>

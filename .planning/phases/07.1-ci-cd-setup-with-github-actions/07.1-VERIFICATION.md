---
phase: 07.1-ci-cd-setup-with-github-actions
verified: 2026-02-16T10:30:00Z
status: human_needed
score: 16/16 must-haves verified
re_verification: false
human_verification:
  - test: "Push to feature branch and verify CI workflow runs"
    expected: "All 4 jobs (typecheck, lint, format, test) run in parallel and pass"
    why_human: "Requires GitHub Actions execution environment"
  - test: "Create PR and verify coverage comment appears"
    expected: "Coverage report posted as PR comment with tables and statistics"
    why_human: "Requires PR context and GitHub API interaction"
  - test: "Push semver tag and verify release pipeline"
    expected: "Build → validate → changelog → publish sequence executes, package appears on PyPI"
    why_human: "Requires PyPI Trusted Publishing setup and GitHub Actions execution"
  - test: "Verify Dependabot PRs group dev dependencies"
    expected: "Weekly Monday PR with grouped dev dependency updates"
    why_human: "Requires Dependabot service execution and time passage"
---

# Phase 07.1: CI/CD Setup with GitHub Actions Verification Report

**Phase Goal:** Automated testing, quality validation, and PyPI releases via GitHub Actions
**Verified:** 2026-02-16T10:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                  | Status     | Evidence                                                                              |
| --- | ---------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------- |
| 1   | All quality checks run on every push to any branch                    | ✓ VERIFIED | ci.yml triggers on push, has 4 jobs                                                   |
| 2   | All 4 checks (typecheck, lint, format, test) must pass for green      | ✓ VERIFIED | 4 independent jobs: typecheck, lint, format, test                                     |
| 3   | Status checks ready for branch protection                             | ✓ VERIFIED | Job names unique and match requirement                                                |
| 4   | CI runs matrix for Python 3.11 and 3.14                               | ✓ VERIFIED | Test job has matrix strategy with both versions                                       |
| 5   | Coverage reports appear as PR comments with visual tables             | ✓ VERIFIED | pr.yml uses MishaKav/pytest-coverage-comment action                                   |
| 6   | Coverage is measured but not enforced                                 | ✓ VERIFIED | No minimum threshold in pr.yml configuration                                          |
| 7   | PR checks run only on pull_request events                             | ✓ VERIFIED | pr.yml triggers on: [pull_request]                                                    |
| 8   | Pushing a semver tag triggers automated PyPI publish                  | ✓ VERIFIED | release.yml triggers on tags: v[0-9]+.[0-9]+.[0-9]+                                   |
| 9   | Build validation runs in isolated venvs for Python 3.11 and 3.14      | ✓ VERIFIED | Validate job creates test-env-wheel and test-env-sdist                                |
| 10  | Failed validation blocks PyPI upload with no override path            | ✓ VERIFIED | Publish job needs: [validate, changelog]                                              |
| 11  | Changelog auto-generates from conventional commits                    | ✓ VERIFIED | Changelog job runs git-cliff, .cliff.toml exists                                      |
| 12  | py.typed marker validated before publishing                           | ✓ VERIFIED | Validation checks: grep "cubano/py.typed" with exit 1 on failure                      |
| 13  | PyPI publishing uses Trusted Publishing (no API tokens)               | ✓ VERIFIED | Publish job has id-token: write, uses pypa/gh-action-pypi-publish                     |
| 14  | Dependabot scans dev dependencies weekly                              | ✓ VERIFIED | dependabot.yml pip ecosystem with weekly schedule, day: monday                        |
| 15  | GitHub Actions dependencies are tracked and updated                   | ✓ VERIFIED | dependabot.yml has github-actions ecosystem                                           |
| 16  | Dev dependencies grouped to reduce PR noise                           | ✓ VERIFIED | groups.dev-dependencies with dependency-type: "development"                           |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact                           | Expected                                           | Status     | Details                                                                  |
| ---------------------------------- | -------------------------------------------------- | ---------- | ------------------------------------------------------------------------ |
| `.github/workflows/ci.yml`         | CI workflow with 4 quality check jobs              | ✓ VERIFIED | 106 lines, 4 jobs (typecheck, lint, format, test), all commands present |
| `.github/workflows/pr.yml`         | PR workflow with coverage reporting                | ✓ VERIFIED | 44 lines, coverage job with pytest-cov and MishaKav action               |
| `.github/workflows/release.yml`    | Release workflow with build→validate→publish       | ✓ VERIFIED | 126 lines, 4 jobs with proper dependencies                               |
| `.cliff.toml`                      | Changelog generation configuration                 | ✓ VERIFIED | 36 lines, conventional commits parsing with 8 groups                     |
| `.github/dependabot.yml`           | Dependabot config for pip and github-actions       | ✓ VERIFIED | 20 lines, 2 ecosystems with weekly schedule                              |
| `pyproject.toml` (pytest-cov)      | pytest-cov in dev dependencies                     | ✓ VERIFIED | pytest-cov>=6.0.0 in [dependency-groups].dev                             |

### Key Link Verification

| From                              | To                                   | Via                         | Status     | Details                                                          |
| --------------------------------- | ------------------------------------ | --------------------------- | ---------- | ---------------------------------------------------------------- |
| `.github/workflows/ci.yml`        | `uv run basedpyright`                | typecheck job step          | ✓ WIRED    | Command present in typecheck job                                 |
| `.github/workflows/ci.yml`        | `uv run ruff check`                  | lint job step               | ✓ WIRED    | Command with --output-format=github in lint job                  |
| `.github/workflows/ci.yml`        | `uv run ruff format --check`         | format job step             | ✓ WIRED    | Command present in format job                                    |
| `.github/workflows/ci.yml`        | `uv run pytest tests/`               | test job step               | ✓ WIRED    | Command present in test job with matrix                          |
| `.github/workflows/pr.yml`        | `pytest --cov`                       | coverage job step           | ✓ WIRED    | Command with --cov=src/cubano --cov-report=xml --cov-report=term |
| `.github/workflows/pr.yml`        | `MishaKav/pytest-coverage-comment`   | action usage                | ✓ WIRED    | Action used with pytest-coverage-path and github-token           |
| `.github/workflows/release.yml`   | `uv build`                           | build job step              | ✓ WIRED    | Command in build job                                             |
| `.github/workflows/release.yml`   | `pypa/gh-action-pypi-publish`        | publish job action          | ✓ WIRED    | Action used after validate+changelog complete                    |
| `.github/workflows/release.yml`   | isolated venv validation             | validate job matrix         | ✓ WIRED    | test-env-wheel and test-env-sdist created for both Python vers   |
| `.github/workflows/release.yml`   | py.typed validation                  | validate job step           | ✓ WIRED    | grep check with error exit if missing                            |
| `.github/workflows/release.yml`   | git-cliff                            | changelog job step          | ✓ WIRED    | Installs and runs git-cliff with .cliff.toml config              |
| `.github/dependabot.yml`          | dev dependencies                     | pip ecosystem with grouping | ✓ WIRED    | dependency-type: "development" targets pyproject.toml dev deps   |

### Anti-Patterns Found

No blocking anti-patterns detected.

**Positive Patterns:**
- ✓ All workflows use proper concurrency control with cancel-in-progress
- ✓ All jobs have timeout-minutes to prevent runaway workflows
- ✓ Proper permission scoping (read-only by default, write only where needed)
- ✓ Cache management with uv cache prune --ci
- ✓ Matrix strategy with fail-fast: false for comprehensive testing
- ✓ Isolated venv validation prevents false positives
- ✓ Hard dependency gates (publish blocked by validation)
- ✓ No API tokens stored (Trusted Publishing via OIDC)

### Human Verification Required

#### 1. CI Workflow Execution

**Test:** Push commits to a feature branch and navigate to GitHub Actions tab

**Expected:**
- All 4 jobs appear: typecheck, lint, format, test
- Jobs run in parallel (no sequential dependencies)
- Test job shows 2 matrix runs (Python 3.11 and 3.14)
- Concurrency control cancels old runs when new commits pushed
- All jobs complete with green checkmarks (assuming code passes quality checks)

**Why human:** Requires GitHub Actions execution environment and GitHub UI verification

#### 2. PR Coverage Reporting

**Test:** Create a pull request and wait for coverage check to complete

**Expected:**
- "Report test coverage" check appears in PR checks section
- Coverage comment posted to PR with:
  - Overall coverage percentage
  - Coverage table by file
  - Test statistics (passed/failed/skipped)
- Pushing additional commits updates same comment (no duplicates)
- PR can be merged even if coverage decreases (no enforcement)

**Why human:** Requires PR context, GitHub API interaction, and pull-requests: write permission

#### 3. Release Pipeline Execution

**Test:** Push a semver tag (e.g., git tag v0.1.0-test && git push origin v0.1.0-test)

**Pre-requisite:** Configure PyPI Trusted Publishing (one-time setup):
1. Sign in to PyPI
2. Account settings → Publishing → Add new publisher
3. Enter: owner=anentropic, repo=cubano, workflow=release.yml, environment=pypi

**Expected:**
- Release workflow triggers automatically on tag push
- Jobs execute in sequence:
  1. build completes and uploads dist/ artifacts
  2. validate and changelog run in parallel (both depend on build)
  3. validate runs matrix for Python 3.11 and 3.14
  4. validate tests wheel and sdist in isolated venvs
  5. validate checks py.typed marker presence
  6. changelog generates CHANGELOG.md from conventional commits
  7. publish waits for both validate and changelog to pass
  8. publish uploads to PyPI using Trusted Publishing
- Package appears on https://pypi.org/p/cubano

**Test validation blocking:** Remove py.typed marker, push tag, verify validation fails and publish is skipped

**Why human:** Requires GitHub Actions execution, PyPI Trusted Publishing setup, and external service integration

#### 4. Dependabot Updates

**Test:** Wait for Dependabot's first scheduled run (or trigger manually in GitHub settings)

**Expected:**
- Two ecosystems appear in Insights → Dependency graph → Dependabot
- Weekly PR created on Mondays for grouped dev dependency updates
- Separate PRs for GitHub Actions updates
- Security updates trigger immediately (separate from weekly schedule)
- Dev dependencies grouped in single PR (not separate PRs per package)

**Why human:** Requires Dependabot service execution, time passage, and GitHub UI verification

---

## Summary

**Status:** human_needed

All automated verifications passed. Phase 07.1 successfully delivered:

1. **CI Workflow** - 4 parallel quality check jobs (typecheck, lint, format, test) running on every push
2. **PR Workflow** - Coverage reporting with visual PR comments, no enforcement threshold
3. **Release Pipeline** - Build → validate → changelog → publish with Trusted Publishing
4. **Dependabot** - Weekly dev dependency tracking with GitHub Actions updates

**Automated Checks:** 16/16 truths verified, all artifacts exist and substantive, all key links wired

**Human Verification:** 4 items require GitHub Actions execution and external services (GitHub API, PyPI, Dependabot)

**Next Steps:**
1. Push commits to trigger CI workflow
2. Create PR to verify coverage reporting
3. Configure PyPI Trusted Publishing (one-time)
4. Push semver tag to test release pipeline
5. Verify Dependabot PRs appear weekly

**Manual Setup Required:**
- Configure branch protection rules to require status checks (typecheck, lint, format, test)
- Set up PyPI Trusted Publishing for password-less releases (instructions in 07.1-03-SUMMARY.md)

---

_Verified: 2026-02-16T10:30:00Z_
_Verifier: Claude Code (gsd-verifier)_
